<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title><![CDATA[音视频开发之ffplay渲染线程分析]]></title>
    <url>%2F%E9%9F%B3%E8%A7%86%E9%A2%91%E5%BC%80%E5%8F%91%E4%B9%8Bffplay%E6%B8%B2%E6%9F%93%E7%BA%BF%E7%A8%8B%E5%88%86%E6%9E%90%2F</url>
    <content type="text"><![CDATA[视频渲染线程视频渲染线程实际就是main线程。 初始化初始化SDL12345678// SDL初始化flags = SDL_INIT_VIDEO | SDL_INIT_AUDIO | SDL_INIT_TIMER;if (SDL_Init(flags)) &#123; av_log(NULL, AV_LOG_FATAL, "Could not initialize SDL - %s\n", SDL_GetError()); av_log(NULL, AV_LOG_FATAL, "(Did you set the DISPLAY variable?)\n"); exit(1);&#125; 创建Window和Renderer123456789101112131415161718192021222324252627282930313233if (borderless) flags |= SDL_WINDOW_BORDERLESS; // 去掉窗口状态栏else flags |= SDL_WINDOW_RESIZABLE; // 窗口是否可缩放// 创建SDL窗口window = SDL_CreateWindow(program_name, SDL_WINDOWPOS_UNDEFINED, SDL_WINDOWPOS_UNDEFINED, default_width, default_height, flags);SDL_SetHint(SDL_HINT_RENDER_SCALE_QUALITY, "linear");if (window) &#123; // 创建SDL渲染器，SDL_RENDERER_ACCELERATED 使用硬件加速 renderer = SDL_CreateRenderer( window, -1, SDL_RENDERER_ACCELERATED | SDL_RENDERER_PRESENTVSYNC); if (!renderer) &#123; av_log(NULL, AV_LOG_WARNING, "Failed to initialize a hardware accelerated renderer: %s\n", SDL_GetError()); // 如果创建SDL渲染器失败，则去掉标记再重试，可能是当前设备不支持该标记 renderer = SDL_CreateRenderer(window, -1, 0); &#125; if (renderer) &#123; // 输出渲染器信息 if (!SDL_GetRendererInfo(renderer, &amp;renderer_info)) av_log(NULL, AV_LOG_VERBOSE, "Initialized %s renderer.\n", renderer_info.name); &#125;&#125;if (!window || !renderer || !renderer_info.num_texture_formats) &#123; // 如果窗口或者渲染器创建失败，或者渲染器中可用的纹理格式为0，则退出 av_log(NULL, AV_LOG_FATAL, "Failed to create window or renderer: %s", SDL_GetError()); do_exit(NULL);&#125; 轮询主线程在开启解复用线程后，就会开始轮询处理SDL事件1234567891011static void event_loop(VideoState *cur_stream) &#123; SDL_Event event; double incr, pos, frac; // 开始轮询SDL消息 for (;;) &#123; double x; // 获取事件，如果有，则执行下面的switch，如果没有，则会尝试刷新视频渲染 refresh_loop_wait_event(cur_stream, &amp;event); ... &#125;&#125; 在refresh_loop_wait_event中，会尝试获取SDL事件，如果获取成功，则返回，如果获取失败，则会执行while中的视频刷新操作。SDL事件会优先处理，在没有SDL事件的时候，就会轮询刷新视频 remaining_time的时间默认是0.01，当video_refresh刷新时，如果继续显示当前帧，则remaining_time的值等于当前帧结束的时间差，如果是显示下一帧，则不改变remaining_time的值，依旧是0.01。1234567891011121314151617181920212223static void refresh_loop_wait_event(VideoState *is, SDL_Event *event) &#123; double remaining_time = 0.0; SDL_PumpEvents(); // 如果获取到事件，则会直接返回，不会进入while while ( !SDL_PeepEvents(event, 1, SDL_GETEVENT, SDL_FIRSTEVENT, SDL_LASTEVENT)) &#123; // 如果没有获取到事件，则会尝试更新视频渲染 if (!cursor_hidden &amp;&amp; av_gettime_relative() - cursor_last_shown &gt; CURSOR_HIDE_DELAY) &#123; SDL_ShowCursor(0); cursor_hidden = 1; &#125; // 第二次之后刷新时，就会先睡个一定时间，这个时间由音视频同步机制计算得出 if (remaining_time &gt; 0.0) av_usleep((int64_t)(remaining_time * 1000000.0)); // 默认刷新间隔是0.01s remaining_time = REFRESH_RATE; // 如果当前是显示视频模式，并且 当前没有暂停或者当前强制刷新 if (is-&gt;show_mode != SHOW_MODE_NONE &amp;&amp; (!is-&gt;paused || is-&gt;force_refresh)) video_refresh(is, &amp;remaining_time); // 刷新视频 SDL_PumpEvents(); &#125;&#125; 刷新视频主要逻辑是计算当前显示的帧是否显示结束，如果显示结束，那么下一帧是否应该显示，根据音视频同步机制，进行丢帧或者对当前帧的结束时间进行缩短和增长。 音视频同步机制就是通过两个时钟的差值，来调整当前显示帧的结束时间，也就是frame_timer的值，进而调整remaining_time的值，也就是线程等待的时间。当视频时钟慢，则减小当前帧显示的时间，当视频时钟快，则加大当前帧显示的时间。 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384858687888990919293949596static void video_refresh(void *opaque, double *remaining_time) &#123; VideoState *is = opaque; double time; Frame *sp, *sp2; ... if (is-&gt;video_st) &#123; retry: if (frame_queue_nb_remaining(&amp;is-&gt;pictq) == 0) &#123; // 如果当前Frame队列中没有数据，则什么也不干 // nothing to do, no picture to display in the queue &#125; else &#123; double last_duration, duration, delay; Frame *vp, *lastvp; /* dequeue the picture */ // 获取当前已经在显示的帧，可以认为是上一帧 lastvp = frame_queue_peek_last(&amp;is-&gt;pictq); // 获取当前帧 vp = frame_queue_peek(&amp;is-&gt;pictq); // 如果当前帧的序号不符合，则丢弃 if (vp-&gt;serial != is-&gt;videoq.serial) &#123; // 读取移动到下一位，并重试 frame_queue_next(&amp;is-&gt;pictq); goto retry; &#125; // 如果上一帧与当前帧的序号不符合，重新更新播放时间frame_timer为当前系统时间 if (lastvp-&gt;serial != vp-&gt;serial) is-&gt;frame_timer = av_gettime_relative() / 1000000.0; // 如果暂停了，则继续显示当前显示的帧 if (is-&gt;paused) goto display; /* compute nominal last_duration */ // 获取上一帧要显示的时长 last_duration = vp_duration(is, lastvp, vp); // 计算音视频同步的延迟时差 delay = compute_target_delay(last_duration, is); time = av_gettime_relative() / 1000000.0; // frame_timer 当前显示的帧要结束的时间 // frame_timer + delay = 当前显示的帧真正要结束的时间 // 如果上一帧还没有结束，则继续显示上一帧 if (time &lt; is-&gt;frame_timer + delay) &#123; // frame_timer + delay - time 就是当前显示的帧结束的时间与当前时间的差值，也就是线程要wait的时间 *remaining_time = FFMIN(is-&gt;frame_timer + delay - time, *remaining_time); goto display; &#125; // 如果上一帧帧的显示时间已经过去了 // 更新frame_timer，指向的是当前显示的帧结束的时间 is-&gt;frame_timer += delay; if (delay &gt; 0 &amp;&amp; time - is-&gt;frame_timer &gt; AV_SYNC_THRESHOLD_MAX) is-&gt;frame_timer = time; // 与系统时间偏离太大，则更新为系统时间 SDL_LockMutex(is-&gt;pictq.mutex); if (!isnan(vp-&gt;pts)) update_video_pts(is, vp-&gt;pts, vp-&gt;pos, vp-&gt;serial); SDL_UnlockMutex(is-&gt;pictq.mutex); // 如果队列中还有剩余的帧 if (frame_queue_nb_remaining(&amp;is-&gt;pictq) &gt; 1) &#123; // 获取下一个帧 Frame *nextvp = frame_queue_peek_next(&amp;is-&gt;pictq); // 计算当前帧的显示时长 duration = vp_duration(is, vp, nextvp); // time &gt; is-&gt;frame_timer + duration // 如果下一帧的时间还没有过去，则丢弃当前帧，队列读索引向后移动一位，重试 if (!is-&gt;step &amp;&amp; (framedrop &gt; 0 || (framedrop &amp;&amp; get_master_sync_type(is) != AV_SYNC_VIDEO_MASTER)) &amp;&amp; time &gt; is-&gt;frame_timer + duration) &#123; is-&gt;frame_drops_late++; frame_queue_next(&amp;is-&gt;pictq); goto retry; &#125; &#125; ... // 队列向后移动一位 frame_queue_next(&amp;is-&gt;pictq); // 强制刷新 is-&gt;force_refresh = 1; if (is-&gt;step &amp;&amp; !is-&gt;paused) stream_toggle_pause(is); &#125; display: /* display picture */ // 显示当前帧 if (!display_disable &amp;&amp; is-&gt;force_refresh &amp;&amp; is-&gt;show_mode == SHOW_MODE_VIDEO &amp;&amp; is-&gt;pictq.rindex_shown) video_display(is); &#125; is-&gt;force_refresh = 0; ...&#125; 显示视频更新纹理数据，刷新屏幕1234567891011121314static void video_display(VideoState *is) &#123; if (!is-&gt;width) video_open(is); // 设置颜色 黑色 SDL_SetRenderDrawColor(renderer, 0, 0, 0, 255); // 使用黑色清空缓冲区 SDL_RenderClear(renderer); if (is-&gt;audio_st &amp;&amp; is-&gt;show_mode != SHOW_MODE_VIDEO) video_audio_display(is); // 如果是音频播放模式，显示音频声波图 else if (is-&gt;video_st) video_image_display(is);// 更新纹理数据 // 将渲染的内容刷新到屏幕上 SDL_RenderPresent(renderer);&#125; 更新纹理数据1234567891011121314151617181920212223242526272829303132static void video_image_display(VideoState *is) &#123; Frame *vp; Frame *sp = NULL; SDL_Rect rect; // 从视频原始数据队列中取出一帧，这里没有调用frame_queue_peek // 是因为更新纹理有两种情况：一种是继续显示当前帧，这种使用frame_queue_peek_last正好，另一种是显示下一帧，在这种情况下，会提前调用frame_queue_next移动，所以这里直接调用frame_queue_peek_last，不管哪种情况都是合适的。 vp = frame_queue_peek_last(&amp;is-&gt;pictq); ... // 计算显示的矩阵 calculate_display_rect(&amp;rect, is-&gt;xleft, is-&gt;ytop, is-&gt;width, is-&gt;height, vp-&gt;width, vp-&gt;height, vp-&gt;sar); // 重复显示当前在显示的帧时，没必要重复上传数据到纹理中 if (!vp-&gt;uploaded) &#123; // 如果视频帧还有没更新到纹理上 if (upload_texture(&amp;is-&gt;vid_texture, vp-&gt;frame, &amp;is-&gt;img_convert_ctx) &lt; 0) return; // 标记已经更新 vp-&gt;uploaded = 1; // TODO vp-&gt;flip_v = vp-&gt;frame-&gt;linesize[0] &lt; 0; &#125; // 设置YUV转换RGB的转换公式 set_sdl_yuv_conversion_mode(vp-&gt;frame); // 将纹理上的内容拷贝到渲染器的默认渲染目标上 SDL_RenderCopyEx(renderer, is-&gt;vid_texture, NULL, &amp;rect, 0, NULL, vp-&gt;flip_v ? SDL_FLIP_VERTICAL : 0); set_sdl_yuv_conversion_mode(NULL); ...&#125; 音频渲染线程音频渲染线程是由SDL创建的，会在该线程中执行我们设置的回调函数。 回调函数在回调函数中主要处理两件事，一件就是从FrameQueue中取出len长度的数据放入到stream中，交给音频设备进行播放；另一件事就是更新音频时钟。 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869/** * 音频渲染线程 */static void sdl_audio_callback(void *opaque, Uint8 *stream, int len) &#123; VideoState *is = opaque; int audio_size, len1; audio_callback_time = av_gettime_relative(); while (len &gt; 0) &#123; // 如果audio_buf中数据已经全部读完，则说明需要加入更多的数据 if (is-&gt;audio_buf_index &gt;= is-&gt;audio_buf_size) &#123; // 获取buffer到 is-&gt;audio_buf 中，返回获取的buffer大小 audio_size = audio_decode_frame(is); if (audio_size &lt; 0) &#123; /* if error, just output silence */ is-&gt;audio_buf = NULL; is-&gt;audio_buf_size = SDL_AUDIO_MIN_BUFFER_SIZE / is-&gt;audio_tgt.frame_size * is-&gt;audio_tgt.frame_size; &#125; else &#123; if (is-&gt;show_mode != SHOW_MODE_VIDEO) update_sample_display(is, (int16_t *)is-&gt;audio_buf, audio_size); // 读取的buffer大小 is-&gt;audio_buf_size = audio_size; &#125; // 已经读取的buffer位置重置 is-&gt;audio_buf_index = 0; &#125; // 计算还未读取的字节大小 len1 = is-&gt;audio_buf_size - is-&gt;audio_buf_index; // 如果为读取的字节大小大于音频设备想要的大小，则使用音频设备想要的大小 if (len1 &gt; len) len1 = len; // 将audio_buf中的数据copy到stream中 if (!is-&gt;muted &amp;&amp; is-&gt;audio_buf &amp;&amp; is-&gt;audio_volume == SDL_MIX_MAXVOLUME) memcpy(stream, (uint8_t *)is-&gt;audio_buf + is-&gt;audio_buf_index, len1); else &#123; memset(stream, 0, len1); if (!is-&gt;muted &amp;&amp; is-&gt;audio_buf) SDL_MixAudioFormat(stream, (uint8_t *)is-&gt;audio_buf + is-&gt;audio_buf_index, AUDIO_S16SYS, len1, is-&gt;audio_volume); &#125; // 减去已经传递给音频设备的buffer大小，如果剩下的还有，则再次读取传递 len -= len1; // stream加上偏移量 stream += len1; // 更新audio_buf的偏移量 is-&gt;audio_buf_index += len1; &#125; // 记录audio_buf中还剩余的大小 is-&gt;audio_write_buf_size = is-&gt;audio_buf_size - is-&gt;audio_buf_index; /* Let's assume the audio driver that is used by SDL has two periods. */ // 更新音频时钟 if (!isnan(is-&gt;audio_clock)) &#123; // 音频时钟中保存的是当前audio_buf开始的时间+时长，也就是结束的时间 // 真正播放的准确时间应该要减去缓冲区中未使用的数据的时长，才是真正当前播放的时长 // 缓冲区中未使用的字节大小 = 2 * is-&gt;audio_hw_buf_size 音频设备的缓冲区大小 + is-&gt;audio_write_buf_size audio_buf中还剩余的大小 // 缓冲区中未使用的时长 = 缓冲区中未使用的字节大小 / （音频设备）1s数据的字节大小 // 音频时钟准确时间 = 当前音频包结束时长 - 音频缓冲中剩余的数据时长 set_clock_at(&amp;is-&gt;audclk, is-&gt;audio_clock - (double)(2 * is-&gt;audio_hw_buf_size + is-&gt;audio_write_buf_size) / is-&gt;audio_tgt.bytes_per_sec, is-&gt;audio_clock_serial, audio_callback_time / 1000000.0); sync_clock_to_slave(&amp;is-&gt;extclk, &amp;is-&gt;audclk); &#125;&#125; 重采样如果音频数据参数与开启音频设备的参数不一致，那么是无法播放的，需要进行重采样123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384858687888990919293949596979899100101102103104105106107108109110111112113114115116static int audio_decode_frame(VideoState *is) &#123; int data_size, resampled_data_size; int64_t dec_channel_layout; av_unused double audio_clock0; int wanted_nb_samples; Frame *af; if (is-&gt;paused) return -1; // 循环从音频原始数据列表中获取一个可读帧，如果帧的序号与音频编码数据队列中的序号不一样，则丢帧 do &#123; ... // 从音频原始数据列表中获取一个可读帧 if (!(af = frame_queue_peek_readable(&amp;is-&gt;sampq))) return -1; // 队列移动到下一个可读帧 frame_queue_next(&amp;is-&gt;sampq); &#125; while (af-&gt;serial != is-&gt;audioq.serial); // 根据音频参数计算一帧的字节大小 data_size = av_samples_get_buffer_size( NULL, af-&gt;frame-&gt;channels, af-&gt;frame-&gt;nb_samples, af-&gt;frame-&gt;format, 1); dec_channel_layout = (af-&gt;frame-&gt;channel_layout &amp;&amp; af-&gt;frame-&gt;channels == av_get_channel_layout_nb_channels(af-&gt;frame-&gt;channel_layout)) ? af-&gt;frame-&gt;channel_layout : av_get_default_channel_layout(af-&gt;frame-&gt;channels); wanted_nb_samples = synchronize_audio(is, af-&gt;frame-&gt;nb_samples); // 如果音频数据的参数发生了变化， audio_src 默认是音频设备开启时的参数 // 只有当音频数据参数发生了变化才会重新创建重采样上下文 if (af-&gt;frame-&gt;format != is-&gt;audio_src.fmt || dec_channel_layout != is-&gt;audio_src.channel_layout || af-&gt;frame-&gt;sample_rate != is-&gt;audio_src.freq || (wanted_nb_samples != af-&gt;frame-&gt;nb_samples &amp;&amp; !is-&gt;swr_ctx)) &#123; swr_free(&amp;is-&gt;swr_ctx); // 创建重采样上下文，将音频重采样成音频设备开启的参数 is-&gt;swr_ctx = swr_alloc_set_opts(NULL, is-&gt;audio_tgt.channel_layout, is-&gt;audio_tgt.fmt, is-&gt;audio_tgt.freq, dec_channel_layout, af-&gt;frame-&gt;format, af-&gt;frame-&gt;sample_rate, 0, NULL); ... // 记录当前帧的参数作为音频源的参数，如果后面的帧的参数发生了变化，则会重新创建重采样上下文 is-&gt;audio_src.channel_layout = dec_channel_layout; is-&gt;audio_src.channels = af-&gt;frame-&gt;channels; is-&gt;audio_src.freq = af-&gt;frame-&gt;sample_rate; is-&gt;audio_src.fmt = af-&gt;frame-&gt;format; &#125; // 重采样 if (is-&gt;swr_ctx) &#123; // 输入 const uint8_t **in = (const uint8_t **)af-&gt;frame-&gt;extended_data; // 输出 uint8_t **out = &amp;is-&gt;audio_buf1; // 计算重采样后AVFrame中具体有几帧数据 int out_count = (int64_t)wanted_nb_samples * is-&gt;audio_tgt.freq / af-&gt;frame-&gt;sample_rate + 256; // 计算重采样后的数据大小 int out_size = av_samples_get_buffer_size(NULL, is-&gt;audio_tgt.channels, out_count, is-&gt;audio_tgt.fmt, 0); int len2; if (out_size &lt; 0) &#123; av_log(NULL, AV_LOG_ERROR, "av_samples_get_buffer_size() failed\n"); return -1; &#125; // 如果想要的帧数和AVFrame中的帧数不符合 if (wanted_nb_samples != af-&gt;frame-&gt;nb_samples) &#123; if (swr_set_compensation(is-&gt;swr_ctx, (wanted_nb_samples - af-&gt;frame-&gt;nb_samples) * is-&gt;audio_tgt.freq / af-&gt;frame-&gt;sample_rate, wanted_nb_samples * is-&gt;audio_tgt.freq / af-&gt;frame-&gt;sample_rate) &lt; 0) &#123; av_log(NULL, AV_LOG_ERROR, "swr_set_compensation() failed\n"); return -1; &#125; &#125; // 分配输出buffer的内存 av_fast_malloc(&amp;is-&gt;audio_buf1, &amp;is-&gt;audio_buf1_size, out_size); if (!is-&gt;audio_buf1) return AVERROR(ENOMEM); // 重采样 len2 = swr_convert(is-&gt;swr_ctx, out, out_count, in, af-&gt;frame-&gt;nb_samples); if (len2 &lt; 0) &#123; av_log(NULL, AV_LOG_ERROR, "swr_convert() failed\n"); return -1; &#125; if (len2 == out_count) &#123; av_log(NULL, AV_LOG_WARNING, "audio buffer is probably too small\n"); if (swr_init(is-&gt;swr_ctx) &lt; 0) swr_free(&amp;is-&gt;swr_ctx); &#125; // 获取buffer is-&gt;audio_buf = is-&gt;audio_buf1; // 计算重采样后数据大小 resampled_data_size = len2 * is-&gt;audio_tgt.channels * av_get_bytes_per_sample(is-&gt;audio_tgt.fmt); &#125; else &#123; // 不需要重采样，则直接使用AVFrame中的data // 获取buffer is-&gt;audio_buf = af-&gt;frame-&gt;data[0]; // 获取buffer大小 resampled_data_size = data_size; &#125; audio_clock0 = is-&gt;audio_clock; /* update the audio clock with the pts */ // 更新音频时钟，音频时钟 = 当前帧的PTS + （样本数量 / 每秒采样率） 也就是这些数据时长是多少秒 if (!isnan(af-&gt;pts)) is-&gt;audio_clock = af-&gt;pts + (double)af-&gt;frame-&gt;nb_samples / af-&gt;frame-&gt;sample_rate; else is-&gt;audio_clock = NAN; // 更新音频时钟序号 is-&gt;audio_clock_serial = af-&gt;serial; return resampled_data_size;&#125;]]></content>
      <categories>
        <category>拳掌指腿（音视频）</category>
        <category>降龙十八掌（FFmpeg）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[音视频开发之ffplay解码线程分析]]></title>
    <url>%2F%E9%9F%B3%E8%A7%86%E9%A2%91%E5%BC%80%E5%8F%91%E4%B9%8Bffplay%E8%A7%A3%E7%A0%81%E7%BA%BF%E7%A8%8B%E5%88%86%E6%9E%90%2F</url>
    <content type="text"><![CDATA[视频解码线程轮询不断从Packet队列中取出一帧数据进行解码，然后将解码数据放入到Frame队列中。123456789101112131415161718192021for (;;) &#123; // 获取一帧解码数据 ret = get_video_frame(is, frame); if (ret &lt; 0) goto the_end; if (!ret) continue; ... // 根据帧率计算每帧的显示时长 duration = (frame_rate.num &amp;&amp; frame_rate.den ? av_q2d((AVRational)&#123;frame_rate.den, frame_rate.num&#125;) : 0); // 将帧的PTS转换为秒 pts = (frame-&gt;pts == AV_NOPTS_VALUE) ? NAN : frame-&gt;pts * av_q2d(tb); // 将解码后的帧数据放入到视频原始数据队列中 ret = queue_picture(is, frame, pts, duration, frame-&gt;pkt_pos, is-&gt;viddec.pkt_serial); av_frame_unref(frame); ... if (ret &lt; 0) goto the_end;&#125; 解码解码123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384858687888990919293949596979899100101102103104105106107108109110111112static int decoder_decode_frame(Decoder *d, AVFrame *frame, AVSubtitle *sub) &#123; int ret = AVERROR(EAGAIN); // 轮询解码，直到解码一帧成功 // 1. 先尝试从解码器中获取，获取成功则直接返回 // 2. 不成功，则从Packet队列中获取一帧数据，顺便丢弃一些过时数据 // 3. 将获取的一帧数据送给解码器进行解码 for (;;) &#123; AVPacket pkt; // 比较Packet队列的序号是否相同，是否是过时的数据 if (d-&gt;queue-&gt;serial == d-&gt;pkt_serial) &#123; // 不断的尝试从解码器中获取已经解码完成的帧，直到获取成功 do &#123; // Packet队列是否退出 if (d-&gt;queue-&gt;abort_request) return -1; // 尝试从解码器中获取已经解码完成的帧 switch (d-&gt;avctx-&gt;codec_type) &#123; case AVMEDIA_TYPE_VIDEO: // 从解码器中获取解码完成的视频帧 ret = avcodec_receive_frame(d-&gt;avctx, frame); if (ret &gt;= 0) &#123; // 获取成功，重置PTS if (decoder_reorder_pts == -1) &#123; frame-&gt;pts = frame-&gt;best_effort_timestamp; &#125; else if (!decoder_reorder_pts) &#123; frame-&gt;pts = frame-&gt;pkt_dts; &#125; &#125; break; case AVMEDIA_TYPE_AUDIO: // 从解码器中获取解码完成的音频帧 ret = avcodec_receive_frame(d-&gt;avctx, frame); if (ret &gt;= 0) &#123; AVRational tb = (AVRational)&#123;1, frame-&gt;sample_rate&#125;; // 获取成功，重置PTS if (frame-&gt;pts != AV_NOPTS_VALUE) frame-&gt;pts = av_rescale_q(frame-&gt;pts, d-&gt;avctx-&gt;pkt_timebase, tb); else if (d-&gt;next_pts != AV_NOPTS_VALUE) frame-&gt;pts = av_rescale_q(d-&gt;next_pts, d-&gt;next_pts_tb, tb); if (frame-&gt;pts != AV_NOPTS_VALUE) &#123; d-&gt;next_pts = frame-&gt;pts + frame-&gt;nb_samples; d-&gt;next_pts_tb = tb; &#125; &#125; break; &#125; if (ret == AVERROR_EOF) &#123; // 如果流解码结束，记录本次结束的Packet队列的序号 d-&gt;finished = d-&gt;pkt_serial; // 刷新解码上下文，以便再次解码，不刷新则不能再解码了 avcodec_flush_buffers(d-&gt;avctx); return 0; &#125; // 如果获取成功，则直接返回 if (ret &gt;= 0) return 1; &#125; while (ret != AVERROR(EAGAIN)); &#125; // 如果Packet队列的序号不相同，则开始丢弃这些过时的数据 do &#123; // 如果Packet队列中已经空了，则唤醒read线程继续读取数据 if (d-&gt;queue-&gt;nb_packets == 0) SDL_CondSignal(d-&gt;empty_queue_cond); // packet_pending用于失败重新发送 if (d-&gt;packet_pending) &#123; // 重新复制pkt av_packet_move_ref(&amp;pkt, &amp;d-&gt;pkt); //去掉重发标记 d-&gt;packet_pending = 0; &#125; else &#123; // 从Packet队列中读取一个新的Packet，存在pkt中，并将获取的pkt的序号赋值给d-&gt;pkt_serial if (packet_queue_get(d-&gt;queue, &amp;pkt, 1, &amp;d-&gt;pkt_serial) &lt; 0) return -1; &#125; &#125; while (d-&gt;queue-&gt;serial != d-&gt;pkt_serial); // 如果获取到的是flush_pkt分割标记 if (pkt.data == flush_pkt.data) &#123; // 清空解码器 avcodec_flush_buffers(d-&gt;avctx); // 重新开始 d-&gt;finished = 0; // pts重新计算 d-&gt;next_pts = d-&gt;start_pts; d-&gt;next_pts_tb = d-&gt;start_pts_tb; &#125; else &#123; // 获取到正常数据 if (d-&gt;avctx-&gt;codec_type == AVMEDIA_TYPE_SUBTITLE) &#123; // 字幕数据 int got_frame = 0; ret = avcodec_decode_subtitle2(d-&gt;avctx, sub, &amp;got_frame, &amp;pkt); if (ret &lt; 0) &#123; ret = AVERROR(EAGAIN); &#125; else &#123; if (got_frame &amp;&amp; !pkt.data) &#123; d-&gt;packet_pending = 1; av_packet_move_ref(&amp;d-&gt;pkt, &amp;pkt); &#125; ret = got_frame ? 0 : (pkt.data ? AVERROR(EAGAIN) : AVERROR_EOF); &#125; &#125; else &#123; // 音频或视频数据，发送给解码器进行解码 if (avcodec_send_packet(d-&gt;avctx, &amp;pkt) == AVERROR(EAGAIN)) &#123; av_log(d-&gt;avctx, AV_LOG_ERROR, "Receive_frame and send_packet both returned EAGAIN, which is " "an API violation.\n"); // 解码失败，标记重发 d-&gt;packet_pending = 1; // 将Packet还给pkt对象，用于下次重新发送 av_packet_move_ref(&amp;d-&gt;pkt, &amp;pkt); &#125; &#125; av_packet_unref(&amp;pkt); &#125; &#125;&#125; 丢帧解码完成后，对过时的帧进行丢弃123456789101112131415161718192021222324252627282930313233343536373839static int get_video_frame(VideoState *is, AVFrame *frame) &#123; int got_picture; // 解码，将解码的结果放入到frame中 if ((got_picture = decoder_decode_frame(&amp;is-&gt;viddec, frame, NULL)) &lt; 0) return -1; if (got_picture) &#123; // 解码成功 double dpts = NAN; // 将当前帧的PTS转换成秒 if (frame-&gt;pts != AV_NOPTS_VALUE) dpts = av_q2d(is-&gt;video_st-&gt;time_base) * frame-&gt;pts; // 采样横纵比 frame-&gt;sample_aspect_ratio = av_guess_sample_aspect_ratio(is-&gt;ic, is-&gt;video_st, frame); // 丢帧操作，如果同步模式不是视频为主，则判断是否要丢帧 if (framedrop &gt; 0 || (framedrop &amp;&amp; get_master_sync_type(is) != AV_SYNC_VIDEO_MASTER)) &#123; if (frame-&gt;pts != AV_NOPTS_VALUE) &#123; // 计算主时钟和当前帧要显示的时间差 double diff = dpts - get_master_clock(is); // 大致意思当当前帧的时间小于主时钟的时间时，就需要进行丢帧操作 if (!isnan(diff) &amp;&amp; fabs(diff) &lt; AV_NOSYNC_THRESHOLD &amp;&amp; diff - is-&gt;frame_last_filter_delay &lt; 0 &amp;&amp; is-&gt;viddec.pkt_serial == is-&gt;vidclk.serial &amp;&amp; is-&gt;videoq.nb_packets) &#123; // 丢帧次数+1 is-&gt;frame_drops_early++; // 是否frame av_frame_unref(frame); // 标记未获取到解码数据 got_picture = 0; &#125; &#125; &#125; &#125; return got_picture;&#125; 入队frame_queue_peek_writable方法中，如果Frame队列已经满了，则会进行阻塞，否则返回一个可写的Frame对象，将数据写入到Frame对象中，调用frame_queue_push通知Frame队列写入完成，整个入队操作就完成了。 1234567891011121314151617181920212223242526static int queue_picture(VideoState *is, AVFrame *src_frame, double pts, double duration, int64_t pos, int serial) &#123; Frame *vp; // 从视频原始数据队列中获取一个可以写入的帧，vp if (!(vp = frame_queue_peek_writable(&amp;is-&gt;pictq))) return -1; // 采样横纵比 vp-&gt;sar = src_frame-&gt;sample_aspect_ratio; vp-&gt;uploaded = 0; // w h 像素格式 vp-&gt;width = src_frame-&gt;width; vp-&gt;height = src_frame-&gt;height; vp-&gt;format = src_frame-&gt;format; // pts 时长 序号 vp-&gt;pts = pts; vp-&gt;duration = duration; vp-&gt;pos = pos; vp-&gt;serial = serial; // 更改窗口的大小 set_default_window_size(vp-&gt;width, vp-&gt;height, vp-&gt;sar); // 将AVFrame数据复制到vp中 av_frame_move_ref(vp-&gt;frame, src_frame); // 通知视频原始数据队列，数据已经写入完成 frame_queue_push(&amp;is-&gt;pictq); return 0;&#125; 音频解码线程解码和入队的逻辑和视频一样，调用decoder_decode_frame进行解码，解码成功则got_frame为1，再调用frame_queue_peek_writable和frame_queue_push进行入队。 1234567891011121314151617181920212223242526272829303132do &#123; // 解码，将解码的结果放入到frame中 if ((got_frame = decoder_decode_frame(&amp;is-&gt;auddec, frame, NULL)) &lt; 0) goto the_end; if (got_frame) &#123; // 解码成功 // 音频帧的采样率 tb = (AVRational)&#123;1, frame-&gt;sample_rate&#125;; ... // 从音频的原始数据队列中取出一个可写入的帧 af, // 如果返回null，则表明Packet队列已经退出，不会再有数据增加了，直接结束解码 if (!(af = frame_queue_peek_writable(&amp;is-&gt;sampq))) goto the_end; // 设置帧的PTS af-&gt;pts = (frame-&gt;pts == AV_NOPTS_VALUE) ? NAN : frame-&gt;pts * av_q2d(tb); // 设置帧在流中的位置 af-&gt;pos = frame-&gt;pkt_pos; // 设置帧的序号 af-&gt;serial = is-&gt;auddec.pkt_serial; // 设置帧的时长 af-&gt;duration = av_q2d((AVRational)&#123;frame-&gt;nb_samples, frame-&gt;sample_rate&#125;); // 将帧数据赋值到af中 av_frame_move_ref(af-&gt;frame, frame); // 通知音频的原始数据队列，数据已经成功写入 frame_queue_push(&amp;is-&gt;sampq); ... &#125;&#125; while (ret &gt;= 0 || ret == AVERROR(EAGAIN) || ret == AVERROR_EOF);]]></content>
      <categories>
        <category>拳掌指腿（音视频）</category>
        <category>降龙十八掌（FFmpeg）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[音视频开发之ffplay解复用线程分析]]></title>
    <url>%2F%E9%9F%B3%E8%A7%86%E9%A2%91%E5%BC%80%E5%8F%91%E4%B9%8Bffplay%E8%A7%A3%E5%A4%8D%E7%94%A8%E7%BA%BF%E7%A8%8B%E5%88%86%E6%9E%90%2F</url>
    <content type="text"><![CDATA[由主线程创建，负责媒体文件的解复用和读取，读取的数据根据流类型放入到对应的编码数据队列中 查询流信息创建解复用上下文1234567891011121314151617181920212223242526272829// 创建解复用上下文ic = avformat_alloc_context();if (!ic) &#123; ...&#125;// 设置中断回调ic-&gt;interrupt_callback.callback = decode_interrupt_cb;// 回调函数的参数ic-&gt;interrupt_callback.opaque = is;...// 打开输入err = avformat_open_input(&amp;ic, is-&gt;filename, is-&gt;iformat, &amp;format_opts);...is-&gt;ic = ic;if (genpts) ic-&gt;flags |= AVFMT_FLAG_GENPTS;av_format_inject_global_side_data(ic);if (find_stream_info) &#123; AVDictionary **opts = setup_find_stream_info_opts(ic, codec_opts); int orig_nb_streams = ic-&gt;nb_streams; // 读取文件头，获取文件的流详细信息 err = avformat_find_stream_info(ic, opts); ...&#125; 查找流索引查找音频和视频流的索引123456789101112131415161718192021222324252627282930for (i = 0; i &lt; ic-&gt;nb_streams; i++) &#123; AVStream *st = ic-&gt;streams[i]; enum AVMediaType type = st-&gt;codecpar-&gt;codec_type; st-&gt;discard = AVDISCARD_ALL; if (type &gt;= 0 &amp;&amp; wanted_stream_spec[type] &amp;&amp; st_index[type] == -1) if (avformat_match_stream_specifier(ic, st, wanted_stream_spec[type]) &gt; 0) st_index[type] = i;&#125;for (i = 0; i &lt; AVMEDIA_TYPE_NB; i++) &#123; if (wanted_stream_spec[i] &amp;&amp; st_index[i] == -1) &#123; av_log(NULL, AV_LOG_ERROR, "Stream specifier %s does not match any %s stream\n", wanted_stream_spec[i], av_get_media_type_string(i)); st_index[i] = INT_MAX; &#125;&#125;// 查找流索引if (!video_disable) st_index[AVMEDIA_TYPE_VIDEO] = av_find_best_stream( ic, AVMEDIA_TYPE_VIDEO, st_index[AVMEDIA_TYPE_VIDEO], -1, NULL, 0);if (!audio_disable) st_index[AVMEDIA_TYPE_AUDIO] = av_find_best_stream( ic, AVMEDIA_TYPE_AUDIO, st_index[AVMEDIA_TYPE_AUDIO], st_index[AVMEDIA_TYPE_VIDEO], NULL, 0);if (!video_disable &amp;&amp; !subtitle_disable) st_index[AVMEDIA_TYPE_SUBTITLE] = av_find_best_stream( ic, AVMEDIA_TYPE_SUBTITLE, st_index[AVMEDIA_TYPE_SUBTITLE], (st_index[AVMEDIA_TYPE_AUDIO] &gt;= 0 ? st_index[AVMEDIA_TYPE_AUDIO] : st_index[AVMEDIA_TYPE_VIDEO]), NULL, 0); 开启输入流创建解码上下文1234567891011121314151617181920// 创建解码上下文avctx = avcodec_alloc_context3(NULL);if (!avctx) return AVERROR(ENOMEM);// 设置解码上下文参数ret = avcodec_parameters_to_context(avctx, ic-&gt;streams[stream_index]-&gt;codecpar);if (ret &lt; 0) goto fail;// 设置解码上下文时间基avctx-&gt;pkt_timebase = ic-&gt;streams[stream_index]-&gt;time_base;// 查找解码器codec = avcodec_find_decoder(avctx-&gt;codec_id);...avctx-&gt;codec_id = codec-&gt;id;...// 开启编码器上下文if ((ret = avcodec_open2(avctx, codec, &amp;opts)) &lt; 0) &#123; goto fail;&#125; 开启音频设备使用SDL_OpenAudioDevice开启音频设备，如果失败，则不断更换音频参数组合来重试123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384858687888990919293949596979899100static int audio_open(void *opaque, int64_t wanted_channel_layout, int wanted_nb_channels, int wanted_sample_rate, struct AudioParams *audio_hw_params) &#123; SDL_AudioSpec wanted_spec, spec; const char *env; static const int next_nb_channels[] = &#123;0, 0, 1, 6, 2, 6, 4, 6&#125;; static const int next_sample_rates[] = &#123;0, 44100, 48000, 96000, 192000&#125;; int next_sample_rate_idx = FF_ARRAY_ELEMS(next_sample_rates) - 1; // 设置SDL音频播放参数 env = SDL_getenv("SDL_AUDIO_CHANNELS"); if (env) &#123; wanted_nb_channels = atoi(env); wanted_channel_layout = av_get_default_channel_layout(wanted_nb_channels); &#125; if (!wanted_channel_layout || wanted_nb_channels != av_get_channel_layout_nb_channels(wanted_channel_layout)) &#123; wanted_channel_layout = av_get_default_channel_layout(wanted_nb_channels); wanted_channel_layout &amp;= ~AV_CH_LAYOUT_STEREO_DOWNMIX; &#125; // 设置 采样率、声道数 wanted_nb_channels = av_get_channel_layout_nb_channels(wanted_channel_layout); wanted_spec.channels = wanted_nb_channels; wanted_spec.freq = wanted_sample_rate; if (wanted_spec.freq &lt;= 0 || wanted_spec.channels &lt;= 0) &#123; av_log(NULL, AV_LOG_ERROR, "Invalid sample rate or channel count!\n"); return -1; &#125; while (next_sample_rate_idx &amp;&amp; next_sample_rates[next_sample_rate_idx] &gt;= wanted_spec.freq) next_sample_rate_idx--; // 采样位数 wanted_spec.format = AUDIO_S16SYS; // 静音 wanted_spec.silence = 0; // 样本数量 wanted_spec.samples = FFMAX(SDL_AUDIO_MIN_BUFFER_SIZE, 2 &lt;&lt; av_log2(wanted_spec.freq / SDL_AUDIO_MAX_CALLBACKS_PER_SEC)); // 回调函数 wanted_spec.callback = sdl_audio_callback; // 回调函数参数 wanted_spec.userdata = opaque; // 开启音频播放设备，如果开启失败，则更换参数不断重试 while ( !(audio_dev = SDL_OpenAudioDevice(NULL, 0, &amp;wanted_spec, &amp;spec, SDL_AUDIO_ALLOW_FREQUENCY_CHANGE | SDL_AUDIO_ALLOW_CHANNELS_CHANGE))) &#123; av_log(NULL, AV_LOG_WARNING, "SDL_OpenAudio (%d channels, %d Hz): %s\n", wanted_spec.channels, wanted_spec.freq, SDL_GetError()); wanted_spec.channels = next_nb_channels[FFMIN(7, wanted_spec.channels)]; if (!wanted_spec.channels) &#123; wanted_spec.freq = next_sample_rates[next_sample_rate_idx--]; wanted_spec.channels = wanted_nb_channels; if (!wanted_spec.freq) &#123; // 如果所有组合都重试完了还是打不开则直接退出 av_log(NULL, AV_LOG_ERROR, "No more combinations to try, audio open failed\n"); return -1; &#125; &#125; wanted_channel_layout = av_get_default_channel_layout(wanted_spec.channels); &#125; // 如果开启音频设备的参数中采样位数不是16位，则退出，只支持16位 if (spec.format != AUDIO_S16SYS) &#123; av_log(NULL, AV_LOG_ERROR, "SDL advised audio format %d is not supported!\n", spec.format); return -1; &#125; if (spec.channels != wanted_spec.channels) &#123; wanted_channel_layout = av_get_default_channel_layout(spec.channels); if (!wanted_channel_layout) &#123; av_log(NULL, AV_LOG_ERROR, "SDL advised channel count %d is not supported!\n", spec.channels); return -1; &#125; &#125; // 将真正开启音频设备的参数保存到 audio_hw_params 中 // 只支持16位 audio_hw_params-&gt;fmt = AV_SAMPLE_FMT_S16; // 采样率 audio_hw_params-&gt;freq = spec.freq; // 声道布局 audio_hw_params-&gt;channel_layout = wanted_channel_layout; // 声道数 audio_hw_params-&gt;channels = spec.channels; // audio_hw_params-&gt;frame_size = av_samples_get_buffer_size( NULL, audio_hw_params-&gt;channels, 1, audio_hw_params-&gt;fmt, 1); // 1s数据的字节大小 audio_hw_params-&gt;bytes_per_sec = av_samples_get_buffer_size( NULL, audio_hw_params-&gt;channels, audio_hw_params-&gt;freq, audio_hw_params-&gt;fmt, 1); if (audio_hw_params-&gt;bytes_per_sec &lt;= 0 || audio_hw_params-&gt;frame_size &lt;= 0) &#123; av_log(NULL, AV_LOG_ERROR, "av_samples_get_buffer_size failed\n"); return -1; &#125; // 返回分配的缓冲区字节大小 return spec.size;&#125; 启动解码线程初始化Decoder结构体123456789static void decoder_init(Decoder *d, AVCodecContext *avctx, PacketQueue *queue, SDL_cond *empty_queue_cond) &#123; memset(d, 0, sizeof(Decoder)); d-&gt;avctx = avctx; d-&gt;queue = queue; d-&gt;empty_queue_cond = empty_queue_cond; d-&gt;start_pts = AV_NOPTS_VALUE; d-&gt;pkt_serial = -1;&#125; 启动解码线程123456789101112static int decoder_start(Decoder *d, int (*fn)(void *), const char *thread_name, void *arg) &#123; // 启动编码数据队列 packet_queue_start(d-&gt;queue); // 创建编码线程 d-&gt;decoder_tid = SDL_CreateThread(fn, thread_name, arg); if (!d-&gt;decoder_tid) &#123; av_log(NULL, AV_LOG_ERROR, "SDL_CreateThread(): %s\n", SDL_GetError()); return AVERROR(ENOMEM); &#125; return 0;&#125; 轮询读取流数据流开启完毕后，就开始轮询从媒体文件中读取数据放入到Packet队列中，当Packet队列中数据足够的时候，会wait等待， // 轮询从文件中读取流数据 for (;;) &#123; // 是否退出 if (is-&gt;abort_request) break; // 如果本次暂停事件没有处理，则处理暂停事件 if (is-&gt;paused != is-&gt;last_paused) &#123; // 更新最新处理的暂停事件 is-&gt;last_paused = is-&gt;paused; // 如果是暂停，就停止读取，如果是播放，则开始读取 if (is-&gt;paused) is-&gt;read_pause_return = av_read_pause(ic); else av_read_play(ic); &#125;#if CONFIG_RTSP_DEMUXER || CONFIG_MMSH_PROTOCOL // 如果是RTSP流，暂停就是等待10ms后再重试 if (is-&gt;paused &amp;&amp; (!strcmp(ic-&gt;iformat-&gt;name, "rtsp") || (ic-&gt;pb &amp;&amp; !strncmp(input_filename, "mmsh:", 5)))) &#123; /* wait 10 ms to avoid trying to get another packet */ /* XXX: horrible */ SDL_Delay(10); continue; &#125;#endif // seek操作 if (is-&gt;seek_req) &#123; // seek的位置 int64_t seek_target = is-&gt;seek_pos; // seek的区间,上下行 int64_t seek_min = is-&gt;seek_rel &gt; 0 ? seek_target - is-&gt;seek_rel + 2 : INT64_MIN; int64_t seek_max = is-&gt;seek_rel &lt; 0 ? seek_target - is-&gt;seek_rel - 2 : INT64_MAX; // FIXME the +-2 is due to rounding being not done in the correct // direction in generation // of the seek_pos/seek_rel variables // seek ret = avformat_seek_file(is-&gt;ic, -1, seek_min, seek_target, seek_max, is-&gt;seek_flags); if (ret &lt; 0) &#123; av_log(NULL, AV_LOG_ERROR, "%s: error while seeking\n", is-&gt;ic-&gt;url); &#125; else &#123; if (is-&gt;audio_stream &gt;= 0) &#123; // 重置音频编码数据队列 packet_queue_flush(&amp;is-&gt;audioq); // 队列中加入flush_pkt分割标记 packet_queue_put(&amp;is-&gt;audioq, &amp;flush_pkt); &#125; if (is-&gt;subtitle_stream &gt;= 0) &#123; // 重置字幕编码数据队列 packet_queue_flush(&amp;is-&gt;subtitleq); // 队列中加入flush_pkt分割标记 packet_queue_put(&amp;is-&gt;subtitleq, &amp;flush_pkt); &#125; if (is-&gt;video_stream &gt;= 0) &#123; // 重置视频编码数据队列 packet_queue_flush(&amp;is-&gt;videoq); // 队列中加入flush_pkt分割标记，让队列的序号+1 packet_queue_put(&amp;is-&gt;videoq, &amp;flush_pkt); &#125; // 更新外部时钟的PTS if (is-&gt;seek_flags &amp; AVSEEK_FLAG_BYTE) &#123; set_clock(&amp;is-&gt;extclk, NAN, 0); &#125; else &#123; set_clock(&amp;is-&gt;extclk, seek_target / (double)AV_TIME_BASE, 0); &#125; &#125; is-&gt;seek_req = 0; is-&gt;queue_attachments_req = 1; is-&gt;eof = 0; if (is-&gt;paused) step_to_next_frame(is); &#125; if (is-&gt;queue_attachments_req) &#123; if (is-&gt;video_st &amp;&amp; is-&gt;video_st-&gt;disposition &amp; AV_DISPOSITION_ATTACHED_PIC) &#123; AVPacket copy = &#123;0&#125;; if ((ret = av_packet_ref(&amp;copy, &amp;is-&gt;video_st-&gt;attached_pic)) &lt; 0) goto fail; packet_queue_put(&amp;is-&gt;videoq, &amp;copy); packet_queue_put_nullpacket(&amp;is-&gt;videoq, is-&gt;video_stream); &#125; is-&gt;queue_attachments_req = 0; &#125; /* if the queue are full, no need to read more */ // 如果音频编码数据队列+视频编码数据队列+字幕编码数据队列中字节数量超过了15 // * 1024 * 1024（15M） 或者说 // 音频流、视频流、字幕流中都已经读取了充足的Packet在队列中缓存着，则wait // 10ms等待 // 也就是说三个队列中缓冲数据超过15M或者三个队列中都已经有了一定的数据(1s的数据)，那么就不能继续读下去了，等待10ms后再来重试 if (infinite_buffer &lt; 1 &amp;&amp; (is-&gt;audioq.size + is-&gt;videoq.size + is-&gt;subtitleq.size &gt; MAX_QUEUE_SIZE || (stream_has_enough_packets(is-&gt;audio_st, is-&gt;audio_stream, &amp;is-&gt;audioq) &amp;&amp; stream_has_enough_packets(is-&gt;video_st, is-&gt;video_stream, &amp;is-&gt;videoq) &amp;&amp; stream_has_enough_packets(is-&gt;subtitle_st, is-&gt;subtitle_stream, &amp;is-&gt;subtitleq)))) &#123; /* wait 10 ms */ SDL_LockMutex(wait_mutex); // 等待10ms后，重新循环 SDL_CondWaitTimeout(is-&gt;continue_read_thread, wait_mutex, 10); SDL_UnlockMutex(wait_mutex); continue; &#125; // 处理播放结束 if (!is-&gt;paused &amp;&amp; (!is-&gt;audio_st || (is-&gt;auddec.finished == is-&gt;audioq.serial &amp;&amp; frame_queue_nb_remaining(&amp;is-&gt;sampq) == 0)) &amp;&amp; (!is-&gt;video_st || (is-&gt;viddec.finished == is-&gt;videoq.serial &amp;&amp; frame_queue_nb_remaining(&amp;is-&gt;pictq) == 0))) &#123; // loop = 0 表示无限循环， 否则表示循环次数，每次结束-1，直到等于1停止循环 if (loop != 1 &amp;&amp; (!loop || --loop)) &#123; stream_seek(is, start_time != AV_NOPTS_VALUE ? start_time : 0, 0, 0); &#125; else if (autoexit) &#123; ret = AVERROR_EOF; goto fail; &#125; &#125; // 读取一个Packet ret = av_read_frame(ic, pkt); if (ret &lt; 0) &#123; // 读取失败 if ((ret == AVERROR_EOF || avio_feof(ic-&gt;pb)) &amp;&amp; !is-&gt;eof) &#123; // 如果文件流已经结束，放入一个空的Packet到队列中 if (is-&gt;video_stream &gt;= 0) packet_queue_put_nullpacket(&amp;is-&gt;videoq, is-&gt;video_stream); if (is-&gt;audio_stream &gt;= 0) packet_queue_put_nullpacket(&amp;is-&gt;audioq, is-&gt;audio_stream); if (is-&gt;subtitle_stream &gt;= 0) packet_queue_put_nullpacket(&amp;is-&gt;subtitleq, is-&gt;subtitle_stream); // EOF 流结束 is-&gt;eof = 1; &#125; if (ic-&gt;pb &amp;&amp; ic-&gt;pb-&gt;error) break; SDL_LockMutex(wait_mutex); // 等待10ms SDL_CondWaitTimeout(is-&gt;continue_read_thread, wait_mutex, 10); SDL_UnlockMutex(wait_mutex); continue; &#125; else &#123; is-&gt;eof = 0; &#125; /* check if packet is in play range specified by user, then queue, otherwise * discard */ // 流的开始时间 stream_start_time = ic-&gt;streams[pkt-&gt;stream_index]-&gt;start_time; // 当前Packet的PTS pkt_ts = pkt-&gt;pts == AV_NOPTS_VALUE ? pkt-&gt;dts : pkt-&gt;pts; // 当前Packet的PTS - 流开始时间 &lt;= 播放时长duration // 则表明当前Packet是在播放范围内的 pkt_in_play_range = duration == AV_NOPTS_VALUE || (pkt_ts - (stream_start_time != AV_NOPTS_VALUE ? stream_start_time : 0)) * av_q2d(ic-&gt;streams[pkt-&gt;stream_index]-&gt;time_base) - (double)(start_time != AV_NOPTS_VALUE ? start_time : 0) / 1000000 &lt;= ((double)duration / 1000000); if (pkt-&gt;stream_index == is-&gt;audio_stream &amp;&amp; pkt_in_play_range) &#123; // 音频Packet，放入到音频编码数据队列中 packet_queue_put(&amp;is-&gt;audioq, pkt); &#125; else if (pkt-&gt;stream_index == is-&gt;video_stream &amp;&amp; pkt_in_play_range &amp;&amp; !(is-&gt;video_st-&gt;disposition &amp; AV_DISPOSITION_ATTACHED_PIC)) &#123; // 音频Packet，放入到音频编码数据队列中 packet_queue_put(&amp;is-&gt;videoq, pkt); &#125; else if (pkt-&gt;stream_index == is-&gt;subtitle_stream &amp;&amp; pkt_in_play_range) &#123; // 音频Packet，放入到音频编码数据队列中 packet_queue_put(&amp;is-&gt;subtitleq, pkt); &#125; else &#123; // 类型都不符合，则释放不处理 av_packet_unref(pkt); &#125; &#125;]]></content>
      <categories>
        <category>拳掌指腿（音视频）</category>
        <category>降龙十八掌（FFmpeg）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[音视频开发之ffplay队列分析]]></title>
    <url>%2F%E9%9F%B3%E8%A7%86%E9%A2%91%E5%BC%80%E5%8F%91%E4%B9%8Bffplay%E9%98%9F%E5%88%97%E5%88%86%E6%9E%90%2F</url>
    <content type="text"><![CDATA[ffplay的整体结构是由5个线程和4个队列组成、运转的（不分析字幕）。 整体结构 5个线程 解复用线程 : read_thread，由主线程创建，负责媒体文件的解复用和读取，读取的数据根据流类型放入到对应的编码数据队列中。 音频解码线程 : audio_thread，由read_thread创建，负责将编码数据队列中的数据解码，放入到原始数据队列中。 视频解码线程 : video_thread，由read_thread创建，负责将编码数据队列中的数据解码，放入到原始数据队列中。 音频渲染线程 : 由SDL创建，负责将音频原始数据队列中的数据发送给音频播放设备。 视频渲染线程 : main线程，负责用视频原始数据队列中的数据不断的更新纹理内容，并刷新显示器进行显示。 4个队列 FrameQueue pictq : 视频原始数据队列 FrameQueue sampq : 音频原始数据队列 PacketQueue audioq : 音频编码数据队列 PacketQueue videoq : 视频编码数据队列 PacketQueuePacket队列是基于链表实现的普通队列，由于编码数据帧比较小，所以这是个无限队列。1234567891011typedef struct PacketQueue &#123; // Packet队列采用的是链表结构 MyAVPacketList *first_pkt, *last_pkt; // 第一个节点和最后一个节点 int nb_packets; // 队列中节点个数 int size; // 队列中所有节点的字节总数 int64_t duration; // 队列中所有节点的总时长 int abort_request; // 是否退出标记 1 退出，0 不退出 int serial; // 序号 SDL_mutex *mutex; // 锁 SDL_cond *cond; // 互斥量&#125; PacketQueue; packet_queue_init初始化队列12345678910111213141516171819static int packet_queue_init(PacketQueue *q) &#123; // 将编码数据队列初始化，清零 memset(q, 0, sizeof(PacketQueue)); // 为队列创建锁 q-&gt;mutex = SDL_CreateMutex(); if (!q-&gt;mutex) &#123; av_log(NULL, AV_LOG_FATAL, "SDL_CreateMutex(): %s\n", SDL_GetError()); return AVERROR(ENOMEM); &#125; // 为队列创建锁的互斥量 q-&gt;cond = SDL_CreateCond(); if (!q-&gt;cond) &#123; av_log(NULL, AV_LOG_FATAL, "SDL_CreateCond(): %s\n", SDL_GetError()); return AVERROR(ENOMEM); &#125; // 队列设置为退出状态 q-&gt;abort_request = 1; return 0;&#125; packet_queue_start启动队列，每次启动都会在队列中插入一个分割标记，并将队列序号+112345678910static void packet_queue_start(PacketQueue *q) &#123; // 加锁 SDL_LockMutex(q-&gt;mutex); // 设置启动标记 q-&gt;abort_request = 0; // 队列中放入一个特殊的flush_pkt，用来做不连续的两段数据的分割标记 packet_queue_put_private(q, &amp;flush_pkt); // 解锁 SDL_UnlockMutex(q-&gt;mutex);&#125; packet_queue_destroy销毁队列123456789101112131415161718192021222324252627static void packet_queue_flush(PacketQueue *q) &#123; MyAVPacketList *pkt, *pkt1; // 加锁 SDL_LockMutex(q-&gt;mutex); // 遍历整个链表，将每个节点的AVPacket对象进行销毁 for (pkt = q-&gt;first_pkt; pkt; pkt = pkt1) &#123; pkt1 = pkt-&gt;next; av_packet_unref(&amp;pkt-&gt;pkt); av_freep(&amp;pkt); &#125; // 将队列重置 q-&gt;last_pkt = NULL; q-&gt;first_pkt = NULL; q-&gt;nb_packets = 0; q-&gt;size = 0; q-&gt;duration = 0; // 解锁 SDL_UnlockMutex(q-&gt;mutex);&#125;static void packet_queue_destroy(PacketQueue *q) &#123; // 销毁队列 packet_queue_flush(q); // 销毁锁 SDL_DestroyMutex(q-&gt;mutex); SDL_DestroyCond(q-&gt;cond);&#125; packet_queue_get从队列中取出头节点，block参数表明当队列为空时是否需要阻塞1234567891011121314151617181920212223242526272829303132333435363738394041424344454647static int packet_queue_get(PacketQueue *q, AVPacket *pkt, int block, int *serial) &#123; MyAVPacketList *pkt1; int ret; // 加锁 SDL_LockMutex(q-&gt;mutex); for (;;) &#123; // 如果退出标记，则退出 if (q-&gt;abort_request) &#123; ret = -1; break; &#125; // 获取队列的头节点 pkt1 = q-&gt;first_pkt; if (pkt1) &#123; // 将头结点的下一个节点作为新的头结点 q-&gt;first_pkt = pkt1-&gt;next; // 如果新头结点为空，则说明当前队列空了，将尾节点也设为空 if (!q-&gt;first_pkt) q-&gt;last_pkt = NULL; // 节点数量-1 q-&gt;nb_packets--; // 队列中总字节数减少 q-&gt;size -= pkt1-&gt;pkt.size + sizeof(*pkt1); // 队列中总时长减少 q-&gt;duration -= pkt1-&gt;pkt.duration; // 获取节点中的AVPacket，赋值给参数中的pkt指针 *pkt = pkt1-&gt;pkt; // 将节点的序号赋值给参数中的serial指针 if (serial) *serial = pkt1-&gt;serial; // 释放节点 av_free(pkt1); ret = 1; break; &#125; else if (!block) &#123; // 如果头结点为空，而block为0，则不阻塞，直接返回 ret = 0; break; &#125; else &#123; // 如果头结点为空，block为1，则wait等待 SDL_CondWait(q-&gt;cond, q-&gt;mutex); &#125; &#125; // 解锁 SDL_UnlockMutex(q-&gt;mutex); return ret;&#125; packet_queue_put在队列尾部插入新节点12345678910111213141516171819202122232425262728293031323334353637383940414243444546static int packet_queue_put_private(PacketQueue *q, AVPacket *pkt) &#123; // 创建节点 MyAVPacketList *pkt1; // 如果退出，则返回 if (q-&gt;abort_request) return -1; pkt1 = av_malloc(sizeof(MyAVPacketList)); if (!pkt1) return -1; // 将AVPacket保存到节点中 pkt1-&gt;pkt = *pkt; pkt1-&gt;next = NULL; // 如果加入的数据是flush_pkt标记，则序号+1 if (pkt == &amp;flush_pkt) q-&gt;serial++; pkt1-&gt;serial = q-&gt;serial; // 如果队列中尾节点为空，则将当前节点作为头结点，否则，将当前节点作为尾节点的下一个节点 if (!q-&gt;last_pkt) q-&gt;first_pkt = pkt1; else q-&gt;last_pkt-&gt;next = pkt1; // 将当前节点作为队列新的尾节点 q-&gt;last_pkt = pkt1; // 节点数量+1 q-&gt;nb_packets++; // 队列中总字节数增加 q-&gt;size += pkt1-&gt;pkt.size + sizeof(*pkt1); // 队列中总时长增加 q-&gt;duration += pkt1-&gt;pkt.duration; /* XXX: should duplicate packet data in DV case */ // 唤醒wait线程 SDL_CondSignal(q-&gt;cond); return 0;&#125;static int packet_queue_put(PacketQueue *q, AVPacket *pkt) &#123; int ret; // 加锁 SDL_LockMutex(q-&gt;mutex); // 入队列 ret = packet_queue_put_private(q, pkt); // 解锁 SDL_UnlockMutex(q-&gt;mutex); // 如果失败，则释放AVPacket if (pkt != &amp;flush_pkt &amp;&amp; ret &lt; 0) av_packet_unref(pkt); return ret;&#125; FrameQueueFrame队列与Packet队列不同，因为Frame中存储的是原始数据，是非常庞大的，所以需要设置成有限队列，这里采用的是滚动数组的方式来实现，分别用读写两个索引来记录位置。 由于将数组进行了读写分离，所以在线程加锁时有了优化，不再需要锁住整个函数，可以局部加锁。1234567891011121314typedef struct FrameQueue &#123; // 使用滚动数组来实现的Frame队列 Frame queue [FRAME_QUEUE_SIZE]; // 数据帧数组，使用数组来实现的队列，默认大小为FRAME_QUEUE_SIZE int rindex; // 读索引 int windex; // 写索引 int size; // 当前写入的数据大小 int max_size; // 外部设置的队列最大允许存储的大小，该值不超过FRAME_QUEUE_SIZE int keep_last; // 是否保留最后一个读数据，如果是1，则rindex_shown的值就为1。并且后面rindex所指向的位置实际上是上个次读的索引，而rindex+rindex_shown才是当前读的索引 int rindex_shown; // 用来配合keep_last，如果keep_last为1，则再第一次next后，rindex_shown也会为1 SDL_mutex *mutex; // 队列锁 SDL_cond *cond; // 队列锁互斥量 PacketQueue *pktq; // 关联的Packet队列&#125; FrameQueue; frame_queue_init初始化队列，并设置队列的最大大小，根据最大大小提前创建好每一个Frame对象。1234567891011121314151617181920212223242526static int frame_queue_init(FrameQueue *f, PacketQueue *pktq, int max_size, int keep_last) &#123; int i; // 初始化原始数据队列，清零 memset(f, 0, sizeof(FrameQueue)); // 创建原始数据队列的锁 if (!(f-&gt;mutex = SDL_CreateMutex())) &#123; av_log(NULL, AV_LOG_FATAL, "SDL_CreateMutex(): %s\n", SDL_GetError()); return AVERROR(ENOMEM); &#125; // 创建原始数据队列的互斥量 if (!(f-&gt;cond = SDL_CreateCond())) &#123; av_log(NULL, AV_LOG_FATAL, "SDL_CreateCond(): %s\n", SDL_GetError()); return AVERROR(ENOMEM); &#125; // 关联原始数据队列和编码数据队列 f-&gt;pktq = pktq; // 设置原始数据队列的最大大小 f-&gt;max_size = FFMIN(max_size, FRAME_QUEUE_SIZE); // 是否保留最后一个读的数据 f-&gt;keep_last = !!keep_last; // 提前分配好所有的Frame for (i = 0; i &lt; f-&gt;max_size; i++) if (!(f-&gt;queue[i].frame = av_frame_alloc())) return AVERROR(ENOMEM); return 0;&#125; frame_queue_destory销毁队列12345678910111213static void frame_queue_destory(FrameQueue *f) &#123; int i; // 释放所有的原始数据帧 for (i = 0; i &lt; f-&gt;max_size; i++) &#123; Frame *vp = &amp;f-&gt;queue[i]; // 主要是销毁AVFrame和AVSubtitle frame_queue_unref_item(vp); av_frame_free(&amp;vp-&gt;frame); &#125; // 销毁锁和互斥量 SDL_DestroyMutex(f-&gt;mutex); SDL_DestroyCond(f-&gt;cond);&#125; frame_queue_peek_readable读取这里也和Packet队列不同，首先通过frame_queue_peek_readable获取到读索引位置的节点，想要获取下一个节点，需要调用frame_queue_next将读索引移动到下一个位置。 读取时如果当前队列为空，则进行阻塞等待。 123456789101112131415161718192021222324/** * 从队列中取出一个可读的帧 * @param 队列 * @return 队列中可读的帧 */static Frame *frame_queue_peek_readable(FrameQueue *f) &#123; /* wait until we have a readable a new frame */ // 同写入，size是读写线程都需要操作的，所以需要加锁 SDL_LockMutex(f-&gt;mutex); // 如果设置了keep_last，则在next后，rindex_shown为1， // rindex_shown为1其实就是标记rindex少走了一步，也等于size少减了1，所以这里计算size时需要减去1 // 如果队列为空 while (f-&gt;size - f-&gt;rindex_shown &lt;= 0 &amp;&amp; !f-&gt;pktq-&gt;abort_request) &#123; // 等待写线程notify SDL_CondWait(f-&gt;cond, f-&gt;mutex); &#125; // 队列解锁 SDL_UnlockMutex(f-&gt;mutex); // 如果关联的Packet队列退出，则返回null if (f-&gt;pktq-&gt;abort_request) return NULL; // rindex + rindex_shown // （当rindex_shown为1时，rindex少了一次+1，所以需要加上1） return &amp;f-&gt;queue[(f-&gt;rindex + f-&gt;rindex_shown) % f-&gt;max_size];&#125; frame_queue_next中并没有对队列进行判空，所以调用该方法的时候需要先用frame_queue_nb_remaining判断一下123456789101112131415161718192021222324252627/** * 通知队列数据已经读取，rindex位置的帧已经被外部使用 * rindex需要向后移动，以便frame_queue_peek_readable获取后续的数据 * @param f */static void frame_queue_next(FrameQueue *f) &#123; // 如果当前队列设置了保留最后一帧，则rindex少走一步，size少减1 if (f-&gt;keep_last &amp;&amp; !f-&gt;rindex_shown) &#123; // 标记rindex_shown来弥补这1步 f-&gt;rindex_shown = 1; return; &#125; // 如果队列设置了keep_last，则这里的删除操作针对的都是上一帧，而不是当前帧 // 删除rindex位置的帧数据，AVFrame和AVSubtitle frame_queue_unref_item(&amp;f-&gt;queue[f-&gt;rindex]); // rindex+1,向后移动一位，如果超出，则从头开始 if (++f-&gt;rindex == f-&gt;max_size) f-&gt;rindex = 0; // 加锁 SDL_LockMutex(f-&gt;mutex); // size减1 f-&gt;size--; // 通知写线程有空位写入，唤醒wait SDL_CondSignal(f-&gt;cond); // 解锁 SDL_UnlockMutex(f-&gt;mutex);&#125; 获取当前队列中可读的节点数量1234static int frame_queue_nb_remaining(FrameQueue *f) &#123; // 返回当前队列中已有的帧数量 return f-&gt;size - f-&gt;rindex_shown;&#125; frame_queue_peek_writable写入的话和读取一样，先通过frame_queue_peek_writable获取当前写索引位置的节点，写入完毕后，调用frame_queue_push通知队列写入完毕，写索引向后移动一位。 写入时如果当前队列已满，那么会进行阻塞等待。 12345678910111213141516171819202122/** * 从队列中取出一个可写入的帧 * @param 队列 * @return 队列中可写入的帧 */static Frame *frame_queue_peek_writable(FrameQueue *f) &#123; /* wait until we have space to put a new frame */ // 队列加锁（因为读线程和写线程都会对队列的size进行修改，所以需要加锁） SDL_LockMutex(f-&gt;mutex); // 如果队列已满，并且关联的Packet队列并没有退出(避免死循环)，则循环wait while (f-&gt;size &gt;= f-&gt;max_size &amp;&amp; !f-&gt;pktq-&gt;abort_request) &#123; // wait，等待notify SDL_CondWait(f-&gt;cond, f-&gt;mutex); &#125; // 队列解锁 SDL_UnlockMutex(f-&gt;mutex); // 只有写线程才需要进行写入，才会改变windex的值，所以获取写入的帧操作并不需要加锁 // 如果关联的Packet队列退出，则返回null if (f-&gt;pktq-&gt;abort_request) return NULL; // 返回windex位置的Frame供外部写入 return &amp;f-&gt;queue[f-&gt;windex];&#125; 123456789101112131415161718/** * 通知队列数据已经写入，windex位置的帧已经写入了数据 * 数据写入的帧是通过frame_queue_peek_writable获取的 * @param 队列 */static void frame_queue_push(FrameQueue *f) &#123; // 将windex+1，往后移一位，如果后面已经到底了，则从头开始（环状） if (++f-&gt;windex == f-&gt;max_size) f-&gt;windex = 0; // 同上，windex只有写线程操作，所以不需要加锁，但是下面的size的操作是读线程和写线程都需要操作的，所以需要加锁 // 加锁 SDL_LockMutex(f-&gt;mutex); // 队列的size+1 f-&gt;size++; // notify通知读线程，唤醒wait SDL_CondSignal(f-&gt;cond); // 释放锁 SDL_UnlockMutex(f-&gt;mutex);&#125; 结构体MyAVPacketListPacket队列中的节点12345typedef struct MyAVPacketList &#123; AVPacket pkt; // 编码数据 struct MyAVPacketList *next; // 下一个节点 int serial; // 序号&#125; MyAVPacketList; FrameFrame队列中的节点123456789101112131415typedef struct Frame &#123; // 对数据帧的统一抽象，包含音频帧、视频帧、字幕帧 AVFrame *frame; // 音视频帧 AVSubtitle sub; // 字幕帧 int serial; // 序号 double pts; // pts double duration; // 显示时长 int64_t pos; // 在流中的位置 int width; // 宽 int height; // 高 int format; // 格式 AVRational sar; // 横纵比 int uploaded; // 是否上传到纹理上 int flip_v; // 是否需要翻转&#125; Frame;]]></content>
      <categories>
        <category>拳掌指腿（音视频）</category>
        <category>降龙十八掌（FFmpeg）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[音视频开发之SDL2播放视频]]></title>
    <url>%2F%E9%9F%B3%E8%A7%86%E9%A2%91%E5%BC%80%E5%8F%91%E4%B9%8BSDL2%E6%92%AD%E6%94%BE%E8%A7%86%E9%A2%91%2F</url>
    <content type="text"><![CDATA[如果熟悉OpenGL，那么SDL来渲染视频就很简单了，Window对象时用来显示的窗口，Renderer是具体渲染的渲染器，Texture可以认为就是一张图像，不断的更新Texture的内容，将其显示在窗口上，这就完成了视频的播放。 创建Window对于渲染视频来说，首先需要创建一个Window对象，这是与Native渲染体系相关联的一个窗口。123456789if (borderless) flags |= SDL_WINDOW_BORDERLESS; // 去掉窗口状态栏else flags |= SDL_WINDOW_RESIZABLE; // 窗口是否可缩放// 创建SDL窗口// 参数（标题，x, y, w, h, 标记）window = SDL_CreateWindow(program_name, SDL_WINDOWPOS_UNDEFINED, SDL_WINDOWPOS_UNDEFINED, default_width, default_height, flags); 创建RendererRenderer负责具体的渲染逻辑123// 创建SDL渲染器，SDL_RENDERER_ACCELERATED 使用硬件加速// 参数（渲染的Window，渲染驱动索引，标记）renderer = SDL_CreateRenderer(window, -1, SDL_RENDERER_ACCELERATED | SDL_RENDERER_PRESENTVSYNC); 创建纹理像素格式: YUV、RGB纹理访问格式: SDL_TEXTUREACCESS_STATIC : 纹理不经常变更 SDL_TEXTUREACCESS_STREAMING : 纹理经常变更 SDL_TEXTUREACCESS_TARGET : 纹理可作为渲染的target123// 创建纹理 // 参数（对应的渲染器，像素格式，纹理访问格式设置，w, h） *texture = SDL_CreateTexture(renderer, new_format,SDL_TEXTUREACCESS_STREAMING, new_width,new_height) 什么是纹理纹理是显卡中一段连续的内存，可以用来存储图片数据，也可以用来存储计算过程中的中间结果等任意数据。而视频图像数据则是内存中的像素数组。 狭义地讲，纹理是存在于显存的，可以存放任意数据；是视频图像数据存在于内存的，内容是像素数组。纹理存放的不是真正的像素数据，而是存放的图像的描述信息。 所以可以清楚的明白，为什么要使用纹理，将CPU中的像素数据拷贝到GPU中进行存储，然后在GPU上进行处理后交由显示屏进行显示。 更新纹理更新YUV格式的纹理数据，就是将YUV三个分量的数据和长度一次传入。123ret = SDL_UpdateYUVTexture( *tex, NULL, frame-&gt;data[0], frame-&gt;linesize[0], frame-&gt;data[1], frame-&gt;linesize[1], frame-&gt;data[2], frame-&gt;linesize[2]); 更新RGB格式的纹理数据1ret = SDL_UpdateTexture(*tex, NULL, frame-&gt;data[0], frame-&gt;linesize[0]); 显示纹理Renderer有一个默认的渲染目标，可以通过SDL_SetRenderTarget来更改。要想将纹理的内容显示在屏幕上，需要先将纹理的内容拷贝到Renderer的默认渲染目标上，然后再通过SDL_RenderPresent来刷新屏幕。1234// 将纹理上的内容拷贝到渲染器的默认渲染目标上SDL_RenderCopy(renderer, texture, nullptr, &amp;rect);// 将渲染目标的内容刷新到屏幕上SDL_RenderPresent(renderer);]]></content>
      <categories>
        <category>拳掌指腿（音视频）</category>
        <category>七伤拳（音视频基础）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[音视频开发之SDL2播放音频]]></title>
    <url>%2F%E9%9F%B3%E8%A7%86%E9%A2%91%E5%BC%80%E5%8F%91%E4%B9%8BSDL2%E6%92%AD%E6%94%BE%E9%9F%B3%E9%A2%91%2F</url>
    <content type="text"><![CDATA[使用SDL来播放音频非常简单，只需要根据指定的参数开启音频设备，然后设置音频回调函数，在音频回调函数中，将数据写入到指定的buffer中即可。对于音频播放来讲，是音频设备主动向我们要数据，而并非我们主动写入数据到音频设备，音频设备维护了一个数据缓冲区，会将数据存放在缓冲区中进行逐一播放，当缓冲区中有空位时，就会通过回调函数向我们要数据，当缓冲区中数据已经填满时，则不会回调。 开启音频设备SDL_AudioSpec是对音频参数的组合，开启音频设备的时候需要传入一组期望的参数组合，开启成功后会返回一个真正开启的参数组合。 1234567891011121314151617181920212223242526272829SDL_AudioSpec wanted_spec, spec;// 设置SDL音频播放参数// 设置 采样率、声道数wanted_nb_channels = av_get_channel_layout_nb_channels(wanted_channel_layout);wanted_spec.channels = wanted_nb_channels;wanted_spec.freq = wanted_sample_rate; // 采样位数wanted_spec.format = AUDIO_S16SYS;// 静音wanted_spec.silence = 0;// 缓冲区样本数量（单声道样本数量）wanted_spec.samples = FFMAX(SDL_AUDIO_MIN_BUFFER_SIZE, 2 &lt;&lt; av_log2(wanted_spec.freq / SDL_AUDIO_MAX_CALLBACKS_PER_SEC));// 回调函数wanted_spec.callback = sdl_audio_callback;// 回调函数参数wanted_spec.userdata = opaque;// 开启音频播放设备，如果开启失败，则更换参数不断重试while ( !(audio_dev = SDL_OpenAudioDevice(NULL, 0, &amp;wanted_spec, &amp;spec, SDL_AUDIO_ALLOW_FREQUENCY_CHANGE | SDL_AUDIO_ALLOW_CHANNELS_CHANGE))) &#123; // 更换参数组合，重新尝试&#125; 回调函数回调函数是在一个单独的线程中，第一个参数就是开启设备时设置的回调函数上下文。第二个参数就是需要写入的缓冲区，第三个参数是需要的数据字节大小。123456789101112131415161718192021222324252627static void sdl_audio_callback(void *opaque, Uint8 *stream, int len) &#123; VideoState *is = opaque; int audio_size, len1; while (len &gt; 0) &#123; // 计算还未读取的字节大小 len1 = is-&gt;audio_buf_size - is-&gt;audio_buf_index; // 如果为读取的字节大小大于音频设备想要的大小，则使用音频设备想要的大小 if (len1 &gt; len) len1 = len; // 将audio_buf中的数据copy到stream中 if (!is-&gt;muted &amp;&amp; is-&gt;audio_buf &amp;&amp; is-&gt;audio_volume == SDL_MIX_MAXVOLUME) memcpy(stream, (uint8_t *)is-&gt;audio_buf + is-&gt;audio_buf_index, len1); else &#123; memset(stream, 0, len1); if (!is-&gt;muted &amp;&amp; is-&gt;audio_buf) SDL_MixAudioFormat(stream, (uint8_t *)is-&gt;audio_buf + is-&gt;audio_buf_index, AUDIO_S16SYS, len1, is-&gt;audio_volume); &#125; // 减去已经传递给音频设备的buffer大小，如果剩下的还有，则再次读取传递 len -= len1; // stream加上偏移量 stream += len1; // 更新audio_buf的偏移量 is-&gt;audio_buf_index += len1; &#125;&#125;]]></content>
      <categories>
        <category>拳掌指腿（音视频）</category>
        <category>七伤拳（音视频基础）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[音视频开发之Android播放视频]]></title>
    <url>%2F%E9%9F%B3%E8%A7%86%E9%A2%91%E5%BC%80%E5%8F%91%E4%B9%8BAndroid%E6%92%AD%E6%94%BE%E8%A7%86%E9%A2%91%2F</url>
    <content type="text"><![CDATA[图像的显示最终都是由显示器完成的，显示器通过接收到的颜色矩阵来进行对应的显示。而颜色矩阵的产生一般有两种，一种是通过GPU来进行渲染生成，另一种是通过CPU来进行渲染生成。其中GPU比较适合来处理这件事情，所以其效率高。（硬件加速也就是指使用GPU来进行渲染加速） 在Android平台上，GPU渲染的API有两套，一套就是OpenGL-ES，另一套就是7.0后推出的Vulkan。目前使用最多的还是OpenGL-ES。 整个渲染流程中主要节点如下:SurfaceFlinger &lt;- SurfaceView &lt;- Surface &lt;- EGLSurface &lt;- EGLContext &lt;- OpenGL-ES 接入原生渲染体系Android原生平台上封装了一整套View体系用来进行图像的渲染和显示，所以任何的渲染都必须基于这个体系才能正确的显示出来。 View系统中提供了两个View可以用来进行自定义渲染，一个是SurfaceView，另一个是TextureView。（关于SurfaceView和TextureView可以看我之前写的Android图形架构总览。 只要将图形数据写入到SurfaceView或TextureView的Surface中，那么最终SurfaceFlinger服务就会将图形内容显示到显卡上。 所以对于播放视频来说，就是要将视频每一帧的图像数据写入到SurfaceView或TextureView的Surface中即可。 SurfaceView可以通过addCallback来接收来自原生渲染的生命周期回调，通过getSurface来获取内部的Surface对象。12this.getHolder().addCallback();this.getHolder().getSurface() TextureView可以通过setSurfaceTextureListener来接收生命周期回调，通过getSurfaceTexture可以获取到SurfaceTexture。12setSurfaceTextureListener();Surface surface = new Surface(getSurfaceTexture()); Surface与EGLSurface一个Surface对象，可以关联一个EGLSurface对象（window_surface)，window_surface可以通过swap方法将其内部图像缓冲数据传入到Surface中（其它类型的EGLSurface是不可以的）。 1234567// 创建一个提供给opengl-es绘制的surface（display，配置，原生window，指定属性）if (!(eglSurface = eglCreateWindowSurface(display, config, aNativeWindow, 0))) &#123; return;&#125; // android中创建NativeWindowANativeWindow *pNativeWindow = ANativeWindow_fromSurface(jenv, surface); EGLSurface与EGLContextEGLContext是与线程相关的，一个线程中只能激活一个EGLContext，激活的EGLContext关联一个EGLSurface，激活后，在当前线程调用OpenGL-ES的API都将作用到EGLSurface的缓冲区中。 12345// 创建context，在context中保存了opengl-es的状态信息 （display，配置，共享context的handle 一般设为null，属性）// 一个display可以创建多个contextif (!(context = eglCreateContext(display, config, 0, context_attribs))) &#123; return;&#125; 12345678// 将上面创建的context绑定到当前线程上，并将context与surface进行关联。当makeCurrent执行后，就可以调用opengl-es的api对context中的状态集进行设定，// 然后进而向surface中绘制内容，再把surface中的内容读取出来。// 一个线程中enable状态的context只能有一个，如果当前线程已经有了一个enable状态的context，那么会先执行其flush操作，将没有执行完成的命令全部执行完成，然后将其改为disable状态，将新传入的context改为enable状态。// 如果想要释放当前的context,也就是将当前的context disable,那么将第二个和第三个参数设置为 EGL_NO_SURFACE,第四个参数设置为 EGL_NO_CONTEXT即可。// context enable后，视口大小和裁剪大小都会被设置为surface的尺寸if (!eglMakeCurrent(display, eglSurface, eglSurface, context)) &#123; return;&#125; OpenGL-ES渲染纹理当我们准备好了EGLSurface和EGLContext后，那么接下来就是创建OpenGL—ES的Program进行纹理的绘制了。 创建Program创建Vertex着色器12345678precision mediump float;attribute highp vec4 aPosition;attribute highp vec2 aTextureCoord;varying vec2 textureCoordinate;void main() &#123; gl_Position = aPosition; textureCoordinate = aTextureCoord.xy;&#125; 创建Fragment着色器1234567891011121314151617precision mediump float;varying highp vec2 textureCoordinate;uniform lowp sampler2D inputTextureY;uniform lowp sampler2D inputTextureU;uniform lowp sampler2D inputTextureV;void main() &#123; vec3 yuv; vec3 rgb; yuv.r = texture2D(inputTextureY, textureCoordinate).r - (16.0 / 255.0); yuv.g = texture2D(inputTextureU, textureCoordinate).r - 0.5; yuv.b = texture2D(inputTextureV, textureCoordinate).r - 0.5; rgb = mat3(1.164, 1.164, 1.164, 0.0, -0.213, 2.112, 1.793, -0.533, 0.0) * yuv; gl_FragColor = vec4(rgb, 1.0);&#125; 创建Program123456789101112131415161718192021222324252627// 创建ProgramprogramHandle = OpenGLUtils::createProgram(vertexShader, fragmentShader);// 获取顶点坐标变量positionHandle = glGetAttribLocation(programHandle, "aPosition");// 获取纹理坐标变量texCoordinateHandle = glGetAttribLocation(programHandle, "aTextureCoord");// 获取YUV纹理变量inputTextureHandle[0] = glGetUniformLocation(programHandle, "inputTextureY");inputTextureHandle[1] = glGetUniformLocation(programHandle, "inputTextureU");inputTextureHandle[2] = glGetUniformLocation(programHandle, "inputTextureV");// 使用ProgramglUseProgram(programHandle);// 创建三个纹理，用来接收YUV数据if (textures[0] == 0) &#123; glGenTextures(3, textures);&#125;for (int i = 0; i &lt; 3; ++i) &#123; glActiveTexture(GL_TEXTURE0 + i); glBindTexture(GL_TEXTURE_2D, textures[i]); glTexParameteri(GL_TEXTURE_2D, GL_TEXTURE_MAG_FILTER, GL_LINEAR); glTexParameteri(GL_TEXTURE_2D, GL_TEXTURE_MIN_FILTER, GL_LINEAR); glTexParameterf(GL_TEXTURE_2D, GL_TEXTURE_WRAP_S, GL_CLAMP_TO_EDGE); glTexParameterf(GL_TEXTURE_2D, GL_TEXTURE_WRAP_T, GL_CLAMP_TO_EDGE); glUniform1i(inputTextureHandle[i], i);&#125; 设置坐标对于图像来说，首先创建一个平面矩形，然后在其上贴上纹理即可。12345678910111213141516171819202122// 矩形坐标，原点在屏幕中心，x、y轴为(-1,1)static const float vertices[] = &#123; -1.0f, -1.0f, // left, bottom 1.0f, -1.0f, // right, bottom -1.0f, 1.0f, // left, top 1.0f, 1.0f, // right, top&#125;;// 纹理坐标，原点在屏幕左上角，x、y轴为(0,1)static const float texture_vertices[] = &#123; 0.0f, 1.0f, // left, top 1.0f, 1.0f, // right, top 0.0f, 0.0f, // left, bottom 1.0f, 0.0f, // right, bottom&#125;;// 绑定顶点坐标glVertexAttribPointer(positionHandle, 2, GL_FLOAT, GL_FALSE, 0, vertices);glEnableVertexAttribArray(positionHandle);// 绑定纹理坐标glVertexAttribPointer(texCoordinateHandle, 2, GL_FLOAT, GL_FALSE, 0, texture_vertices);glEnableVertexAttribArray(texCoordinateHandle); 设置纹理12345678910111213141516171819202122// 更新绑定纹理的数据// 这里处理的是YUV420p格式的数据，所以UV是Y的一半const GLsizei heights[3] = &#123; texture-&gt;height, texture-&gt;height / 2, texture-&gt;height / 2&#125;;for (int i = 0; i &lt; 3; ++i) &#123; // 激活前面创建的YUV纹理 glActiveTexture(GL_TEXTURE0 + i); // 绑定纹理 glBindTexture(GL_TEXTURE_2D, textures[i]); // 将对应的YUV数据设置到纹理上 glTexImage2D(GL_TEXTURE_2D, 0, GL_LUMINANCE, texture-&gt;pitches[i], heights[i], 0, GL_LUMINANCE, GL_UNSIGNED_BYTE, // 具体数据 texture-&gt;pixels[i]); // 设置纹理变量对应的纹理单元层 glUniform1i(inputTextureHandle[i], i);&#125; 绘制12// 绘制平面矩形glDrawArrays(GL_TRIANGLE_STRIP, 0, 4); 将OpenGL-ES生成的图像数据加入到EGLSurface绑定的Surface中1234// 当opengl-es将内容绘制完成，调用该方法将该缓冲区加入到Surface的BufferQueue中if (!eglSwapBuffers(display, eglSurface)) &#123; return;&#125;]]></content>
      <categories>
        <category>拳掌指腿（音视频）</category>
        <category>七伤拳（音视频基础）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[音视频开发之Android播放音频]]></title>
    <url>%2F%E9%9F%B3%E8%A7%86%E9%A2%91%E5%BC%80%E5%8F%91%E4%B9%8BAndroid%E6%92%AD%E6%94%BE%E9%9F%B3%E9%A2%91%2F</url>
    <content type="text"><![CDATA[SDL是一个跨平台的音视频渲染库，是支持Android平台的，所以可以直接使用SDL库进行音频的播放。 但是SDL库在Android平台上的实现只有一种，就是通过JNI来调用Java层的AudioTrack来进行播放。所以如果你有其它的需求不想使用SDL库，那么可以直接使用Android平台原生API来播放音频。 Android平台音频播放API前面录制的时候讲过，Android上音频输出的API比较繁琐，有多套实现。有Java层的实现AudioTrack，也有native层实现OpenSLES，在 Android O上又推出了新的native层实现AAudio，并且提供了Oboe库，对OpenSLES和AAudio进行了封装。 下面来详细的介绍下每套API的使用 AudioTrackAudioTrack是Android平台提供的播放音频的Java层API，使用起来非常简单。通过设置待播放音频参数就可以创建一个AudioTrack对象，调用了play方法后，就可以开始写入数据了，通过write方法将数据写入，调用stop后就停止播放。 需要注意的是，在创建AudioTrack的时候，对于待播放的音频数据格式都已经设定好了，也就是说这个创建的AudioTrack只能播放这种格式的音频数据。所以一般在获取到音频流解码后，都需要将PCM数据进行重采样，重采样成和AudioTrack一样的数据格式，才能交给AudioTrack播放。 创建AudioTrackAudioTrack的构造函数有两套，新的一套是在Android L推出的，老的一套已经被标记为deprecated。 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354// 根据指定采样率、声道数、位数获取最小需要的缓冲区大小，后续再创建AudioTrack时设置的缓冲区大小必须大于这个值// 如果想要将缓冲区设大一点，比如 bufferSizeFactor = 1.5final int minBufferSizeInBytes = (int) (AudioTrack.getMinBufferSize(sampleRate, channelConfig, AudioFormat.ENCODING_PCM_16BIT) * bufferSizeFactor);// 一个音频帧的字节大小 = 声道数 *（位数 /8） final int bytesPerFrame = channels * (BITS_PER_SAMPLE / 8);// 创建buffer用来存放每一次要写入的数据，使用堆外内存，避免JNI内存拷贝// BUFFERS_PER_SECOND = 100，预测一秒钟回调100次// 那么44100的采样率，每次回调应该给的数据帧个数为4410个byteBuffer = ByteBuffer.allocateDirect(bytesPerFrame * (sampleRate / BUFFERS_PER_SECOND)); AudioTrack audioTrack;if (Build.VERSION.SDK_INT &gt;= 21) &#123; // AudioFormat是对音频的格式进行封装，包括采样率、声道数、位数等。 // AudioAttributes是对播放内容的描述 audioTrack = new AudioTrack( new AudioAttributes.Builder() // 音频的用途 .setUsage(usageAttribute) // 音频内容的类型 .setContentType(AudioAttributes.CONTENT_TYPE_SPEECH) .build(), new AudioFormat.Builder() // 位数 .setEncoding(AudioFormat.ENCODING_PCM_16BIT) // 采样率 .setSampleRate(sampleRateInHz) // 声道数 .setChannelMask(channelConfig) .build(), bufferSizeInBytes,// 缓冲区大小 AudioTrack.MODE_STREAM,// 模式 // 生成新的会话ID AudioManager.AUDIO_SESSION_ID_GENERATE );&#125; else &#123; // 构造参数： // 音频流类型 // 采样率 // 声道数 // 位数 // 缓冲区大小 // 模式 : MODE_STATIC 预先将需要播放的音频数据读取到内存中，然后才开始播放。MODE_STREAM 边读边播，不会将数据直接加载到内存 audioTrack = new AudioTrack(AudioManager.STREAM_VOICE_CALL, sampleRateInHz, channelConfig, AudioFormat.ENCODING_PCM_16BIT, bufferSizeInBytes, AudioTrack.MODE_STREAM); &#125;if (audioTrack == null || audioTrack.getState() != AudioTrack.STATE_INITIALIZED) &#123; // 创建失败&#125; 播放模式AudioTrack支持两种模式，MODE_STATIC和 MODE_STREAM MODE_STREAM : 预先将需要播放的音频数据读取到内存中,所以如果使用这种模式，那么在创建AudioTrack的时候，传入的缓冲区大小就必须是整个播放文件的大小，AudioTrack只会播放这么多数据。 MODE_STREAM : 边读边播，不会将数据直接加载到内存，如果使用这种模式，需要注意每次写入的数据大小，需要确保小于或者等于创建时传入的缓冲区大小。一般使用这种模式。 音频流类型 AudioManager.STREAM_VOICE_CALL : 电话 AudioManager.STREAM_SYSTEM : 系统 AudioManager.STREAM_RING : 响铃和消息 AudioManager.STREAM_MUSIC : 音乐 AudioManager.STREAM_ALARM : 闹钟 AudioManager.STREAM_NOTIFICATION : 通知 AudioManager.STREAM_BLUETOOTH_SCO : 蓝牙 AudioManager.STREAM_SYSTEM_ENFORCED : 强制系统声音 AudioManager.STREAM_DTMF : 双音多频 AudioManager.STREAM_TTS : 语音 写入数据123456789101112131415// 开始播放audioTrack.play();// 写入数据，使用阻塞模式WRITE_BLOCKING，如果缓冲区满了，则阻塞等待if (Build.VERSION.SDK_INT &gt;= 21) &#123; // 使用Buffer来传递到Native层，buffer创建时分配堆外内存，这样在JNI传递时避免了内存拷贝 audioTrack.write(byteBuffer, sizeInBytes, AudioTrack.WRITE_BLOCKING);&#125; else &#123; // 使用byte array来传递到Native层，需要内存拷贝 audioTrack.write(byteBuffer.array(), byteBuffer.arrayOffset(), sizeInBytes);&#125;// 停止播放audioTrack.stop(); OpenSL-ESOpenSL-ES是Android平台上提供的高性能音频的API，相对Java层API有着一定的低延迟性。由于是C库，所以使用起来API相对繁琐一些。 配置数据参数1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253 // 创建播放数据格式 SLDataFormat_PCM format;// 数据格式format.formatType = SL_DATAFORMAT_PCM;// 声道数format.numChannels = static_cast&lt;SLuint32&gt;(channels);// 采样率switch (sample_rate) &#123; case 8000: format.samplesPerSec = SL_SAMPLINGRATE_8; break; case 16000: format.samplesPerSec = SL_SAMPLINGRATE_16; break; case 22050: format.samplesPerSec = SL_SAMPLINGRATE_22_05; break; case 32000: format.samplesPerSec = SL_SAMPLINGRATE_32; break; case 44100: format.samplesPerSec = SL_SAMPLINGRATE_44_1; break; case 48000: format.samplesPerSec = SL_SAMPLINGRATE_48; break; case 64000: format.samplesPerSec = SL_SAMPLINGRATE_64; break; case 88200: format.samplesPerSec = SL_SAMPLINGRATE_88_2; break; case 96000: format.samplesPerSec = SL_SAMPLINGRATE_96; break; default: RTC_CHECK(false) &lt;&lt; "Unsupported sample rate: " &lt;&lt; sample_rate; break;&#125;// 位数format.bitsPerSample = SL_PCMSAMPLEFORMAT_FIXED_16;format.containerSize = SL_PCMSAMPLEFORMAT_FIXED_16;// 小端format.endianness = SL_BYTEORDER_LITTLEENDIAN;if (format.numChannels == 1) &#123; format.channelMask = SL_SPEAKER_FRONT_CENTER;&#125; else if (format.numChannels == 2) &#123; format.channelMask = SL_SPEAKER_FRONT_LEFT | SL_SPEAKER_FRONT_RIGHT;&#125; else &#123; RTC_CHECK(false) &lt;&lt; "Unsupported number of channels: " &lt;&lt; format.numChannels;&#125; 创建引擎在OpenSLES中，任何API接口对象都要由引擎来创建1234567891011121314151617181920// 设置线程安全模式const SLEngineOption option[] = &#123; &#123;SL_ENGINEOPTION_THREADSAFE, static_cast&lt;SLuint32&gt;(SL_BOOLEAN_TRUE)&#125;&#125;;// 创建引擎接口SLresult result = slCreateEngine(engine_object_, 1, option, 0, NULL, NULL);if (result != SL_RESULT_SUCCESS) &#123; return nullptr;&#125;// 设置引擎实现模式为同步模式result = engine_object_-&gt;Realize(engine_object_, SL_BOOLEAN_FALSE);if (result != SL_RESULT_SUCCESS) &#123; return nullptr;&#125;// 根据引擎接口获取到隐藏的引擎实现if ((*engine_object) -&gt;GetInterface(engine_object, SL_IID_ENGINE, &amp;engine_)) &#123; return false;&#125; 创建播放器在OpenSLES中，播放需要通过播放器对象来进行， 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657// 通过引擎实现创建混音器接口(*engine_)-&gt;CreateOutputMix(engine_, output_mix_, 0, nullptr, nullptr);// 设置混音器实现模式为同步模式output_mix_-&gt;Realize(output_mix_.Get(), SL_BOOLEAN_FALSE);// 设置缓冲队列大小SLDataLocator_AndroidSimpleBufferQueue simple_buffer_queue = &#123; SL_DATALOCATOR_ANDROIDSIMPLEBUFFERQUEUE, static_cast&lt;SLuint32&gt;(kNumOfOpenSLESBuffers)&#125;;// 设置数据源的缓冲队列和数据格式SLDataSource audio_source = &#123;&amp;simple_buffer_queue, &amp;pcm_format_&#125;;// 设置输出混音器SLDataLocator_OutputMix locator_output_mix = &#123;SL_DATALOCATOR_OUTPUTMIX, output_mix_&#125;;// 设置数据接收器的混音器 SLDataSink audio_sink = &#123;&amp;locator_output_mix, nullptr&#125;;// const SLInterfaceID interface_ids[] = &#123;SL_IID_ANDROIDCONFIGURATION, SL_IID_BUFFERQUEUE, SL_IID_VOLUME&#125;;const SLboolean interface_required[] = &#123;SL_BOOLEAN_TRUE, SL_BOOLEAN_TRUE, SL_BOOLEAN_TRUE&#125;;// 通过引擎实现创建播放器接口(*engine_)-&gt;CreateAudioPlayer( engine_, player_object_, &amp;audio_source, &amp;audio_sink, arraysize(interface_ids), interface_ids, interface_required);// 通过播放器接口创建播放器配置实现，用来设置相关播放配置参数SLAndroidConfigurationItf player_config;player_object_-&gt;GetInterface(player_object_, SL_IID_ANDROIDCONFIGURATION, &amp;player_config);// 设置音频流类型SLint32 stream_type = SL_ANDROID_STREAM_VOICE;// 设置参数(*player_config)-&gt;SetConfiguration(player_config, SL_ANDROID_KEY_STREAM_TYPE, &amp;stream_type, sizeof(SLint32));// 设置播放器实现模式为同步模式player_object_-&gt;Realize(player_object_, SL_BOOLEAN_FALSE);// 通过播放器接口创建播放器实现，用来播放数据player_object_-&gt;GetInterface(player_object_, SL_IID_PLAY, &amp;player_);// 通过播放器接口创建缓冲队列实现，用来传递数据player_object_-&gt;GetInterface(player_object_, SL_IID_BUFFERQUEUE, &amp;simple_buffer_queue_);// 注册在缓冲队列上的回调，通过回调来通知写入数据，SimpleBufferQueueCallback就是回调函数，this是回调参数的context参数// 当回调触发，则说明播放器需要数据，将数据写入到缓冲队列中即可(*simple_buffer_queue_)-&gt;RegisterCallback(simple_buffer_queue_, SimpleBufferQueueCallback, this);// 通过播放器接口创建音量实现，用来控制音量player_object_-&gt;GetInterface(player_object_, SL_IID_VOLUME, &amp;volume_); 写入数据每次回调函数被触发，则说明播放器需要数据，通过调用Enqueue函数将数据写入到缓冲队列中 123456789void OpenSLESPlayer::SimpleBufferQueueCallback( SLAndroidSimpleBufferQueueItf caller, void* context) &#123; // 数据回调函数，context就是在设置回调时设置的this OpenSLESPlayer* stream = reinterpret_cast&lt;OpenSLESPlayer*&gt;(context); // 写入数据，Enqueue（缓冲队列，buffer数据，数据size） SLresult err = (*simple_buffer_queue_)-&gt;Enqueue(simple_buffer_queue_, audio_ptr8, audio_parameters_.GetBytesPerBuffer());&#125;]]></content>
      <categories>
        <category>拳掌指腿（音视频）</category>
        <category>七伤拳（音视频基础）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[音视频开发之Android录制视频]]></title>
    <url>%2F%E9%9F%B3%E8%A7%86%E9%A2%91%E5%BC%80%E5%8F%91%E4%B9%8BAndroid%E5%BD%95%E5%88%B6%E8%A7%86%E9%A2%91%2F</url>
    <content type="text"><![CDATA[FFmpeg对于Android平台的视频输入设备API有一定的支持，但是比较局限，只支持Android N以上的版本，低版本无法使用。 其原因是因为FFmpeg采用的是Android N推出的native层camera API来实现的(NDK中的libcamera2ndk.so），而并非采用JNI的方式来调用Java层camera API。 Android平台视频录制API对于视频录制API，Android平台上有两套API实现，一套是老版的Camera1，另一套是Android L之后推出的Camera2。 Camera1使用起来较为简单，但是功能相对较少，不支持多纹理输出。Camera2的API功能上虽然更加强大，但是API设计的非常底层化，不利于理解，并且YUV_420_888的数据格式，国内各大产商在实现上留下的坑太多。 Jetpack组件中，Google推出了全新的CameraX组件，对Camera1和Camera2进行了统一的封装，使得API更加简单易用。 Camera1获取Camera设备信息 Camera.getNumberOfCameras() : 获取Camera设备数量 index : 索引就是后续会使用的CameraID Camera.CameraInfo : Camera设备信息 Camera.getCameraInfo(index, info) : 获取指定索引的Camera设备信息，存储到info对象中 Camera.CameraInfo.facing : 相机面对的方向，前置还是后置 CAMERA_FACING_BACK or CAMERA_FACING_FRONT. Camera.CameraInfo.orientation : 相机图像的方向，获取到的图像需要顺时针旋转该角度才能正常显示。值为0、90、180、270。（因为我们拿手机一般是竖着拿，但是摄像头可能是向左横着或向右横着被安装的） Camera.open : 获取指定索引位置的Camera设备实例。 12345678910111213141516// 遍历所有的camera设备for (int index = 0; index &lt; android.hardware.Camera.getNumberOfCameras(); ++index) &#123; // 获取camera设备信息 android.hardware.Camera.CameraInfo info = new android.hardware.Camera.CameraInfo(); android.hardware.Camera.getCameraInfo(index, info); String facing = (info.facing == android.hardware.Camera.CameraInfo.CAMERA_FACING_FRONT) ? "front" : "back"; final android.hardware.Camera camera; try &#123; // 开启camera设备，获取camera实例 camera = android.hardware.Camera.open(cameraId); &#125; catch (RuntimeException e) &#123; callback.onFailure(FailureType.ERROR, e.getMessage()); return; &#125;&#125; 设置采集参数设置采集的尺寸、格式、对焦、闪光灯、白平衡等 123456789101112131415161718192021222324// 获取相机设备的参数集final android.hardware.Camera.Parameters parameters = camera.getParameters();// 获取手机支持的对焦模式（为了应对各手机厂商的差异，一般在设置某个功能时，先判断当前手机是否支持该功能）final List&lt;String&gt; focusModes = parameters.getSupportedFocusModes();// 设置FPS的区间parameters.setPreviewFpsRange(framerate.min, framerate.max);// 设置预览尺寸parameters.setPreviewSize(width, height);// 设置预览数据的像素格式，一般设置NV21parameters.setPreviewFormat(ImageFormat.NV21);// 视频稳像if (parameters.isVideoStabilizationSupported()) &#123; parameters.setVideoStabilization(true);&#125;// 如果当前手机支持，则设置对焦模式为 FOCUS_MODE_CONTINUOUS_VIDEOif (focusModes.contains(android.hardware.Camera.Parameters.FOCUS_MODE_CONTINUOUS_VIDEO)) &#123; parameters.setFocusMode(android.hardware.Camera.Parameters.FOCUS_MODE_CONTINUOUS_VIDEO);&#125;// 设置参数，重启相机才生效camera.setParameters(parameters);// 设置相机预览显示的图像方向camera.setDisplayOrientation(0 /* degrees */); 下面的代码就是设置相机预览显示的图像角度根据activity显示的方向来调整1234567891011121314151617181920212223242526public static void setCameraDisplayOrientation(Activity activity, int cameraId, android.hardware.Camera camera) &#123; android.hardware.Camera.CameraInfo info = new android.hardware.Camera.CameraInfo(); android.hardware.Camera.getCameraInfo(cameraId, info); int rotation = activity.getWindowManager().getDefaultDisplay() .getRotation(); int degrees = 0; switch (rotation) &#123; case Surface.ROTATION_0: degrees = 0; break; case Surface.ROTATION_90: degrees = 90; break; case Surface.ROTATION_180: degrees = 180; break; case Surface.ROTATION_270: degrees = 270; break; &#125; int result; if (info.facing == Camera.CameraInfo.CAMERA_FACING_FRONT) &#123; // 前置相机是镜像的 result = (info.orientation + degrees) % 360; result = (360 - result) % 360; &#125; else &#123; // 后置相机 result = (info.orientation - degrees + 360) % 360; &#125; camera.setDisplayOrientation(result);&#125; 设置输出获取输出数据有两种方式，一种是设置一个SurfaceTexture采用纹理进行接收；另一种是通过设置回调直接接收byte array数据。 Texture格式SurfaceTexture有两种方式获取，一种是通过系统的SurfaceView或者TextureView来获取，另一种就是自建OES纹理，通过构造函数new。 通过系统的View来获取的SurfaceTexture，相机设备将数据写入后，系统View会自动取出数据更新显示。 自己创建的SurfaceTexture可以通过设置OnFrameAvailableListener来监听数据的变更，当相机设备写入数据时，会触发回调，在回调中使用updateTexImage从队列中取出一个数据绑定到OES纹理中，使用OES纹理来进行绘制显示或者编码输入等操作。1234567try &#123; camera.setPreviewTexture(getSurfaceTexture());&#125; catch (IOException | RuntimeException e) &#123; camera.release(); callback.onFailure(FailureType.ERROR, e.getMessage()); return;&#125; ByteArray格式对于ByteArray格式的回调设置，又有两种方式，一种是setPreviewCallback方法，另一种是setPreviewCallbackWithBuffer方法。 第一种onPreviewFrame回调方法会在每一帧数据准备好了就调用，回调时机是不可控的。 第二种方式是在需要在前一帧的onPreviewFrame方法中调用addCallbackBuffer方法，下一帧的onPreviewFrame才会调用，同时addCallbackBuffer方法的参数的byte数据就是每一帧的原数据。 1234567// 设置回调camera.setPreviewCallback(new Camera.PreviewCallback() &#123; @Override public void onPreviewFrame(byte[] data, Camera camera) &#123; &#125;&#125;); 123456789101112final int frameSize = captureFormat.frameSize();final ByteBuffer buffer = ByteBuffer.allocateDirect(frameSize);// 设置数据回调的buffercamera.addCallbackBuffer(buffer.array());// 设置回调camera.setPreviewCallbackWithBuffer(new android.hardware.Camera.PreviewCallback() &#123; @Override public void onPreviewFrame(final byte[] data, android.hardware.Camera callbackCamera) &#123; // 获取下一个回调 camera.addCallbackBuffer(data); &#125;&#125; 采集 startPreview : 开启预览，数据开始回调，更改采集参数后需要重新开启预览才生效 stopPreview : 停止预览12345678try &#123; camera.startPreview();&#125; catch (RuntimeException e) &#123;&#125;camera.stopPreview();camera.release(); Camera2获取Camera设备信息Camera设备的获取需要通过系统服务来操作。 getCameraIdList : 获取camera设备列表 getCameraCharacteristics : 获取指定camera设备参数集，通过对应的KEY从参数集中获取到各种参数值 123456789101112131415this.cameraManager = (CameraManager) context.getSystemService(Context.CAMERA_SERVICE);String[] cameraIdList = cameraManager.getCameraIdList();for (String cameraId : cameraIdList) &#123; CameraCharacteristics characteristics = cameraManager.getCameraCharacteristics(cameraId); // 获取相机面对的方向，CameraMetadata.LENS_FACING_BACK or CameraMetadata.LENS_FACING_FRONT String facing = characteristics.get(CameraCharacteristics.LENS_FACING) == CameraMetadata.LENS_FACING_BACK; try &#123; // 开启camera设备，设置开启回调，以及指定camera回调执行的线程 cameraManager.openCamera(cameraId, new CameraStateCallback(), cameraThreadHandler); &#125; catch (CameraAccessException e) &#123; return; &#125;&#125; camera设备状态回调，在回调中可以开启一次会话，在会话中可以进行数据的请求123456789101112131415161718192021222324252627282930313233343536373839404142434445464748private class CameraStateCallback extends CameraDevice.StateCallback &#123; private String getErrorDescription(int errorCode) &#123; switch (errorCode) &#123; case CameraDevice.StateCallback.ERROR_CAMERA_DEVICE: return "Camera device has encountered a fatal error."; case CameraDevice.StateCallback.ERROR_CAMERA_DISABLED: return "Camera device could not be opened due to a device policy."; case CameraDevice.StateCallback.ERROR_CAMERA_IN_USE: return "Camera device is in use already."; case CameraDevice.StateCallback.ERROR_CAMERA_SERVICE: return "Camera service has encountered a fatal error."; case CameraDevice.StateCallback.ERROR_MAX_CAMERAS_IN_USE: return "Camera device could not be opened because" + " there are too many other open camera devices."; default: return "Unknown camera error: " + errorCode; &#125; &#125; @Override public void onDisconnected(CameraDevice camera) &#123; // camera设备断开连接 &#125; @Override public void onError(CameraDevice camera, int errorCode) &#123; // 发生错误 &#125; @Override public void onOpened(CameraDevice camera) &#123; // camera设备成功开启 cameraDevice = camera; // 创建采集会话，设置采集输出的Surface集合，可同时输出到多个Surface中 try &#123; camera.createCaptureSession( Arrays.asList(surface), new CaptureSessionCallback(), cameraThreadHandler); &#125; catch (CameraAccessException e) &#123; return; &#125; &#125; @Override public void onClosed(CameraDevice camera) &#123; // camera设备关闭 &#125; &#125; 设置采集参数采集会话创建成功后，可以设置采集的参数，然后创建采集请求，进行数据的采集123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384private class CaptureSessionCallback extends CameraCaptureSession.StateCallback &#123; @Override public void onConfigureFailed(CameraCaptureSession session) &#123; // 配置失败 &#125; @Override public void onConfigured(CameraCaptureSession session) &#123; // 设置采集参数 captureSession = session; try &#123; // 创建视频采集请求构建器，设置采集的参数 final CaptureRequest.Builder captureRequestBuilder = cameraDevice.createCaptureRequest(CameraDevice.TEMPLATE_RECORD); // 设置FPS的区间 captureRequestBuilder.set(CaptureRequest.CONTROL_AE_TARGET_FPS_RANGE, new Range&lt;Integer&gt;(framerate.min,framerate.max)); // 设置闪光灯模式 captureRequestBuilder.set( CaptureRequest.CONTROL_AE_MODE, CaptureRequest.CONTROL_AE_MODE_ON); captureRequestBuilder.set(CaptureRequest.CONTROL_AE_LOCK, false); // 设置视频稳像 chooseStabilizationMode(captureRequestBuilder); // 设置对焦模式 chooseFocusMode(captureRequestBuilder); // 添加采集输出目标，可添加多个，添加的目标必须在createCaptureSession的集合中 captureRequestBuilder.addTarget(surface); // 重复发送请求，开始采集 session.setRepeatingRequest( captureRequestBuilder.build(), new CameraCaptureCallback(), cameraThreadHandler); &#125; catch (CameraAccessException e) &#123; reportError("Failed to start capture request. " + e); return; &#125; &#125; // 优先采用光学稳像，其次采用软件稳像 private void chooseStabilizationMode(CaptureRequest.Builder captureRequestBuilder) &#123; final int[] availableOpticalStabilization = cameraCharacteristics.get( CameraCharacteristics.LENS_INFO_AVAILABLE_OPTICAL_STABILIZATION); if (availableOpticalStabilization != null) &#123; for (int mode : availableOpticalStabilization) &#123; if (mode == CaptureRequest.LENS_OPTICAL_STABILIZATION_MODE_ON) &#123; captureRequestBuilder.set(CaptureRequest.LENS_OPTICAL_STABILIZATION_MODE, CaptureRequest.LENS_OPTICAL_STABILIZATION_MODE_ON); captureRequestBuilder.set(CaptureRequest.CONTROL_VIDEO_STABILIZATION_MODE, CaptureRequest.CONTROL_VIDEO_STABILIZATION_MODE_OFF); Logging.d(TAG, "Using optical stabilization."); return; &#125; &#125; &#125; // If no optical mode is available, try software. final int[] availableVideoStabilization = cameraCharacteristics.get( CameraCharacteristics.CONTROL_AVAILABLE_VIDEO_STABILIZATION_MODES); for (int mode : availableVideoStabilization) &#123; if (mode == CaptureRequest.CONTROL_VIDEO_STABILIZATION_MODE_ON) &#123; captureRequestBuilder.set(CaptureRequest.CONTROL_VIDEO_STABILIZATION_MODE, CaptureRequest.CONTROL_VIDEO_STABILIZATION_MODE_ON); captureRequestBuilder.set(CaptureRequest.LENS_OPTICAL_STABILIZATION_MODE, CaptureRequest.LENS_OPTICAL_STABILIZATION_MODE_OFF); Logging.d(TAG, "Using video stabilization."); return; &#125; &#125; Logging.d(TAG, "Stabilization not available."); &#125; // 设置对焦模式 private void chooseFocusMode(CaptureRequest.Builder captureRequestBuilder) &#123; final int[] availableFocusModes = cameraCharacteristics.get(CameraCharacteristics.CONTROL_AF_AVAILABLE_MODES); for (int mode : availableFocusModes) &#123; if (mode == CaptureRequest.CONTROL_AF_MODE_CONTINUOUS_VIDEO) &#123; captureRequestBuilder.set( CaptureRequest.CONTROL_AF_MODE, CaptureRequest.CONTROL_AF_MODE_CONTINUOUS_VIDEO); Logging.d(TAG, "Using continuous video auto-focus."); return; &#125; &#125; Logging.d(TAG, "Auto-focus is not available."); &#125; &#125; 设置输出Camera2中数据的输出都是通过设置Surface来接收，可以同时设置多个Surface，Surface可以通过SurfaceTexture来创建。 采用系统的SurfaceView、TextureView生成的Surface，设置给Camera，对相机采集的数据进行预览显示 采用系统的ImageReader生成的Surface来接收指定格式的图像数据 采用自己创建的SurfaceTexture来接收数据，通过设置OnFrameAvailableListener来监听数据的变更，当相机设备写入数据时，会触发回调，在回调中使用updateTexImage从队列中取出一个数据绑定到OES纹理中，使用OES纹理来进行绘制显示或者编码输入等操作。 123456789101112131415// 创建采集会话，设置采集输出的Surface集合，可同时输出到多个Surface中try &#123; camera.createCaptureSession( Arrays.asList(surface), new CaptureSessionCallback(), cameraThreadHandler);&#125; catch (CameraAccessException e) &#123; return;&#125;// 会话创建成功后，在创建采集请求时，添加对应的输出目标就可以将采集请求到的数据传入到目标中// 添加采集输出目标，可添加多个，添加的目标必须在createCaptureSession的集合中captureRequestBuilder.addTarget(surface);// 重复请求，开始采集session.setRepeatingRequest( captureRequestBuilder.build(), new CameraCaptureCallback(), cameraThreadHandler); ImageReaderImageReader是跟随Camera2出来的，用来接收相机采集的数据，在拍照时可以设置ImageFormat.JPEG格式，如果想要获取原始数据，则需要设置成ImageFormat.YUV_420_888。 ImageFormat.YUV_420_888格式，是指YUV_420格式，其内部具体格式可以是NV12、NV21、YV12、I420中的任何一种，也就是说如果设置的是YUV_420_888格式，则设备返回的数据格式需要根据其返回的具体数据去判断。（这点在国内手机上各不相同，有些个别低端手机给的数据不符合官方文档上的要求） 对于ImageReader来说，最最重要的就是理解好YUV_420_888格式。 1234567891011121314151617// 创建ImageReader对象，指定接收的数据尺寸和数据格式以及缓冲区大小ImageReader imageReader = ImageReader.newInstance(width, height, ImageFormat.JPEG, 2);// 获取Surface，添加到会话请求的目标中作为输出Surface surface = imageReader.getSurface();// 设置数据回调imageReader.setOnImageAvailableListener(new ImageReader.OnImageAvailableListener() &#123; @Override public void onImageAvailable(ImageReader reader) &#123; // 获取采集到的数据 Image image = reader.acquireNextImage(); // 操作 // 释放 image.close(); &#125;&#125;, mCameraHandler);]]></content>
      <categories>
        <category>拳掌指腿（音视频）</category>
        <category>七伤拳（音视频基础）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[音视频开发之Android录制音频]]></title>
    <url>%2F%E9%9F%B3%E8%A7%86%E9%A2%91%E5%BC%80%E5%8F%91%E4%B9%8BAndroid%E5%BD%95%E5%88%B6%E9%9F%B3%E9%A2%91%2F</url>
    <content type="text"><![CDATA[由于FFmpeg一直都没有支持Android平台的音频输入设备API，所以无法使用FFmpeg在Android上录制音频。 Android平台音频录制APIAndroid上音频输入的API比较繁琐，有多套实现。有Java层的实现AudioRecord，也有native层实现OpenSLES，在 Android O之后又推出了新的native层实现AAudio，并且提供了Oboe库，对OpenSLES和AAudio进行了封装。 下面来详细的介绍下每套API的使用 AudioRecordAudioRecord是Android平台提供的录制音频的Java层API，使用起来非常简单。通过设置采集参数就可以创建一个AudioRecord对象，调用了start方法后，就可以开始读取数据了，通过read方法将数据读取到指定的缓冲区中，调用stop后就停止采集。 创建AudioRecord1234567891011121314151617181920212223242526272829303132// 一个音频帧的字节大小 = 声道数 *（位数 /8）final int bytesPerFrame = channels * (BITS_PER_SAMPLE / 8);// BUFFERS_PER_SECOND = 100，预测一秒钟回调100次// 那么44100的采样率，每次回调应该给的数据帧个数为4410个final int framesPerBuffer = sampleRate / BUFFERS_PER_SECOND;// 一次回调给的总数据字节大小 = 一次给的数据帧个数 * 一个数据帧的字节大小// 创建一个buffer用来接收回调数据，使用堆外内存，在通过JNI传递的时候避免内存拷贝byteBuffer = ByteBuffer.allocateDirect(bytesPerFrame * framesPerBuffer); emptyBytes = new byte[byteBuffer.capacity()];// 避免每次读取数据都需要传递buffer到JNI层，这里提前将buffer的地址保存在JNI层nativeCacheDirectBufferAddress(byteBuffer, nativeAudioRecord);final int channelConfig = channelCountToConfiguration(channels);// 根据音频参数(采样率，声道数，位数)得到系统最小需要的缓冲区大小，创建AudioRecord时设置的缓冲区大小必须大于这个值int minBufferSize = AudioRecord.getMinBufferSize(sampleRate, channelConfig, AudioFormat.ENCODING_PCM_16BIT); // BUFFER_SIZE_FACTOR = 2// 缓冲区一般要大一点，所以下面比较了两倍的最小缓冲区和我们自己计算的缓冲区大小// 也就是说设置的缓冲区大小最小也要是最小缓冲区的两倍。int bufferSizeInBytes = Math.max(BUFFER_SIZE_FACTOR * minBufferSize, byteBuffer.capacity());try &#123; // 根据音频参数创建AudioRecord，audioSource是指音频采集的来源 audioRecord = new AudioRecord(audioSource, sampleRate, channelConfig, AudioFormat.ENCODING_PCM_16BIT, bufferSizeInBytes);&#125; catch (IllegalArgumentException e) &#123; return -1;&#125;if (audioRecord == null || audioRecord.getState() != AudioRecord.STATE_INITIALIZED) &#123; return -1;&#125; 读取数据123456789101112131415161718192021222324252627// 开始后才可以读取数据audioRecord.startRecording(); // 循环读取数据while (keepAlive) &#123; // 将数据读取到之前创建的buffer中，返回读取的字节大小 int bytesRead = audioRecord.read(byteBuffer, byteBuffer.capacity()); if (bytesRead == byteBuffer.capacity()) &#123; // 将读取的字节大小通知给JNI层，因为前面在创建的时候已经将buffer的地址保存在JNI层中，所以这里传递读取的字节大小就行 if (keepAlive) &#123; nativeDataIsRecorded(bytesRead, nativeAudioRecord); &#125; if (audioSamplesReadyCallback != null) &#123; // 拷贝整个数据回调出去 byte[] data = Arrays.copyOf(byteBuffer.array(), byteBuffer.capacity()); audioSamplesReadyCallback.onWebRtcAudioRecordSamplesReady( new AudioSamples(audioRecord, data)); &#125; &#125; else &#123; // 读取错误 &#125; &#125;// 停止后不能再读取数据audioRecord.stop(); 获取设备音频相关信息通过AudioManager可以获取到当前设备默认的采样率以及最小缓冲区大小123AudioManager myAudioMgr = (AudioManager) getSystemService(Context.AUDIO_SERVICE);String nativeParam = myAudioMgr.getProperty(AudioManager.PROPERTY_OUTPUT_SAMPLE_RATE);nativeParam = myAudioMgr.getProperty(AudioManager.PROPERTY_OUTPUT_FRAMES_PER_BUFFER); AudioSource音频来源 DEFAULT：默认为MIC MIC：麦克风 VOICE_UPLINK：电话录音上行线路，系统权限不允许第三方app使用 VOICE_DOWNLINK：电话录音下行线路，系统权限不允许第三方app使用 VOICE_CALL：电话录音上下线路，系统权限不允许第三方app使用 CAMCORDER：摄像头的麦克风 VOICE_RECOGNITION：语音识别 VOICE_COMMUNICATION：网络电话 REMOTE_SUBMIX：传输到远程的音频混合流，默认情况下如果用该项录音，本地扬声器或者耳机的声音将会被截走，系统权限不允许第三方app使用 OpenSL-ESOpenSL-ES是Android平台上提供的高性能音频的API，相对Java层API有着一定的低延迟性。由于是C库，所以使用起来API相对繁琐一些。 配置数据参数123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051SLDataFormat_PCM format;// PCM数据格式format.formatType = SL_DATAFORMAT_PCM;// 声道数format.numChannels = static_cast&lt;SLuint32&gt;(channels);// 设置采样率switch (sample_rate) &#123; case 8000: format.samplesPerSec = SL_SAMPLINGRATE_8; break; case 16000: format.samplesPerSec = SL_SAMPLINGRATE_16; break; case 22050: format.samplesPerSec = SL_SAMPLINGRATE_22_05; break; case 32000: format.samplesPerSec = SL_SAMPLINGRATE_32; break; case 44100: format.samplesPerSec = SL_SAMPLINGRATE_44_1; break; case 48000: format.samplesPerSec = SL_SAMPLINGRATE_48; break; case 64000: format.samplesPerSec = SL_SAMPLINGRATE_64; break; case 88200: format.samplesPerSec = SL_SAMPLINGRATE_88_2; break; case 96000: format.samplesPerSec = SL_SAMPLINGRATE_96; break; default: RTC_CHECK(false) &lt;&lt; "Unsupported sample rate: " &lt;&lt; sample_rate; break;&#125;// 设置位数format.bitsPerSample = SL_PCMSAMPLEFORMAT_FIXED_16;format.containerSize = SL_PCMSAMPLEFORMAT_FIXED_16;// 小端format.endianness = SL_BYTEORDER_LITTLEENDIAN;if (format.numChannels == 1) &#123; format.channelMask = SL_SPEAKER_FRONT_CENTER;&#125; else if (format.numChannels == 2) &#123; format.channelMask = SL_SPEAKER_FRONT_LEFT | SL_SPEAKER_FRONT_RIGHT;&#125; else &#123; RTC_CHECK(false) &lt;&lt; "Unsupported number of channels: " &lt;&lt; format.numChannels;&#125; 创建引擎在OpenSLES中，任何API接口对象都要由引擎来创建1234567891011121314151617181920// 设置线程安全模式const SLEngineOption option[] = &#123; &#123;SL_ENGINEOPTION_THREADSAFE, static_cast&lt;SLuint32&gt;(SL_BOOLEAN_TRUE)&#125;&#125;;// 创建引擎接口SLresult result = slCreateEngine(engine_object_, 1, option, 0, NULL, NULL);if (result != SL_RESULT_SUCCESS) &#123; return nullptr;&#125;// 设置引擎实现模式为同步模式result = engine_object_-&gt;Realize(engine_object_, SL_BOOLEAN_FALSE);if (result != SL_RESULT_SUCCESS) &#123; return nullptr;&#125;// 根据引擎接口获取到隐藏的引擎实现if ((*engine_object) -&gt;GetInterface(engine_object, SL_IID_ENGINE, &amp;engine_)) &#123; return false;&#125; 创建录制器在OpenSLES中，录制需要通过录制器对象来进行，而数据的获取是通过回调的方式来获取，但是并非将采集的数据放在回调函数的参数中进行传递，回调函数仅仅只是一个通知的意义，真正的数据是在Enqueue函数中传入的buffer中。123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566// 设置IO设备SLDataLocator_IODevice mic_locator = &#123;SL_DATALOCATOR_IODEVICE, SL_IODEVICE_AUDIOINPUT, SL_DEFAULTDEVICEID_AUDIOINPUT, NULL&#125;;// 设置数据源SLDataSource audio_source = &#123;&amp;mic_locator, NULL&#125;;// 设置缓冲队列大小SLDataLocator_AndroidSimpleBufferQueue buffer_queue = &#123; SL_DATALOCATOR_ANDROIDSIMPLEBUFFERQUEUE, static_cast&lt;SLuint32&gt;(kNumOfOpenSLESBuffers)&#125;;// 设置数据接收器的缓冲队列和数据参数 SLDataSink audio_sink = &#123;&amp;buffer_queue, &amp;pcm_format_&#125;;// const SLInterfaceID interface_id[] = &#123;SL_IID_ANDROIDSIMPLEBUFFERQUEUE, SL_IID_ANDROIDCONFIGURATION&#125;;const SLboolean interface_required[] = &#123;SL_BOOLEAN_TRUE, SL_BOOLEAN_TRUE&#125;;// 通过引擎实现创建录制器接口if ((*engine_)-&gt;CreateAudioRecorder( engine_, recorder_object_, &amp;audio_source, &amp;audio_sink, arraysize(interface_id), interface_id, interface_required)) &#123; return false;&#125;// 通过录制器接口创建录制器配置实现，用来设置相关录制配置参数SLAndroidConfigurationItf recorder_config;if ((recorder_object_-&gt;GetInterface(recorder_object_, SL_IID_ANDROIDCONFIGURATION, &amp;recorder_config))) &#123; return false;&#125;// 设置音频采集源SLint32 stream_type = SL_ANDROID_RECORDING_PRESET_VOICE_COMMUNICATION;if (((*recorder_config) -&gt;SetConfiguration(recorder_config, SL_ANDROID_KEY_RECORDING_PRESET, &amp;stream_type, sizeof(SLint32)))) &#123; return false;&#125;// 设置录制器实现模式为同步模式if ((recorder_object_-&gt;Realize(recorder_object_, SL_BOOLEAN_FALSE))) &#123; return false;&#125;// 通过录制器接口创建录制器实现，用来采集数据if ((recorder_object_-&gt;GetInterface( recorder_object_, SL_IID_RECORD, &amp;recorder_))) &#123; return false;&#125;// 通过录制器接口创建缓冲队列实现，用来传递数据if ((recorder_object_-&gt;GetInterface( recorder_object_.Get(), SL_IID_ANDROIDSIMPLEBUFFERQUEUE, &amp;simple_buffer_queue_))) &#123; return false;&#125;// 注册在缓冲队列上的回调，通过回调来获取到数据，SimpleBufferQueueCallback就是回调函数，this是回调参数的context参数if (((*simple_buffer_queue_) -&gt;RegisterCallback(simple_buffer_queue_, SimpleBufferQueueCallback, this))) &#123; return false;&#125; 采集数据每调用一次Enqueue函数都会触发一次数据回调，如果需要循环采集，则需要在回调函数中递归调用Enqueue函数。 1234567891011// 每次调用Enqueue方法设置一个buffer入队，数据就会被采集到该buffer中，然后通过回调函数通知，在回调函数中才该buffer中获取对应数据// 第一个参数就是缓冲队列实现对象，第二个参数就是buffer，第三个参数是采集的数据字节大小SLresult err = (*simple_buffer_queue_) -&gt;Enqueue( simple_buffer_queue_, reinterpret_cast&lt;SLint8*&gt;(audio_buffers_[buffer_index_].get()), audio_parameters_.GetBytesPerBuffer()); if (SL_RESULT_SUCCESS != err) &#123; return false; &#125; 123456789void OpenSLESRecorder::SimpleBufferQueueCallback( SLAndroidSimpleBufferQueueItf buffer_queue, void* context) &#123; // 数据回调函数，context就是在设置回调时设置的this OpenSLESRecorder* stream = static_cast&lt;OpenSLESRecorder*&gt;(context); // 接收到数据采集成功回调，从缓冲队列中读取数据 stream-&gt;ReadBufferQueue(); // Enqueue()&#125;]]></content>
      <categories>
        <category>拳掌指腿（音视频）</category>
        <category>七伤拳（音视频基础）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[音视频开发之FFmpeg录制视频]]></title>
    <url>%2F%E9%9F%B3%E8%A7%86%E9%A2%91%E5%BC%80%E5%8F%91%E4%B9%8BFFmpeg%E5%BD%95%E5%88%B6%E8%A7%86%E9%A2%91%2F</url>
    <content type="text"><![CDATA[视频录制流程前面讲解录制音频时讲过，FFmpeg对各大平台的输入和输出API进行了统一的封装，这里就不在阐述。 视频的录制相比音频的录制流程基本差不多，主要是去掉了FIFO队列的逻辑。另外对于视频帧来说，FFmpeg提供的SwsContext API处理较慢，一般不太使用，可以用libyuv库，或者使用OpenGL API用GPU来提速。 打开视频输入注册视频设备所有的输入和输出设备默认都是没有注册的，如果需要使用，需要手动调用下面的注册函数。 1avdevice_register_all(); 创建输入上下文FFmpeg将对视频文件的解封装和封装等操作都抽象到AVFormatContext中，所以视频文件的打开和输出都是要通道AVFormatContext来完成。 1234567891011121314// Mac平台上依旧使用avfoundation APIAVInputFormat* format = av_find_input_format("avfoundation");// 视频的采集必须设置采集的分辨率和帧率以及像素格式，如果设置的组合在设备上不支持，会出错AVDictionary* options = nullptr;av_dict_set(&amp;options, "video_size", "1280x720", 0);av_dict_set(&amp;options, "framerate", "30", 0);av_dict_set(&amp;options, "pixel_format", "nv12", 0);// 开启输入上下文if ((error = avformat_open_input(&amp;fmt_ctx_in_, "0", format, &amp;options)) &lt; 0) &#123; std::cout &lt;&lt; "Could not open input device " &lt;&lt; av_err2str(error) &lt;&lt; std::endl; fmt_ctx_in_ = nullptr; return error;&#125; 获取设备输入数据格式FFmpeg将视频文件中的每一个轨道都封装成了AVStream对象，一个正常的媒体文件一般都包含两个Stream，一个音频流和一个视频流。 对于这里，视频设备只有一个输入流，AVStream中包含了输入视频数据的分辨率、帧率、像素格式等视频相关信息 12// 视频设备只有一个输入流stream_in_ = fmt_ctx_in_-&gt;streams[0]; 创建解码上下文FFmpeg将对视频文件的编码和解码等操作都抽象到AVCodecContext中，所以视频文件的编码和解码都是要通过AVCodecContext来完成。 12345678910111213141516171819202122232425// 获取视频输入流的编码格式，进行解码codec = avcodec_find_decoder(stream_in_-&gt;codecpar-&gt;codec_id);if (!codec) &#123; std::cout &lt;&lt; "Could not find input codec" &lt;&lt; std::endl; return AVERROR_EXIT;&#125;// 创建解码上下文codec_ctx_in_ = avcodec_alloc_context3(codec);if (!codec_ctx_in_) &#123; std::cout &lt;&lt; "Could not allocate input codec context" &lt;&lt; std::endl; return AVERROR(ENOMEM);&#125;// 将输入流中的解码参数设置给解码上下文if ((error = avcodec_parameters_to_context(codec_ctx_in_, stream_in_-&gt;codecpar)) &lt; 0) &#123; std::cout &lt;&lt; "Could not copy parameters to input codec" &lt;&lt; av_err2str(error) &lt;&lt; std::endl; return error;&#125;// 开启解码器if ((error = avcodec_open2(codec_ctx_in_, codec, nullptr)) &lt; 0) &#123; std::cout &lt;&lt; "Could not open input codec " &lt;&lt; av_err2str(error) &lt;&lt; std::endl; return error;&#125; 打开视频输出创建输出上下文下面是创建了一个aac文件的输出上下文 12345678910111213const char* filename = "/Users/gaozhenyu/Desktop/video.h264";AVOutputFormat* oformat = av_guess_format(nullptr, filename, nullptr);// 创建输出上下文if ((error = avformat_alloc_output_context2(&amp;fmt_ctx_out_, oformat, nullptr, filename)) &lt; 0) &#123; std::cout &lt;&lt; "Could not allocate output format context" &lt;&lt; std::endl; return error;&#125;// 开启输出IOif ((error = avio_open(&amp;(fmt_ctx_out_-&gt;pb), filename, AVIO_FLAG_WRITE)) &lt; 0) &#123; std::cout &lt;&lt; "Could not open output file" &lt;&lt; av_err2str(error) &lt;&lt; std::endl; return error;&#125; 创建指定编码器上下文使用libx264编码器来进行编码，并设置音频编码的相关参数 123456789101112131415161718192021222324252627282930313233343536// 找到libx264编码器codec = avcodec_find_encoder_by_name("libx264");if (!codec) &#123; std::cout &lt;&lt; "Could not find libfdk_aac encoder" &lt;&lt; std::endl; return AVERROR_EXIT;&#125;// 创建输出编码上下文codec_ctx_out_ = avcodec_alloc_context3(codec);if (!stream_out_) &#123; std::cout &lt;&lt; "Could not allocate output codec context" &lt;&lt; std::endl; return AVERROR(ENOMEM);&#125;// 设置输出编码参数codec_ctx_out_-&gt;profile = FF_PROFILE_H264_HIGH;codec_ctx_out_-&gt;level = 50;codec_ctx_out_-&gt;width = 640;codec_ctx_out_-&gt;height = 520;codec_ctx_out_-&gt;gop_size = 300;codec_ctx_out_-&gt;keyint_min = 30;codec_ctx_out_-&gt;max_b_frames = 3;codec_ctx_out_-&gt;has_b_frames = 1;codec_ctx_out_-&gt;refs = 3;codec_ctx_out_-&gt;pix_fmt = AV_PIX_FMT_YUV420P;codec_ctx_out_-&gt;bit_rate = 6000000;codec_ctx_out_-&gt;time_base = AVRational&#123;1, 25&#125;;codec_ctx_out_-&gt;framerate = AVRational&#123;25, 1&#125;;// 开启编码器if ((error = avcodec_open2(codec_ctx_out_, codec, nullptr)) &lt; 0) &#123; std::cout &lt;&lt; "Could not open output codec " &lt;&lt; av_err2str(error) &lt;&lt; std::endl; return error;&#125; 创建输出流这里我们需要输出的轨道只有一个视频轨道，所以创建一个流就可以。创建完流后还需要将上面设置的视频编码相关参数设置给流。 12345678910// 创建指定编码器的输出视频流，添加到输出上下文中stream_out_ = avformat_new_stream(fmt_ctx_out_, codec);// 将编码上下文中的参数设置给输出流if ((error = avcodec_parameters_from_context(stream_out_-&gt;codecpar, codec_ctx_out_)) &lt; 0) &#123; std::cout &lt;&lt; "Could not initialize output stream parameters " &lt;&lt; av_err2str(error) &lt;&lt; std::endl; return error;&#125; 初始化图像处理上下文图像处理上下文FFmpeg将对视频的图像处理操作抽象到SwsContext中，根据输入的视频帧数据格式和处理后的视频帧数据格式来创建一个SwsContext。 实际上决定视频帧数据格式的就是三要素：宽、高、像素格式 12345678910// 创建图像处理上下文，设置输入输出参数sws_ctx_ = sws_getContext(codec_ctx_in_-&gt;width, codec_ctx_in_-&gt;height, codec_ctx_in_-&gt;pix_fmt, codec_ctx_out_-&gt;width, codec_ctx_out_-&gt;height, codec_ctx_out_-&gt;pix_fmt, SWS_BILINEAR, nullptr, nullptr, nullptr);if (!sws_ctx_) &#123; std::cout &lt;&lt; "Could not allocate sws context" &lt;&lt; std::endl; return AVERROR_EXIT;&#125;return 0; 录制对于AVFormatContext来进行视频数据的输出，需要调用三个write函数，avformat_write_header、av_write_frame、av_write_trailer。 整体流程就是不断的从视频设备中读取数据，解码后进行图像处理，将处理后的视频帧进行编码，编码后写入到输出文件中。 12345678910111213141516171819202122232425262728293031323334// 5. 读取数据并编码while (request_abort_) &#123; AVFrame* frame = av_frame_alloc(); int data_present; AVFrame* out = av_frame_alloc(); // 读取并解码 if ((error = DecodeVideoFrame(frame, &amp;data_present)) &lt; 0) &#123; av_frame_free(&amp;frame); std::cout &lt;&lt; "Could not decoding video packet " &lt;&lt; av_err2str(error) &lt;&lt; std::endl; goto cleanup; &#125; if (data_present) &#123; // 视频图像处理 out-&gt;format = codec_ctx_out_-&gt;pix_fmt; out-&gt;width = codec_ctx_out_-&gt;width; out-&gt;height = codec_ctx_out_-&gt;height; if ((error = av_frame_get_buffer(out, 0)) &lt; 0) &#123; std::cout &lt;&lt; "Could not alloc av image" &lt;&lt; av_err2str(error) &lt;&lt; std::endl; goto cleanup; &#125; sws_scale(sws_ctx_, (const uint8_t* const*)frame-&gt;data, frame-&gt;linesize, 0, frame-&gt;height, out-&gt;data, out-&gt;linesize); &#125; // 编码并写入 if ((error = EncodeVideoFrame(out, data_present)) &lt; 0) &#123; av_frame_free(&amp;frame); std::cout &lt;&lt; "Could not encodeing video frame " &lt;&lt; av_err2str(error) &lt;&lt; std::endl; goto cleanup; &#125; 读取数据并解码读取数据非常简单，创建一个AVPacket对象，通过av_read_frame就可以读取一帧数据 1234// 创建AVPacket，用来从视频设备中接收一帧数据av_init_packet(&amp;pkt);// 从视频设备中读取一帧数据到AVPacket中av_read_frame(fmt_ctx_in_, &amp;pkt); 通过avcodec_send_packet、avcodec_receive_frame来进行解码，avcodec_send_packet负责将一个AVPacket送给解码器，avcodec_receive_frame负责从解码器中取出一个已经解码好的AVFrame，如果返回AVERROR(EAGAIN)表明当前没有已经解码好的数据，返回AVERROR_EOF表明已经全部解码完成，返回大于0则表明获取解码数据成功。 12345678910111213141516171819202122 // 发送到解码器中进行解码 if ((error = avcodec_send_packet(codec_ctx_in_, &amp;pkt)) &lt; 0) &#123; goto cleanup; &#125; // 从解码器中获取一帧解码后的数据，存到AVFrame中 error = avcodec_receive_frame(codec_ctx_in_, frame); if (error == AVERROR(EAGAIN)) &#123; // 没有获取到数据 error = 0; goto cleanup; &#125; else if (error == AVERROR_EOF) &#123; // 解码器中数据已经全部解码完成 error = 0; *finished = 1; &#125; else if (error &lt; 0) &#123; // 解码错误 goto cleanup; &#125; else &#123; // 数据成功解码到AVFrame中 *data_present = 1; error = 0; &#125; 图像处理创建一个AVFrame用来接收处理后的视频帧1234567891011121314AVFrame* out = av_frame_alloc();// 设置输出参数给视频帧out-&gt;format = codec_ctx_out_-&gt;pix_fmt;out-&gt;width = codec_ctx_out_-&gt;width;out-&gt;height = codec_ctx_out_-&gt;height;// 分配bufferif ((error = av_frame_get_buffer(out, 0)) &lt; 0) &#123; std::cout &lt;&lt; "Could not alloc av image" &lt;&lt; av_err2str(error) &lt;&lt; std::endl; goto cleanup;&#125;// 图像处理，将解码后的AVFrame数据处理后，存储到上面创建的AVFrame中sws_scale(sws_ctx_, (const uint8_t* const*)frame-&gt;data, frame-&gt;linesize, 0, frame-&gt;height, out-&gt;data, out-&gt;linesize); 编码数据并输出通过avcodec_send_frame、avcodec_receive_packet来进行编码。avcodec_send_frame负责将一个AVFrame帧发送给编码器，avcodec_receive_packet负责从编码器中取出一帧已经编码好的数据。 1234567891011121314151617181920212223242526272829303132AVPacket pkt;// 创建AVPacket，接收编码后的数据av_init_packet(&amp;pkt);// 如果待编码数据不为空，则累计计算当前帧的ptsif (frame) &#123; frame-&gt;pts = pts; pts += frame-&gt;nb_samples;&#125;// 发送AVFrame到编码器中进行编码if ((error = avcodec_send_frame(codec_ctx_out_, frame)) &lt; 0) &#123; // 发送失败 goto cleanup;&#125;// 接收编码后的数据到AVPacket中error = avcodec_receive_packet(codec_ctx_out_, &amp;pkt);if (error == AVERROR(EAGAIN)) &#123; // 没有获取到编码后数据 error = 0; goto cleanup;&#125; else if (error == AVERROR_EOF) &#123; // 编码器中所有数据编码完成 error = 0; goto cleanup;&#125; else if (error &lt; 0) &#123; // 编码出错 goto cleanup;&#125; else &#123; // 成功获取编码后数据到AVPacket中 error = 0; *data_written = 1;&#125; 写入到输出12345// 如果获取数据，则写入到输出上下文中if (*data_written &amp;&amp; (error = av_write_frame(fmt_ctx_out_, &amp;pkt)) &lt; 0) &#123; // 写入失败 goto cleanup;&#125;]]></content>
      <categories>
        <category>拳掌指腿（音视频）</category>
        <category>降龙十八掌（FFmpeg）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[音视频开发之FFmpeg录制音频]]></title>
    <url>%2F%E9%9F%B3%E8%A7%86%E9%A2%91%E5%BC%80%E5%8F%91%E4%B9%8BFFmpeg%E5%BD%95%E5%88%B6%E9%9F%B3%E9%A2%91%2F</url>
    <content type="text"><![CDATA[音频录制流程FFmpeg对各大平台的音频输入和输出的API进行了统一的封装，将输入API封装成AVInputFormat，将输出API封装成AVOutputFormat，并且将音频采集设备封装成一个音频文件进行处理，所以我们在采集音频数据的时候，可以将音频设备当做一个无限大的音频文件，不断的从音频文件中读取数据。 打开音频输入注册音频设备所有的输入和输出设备默认都是没有注册的，如果需要使用，需要手动调用下面的注册函数。 1avdevice_register_all(); 创建输入上下文FFmpeg将对音频文件的解封装和封装等操作都抽象到AVFormatContext中，所以音频文件的打开和输出都是要通道AVFormatContext来完成。 1234567891011// 设置音频设备API Mac上为avfoundationAVInputFormat* format = av_find_input_format("avfoundation");// :0 为音频第一个设备，开启音频设备，FFmpeg中将音频设备封装为一个音频文件// video:audio 如果视频设备为空，则写成 :音频设备// 获取到输入上下文if ((error = avformat_open_input(&amp;fmt_ctx_in_, ":0", format, nullptr)) &lt; 0) &#123; std::cout &lt;&lt; "Could not open input device " &lt;&lt; av_err2str(error) &lt;&lt; std::endl; fmt_ctx_in_ = nullptr; return error;&#125; 获取设备输入数据格式FFmpeg将音频文件中的每一个轨道都封装成了AVStream对象，一个正常的音频文件一般都包含两个Stream，一个音频流和一个视频流。 对于这里，音频设备只有一个输入流，AVStream中包含了输入音频数据的采样率、采样位数、声道数等音频相关信息 12// 音频设备只有一个输入流stream_in_ = fmt_ctx_in_-&gt;streams[0]; 创建解码上下文由于FFmpeg是将音频设备封装为音频文件来处理的，所以我们从输入流中读取的是AVPacket对象，其data数据是PCM原始音频数据，虽然不解码也可以直接将AVPacket中数据copy到AVFrame中，但最好通过解码转换为AVFrame。 FFmpeg将对音频文件的编码和解码等操作都抽象到AVCodecContext中，所以音频文件的编码和解码都是要通过AVCodecContext来完成。 12345678910111213141516171819202122232425// 获取音频输入流的编码格式，进行解码codec = avcodec_find_decoder(stream_in_-&gt;codecpar-&gt;codec_id);if (!codec) &#123; std::cout &lt;&lt; "Could not find input codec" &lt;&lt; std::endl; return AVERROR_EXIT;&#125;// 创建解码上下文codec_ctx_in_ = avcodec_alloc_context3(codec);if (!codec_ctx_in_) &#123; std::cout &lt;&lt; "Could not allocate input codec context" &lt;&lt; std::endl; return AVERROR(ENOMEM);&#125;// 将输入流中的解码参数设置给解码上下文if ((error = avcodec_parameters_to_context(codec_ctx_in_, stream_in_-&gt;codecpar)) &lt; 0) &#123; std::cout &lt;&lt; "Could not copy parameters to input codec" &lt;&lt; av_err2str(error) &lt;&lt; std::endl; return error;&#125;// 开启解码器if ((error = avcodec_open2(codec_ctx_in_, codec, nullptr)) &lt; 0) &#123; std::cout &lt;&lt; "Could not open input codec " &lt;&lt; av_err2str(error) &lt;&lt; std::endl; return error;&#125; 打开音频输出创建输出上下文下面是创建了一个aac文件的输出上下文 12345678910111213const char* filename = "/Users/gaozhenyu/Desktop/audio.aac";AVOutputFormat* oformat = av_guess_format(nullptr, filename, nullptr);// 创建输出上下文if ((error = avformat_alloc_output_context2(&amp;fmt_ctx_out_, oformat, nullptr, filename)) &lt; 0) &#123; std::cout &lt;&lt; "Could not allocate output format context" &lt;&lt; std::endl; return error;&#125;// 开启输出IOif ((error = avio_open(&amp;(fmt_ctx_out_-&gt;pb), filename, AVIO_FLAG_WRITE)) &lt; 0) &#123; std::cout &lt;&lt; "Could not open output file" &lt;&lt; av_err2str(error) &lt;&lt; std::endl; return error;&#125; 创建指定编码器上下文使用libfdk_aac编码器来进行编码，并设置音频编码的相关参数 1234567891011121314151617181920212223242526// 找到libfdk_aac编码器codec = avcodec_find_encoder_by_name("libfdk_aac");if (!codec) &#123; std::cout &lt;&lt; "Could not find libfdk_aac encoder" &lt;&lt; std::endl; return AVERROR_EXIT;&#125;// 创建输出编码上下文codec_ctx_out_ = avcodec_alloc_context3(codec);if (!stream_out_) &#123; std::cout &lt;&lt; "Could not allocate output codec context" &lt;&lt; std::endl; return AVERROR(ENOMEM);&#125;// 设置输出编码参数codec_ctx_out_-&gt;sample_fmt = AV_SAMPLE_FMT_S16;codec_ctx_out_-&gt;sample_rate = 48000;codec_ctx_out_-&gt;channel_layout = AV_CH_LAYOUT_STEREO;codec_ctx_out_-&gt;channels = 2;codec_ctx_out_-&gt;bit_rate = 128000;// codec_ctx_out_-&gt;profile = FF_PROFILE_AAC_LD;// 开启编码器if ((error = avcodec_open2(codec_ctx_out_, codec, nullptr)) &lt; 0) &#123; std::cout &lt;&lt; "Could not open output codec " &lt;&lt; av_err2str(error) &lt;&lt; std::endl; return error;&#125; 创建输出流上面说过，音频文件中每一个轨道都被封装成一个AVStream，那么想要向输出一个音频文件，除了上面创建了AVFormatContext外，还需要对每一个轨道创建一个AVStream，并添加到对应的输出AVFormatContext上。 这里我们需要输出的轨道只有一个音频轨道，所以创建一个流就可以。创建完流后还需要将上面设置的音频编码相关参数设置给流。 12345678910// 创建指定编码器的输出音频流，添加到输出上下文中stream_out_ = avformat_new_stream(fmt_ctx_out_, codec);// 将编码上下文中的参数设置给输出流if ((error = avcodec_parameters_from_context(stream_out_-&gt;codecpar, codec_ctx_out_)) &lt; 0) &#123; std::cout &lt;&lt; "Could not initialize output stream parameters " &lt;&lt; av_err2str(error) &lt;&lt; std::endl; return error;&#125; 初始化重采样、FIFO队列重采样上下文FFmpeg将对音频的重采样操作抽象到SwrContext中，根据输入的音频数据格式和重采样后的音频数据格式来创建一个SwrContext。 实际上决定音频数据格式的就是三要素：采样率、声道数、采样位数 12345678910111213141516// 创建重采样上下文，设置重采样输入输出参数swr_ctx_ = swr_alloc_set_opts( nullptr, av_get_default_channel_layout(codec_ctx_out_-&gt;channels), codec_ctx_out_-&gt;sample_fmt, codec_ctx_out_-&gt;sample_rate, av_get_default_channel_layout(codec_ctx_in_-&gt;channels), codec_ctx_in_-&gt;sample_fmt, codec_ctx_in_-&gt;sample_rate, 0, nullptr);if (!swr_ctx_) &#123; std::cout &lt;&lt; "Could not allocate swr context" &lt;&lt; std::endl; return AVERROR_EXIT;&#125;// 初始化重采样上下文if ((error = swr_init(swr_ctx_)) &lt; 0) &#123; std::cout &lt;&lt; "Could not open swr context" &lt;&lt; av_err2str(error) &lt;&lt; std::endl; swr_free(&amp;swr_ctx_); return error;&#125; FIFO队列音频数据和视频数据在对一帧数据的定义上不太一样，对于视频来说一帧就是一张图像，每次从设备上采集来的都是一帧数据，而对于音频来说，都是散列的采样点，多少个采样点作为一帧需要看具体使用的编码器的要求。所以从设备上采集来的数据数量并不一定满足编码器需要的采样数量。 所以FFmpeg提供了AVAudioFifo队列来完成对数据的缓存。 12345678// 创建音频FIFO队列，用来作为缓冲区，音频编码器需要一定的帧数才可以进行编码// 所以将读取的音频帧存放在FIFO队列中，当大于音频编码器需要的帧数时，才进行编码fifo_ = av_audio_fifo_alloc(codec_ctx_out_-&gt;sample_fmt, codec_ctx_out_-&gt;channels, 1);if (!fifo_) &#123; std::cout &lt;&lt; "Could not allocate FIFO" &lt;&lt; std::endl; return AVERROR_EXIT;&#125; 录制对于AVFormatContext来进行音频数据的输出，需要调用三个write函数，avformat_write_header、av_write_frame、av_write_trailer。 整体流程就是不断的从音频设备中读取数据，解码后进行重采样，将重采样结果放入到FIFO队列中，当FIFO中数据足够时，则从FIFO中取出数据进行编码，编码后写入到输出文件中。 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556 if (avformat_write_header(fmt_ctx_out_, nullptr) &lt; 0) &#123; std::cout &lt;&lt; "Could not write output file header" &lt;&lt; std::endl; goto cleanup; &#125;while (request_abort_) &#123; int finished = 0; // 一个音频帧中每个通道的样本数。 // 编码：由libavcodec在avcodec_open2（）中设置。 // 除最后一个帧外，每个提交的帧必须在每个通道中完全包含frame_size样本。 // 解码：可能由某些解码器设置以指示恒定的帧大小 const int frame_size_out = codec_ctx_out_-&gt;frame_size; // 如果FIFO队列中音频样本数不足编码器需要的样本数，则循环读取数据，直到FIFO中数据满足需要的样本量 while (av_audio_fifo_size(fifo_) &lt; frame_size_out) &#123; // 读取音频样本加入到FIFO队列中 if (ReadAndStore(&amp;finished) &lt; 0) &#123; // 数据读取失败，则直接退出 goto cleanup; &#125; // 如果输入数据读取完了，则最后一帧的样本数可以小于指定的样本数 if (finished) &#123; // 跳出 break; &#125; &#125; // 如果FIFO队列中音频样本数大于编码器需要的 或者 // 输入数据读取完但是FIFO中还有未处理的数据 // 则循环不断处理，直到FIFO中数据不足一帧 或者 数据全部处理完成 才退出 while (av_audio_fifo_size(fifo_) &gt;= frame_size_out || (finished &amp;&amp; av_audio_fifo_size(fifo_) &gt; 0)) &#123; // 从FIFO中取出数据进行编码并写入到输出文件中 if (EncodeAndWrite() &lt; 0) &#123; // 编码或写入失败，则直接退出 goto cleanup; &#125; &#125; // 输入数据读取完，但是编码器中可能还有最后几帧数据还在编码，所以需要重新从编码器中读取 if (finished) &#123; int data_written; do &#123; data_written = 0; // 传入空的数据，进行编码，如果编码器中有数据，则继续 if (EncodeAudioFrame(nullptr, &amp;data_written) &lt; 0) &#123; goto cleanup; &#125; &#125; while (data_written); break; &#125; &#125; if (av_write_trailer(fmt_ctx_out_) &lt; 0) &#123; std::cout &lt;&lt; "Could not write output file trailer" &lt;&lt; std::endl; goto cleanup; &#125; 读取数据并解码读取数据非常简单，创建一个AVPacket对象，通过av_read_frame就可以读取一帧数据 1234// 创建AVPacket，用来从音频设备中接收一帧数据av_init_packet(&amp;pkt);// 从音频设备中读取一帧数据到AVPacket中av_read_frame(fmt_ctx_in_, &amp;pkt); 通过avcodec_send_packet、avcodec_receive_frame来进行解码，avcodec_send_packet负责将一个AVPacket送给解码器，avcodec_receive_frame负责从解码器中取出一个已经解码好的AVFrame，如果返回AVERROR(EAGAIN)表明当前没有已经解码好的数据，返回AVERROR_EOF表明已经全部解码完成，返回大于0则表明获取解码数据成功。 12345678910111213141516171819202122 // 发送到解码器中进行解码 if ((error = avcodec_send_packet(codec_ctx_in_, &amp;pkt)) &lt; 0) &#123; goto cleanup; &#125; // 从解码器中获取一帧解码后的数据，存到AVFrame中 error = avcodec_receive_frame(codec_ctx_in_, frame); if (error == AVERROR(EAGAIN)) &#123; // 没有获取到数据 error = 0; goto cleanup; &#125; else if (error == AVERROR_EOF) &#123; // 解码器中数据已经全部解码完成 error = 0; *finished = 1; &#125; else if (error &lt; 0) &#123; // 解码错误 goto cleanup; &#125; else &#123; // 数据成功解码到AVFrame中 *data_present = 1; error = 0; &#125; 重采样创建缓冲区，用来存放重采样后的数据 1234567if ((error = av_samples_alloc_array_and_samples( data, nullptr, codec_ctx_out_-&gt;channels, nb_samples, codec_ctx_out_-&gt;sample_fmt, 0)) &lt; 0) &#123; av_freep(*data[0]); free(*data); return error;&#125; 将AVFrame中的数据重采样到上面创建的缓冲区中 123if ((error = swr_convert(swr_ctx_, dst, nb_samples, src, nb_samples)) &lt; 0) &#123; return error;&#125; 入队列将重采样后的数据放入到缓冲队列中 123456789101112// 重新分配FIFO队列的大小，新的大小=已有的大小加上当前音频帧的样本量if ((error = av_audio_fifo_realloc( fifo_, av_audio_fifo_size(fifo_) + input_frame-&gt;nb_samples)) &lt; 0) &#123; goto cleanup;&#125;// 将重采样数据写入到FIFO中，如果写入的样本量小于当前帧的样本量，则表明写入有错误if (av_audio_fifo_write(fifo_, (void**)converted_input_samples, input_frame-&gt;nb_samples) &lt; input_frame-&gt;nb_samples) &#123; error = AVERROR_EXIT; goto cleanup;&#125; 出队列当队列中采样数据量满足编码器需要的大小后，从队列中取出需要的采样数据。 123456789101112131415161718192021AVFrame* frame = av_frame_alloc();// 用来接收FIFO中数据// 将AVFrame的样本量，设置为当前会读取的样本量frame-&gt;nb_samples = frame_size;// 设置AVFrame的参数frame-&gt;channel_layout = codec_ctx_out_-&gt;channel_layout;frame-&gt;format = codec_ctx_out_-&gt;sample_fmt;frame-&gt;sample_rate = codec_ctx_out_-&gt;sample_rate;// 初始化AVFrame中的数据缓冲区if ((error = av_frame_get_buffer(frame, 0)) &lt; 0) &#123; av_frame_free(&amp;frame); goto cleanup;&#125;// 从FIFO中读取数据到AVFrame的缓冲区中，如果读取的样本数小于预定的样本数，则说明读取出错if (av_audio_fifo_read(fifo_, reinterpret_cast&lt;void**&gt;(frame-&gt;data), frame_size) &lt; frame_size) &#123; error = AVERROR_EXIT; goto cleanup;&#125; 编码数据并输出通过avcodec_send_frame、avcodec_receive_packet来进行编码。avcodec_send_frame负责将一个AVFrame帧发送给编码器，avcodec_receive_packet负责从编码器中取出一帧已经编码好的数据。 12345678910111213141516171819202122232425262728293031323334AVPacket pkt;// 创建AVPacket，接收编码后的数据av_init_packet(&amp;pkt);// 如果待编码数据不为空，则累计计算当前帧的ptsif (frame) &#123; frame-&gt;pts = pts; // 这里用样本量的个数来计算pts // 假设第一帧的样本量为1024，则第一帧的pts=1024，第二帧的样本量也是1024，则第二帧的pts=2048 pts += frame-&gt;nb_samples;&#125;// 发送AVFrame到编码器中进行编码if ((error = avcodec_send_frame(codec_ctx_out_, frame)) &lt; 0) &#123; // 发送失败 goto cleanup;&#125;// 接收编码后的数据到AVPacket中error = avcodec_receive_packet(codec_ctx_out_, &amp;pkt);if (error == AVERROR(EAGAIN)) &#123; // 没有获取到编码后数据 error = 0; goto cleanup;&#125; else if (error == AVERROR_EOF) &#123; // 编码器中所有数据编码完成 error = 0; goto cleanup;&#125; else if (error &lt; 0) &#123; // 编码出错 goto cleanup;&#125; else &#123; // 成功获取编码后数据到AVPacket中 error = 0; *data_written = 1;&#125; 写入到输出12345// 如果获取数据，则写入到输出上下文中if (*data_written &amp;&amp; (error = av_write_frame(fmt_ctx_out_, &amp;pkt)) &lt; 0) &#123; // 写入失败 goto cleanup;&#125;]]></content>
      <categories>
        <category>拳掌指腿（音视频）</category>
        <category>降龙十八掌（FFmpeg）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[音视频开发之OpenGL-ES入门]]></title>
    <url>%2F%E9%9F%B3%E8%A7%86%E9%A2%91%E5%BC%80%E5%8F%91%E4%B9%8BOpenGL-ES%E5%85%A5%E9%97%A8%2F</url>
    <content type="text"><![CDATA[名词解释OpenGL通俗来讲，OpenGL是一个图形库，提供了一系列操作图形图像的API。因为OpenGL只是一个规范，具体的实现一般是由显卡提供。类似的图形库还有Windows平台的DirectX。 OpenGL-ESOpenGL-ES是专为嵌入式设备准备的一个OpenGL的裁剪版（去除了一些复杂的图元）。OpenGL-ES支持Android、iOS等平台，并且它也是WebGL的基础。 EGLEGL是OpenGL-ES与本地窗口系统之间的一个中间层API，一般由系统实现。EGL作为OpenGL-ES与设备之间的桥梁，将OpenGL-ES绘制在设备上。 WebGLWebGL是OpenGL-ES在浏览器上的实现，WebGL1.0基于OpenGL-ES2.0实现，WebGL2.0基于OpenGl-ES3.0实现。 图形渲染管线图形渲染管线也叫做渲染流水线，指的是将输入的原始图形数据经过渲染管线处理，输出一帧想要的图像的过程。 在OpenGL-ES中，任何事物都在3D空间中，而屏幕和窗口却都是2D的。所以图形渲染管线主要是在将输入的3D坐标转换成2D坐标，再讲2D坐标转换成实际有颜色的像素。(将输入的3D坐标画在3D坐标系中，然后根据视锥范围截取一个2D平面，将截取的平面转换为平面坐标显示) 2D坐标精确的表示一个点在2D空间的位置；而像素是这个点的近似值，像素的显示收到屏幕分辨率的限制 与CPU不同，GPU适合处理的问题都有这样的特征，一个问题可以分解为多个相同的小问题，每个问题之间相互独立不影响，所以GPU拥有大量的处理单元，处理单元数量越多，性能也就越好。而每个单元都可以并行处理，所以GPU的并行能力非常强。 可编程的渲染管线自OpenGL-ES 2.0开始，采用的都是可编程的渲染管线，也就是开发者可以自定义部分流水线中的着色器，使得可以更细致的控制图形渲染管线。 着色器是运行在GPU上的小程序，这些小程序在图形渲染管线的某个特定部分运行，着色器是一个非常独立的程序，是一种把输入转化为输出的程序，着色器与着色器之间无法通信。 开发者可编程的阶段有两个，一个是顶点着色器，一个是片段着色器。 顶点着色器着色器可以看成是一个纯函数，输入的所有顶点，都会依次执行该函数，在该函数中对输入的顶点进行处理，然后输出新的顶点位置。 图元装配所谓图元就是几何形状，OpenGL-ES支持的图元有三种：点、线、三角形。而图元装配就是根据定义的图元类型将顶点着色器输出的顶点渲染成单独的图元，然后会图元进行裁剪、透视分割、视口变换。 裁剪：图元根据视锥体进行裁剪。根据视锥体的六个平面进行裁剪，超出视锥体之外的图元都将被裁剪抛弃（并不会真正裁剪，只是标记，x、y、z的绝对值大于1则表示不在视锥体内，会被裁剪）。裁剪后得到的坐标体系为(x，y，z，w)，其中w用来在透视分割是处理远近效果。 透视分割：将裁剪后的坐标转换为规范化设备坐标。在该转换中会使用w坐标来进行处理，正交投影中w为1，而透视投影中w会根据远近值来生成，越远值越大。透视分割就是讲x、y、z坐标分别除以w，得到新的x、y、z坐标，由于坐标值都是小于w的，所以最终得到的值都是位于[-1，1]之间，这也就是所谓的规范化设备坐标。而对于透视投影来说，w的值越远越大，所以转换后，越远的坐标，转换后值越小，就会形成近大远小的效果。 视口变换：将设备坐标转换为窗口坐标，每个坐标都关联屏幕上的一个点。 光栅化 为装配后的图元生成对应的片段，每个片段代表着一个指定的像素位置。这些片段就是片段着色器中使用的片段。 片段着色器定义每个像素应该输出什么颜色。在片段着色器中可以接收到顶点着色器输出的变量。 逐片段片段着色器输出每个片段的颜色和深度，在最终加入到缓冲区前需要进过一系列测试和操作，这些操作称为逐片段。包括裁剪测试、模板测试、深度测试、混合、抖动等。最终通过的片段都会被加入到帧缓冲区中。 帧缓冲区缓冲区是在EGL创建surface的时候生成的，缓冲区包含颜色缓冲区和深度缓冲区等。 GLSL着色器编写语言，类C风格。 变量类型123456789OpenGL-ES 2.0 中变量类型为 uniform 可以在着色器中共享的变量，并且在着色器中只能读不能修改。 attribute 属性（数据可以放入到缓冲区中）顶点着色器中使用，类似in。 varying 顶点着色器和片段着色器中通信，在顶点着色器中的varying类似out,在片段着色器中的varying类似in。 OpenGL-ES 3.0 中变量类型 uniform in out 内建变量123456789101112131415161718192021222324顶点着色器： - gl_VertexID: 输入变量，用于保存顶点的整数索引。 - gl_InstanceID: 输入变量，用于保存实例化绘图调用中图元的实例编号。 - gl_Position: 输出变量，输出顶点位置的裁剪坐标。该值在裁剪和视口阶段用于执行相应的图元裁剪以及从裁剪坐标到屏幕坐标的顶点位置转换。 - gl_PointSize: 输出变量，输出点精灵尺寸。 - gl_FrontFacing: 不能直接写入，根据顶点位置和图元类型生成。 - gl_DepthRange: 窗口坐标的深度范围。 该变量类型为struct gl_DepthRangeParameters&#123; highp float near; highp float far; highp float diff; &#125; 片段着色器： - gl_FragCoord: 只读变量，保存片段的窗口相对坐标。 - gl_FrontFacing: 只读变量，片段是正面图元的一部分时为true，否则为false。 - gl_PointCoord: 只读变量，保存点精灵的纹理坐标，该坐标在点光栅化期间自动生成。 - gl_FragDepth：输出变量，用于覆盖片段的固定功能深度值。 - gl_FragColor：输出变量，输出该片段的颜色。(该变量在3.0中已删除，需要自行定义) - texture2D(sampler2D, vec2 coord)：从标准纹理中获取纹理数据。 - textureCube(samplerCube, vec3 coord：从立方体贴图纹理中获取纹理数据。 (3.0中上面两个函数被合成了一个函数) - texture(sampler,coord): 内建函数，从纹理贴图中读取，sampler是绑定到纹理单元的采样器，制定纹理为读取来源。coord是从纹理贴图中读取的2d纹理坐标。该函数返回一个代表从纹理贴图中读取颜色的vec4，默认格式为RGBA。 精度限定符1234顶点着色器有默认的精度，是highp，但是片段着色器没有默认的精度。 - highp - mediump - lowp 顶点着色器样例 123456789101112131415161718192021222324// 3.0版本 需要特别声明版本信息#version 300 es// 定义两个属性，通过location指定该属性的索引值layout(location = 0) in vec4 vPosition;layout(location = 1) in vec4 aTextureCoord;// 定义一个全局常量uniform mat4 uTextureMatrix;// 输出一个属性给片段着色器out vec2 yuvTexCoords;void main() &#123; // 输出顶点坐标 gl_Position = uTextureMatrix * vPosition; yuvTexCoords = aTextureCoord;&#125;// 2.0版本attribute vec4 vPosition;attribute vec2 aTextureCoord;uniform mat4 uTextureMatrix;varying vec2 yuvTexCoords;void main() &#123; gl_Position = uTextureMatrix * vPosition; yuvTexCoords = aTextureCoord;&#125; 片段着色器样例12345678910111213141516171819// 3.0版本#version 300 es#extension GL_OES_EGL_image_external_essl3 : requireprecision mediump float;uniform samplerExternalOES yuvTexSampler;in vec2 yuvTexCoords;out vec4 fragColor;void main() &#123; fragColor = texture(yuvTexSampler,yuvTexCoords);&#125;// 2.0版本precision mediump float;uniform sampler2D yuvTexSampler;varying vec2 yuvTexCoords;void main() &#123; gl_FragColor = texture2D(yuvTexSampler,yuvTexCoords);&#125; EGL通过Surface对象创建一个EGLSurface，将其连接到Surface的BufferQueue上，EGLSurface将会作为一个生产方，会将OpenGL-ES生产的缓冲区加入到该BufferQueue中，然后由该Surface的消费方进行处理。 Surface是一个生产方，EGLSurface也是一个独立的生产方。 一个Surface一次只能和一个EGLSurface进行连接，可以通过断开，然后重新连接另一个EGLSurface。 eglGetDisplay123456// 获取当前设备中默认屏幕的handle，display对象可以认为就是一块物理的屏幕，如果没有对应的display，则返回EGL_NO_DISPLAY。if ((display = eglGetDisplay(EGL_DEFAULT_DISPLAY)) == EGL_NO_DISPLAY) &#123; LOGE("witgao", "eglGetDisplay"); return;&#125; eglInitialize123456// 针对获取的屏幕对egl进行初始化（display，返回的主版本号,返回的次版本号）EGLint major, minor;if (!eglInitialize(display, &amp;major, &amp;minor) || major == NULL || minor == NULL) &#123; LOGE("witgao", "eglInitialize"); return;&#125; eglChooseConfig surface类型有三种，window_surface、pbuffer_surface、pixmap_surface，只有window_surface和window有关联，可以在window上显示。 绘制模式有两种，back buffer、single buffer，window_surface使用的是back buffer，back buffer是一块GPU中的buffer，可以通过EGL显示在屏幕上；而single buffer 是保存在内存中的位图，不支持显示在屏幕上。 123456789101112131415161718const EGLint attribs[] = &#123; // 设置surface类型 EGL_SURFACE_TYPE, EGL_WINDOW_BIT, // 指定opengl-es版本为2.0 EGL_RENDERABLE_TYPE, EGL_OPENGL_ES2_BIT, EGL_BLUE_SIZE, 8, EGL_GREEN_SIZE, 8, EGL_RED_SIZE, 8, EGL_NONE&#125;;EGLConfig config;EGLint numConfigs;// 获取设备中支持指定属性的配置集合（display，指定的属性，输出的配置，输出配置的个数，所有支持指定属性的配置的个数）if (!eglChooseConfig(display, attribs, &amp;config, 1, &amp;numConfigs)) &#123; LOGE("witgao", "eglChooseConfig"); return;&#125; eglCreateWindowSurface12345678// 创建一个提供给opengl-es绘制的surface（display，配置，原生window，指定属性）if (!(eglSurface = eglCreateWindowSurface(display, config, aNativeWindow, 0))) &#123; LOGE("witgao", "eglCreateWindowSurface"); return;&#125; // android中创建NativeWindowANativeWindow *pNativeWindow = ANativeWindow_fromSurface(jenv, surface); eglCreateContext123456789101112const EGLint context_attribs[] = &#123; // 设置context针对的opengl-es的版本，（EGL_NONE设置的是默认值，为1） // 此处的版本需要和上面的EGL_RENDERABLE_TYPE 对应 EGL_CONTEXT_CLIENT_VERSION, 2, EGL_NONE&#125;;// 创建context，在context中保存了opengl-es的状态信息 （display，配置，共享context的handle 一般设为null，属性）// 一个display可以创建多个contextif (!(context = eglCreateContext(display, config, 0, context_attribs))) &#123; LOGE("witgao", "eglCreateContext"); return;&#125; eglMakeCurrent123456789// 将上面创建的context绑定到当前线程上，并将context与surface进行关联。当makeCurrent执行后，就可以调用opengl-es的api对context中的状态集进行设定，// 然后进而向surface中绘制内容，再把surface中的内容读取出来。// 一个线程中enable状态的context只能有一个，如果当前线程已经有了一个enable状态的context，那么会先执行其flush操作，将没有执行完成的命令全部执行完成，然后将其改为disable状态，将新传入的context改为enable状态。// 如果想要释放当前的context,也就是将当前的context disable,那么将第二个和第三个参数设置为 EGL_NO_SURFACE,第四个参数设置为 EGL_NO_CONTEXT即可。// context enable后，视口大小和裁剪大小都会被设置为surface的尺寸if (!eglMakeCurrent(display, eglSurface, eglSurface, context)) &#123; LOGE("witgao", "eglMakeCurrent"); return;&#125; eglSwapBuffers12345// 当opengl-es将内容绘制完成，调用该方法将该缓冲区加入到Surface的BufferQueue中if (!eglSwapBuffers(display, eglSurface)) &#123; LOGE("witgao", "eglSwapBuffers"); return;&#125;]]></content>
      <categories>
        <category>拳掌指腿（音视频）</category>
        <category>七伤拳（音视频基础）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[音视频开发之Android硬件编解码]]></title>
    <url>%2F%E9%9F%B3%E8%A7%86%E9%A2%91%E5%BC%80%E5%8F%91%E4%B9%8BAndroid%E7%A1%AC%E4%BB%B6%E7%BC%96%E8%A7%A3%E7%A0%81%2F</url>
    <content type="text"><![CDATA[MediaCodec介绍MediaCodec是Android平台上用来访问编码器和解码器的组件。 工作流程 MediaCodec中维护了两个BufferQueue，一个用来存放输入数据，一个用来存放输出数据。对于编码来说，InputBufferQueue用来接收视频原始YUV数据，经过Codec编码处理后，将编码后的数据放入到OutputBufferQueue中输出。而对于解码来说，InputBufferQueue用来接收视频编码数据，经过Codec解码处理后，将解码后的视频原始YUV数据放入到OutputBufferQueue中输出。 BufferQueue的数量是有限的，每次通过dequeueInputBuffer从InputBufferQueue中获取一个空的buffer索引，将输入数据填充到该buffer，通过queueInputBuffer通知Codec该索引位置的buffer已经填充了数据，可以开始处理了。然后通过dequeueOutputBuffer从OutputBufferQueue中获取处理完毕的buffer索引，获取到该buffer中的数据进行渲染或封装，使用完后，通过releaseOutputBuffer通知Codec释放该buffer中的数据（也可以让Codec进行数据的渲染）。 MediaCodec的数据分为两种，一种是原始音视频数据，一种是压缩数据。上面讲到这两种数据都是使用bytebuffer来处理的，输入层获取到空的buffer后需要将数据copy到该buffer中，而输出层获取到buffer后也需要将数据copy出来进行消费，大量的数据copy是很影响性能的。 针对原始音视频数据，MediaCodec提供了更加高效的方式来避免了数据的copy，那就是Surface。对于解码器来说，可以通过在configure的时候，设置OutputSurface来接收输出的Buffer，这样就不需要从OutputBufferQueue中copy数据了。而对于编码器来说，可以通过createInputSurface方法创建一个用来输入的Surface，这样就不需要往InputBufferQueue里copy数据了。 数据类型原始数据音频原始数据类型是一个PCM音频数据帧，视频原始数据类型由color_format决定，常用的有以下两种： Surface Format ：这种类型的format数据，表明使用的GraphicBuffer，这是一个内存共享的缓冲区。（CodecCapabilities#COLOR_FormatSurface ）。 YUV Format ：YUV颜色格式，支持多种YUV格式（CodecCapabilities#COLOR_FormatYUV420Flexible ）。 在configure的时候，对于编码器来说可以指定编码器输出的数据的format，对于解码器来说可以指定解码器输入的format。 对于编码器，如果输入的时候使用的是Surface，则format需要设置为COLOR_FormatSurface。而对于解码器来说输入的format一般从媒体文件中读取。 压缩数据对于视频来说，buffer中是一帧的压缩数据，对于音频来说，buffer中是一个单元的压缩数据，buffer中包含的都是完整的一帧或一个单元的数据。 组件状态 Stopped：创建了一个MediaCodec对象后，默认为Uninitialized状态，调用configure方法后，状态变为Configured，通过reset可以重新配置。 Executing：调用start方法后，状态就变为了Flushed，第一次调用dequeueInputBuffer时，状态变为Running，当通过queueInputBuffer写入了一个EOS标记后，则状态变为End of Stream，可以通过flush方法重新回到Flushed状态。 Released：调用release进入到Released状态。 使用指南创建编解码器编码器通过制定的mimeType创建一个编码器，比如 “video/avc” ，如果当前设备不支持该type则会抛IllegalArgumentException异常。 1mVideoEncoder = MediaCodec.createEncoderByType("mimeType"); 通过MediaCodecList查询当前设备是否支持该mimeType的编码。 12345678910111213final int numCodecs = MediaCodecList.getCodecCount();for (int i = 0; i &lt; numCodecs; i++) &#123; final MediaCodecInfo codecInfo = MediaCodecList.getCodecInfoAt(i); if (!codecInfo.isEncoder()) &#123; continue; &#125; // select first codec that match a specific MIME type and color format final String[] types = codecInfo.getSupportedTypes(); for (int j = 0; j &lt; types.length; j++) &#123; if (types[j].equalsIgnoreCase(mimeType)) &#123; ... &#125;&#125; 解码器通过mimeType就可以快速的创建一个解码器，关于媒体文件的mimeType获取可以使用MediaExtractor.getTrackFormat，如果当前设备不支持该type则会抛IllegalArgumentException异常。 1mVideoDecoder = MediaCodec.createDecoderByType("mimeType") 通过MediaCodecList查询当前设备是否支持该mimeType的解码。 12345678910111213final int numCodecs = MediaCodecList.getCodecCount();for (int i = 0; i &lt; numCodecs; i++) &#123; final MediaCodecInfo codecInfo = MediaCodecList.getCodecInfoAt(i); if (codecInfo.isEncoder()) &#123; continue; &#125; // select first codec that match a specific MIME type and color format final String[] types = codecInfo.getSupportedTypes(); for (int j = 0; j &lt; types.length; j++) &#123; if (types[j].equalsIgnoreCase(mimeType)) &#123; ... &#125;&#125; 初始化编码器在创建完编码器之后，需要对编码器进行confiigure。 123456 public void configure( @Nullable MediaFormat format, @Nullable Surface surface, @Nullable MediaCrypto crypto, @ConfigureFlag int flags) &#123; &#125; 对于编码器来说 第一个参数MediaFormat是指定编码器输出的数据格式。这个格式需要我们自己去计算制定， 12345678910// 配置编码器输出格式mVideoEncoderFormat = MediaFormat.createVideoFormat(MIME_TYPE, mNewWidth, mNewHeight);// 设置color_format，如果使用的是InputSurface，则设置为FormatSurfacemVideoEncoderFormat.setInteger(MediaFormat.KEY_COLOR_FORMAT, MediaCodecInfo.CodecCapabilities.COLOR_FormatSurface);// 码率mVideoEncoderFormat.setInteger(MediaFormat.KEY_BIT_RATE, mNewBitRate);// 帧率mVideoEncoderFormat.setInteger(MediaFormat.KEY_FRAME_RATE, OUTPUT_FRAME_RATE);// gopmVideoEncoderFormat.setInteger(MediaFormat.KEY_I_FRAME_INTERVAL, OUTPUT_IFRAME_INTERVAL); 第二个参数是OutputSurface，对于编码器来说忽略。 第三个参数是加密相关，可忽略。 第四个参数是编码标记，编码器需要加上 MediaCodec.CONFIGURE_FLAG_ENCODE 标记。 解码器在创建完解码器之后，需要对解码器进行configure。 123456 public void configure( @Nullable MediaFormat format, @Nullable Surface surface, @Nullable MediaCrypto crypto, @ConfigureFlag int flags) &#123; &#125; 对于解码器来说 第一个参数MediaFormat是指定解码器输入的数据格式。一般来说是通过MediaExtractor.getTrackFormat从媒体文件中获取，但是其中有几点需要注意。 最好先将KEY_LEVEL的值设置为null（MediaFormat.setString(KEY_LEVEL, null)），因为这个值经常不准。 在5.0的设备上，MediaCodecList.findDecoder/EncoderForFormat不得包含frame rate。使用（MediaFormat.setString(MediaFormat.KEY_FRAME_RATE, null)）清除格式中的任何现有帧率设置。 第二个参数是OutputSurface，解码完成后，通过releaseOutputBuffer可将解码后数据直接输出到该Surface中。 第三个参数是加密相关，可忽略。 第四个参数是解码器也可忽略。 Codec-specific Data对于某些格式，特别是AAC音频和H.264和H.265视频格式要求实际数据前需要包含设置数据或编解码器特定数据的多个缓冲区。 处理这种压缩格式时，必须在start()之后和任何帧数据之前将此数据提交给编解码器。 此类数据必须在输入时queueInputBuffer使用标BUFFER_FLAG_CODEC_CONFIG进行标记。 关于CSD，其实在封装篇讲过，在muxer写入第一帧之前，需要写入CSD数据。 对于解码器来说，MediaExtractor.getTrackFormat可以直接获取到媒体文件的CSD信息。 对于编码器来说，在MediaCodec.INFO_OUTPUT_FORMAT_CHANGED回调时，使用mVideoEncoder.getOutputFormat() 可以获取到附带CSD信息的Format。 处理根据API历史，MediaCodec提供了三套处理API。 Processing Mode API version &lt;= 20 API version &gt;= 21 Synchronous API using buffer arrays Supported 已过时 Synchronous API using buffers Not Available Supported Asynchronous API using buffers Not Available Supported 同步API12345678910111213141516171819202122232425262728293031323334353637383940// 创建MediaCodec codec = MediaCodec.createByCodecName(name);// 初始化codec.configure(format, …);MediaFormat outputFormat = codec.getOutputFormat(); // 启动codec.start();for (;;) &#123; // 从InputBufferQueue中获取空闲索引 int inputBufferId = codec.dequeueInputBuffer(timeoutUs); if (inputBufferId &gt;= 0) &#123; // 获取空闲索引对应的buffer ByteBuffer inputBuffer = codec.getInputBuffer(…); // 向buffer中写入数据 … if (inputDown) &#123; // 输入结束的时候，向InputBufferQueue中写入一个EOS的标记，通知编码器。 codec.queueInputBuffer(inputBufferId, 0, 0, 0L,MediaCodec.BUFFER_FLAG_END_OF_STREAM); &#125; else &#123; // 通知Codec数据写入 codec.queueInputBuffer(inputBufferId, …); &#125; &#125; // 从OutputBufferQueue中获取处理完成的索引 int outputBufferId = codec.dequeueOutputBuffer(…); if (outputBufferId &gt;= 0) &#123; // 获取到处理完成的数据和格式 ByteBuffer outputBuffer = codec.getOutputBuffer(outputBufferId); MediaFormat bufferFormat = codec.getOutputFormat(outputBufferId); // 封装或渲染 … // 释放该buffer codec.releaseOutputBuffer(outputBufferId, …); &#125; else if (outputBufferId == MediaCodec.INFO_OUTPUT_FORMAT_CHANGED) &#123; // 数据格式发生变化 outputFormat = codec.getOutputFormat(); &#125;&#125;codec.stop();codec.release(); 异步API12345678910111213141516171819202122232425262728293031323334353637383940414243444546// 创建MediaCodec codec = MediaCodec.createByCodecName(name);MediaFormat mOutputFormat; // 异步回调codec.setCallback(new MediaCodec.Callback() &#123; @Override void onInputBufferAvailable(MediaCodec mc, int inputBufferId) &#123; // InputBufferQueue有空闲buffer回调 ByteBuffer inputBuffer = codec.getInputBuffer(inputBufferId); // 写入数据 … // 通知Codec codec.queueInputBuffer(inputBufferId, …); &#125; @Override void onOutputBufferAvailable(MediaCodec mc, int outputBufferId, …) &#123; // OutputBufferQueue有处理完成的buffer回调 ByteBuffer outputBuffer = codec.getOutputBuffer(outputBufferId); MediaFormat bufferFormat = codec.getOutputFormat(outputBufferId); // 处理数据 … // 释放buffer codec.releaseOutputBuffer(outputBufferId, …); &#125; @Override void onOutputFormatChanged(MediaCodec mc, MediaFormat format) &#123; // Codec数据格式发生变化 mOutputFormat = format; &#125; @Override void onError(…) &#123; … &#125;&#125;);// 初始化codec.configure(format, …);mOutputFormat = codec.getOutputFormat(); // 启动codec.start();// 等待处理…codec.stop();codec.release(); 在输入结束后，需要写入EOS标记，此时buffer可为空，需要注意的是一旦写入EOS标记后，就不可以再向InputBufferQueue中写入数据了。 一旦设置了OutputSurface，则不可以访问OutputBufferQueue，对应API会返回null。 一旦设置了InputSurface，则不可以访问InputBufferQueue，对应API会抛异常或返回null。 在M版本之前，软件解码器在渲染到Surface上时可能未应用旋转。 Codec在处理的第一帧的数据一定要是关键帧。 源码分析创建MediaCodec 创建123456789101112131415161718192021222324252627282930// staticsp&lt;MediaCodec&gt; MediaCodec::CreateByType( const sp&lt;ALooper&gt; &amp;looper, const AString &amp;mime, bool encoder, status_t *err, pid_t pid, uid_t uid) &#123; Vector&lt;AString&gt; matchingCodecs; // 查找该mimeType对应的编解码器 MediaCodecList::findMatchingCodecs( mime.c_str(), encoder, 0, &amp;matchingCodecs); if (err != NULL) &#123; *err = NAME_NOT_FOUND; &#125; for (size_t i = 0; i &lt; matchingCodecs.size(); ++i) &#123; // 创建MediaCodec sp&lt;MediaCodec&gt; codec = new MediaCodec(looper, pid, uid); AString componentName = matchingCodecs[i]; // 初始化编解码器 status_t ret = codec-&gt;init(componentName); if (err != NULL) &#123; *err = ret; &#125; if (ret == OK) &#123; return codec; &#125; &#125; return NULL;&#125; ACode 创建123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960status_t MediaCodec::init(const AString &amp;name) &#123; mResourceManagerService-&gt;init(); // 编解码器名称 mInitName = name; AString tmp = name; const sp&lt;IMediaCodecList&gt; mcl = MediaCodecList::getInstance(); ... // 查找该编解码器 for (const AString &amp;codecName : &#123; name, tmp &#125;) &#123; ssize_t codecIdx = mcl-&gt;findCodecByName(codecName.c_str()); if (codecIdx &lt; 0) &#123; continue; &#125; // 获取该编解码器信息 mCodecInfo = mcl-&gt;getCodecInfo(codecIdx); ... break; &#125; ... // 创建ACodec mCodec = GetCodecBase(name, mCodecInfo-&gt;getOwnerName()); ... mLooper-&gt;registerHandler(this); mCodec-&gt;setCallback( std::unique_ptr&lt;CodecBase::CodecCallback&gt;( new CodecCallback(new AMessage(kWhatCodecNotify, this)))); mBufferChannel = mCodec-&gt;getBufferChannel(); mBufferChannel-&gt;setCallback( std::unique_ptr&lt;CodecBase::BufferCallback&gt;( new BufferCallback(new AMessage(kWhatCodecNotify, this)))); // 创建Init消息 sp&lt;AMessage&gt; msg = new AMessage(kWhatInit, this); msg-&gt;setObject("codecInfo", mCodecInfo); msg-&gt;setString("name", name); ... status_t err; Vector&lt;MediaResource&gt; resources; MediaResource::Type type = secureCodec ? MediaResource::kSecureCodec : MediaResource::kNonSecureCodec; MediaResource::SubType subtype = mIsVideo ? MediaResource::kVideoCodec : MediaResource::kAudioCodec; resources.push_back(MediaResource(type, subtype, 1)); for (int i = 0; i &lt;= kMaxRetry; ++i) &#123; if (i &gt; 0) &#123; // Don't try to reclaim resource for the first time. if (!mResourceManagerService-&gt;reclaimResource(resources)) &#123; break; &#125; &#125; sp&lt;AMessage&gt; response; // 发送Init消息，连接OMX服务，创建解码器实例 err = PostAndAwaitResponse(msg, &amp;response); if (!isResourceError(err)) &#123; break; &#125; &#125; return err;&#125; OMX Codec 创建12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455565758596061626364656667686970717273bool ACodec::UninitializedState::onAllocateComponent(const sp&lt;AMessage&gt; &amp;msg) &#123; ALOGV("onAllocateComponent"); CHECK(mCodec-&gt;mOMXNode == NULL); sp&lt;AMessage&gt; notify = new AMessage(kWhatOMXMessageList, mCodec); notify-&gt;setInt32("generation", mCodec-&gt;mNodeGeneration + 1); sp&lt;RefBase&gt; obj; CHECK(msg-&gt;findObject("codecInfo", &amp;obj)); sp&lt;MediaCodecInfo&gt; info = (MediaCodecInfo *)obj.get(); if (info == nullptr) &#123; ALOGE("Unexpected nullptr for codec information"); mCodec-&gt;signalError(OMX_ErrorUndefined, UNKNOWN_ERROR); return false; &#125; AString owner = (info-&gt;getOwnerName() == nullptr) ? "default" : info-&gt;getOwnerName(); AString componentName; CHECK(msg-&gt;findString("componentName", &amp;componentName)); sp&lt;CodecObserver&gt; observer = new CodecObserver(notify); sp&lt;IOMX&gt; omx; sp&lt;IOMXNode&gt; omxNode; status_t err = NAME_NOT_FOUND; OMXClient client; // 连接OMX服务 if (client.connect(owner.c_str()) != OK) &#123; mCodec-&gt;signalError(OMX_ErrorUndefined, NO_INIT); return false; &#125; omx = client.interface(); pid_t tid = gettid(); int prevPriority = androidGetThreadPriority(tid); androidSetThreadPriority(tid, ANDROID_PRIORITY_FOREGROUND); // 创建编解码器实例 err = omx-&gt;allocateNode(componentName.c_str(), observer, &amp;omxNode); androidSetThreadPriority(tid, prevPriority); if (err != OK) &#123; ALOGE("Unable to instantiate codec '%s' with err %#x.", componentName.c_str(), err); mCodec-&gt;signalError((OMX_ERRORTYPE)err, makeNoSideEffectStatus(err)); return false; &#125; mDeathNotifier = new DeathNotifier(new AMessage(kWhatOMXDied, mCodec)); auto tOmxNode = omxNode-&gt;getHalInterface&lt;IOmxNode&gt;(); if (tOmxNode &amp;&amp; !tOmxNode-&gt;linkToDeath(mDeathNotifier, 0)) &#123; mDeathNotifier.clear(); &#125; ++mCodec-&gt;mNodeGeneration; mCodec-&gt;mComponentName = componentName; mCodec-&gt;mRenderTracker.setComponentName(componentName); mCodec-&gt;mFlags = 0; if (componentName.endsWith(".secure")) &#123; mCodec-&gt;mFlags |= kFlagIsSecure; mCodec-&gt;mFlags |= kFlagIsGrallocUsageProtected; mCodec-&gt;mFlags |= kFlagPushBlankBuffersToNativeWindowOnShutdown; &#125; mCodec-&gt;mOMX = omx; mCodec-&gt;mOMXNode = omxNode; mCodec-&gt;mCallback-&gt;onComponentAllocated(mCodec-&gt;mComponentName.c_str()); mCodec-&gt;changeState(mCodec-&gt;mLoadedState); return true;&#125; Omx 创建 123456Omx::Omx() : mMaster(new OMXMaster()), mParser() &#123; (void)mParser.parseXmlFilesInSearchDirs(); (void)mParser.parseXmlPath(mParser.defaultProfilingResultsXmlPath);&#125; OMXMaster 创建 123456789101112131415161718192021222324 OMXMaster::OMXMaster() &#123; pid_t pid = getpid(); char filename[20]; snprintf(filename, sizeof(filename), "/proc/%d/comm", pid); int fd = open(filename, O_RDONLY); if (fd &lt; 0) &#123; ALOGW("couldn't determine process name"); strlcpy(mProcessName, "&lt;unknown&gt;", sizeof(mProcessName)); &#125; else &#123; ssize_t len = read(fd, mProcessName, sizeof(mProcessName)); if (len &lt; 2) &#123; ALOGW("couldn't determine process name"); strlcpy(mProcessName, "&lt;unknown&gt;", sizeof(mProcessName)); &#125; else &#123; // the name is newline terminated, so erase the newline mProcessName[len - 1] = 0; &#125; close(fd); &#125; addVendorPlugin(); addPlatformPlugin();&#125; 1234567891011121314151617181920212223242526272829303132333435void OMXMaster::addVendorPlugin() &#123; addPlugin("libstagefrighthw.so");&#125;void OMXMaster::addPlatformPlugin() &#123; addPlugin("libstagefright_softomx_plugin.so");&#125;void OMXMaster::addPlugin(const char *libname) &#123; void *libHandle = android_load_sphal_library(libname, RTLD_NOW); if (libHandle == NULL) &#123; return; &#125; typedef OMXPluginBase *(*CreateOMXPluginFunc)(); CreateOMXPluginFunc createOMXPlugin = (CreateOMXPluginFunc)dlsym( libHandle, "createOMXPlugin"); if (!createOMXPlugin) createOMXPlugin = (CreateOMXPluginFunc)dlsym( libHandle, "_ZN7android15createOMXPluginEv"); OMXPluginBase *plugin = nullptr; if (createOMXPlugin) &#123; plugin = (*createOMXPlugin)(); &#125; if (plugin) &#123; mPlugins.push_back(&#123; plugin, libHandle &#125;); addPlugin(plugin); &#125; else &#123; android_unload_sphal_library(libHandle); &#125;&#125; 创建 编解码器实例 12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455565758596061626364Return&lt;void&gt; Omx::allocateNode( const hidl_string&amp; name, const sp&lt;IOmxObserver&gt;&amp; observer, allocateNode_cb _hidl_cb) &#123; using ::android::IOMXNode; using ::android::IOMXObserver; sp&lt;OMXNodeInstance&gt; instance; &#123; Mutex::Autolock autoLock(mLock); if (mLiveNodes.size() == kMaxNodeInstances) &#123; _hidl_cb(toStatus(NO_MEMORY), nullptr); return Void(); &#125; // 创建编解码器实例 instance = new OMXNodeInstance( this, new LWOmxObserver(observer), name.c_str()); OMX_COMPONENTTYPE *handle; // 生成实例 OMX_ERRORTYPE err = mMaster-&gt;makeComponentInstance( name.c_str(), &amp;OMXNodeInstance::kCallbacks, instance.get(), &amp;handle); if (err != OMX_ErrorNone) &#123; LOG(ERROR) &lt;&lt; "Failed to allocate omx component " "'" &lt;&lt; name.c_str() &lt;&lt; "' " " err=" &lt;&lt; asString(err) &lt;&lt; "(0x" &lt;&lt; std::hex &lt;&lt; unsigned(err) &lt;&lt; ")"; _hidl_cb(toStatus(StatusFromOMXError(err)), nullptr); return Void(); &#125; instance-&gt;setHandle(handle); // Find quirks from mParser const auto&amp; codec = mParser.getCodecMap().find(name.c_str()); if (codec == mParser.getCodecMap().cend()) &#123; LOG(WARNING) &lt;&lt; "Failed to obtain quirks for omx component " "'" &lt;&lt; name.c_str() &lt;&lt; "' " "from XML files"; &#125; else &#123; uint32_t quirks = 0; for (const auto&amp; quirk : codec-&gt;second.quirkSet) &#123; if (quirk == "quirk::requires-allocate-on-input-ports") &#123; quirks |= OMXNodeInstance:: kRequiresAllocateBufferOnInputPorts; &#125; if (quirk == "quirk::requires-allocate-on-output-ports") &#123; quirks |= OMXNodeInstance:: kRequiresAllocateBufferOnOutputPorts; &#125; &#125; instance-&gt;setQuirks(quirks); &#125; mLiveNodes.add(observer.get(), instance); mNode2Observer.add(instance.get(), observer.get()); &#125; observer-&gt;linkToDeath(this, 0); _hidl_cb(toStatus(OK), new TWOmxNode(instance)); return Void();&#125;]]></content>
      <categories>
        <category>拳掌指腿（音视频）</category>
        <category>七伤拳（音视频基础）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[音视频开发之Android硬件解封装]]></title>
    <url>%2F%E9%9F%B3%E8%A7%86%E9%A2%91%E5%BC%80%E5%8F%91%E4%B9%8BAndroid%E7%A1%AC%E4%BB%B6%E8%A7%A3%E5%B0%81%E8%A3%85%2F</url>
    <content type="text"><![CDATA[硬件解封装Android平台针对音视频封装提供了MediaMuxer API，支持.mp4格式的封装；针对解封装提供了MediaExtractor API，支持.mp4等格式。 API架构在 frameworks/base/media 文件夹中提供了所有音视频相关的Java层API，本文中讲述的解封装API都定义在该处。而具体的服务实现都在 frameworks/av 文件夹中 自7.0开始，MediaService被拆分成多个服务，每个服务都运行在各自的进程中。 MediaExtractor 解封装MediaExtractor类是官方提供的音视频解封装类。其官方使用Demo如下所示： 12345678910111213141516171819202122232425// 创建解封装器 MediaExtractor extractor = new MediaExtractor();// 设置数据源 extractor.setDataSource(...);// 遍历所有轨道 int numTracks = extractor.getTrackCount(); for (int i = 0; i &lt; numTracks; ++i) &#123; MediaFormat format = extractor.getTrackFormat(i); String mime = format.getString(MediaFormat.KEY_MIME); // 根据轨道类型选择想要的轨道 if (weAreInterestedInThisTrack) &#123; extractor.selectTrack(i); &#125; &#125; ByteBuffer inputBuffer = ByteBuffer.allocate(...) // 读取选中轨道的样本数据 while (extractor.readSampleData(inputBuffer, ...) &gt;= 0) &#123; int trackIndex = extractor.getSampleTrackIndex(); long presentationTimeUs = extractor.getSampleTime(); ... // 下一个样本 extractor.advance(); &#125; extractor.release(); extractor = null; 支持的格式 注册解封装器在MediaExtractorService被创建的时候，会注册所有的Extractor实现。 1234567891011121314151617181920212223242526272829303132// libDirPath 有两个，一个是 /apex/com.android.media/lib/extractors/ ，另一个是/system/lib/extractors/ void MediaExtractorFactory::RegisterExtractors( const char *libDirPath, const android_dlextinfo* dlextinfo, std::list&lt;sp&lt;ExtractorPlugin&gt;&gt; &amp;pluginList) &#123; DIR *libDir = opendir(libDirPath); if (libDir) &#123; struct dirent* libEntry; while ((libEntry = readdir(libDir))) &#123; if (libEntry-&gt;d_name[0] == '.') &#123; continue; &#125; String8 libPath = String8(libDirPath) + "/" + libEntry-&gt;d_name; if (!libPath.contains("extractor.so")) &#123; continue; &#125; // 加载 extractor.so void *libHandle = android_dlopen_ext( libPath.string(), RTLD_NOW | RTLD_LOCAL, dlextinfo); // 查找GETEXTRACTORDEF函数 GetExtractorDef getDef = (GetExtractorDef) dlsym(libHandle, "GETEXTRACTORDEF"); // 执行GETEXTRACTORDEF函数，获取到解封装器配置，创建一个Plugin，加入到list中 RegisterExtractor( new ExtractorPlugin(getDef(), libHandle, libPath), pluginList); &#125; closedir(libDir); &#125; else &#123; ALOGE("couldn't opendir(%s)", libDirPath); &#125;&#125; MPEG-4的GETEXTRACTORDEF函数，其中的 Sniff方法就是创建对象的构造器。 123456789ExtractorDef GETEXTRACTORDEF() &#123; return &#123; EXTRACTORDEF_VERSION, UUID("27575c67-4417-4c54-8d3d-8e626985a164"), 2, // version "MP4 Extractor", &#123; .v3 = &#123;Sniff, extensions&#125; &#125;, &#125;;&#125; 创建解封装器12345678910111213141516171819202122sp&lt;IMediaExtractor&gt; MediaExtractorFactory::CreateFromService( const sp&lt;DataSource&gt; &amp;source, const char *mime) &#123; // 根据上面注册的Sniff函数，获取最合适的解封装器的CreateExtractor函数。 creator = sniff(source, &amp;confidence, &amp;meta, &amp;freeMeta, plugin, &amp;creatorVersion); if (!creator) &#123; ALOGV("FAILED to autodetect media content."); return NULL; &#125; MediaExtractor *ex = nullptr; if (creatorVersion == EXTRACTORDEF_VERSION_NDK_V1 || creatorVersion == EXTRACTORDEF_VERSION_NDK_V2) &#123; // 创建具体的解封装器实例 CMediaExtractor *ret = ((CreatorFunc)creator)(source-&gt;wrap(), meta); if (meta != nullptr &amp;&amp; freeMeta != nullptr) &#123; freeMeta(meta); &#125; ex = ret != nullptr ? new MediaExtractorCUnwrapper(ret) : nullptr; &#125; return CreateIMediaExtractorFromMediaExtractor(ex, source, plugin);&#125; 选择最合适的解封装器实例选择具体格式的封装器是通过将DataSource依次给所有已经注册的解封装器的Sniff函数进行尝试，每次尝试都会有一个分数返回，保存在confidence中，最终会选择分数最大的做为最终的解封装器。 12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455void *MediaExtractorFactory::sniff( const sp&lt;DataSource&gt; &amp;source, float *confidence, void **meta, FreeMetaFunc *freeMeta, sp&lt;ExtractorPlugin&gt; &amp;plugin, uint32_t *creatorVersion) &#123; // 初始分数为0 *confidence = 0.0f; *meta = nullptr; // 所有注册的解封装器实例 std::shared_ptr&lt;std::list&lt;sp&lt;ExtractorPlugin&gt;&gt;&gt; plugins; &#123; Mutex::Autolock autoLock(gPluginMutex); if (!gPluginsRegistered) &#123; return NULL; &#125; plugins = gPlugins; &#125; // 遍历 void *bestCreator = NULL; for (auto it = plugins-&gt;begin(); it != plugins-&gt;end(); ++it) &#123; ALOGV("sniffing %s", (*it)-&gt;def.extractor_name); float newConfidence; void *newMeta = nullptr; FreeMetaFunc newFreeMeta = nullptr; // 执行对应实例的Sniff函数 void *curCreator = NULL; if ((*it)-&gt;def.def_version == EXTRACTORDEF_VERSION_NDK_V1) &#123; curCreator = (void*) (*it)-&gt;def.u.v2.sniff( source-&gt;wrap(), &amp;newConfidence, &amp;newMeta, &amp;newFreeMeta); &#125; else if ((*it)-&gt;def.def_version == EXTRACTORDEF_VERSION_NDK_V2) &#123; curCreator = (void*) (*it)-&gt;def.u.v3.sniff( source-&gt;wrap(), &amp;newConfidence, &amp;newMeta, &amp;newFreeMeta); &#125; // 如果实例创建成功 if (curCreator) &#123; // 则对比分数，分数大于上次的则将该实例作为最合适实例； if (newConfidence &gt; *confidence) &#123; *confidence = newConfidence; if (*meta != nullptr &amp;&amp; *freeMeta != nullptr) &#123; (*freeMeta)(*meta); &#125; *meta = newMeta; *freeMeta = newFreeMeta; plugin = *it; bestCreator = curCreator; *creatorVersion = (*it)-&gt;def.def_version; &#125; else &#123; // 小于则释放 if (newMeta != nullptr &amp;&amp; newFreeMeta != nullptr) &#123; newFreeMeta(newMeta); &#125; &#125; &#125; &#125; // 返回分数最高的实例 return bestCreator;&#125; WAV解封装器下面就是WAV格式的解封装器的Sniff函数 123456789101112131415161718192021222324static CreatorFunc Sniff( CDataSource *source, float *confidence, void **, FreeMetaFunc *) &#123; DataSourceHelper *helper = new DataSourceHelper(source); char header[12]; if (helper-&gt;readAt(0, header, sizeof(header)) &lt; (ssize_t)sizeof(header)) &#123; delete helper; return NULL; &#125; if (memcmp(header, "RIFF", 4) || memcmp(&amp;header[8], "WAVE", 4)) &#123; delete helper; return NULL; &#125; WAVExtractor *extractor = new WAVExtractor(helper); // extractor owns the helper int numTracks = extractor-&gt;countTracks(); delete extractor; if (numTracks == 0) &#123; return NULL; &#125; *confidence = 0.3f; return CreateExtractor;&#125; 重要方法详解setDataSource​ 方法很简单，就是设置数据源，对于数据源的协议目前只支持本地文件和HTTP、HTTPS。 12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455sp&lt;DataSource&gt; DataSourceFactory::CreateFromURI( const sp&lt;MediaHTTPService&gt; &amp;httpService, const char *uri, const KeyedVector&lt;String8, String8&gt; *headers, String8 *contentType, HTTPBase *httpSource) &#123; if (contentType != NULL) &#123; *contentType = ""; &#125; sp&lt;DataSource&gt; source; if (!strncasecmp("file://", uri, 7)) &#123; // 本地文件 source = CreateFileSource(uri + 7); &#125; else if (!strncasecmp("http://", uri, 7) || !strncasecmp("https://", uri, 8)) &#123; // HTTP HTTPS sp&lt;HTTPBase&gt; mediaHTTP = httpSource; if (mediaHTTP == NULL) &#123; mediaHTTP = static_cast&lt;HTTPBase *&gt;(CreateMediaHTTP(httpService).get()); &#125; String8 cacheConfig; bool disconnectAtHighwatermark = false; KeyedVector&lt;String8, String8&gt; nonCacheSpecificHeaders; if (headers != NULL) &#123; nonCacheSpecificHeaders = *headers; NuCachedSource2::RemoveCacheSpecificHeaders( &amp;nonCacheSpecificHeaders, &amp;cacheConfig, &amp;disconnectAtHighwatermark); &#125; if (mediaHTTP-&gt;connect(uri, &amp;nonCacheSpecificHeaders) != OK) &#123; ALOGE("Failed to connect http source!"); return NULL; &#125; if (contentType != NULL) &#123; *contentType = mediaHTTP-&gt;getMIMEType(); &#125; source = NuCachedSource2::Create( mediaHTTP, cacheConfig.isEmpty() ? NULL : cacheConfig.string(), disconnectAtHighwatermark); &#125; else if (!strncasecmp("data:", uri, 5)) &#123; source = DataURISource::Create(uri); &#125; else &#123; // 如果scheme都不符合，则假设其是个本地文件 source = CreateFileSource(uri); &#125; if (source == NULL || source-&gt;initCheck() != OK) &#123; return NULL; &#125; return source;&#125; selectTrack、unselectTrack选中指定轨道和解除选中。一个媒体文件中会包含着多个轨道，比如音频轨道和视频轨道等，在MediaExtractor中对样本数据的操作都是基于指定的轨道来的，所以在操作样本数据之前，必须先选中一个轨道。 当一个轨道被选中后，后续的getSamplexxx方法都是针对该轨道，如果想换一个轨道进行处理，则必须先解除当前轨道的选中，调用unselectTrack函数。 对于如何选择想要的轨道，可以通过getTrackFormat方法来获取每一个轨道的详细信息，通过这些信息筛选出想要处理的轨道。 getTrackCount获取当前媒体文件中的轨道数量，通常用来遍历所有的轨道。 getTrackFormat获取指定索引的轨道的详细信息，索引的值从0开始递增。轨道信息在不同Android版本上有不同的支持，在使用的时候需要注意。 这里有一点需要注意，就是在使用轨道的MediaFormat对象去创建解码器的时候，最好先将KEY_LEVEL的值设置为null（MediaFormat.setString(KEY_LEVEL, null)），因为这个值经常不准。 readSampleData读取当前选中的轨道的一个样本数据，返回读取的size，这里要注意的是buffer的大小，需要足够大，否则无法一次读取完一个样本，通常可以使用MediaFormat.KEY_MAX_INPUT_SIZE来获取。 返回-1则表示当前轨道已经没有可读取的样本数据了。 getSampleTrackIndex查询当前样本来源的轨道索引，也就是当前选中的轨道索引。如果当前轨道没有可读取的样本数据，会返回-1。 getSampleTime返回当前样本的PTS，微秒。 getSmapleFlags返回当前样本的标记，通过标记可以判断当前样本的一些特征。 SAMPLE_FLAG_ENCRYPTED 样本数据被加密 SAMPLE_FLAG_SYNC 样本为关键帧 seekTo根据指定模式跳转到指定时间，只能跳转到关键帧，所以最终时间可能会和指定时间有误差。模式有三种： SEEK_TO_CLOSEST_SYNC 在指定时间的前后查找最近关键帧 SEEK_TO_NEXT_SYNC 在指定时间的后面查找最近关键帧 SEEK_TO_PREVIOUS_SYNC 在指定时间的前面查找最近关键帧 advance移动到下一个样本，每次读取完样本数据后，都需要调用该方法，此时调用getSmaplexxx方法读取到的才是下一个样本的数据。 如果当前轨道没有可读样本，则返回false。 release释放资源 MediaMuxer 封装MediaMuxer类是官方提供的音视频封装类。其官方使用Demo如下所示： 1234567891011121314151617181920212223242526// 创建封装器，指定输出文件和封装格式。MediaMuxer muxer = new MediaMuxer("temp.mp4", OutputFormat.MUXER_OUTPUT_MPEG_4);// 创建音频和视频轨道MediaFormat audioFormat = new MediaFormat(...);MediaFormat videoFormat = new MediaFormat(...);// 将轨道添加到封装器中int audioTrackIndex = muxer.addTrack(audioFormat);int videoTrackIndex = muxer.addTrack(videoFormat);// 创建一个buffer用来接收样本数据ByteBuffer inputBuffer = ByteBuffer.allocate(bufferSize);boolean finished = false;BufferInfo bufferInfo = new BufferInfo();// 启动封装器，启动后不可再更改muxer.start();while(!finished) &#123; // 循环写入样本数据到指定轨道中 finished = getInputBuffer(inputBuffer, isAudioSample, bufferInfo); if (!finished) &#123; int currentTrackIndex = isAudioSample ? audioTrackIndex : videoTrackIndex; muxer.writeSampleData(currentTrackIndex, inputBuffer, bufferInfo); &#125;&#125;;// 停止封装器muxer.stop();// 释放muxer.release(); 支持的格式 创建封装器封装器的创建比较简单，根据指定的OutputFormat来创建对应格式的封装器。 1234567891011121314151617181920212223242526272829static bool isMp4Format(MediaMuxer::OutputFormat format) &#123; return format == MediaMuxer::OUTPUT_FORMAT_MPEG_4 || format == MediaMuxer::OUTPUT_FORMAT_THREE_GPP || format == MediaMuxer::OUTPUT_FORMAT_HEIF;&#125;MediaMuxer::MediaMuxer(int fd, OutputFormat format) : mFormat(format), mState(UNINITIALIZED) &#123; // 根据指定format创建对应的writer if (isMp4Format(format)) &#123; mWriter = new MPEG4Writer(fd); &#125; else if (format == OUTPUT_FORMAT_WEBM) &#123; mWriter = new WebmWriter(fd); &#125; else if (format == OUTPUT_FORMAT_OGG) &#123; mWriter = new OggWriter(fd); &#125; if (mWriter != NULL) &#123; mFileMeta = new MetaData; if (format == OUTPUT_FORMAT_HEIF) &#123; // Note that the key uses recorder file types. mFileMeta-&gt;setInt32(kKeyFileType, output_format::OUTPUT_FORMAT_HEIF); &#125; else if (format == OUTPUT_FORMAT_OGG) &#123; mFileMeta-&gt;setInt32(kKeyFileType, output_format::OUTPUT_FORMAT_OGG); &#125; mState = INITIALIZED; &#125;&#125; 重要方法详解addTrack创建了封装器后，需要添加对应的轨道，轨道的信息通过MediaFormat来描述，也就是上面解封装器中通过getTrackFormat方法获取到的信息，通过这些信息添加一个轨道，添加成功返回轨道的索引，后续write数据的时候使用该索引即可。 对于音频轨道，需要添加的是KEY_SAMPLE_RATE 采样率以及KEY_CHANNEL_COUT声道数，需要注意的是CSD的添加。 对于视频轨道，处理必要的KEY_WIDTH和KEY_HEIGHT外，需要注意的有KEY_BIT_RATE码率和CSD的添加。 关于CSD这里简单介绍下，也就是Codec-Specific Data，对于H.264来说，csd-0的数据为SPS序列参数集，csd-1的数据为PPS图像参数集，而对于AAC来说，csd-0的数据为ESDS。 那么对于MediaFormat的key在不同版本中的支持情况与接封装器中的又不太一样，使用的时候需要注意参考下表。 writeSampleData将编码后的样本数据写入到封装器中的指定轨道中，参数为轨道索引、数据buffer、bufferInfo。buffer和bufferInfo都是通过MediaCodec编码后产生的，需要注意的是bufferInfo的flag标记. BUFFER_FLAG_CODEC_CONFIG 表明buffer中包含CSD数据，而并非样本数据。 BUFFER_FLAG_END_OF_STREAM 表明流结束。 BUFFER_FLAG_KEY_FRAME 关键帧 BUFFER_FLAG_SYNC_FRAME 关键帧，API21废弃 BUFFER_FLAG_PARTIAL_FRAME 表明当前数据并非完整的一帧，API26添加 这里需要注意的一点是，CSD数据是不能通过该方法来写入的，所以在调用该方法的时候需要判断bufferInfo的flag是否有BUFFER_FLAG_CODEC_CONFIG标记，没有BUFFER_FLAG_CODEC_CONFIG标记的数据才可以用该方法写入。CSD数据需要通过添加到轨道的MediaFormat中，跟随MediaFormt被addTrack到封装器中。 start启动封装器，启动后无法再添加轨道。 stop停止封装器。 release释放封装器。 MediaMetadataRetriever 获取元信息 （扩展）MediaMetadataRetriever是用于获取媒体文件信息和指定帧数据的API。 重要方法详解extractMetadata查询指定的媒体信息。常用的有以下几种KEY，需要注意返回的值很有可能为空。 METADATA_KEY_BITRATE 如果可用，此键检索平均比特率（以比特/秒为单位）。 METADATA_KEY_CAPTURE_FRAMERATE 该键可检索原始捕获帧率（如果可用） METADATA_KEY_DATE 用于检索数据源创建或修改日期的元数据键。 METADATA_KEY_HAS_AUDIO 如果此键存在，则媒体包含音频内容。 METADATA_KEY_HAS_VIDEO 如果该键存在，则媒体包含视频内容。 METADATA_KEY_LOCATION 该键检索位置信息（如果可用）。 METADATA_KEY_MIMETYPE 用于检索数据源的MIME类型的元数据密钥。 METADATA_KEY_TITLE 用于检索数据源标题的元数据键。 METADATA_KEY_VIDEO_HEIGHT 如果媒体包含视频，则此键检索其高度。 METADATA_KEY_VIDEO_ROTATION 如果可用，此键以度数检索视频旋转角度。 METADATA_KEY_VIDEO_WIDTH 如果媒体包含视频，则此键检索其宽度。 getFrameAtTime根据指定模式，返回接近指定时间的关键帧数据，以bitmap格式返回。一般使用该方法获取封面图。 OPTION_CLOSEST_SYNC 在指定时间的前后查找最近关键帧 OPTION_NEXT_SYNC 在指定时间的后面查找最近关键帧 OPTION_PREVIOUS_SYNC 在指定时间的前面查找最近关键帧]]></content>
      <categories>
        <category>拳掌指腿（音视频）</category>
        <category>七伤拳（音视频基础）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[音视频开发之基础篇11]]></title>
    <url>%2F%E9%9F%B3%E8%A7%86%E9%A2%91%E5%BC%80%E5%8F%91%E4%B9%8B%E5%9F%BA%E7%A1%80%E7%AF%87%2F</url>
    <content type="text"><![CDATA[本篇重点 音视频基础概念与相关术语的讲解 什么是视频图像的组成在说视频之前，我们来聊一下图像，图像是人对视觉感知的物质再现。图像是使用像素点阵来表示的，每个像素点都分配有特定的颜色和位置值。像素也是图像显示的基本单位，通常一幅1080x1920的图片，就是长度1920个像素点，宽度1080个像素点组成的矩阵。 颜色空间像素点除了有位置外还需要有颜色信息，那么颜色在计算机中是如何表示呢？ 在计算机中颜色空间有很多，这里我们介绍下常用的两种颜色空间，RGB颜色空间和YUV颜色空间。 RGB颜色空间RGB颜色空间是由红绿蓝三原色来定义，通过三原色混合成所有的颜色，这应该是我们最熟悉的一种颜色空间。 YUV颜色空间Y表示亮度，U和V表示色度和浓度。早期的YUV实际上是用在彩色电视和黑白电视的交替上，感兴趣历史的同学可以点击YUV查看相关资料。YUV颜色空间在数字媒体中广泛使用是由于人类对亮度的的敏感度高于对色度的敏感度，但是在数字媒体中使用的是个变体，叫YCbCr，其中Cb是指蓝色色度，Cr就是红色色度。像JPEG、MPEG都是采用此格式，后续一般讲的YUV都是指YCbCr。(Y的值是根据RGB值计算出来的，所以Cg的值可以通过CbCr计算出来，所以在传输的时候只需要传输YCbCr) 12345计算公式: Y = KrR+KgG+Kb*B（k为权重值,ITU-R推荐的BT.601中定义Kb=0.114,Kr=0.299,Kb+Kr+Kg=1） Cb=B-Y Cr=R-Y Cg=G-Y YCbCr会按照一定的采样比对色度数据进行采样，采样比通常表示为 J​ : a : ​b，表示在一个宽为J像素、高为2像素的采样区域中进行采样，J表示采样的宽度，通常是4，a表示第一行色度采样数，b表示第二行色度采样数与第一行色度采样的不同样点数。 最常见的采样比是 4:2:0 ，表示宽为4，第一行有两个色度采样点，第二行与第一行完全相同。所以总共有八个亮度采样点和两个色度采样点，每个亮度采样点需要记录一个值Y，而每个色度采样点都需要记录两个值Cb和Cr。最终需要记录的值为 8Y+2Cr+2Cb = 12，而如果使用RGB来记录则会需要3x8=24。 YCbCr的优势就在于，在达到最大压缩率的情况下，能够保证对人眼感知的失真度最小。拿4:2:0来说足足压缩了一半。 YUV按照存储方式又可以分为下面两种 planarYCbCr分别存储在不同的平面中。根据CbCr存储的顺序不同，又分为I420和YV12。 I420 : 也叫YU12，先存储Y，再存Cb，最后存Cr，最终数据结果是 YYYY CbCb CrCr，也就是上图所展示的。 YV12：先存储Y，再存Cr，最后存Cb，最终数据结果是 YYYY CrCr CbCb semi planarY单独一个平面，CbCr共用一个平面。根据CbCr存储的顺序不同，又分为NV12和NV21。 NV12：先存储Y，然后CbCr交替存储，最终数据结果为 YYYY CbCr CbCr，也就是上图所展示的。（IOS中使用） NV21：先存储Y，然后CrCb交替存储，最终数据结果为 YYYY CrCb CrCb。（Android中使用） 视频的组成前面说了这么多图像，现在我们来说说视频，所谓视频就像下面动图中展示的一样，视频是由连续的图像组成。视频的原理就是利用人眼的特殊结构，当画面在快速切换时，画面会有残留，所以人感觉起来就像是连贯的动作。 视频帧帧是视频流的一个基本单位，可以将其联想成一幅静态图像，一个YCbCr格式的图像。 帧率帧率指的是视频每秒包含的帧数（FPS，Frame per second），帧率是衡量视频质量的一个重要指标。 帧率越高，则视频会越流畅，一般视频都是24-30帧之间，电影一般都保持在24帧，而手机一般拍摄都默认在30帧，最高可以拍摄60帧的视频。 色彩位数表示一个像素中存储颜色所用的位数，位数越高，则颜色越丰富。常见的有8位、16位、24位、32位，其中24位是最常用的，称为真彩，而32位是在24位上加了8位表示透明度。 场（扩展）大家应该都听说过1080P，这是一种视频显示格式，那么大家有没有听过说1080I呢？ 1080I指的是在电视信号中，采用隔行交错扫描视频的信号格式。而1080P指的是用逐行扫描视频的显示格式。 为什么会有隔行扫描呢？因为早期电视机没办法处理大量复杂的数据，没发观看高清电视，所以采用了隔行扫描，将每一帧图像，按照单行和双行分为两场，顶场和低场，两场交替显示。在PAL制式的电视台规定是每秒25帧，每帧两场，也就是每秒50场，所以PAL制式的扫描频率就是50HZ。 人类视觉系统HVS（扩展）HVS由眼睛+神经+大脑组成。视频的一些设计就是根据HVS的特点来的。 对高频信息不敏感 -&gt; 丢弃高频信息，只编码低频信息 对高对比度更敏感 -&gt; 提高边缘信息的主观质量 对亮度信息比色度信息更敏感 -&gt; 降低色度的解析度 对运动的信息更敏感 -&gt; 对ROI区域进行特殊处理 什么是音频上面讲了图像的数字化表现形式，那么声音如何数字化呢？ 声音的产生声音是由物体振动产生的，再通过空气、固体、液体等传输，传入人耳后振动耳膜，再通过听觉神经传递给大脑 声音的三要素 音调 ：音频振动的快慢 （男生 &lt; 女生 &lt; 儿童） 音量 ：振动的幅度 音色 ：声波是由基波和谐波组合成，不同频谱的谐波比例不同，音色也就不同 ​ 人类听觉范围 = 次声波 ~ 20Hz ~ 20kHz ~ 超声波 PCM声音是一种波，是一种模拟信号，模拟信号是指时间上和数值上连续的信号。而模拟信号想要用于计算机，就必须将模拟信号转为数字信号。而PCM（脉冲编码调制）就是将输入的模拟信号进行采样、量化、编码，最终输出为数字信号。 ​ 模拟信号 -&gt; 采样 -&gt; 量化 -&gt; 编码 -&gt; 数字信号 采样采样就是把时间连续的模拟信号变成时间离散、幅度连续的数字信号。 采样率采样率就是指每秒采集的样本数，常见的有 8k、16k、32k、44.1k、48k等。 Nyquist采样定律：采样率大于或等于连续信号最高频率分量的2倍时，采样信号可以用来完美重构连续信号。而人类正常感知的频率在22.05kHz以下，所以最常见的采样率是44.1kHz，也就是每秒采样44100个采样点。 采样位数就是采样值的范围，一般有8位、16位、32位。位数越多，音量起伏的大小变化就能够更精细的被记录下来，还原度越高。一般采用的都是16位，32位太扩张了。 声道声音的通道的数目，指声音在录制和播放的时候在不同空间位置采集或回放的相互独立的音频信号，简单来说就是声音录制时的音频源数量或回放时相应的扬声器数量。分为单声道、双声道、多声道。其中双声道是使用最普遍的。 量化采样后的信号虽然在时间上是离散的，但是其值的取值范围依旧是无限的，所以需要将无限的值量化到一定范围内的有限值。 编码将量化后的采样值编码成二进制码流的过程称为编码。 为什么要编码当一个音视频文件生产的时候，就面临了两个问题： 存储 传输 为什么存储和传输会是个问题呢？我们来计算下一个原始未压缩的视频会有多大，已一个1080x1920分辨率、帧率是30的90分钟视频为例 1234567单帧大小 1920x1080 = 2073600个像素点，按照RGB来算就是 2073600x3byte = 6220800byte ≈ 5.9MB，按照YUV来计算就是 ≈ 2.9MB。一秒视频大小 2.9MBx30 = 87MB一秒音频大小 44100x2bytex2(声道) = 176400byte 约等于 0.168MB90分钟的视频大小 = 87MBx60x90 = 469800MB ≈ 459GB90分钟的音频大小 = 0.168MBx60x90 = 907.2MB 是不是很恐怖？一部电影居然就接近460GB了，想想当前的硬盘大小和网速，怎么可能存储和传输呢。 所以必须要对视频进行编码，编码就是按指定的方式，将信息从一种形式转换成另一种形式。编码的目的，就是为了压缩，各种视频编码方式，都是为了将视频变得更小，便于存储和传输。 视频编码编码原理压缩编码的核心思想就是去除冗余信息。常见的编码方式有预测编码和变换编码以及熵编码，其中预测编码是压缩率最大的一种。 预测编码帧内预测编码 从空间角度出发，图像相邻像素之间有较强的相关性，可以从空间上去帧内像素之间的冗余。比如说下面这一帧，其中尘土的颜色、背景的颜色都是大致相同的，完全没有必要每个像素都记录一次颜色。 帧间预测编码 从时间角度出发，视频序列的相邻图像之间的内容有相似性，可以从时间上去除帧与帧之间的冗余。比如说下面这一个视频序列（假设这是一个2秒50帧的视频序列），我们可以发现这50帧里，除了结印的手在变动，其它地方都没有变化。那么其它未变的地方真的要存储50份一样的数据吗？是不是可以省略掉，存储一份就够了。 变换编码大多数视频图像都和上面展示的一样，变动的区域只是一小部分，大部分区域是不变的，可就是说低频区占大部分，高频区占小部分。然后将空间域的图像变换到频率域，会产生相关性很小的变换系数，然后再对变换系数进行量化，利用上面人类视觉体统特点所说到的，人对高频信息不敏感的特征，对低频区的系数进行细量化，对高频区的系数进行粗量化，从而降低信息传递量，达到压缩的目的。 简单的解释变换，就是如下图所示，坐标系中有ABC三点，我们将坐标轴调整一下，ABC三点的坐标值就会变小。 一个8x8的像素区域，假设其像素颜色值如下图所示 像素区域经过DCT（离散余弦变换）变换后，生产的变换系数，如下图所示 对变换系数进行量化，量化后大部分都会变成0，此时只需要将这些非0的值进行编码即可，入下图所示 熵编码利用信息的统计特性，使用新的编码来表示输入的数据，比如说哈夫曼编码、CABAC。简单解释就是给高频数据短码，给低频数据长码，比如 A 是高频数据，则编码为 01，G是低频数据，则编码为 01010001。 编码模型（扩展） 常见编码格式常见的编码格式有H.26x系列和MPEG系列，其中H.26x系统是由ITU组织（国际电信联盟）主导，而MPEG系列由ISO（国际标准化组织）下的MPEG（动态图像专家组）组织主导的。 目前主流的H.264和H.265编码格式是由ITU和MPEG两个组织合力制定的。 H.264与MPEG-4 AVC的关系在早期，ITU和MPEG两家组织都是各搞各的，ITU组织推行了H.261、H.262、H.263编码格式，而MPEG组织则推行了MPEG-1、MPEG-2、MPEG-3标准族群。后来两家组织准备合力制作新一代的视频编码标准，对于ITU组织来说，将这个新一代的编码标准命名为H.264，而对于MPEG来说，这个新一代的压缩标准只是其MPEG-4标准的第10部分，其第10部分叫做高级视频编码AVC（Advanced Video Coding）。 所以可以简单的认为H.264就是MPEG-4 AVC。 Annex B格式和AVCC格式（扩展） Annex B格式 这种格式的H.264码流用于实时流的传播中，其特点是码流中每个NALU（单元块）之间通过起始码来分割，起始码分为两种，一帧开始则用四个字节的1来表示，不是一帧开始就用三个字节的1来表示。 H.264原始码流是由一系列的NALU组成（Network Abstraction Layer Unit），不同的NALU数据量不同。 AVCC格式 这种格式的H.264码流用于磁盘文件中（MP4等媒体文件），其特点是没有起始码，而是将起始码替换成了size，是为了应对存储文件的可随机访问特性。 H.265与MPEG-H HEVCH.265和H.264一样，依旧是ITU和MPEG两家合力制定的。H.265被定义在MPEG-H的第2部分，称为高效率视频编码 High Efficiency Video Coding。 H.265的压缩率是H.264的两倍，并且支持4K清晰度，最高支持到8K。目前主流的设备基本都支持H.265编码。 音频编码编码原理音频编码是在保证信号在听觉方面不产生失真的前提下，对音频数据进行尽可能大的压缩。其原理是通过去除声音信号中冗余成分来实现，所谓冗余成分就是指不在人耳听觉范围内的音频信号以及被掩蔽掉的音频信号。 不在人耳听觉范围内的音频信号是因为人耳能够察觉的声音信号的频率范围为20Hz-20KHz，除此之外的频率是无法察觉的。 被掩蔽掉的音频信号是因为当一个强音信号与一个弱音信号同时存在时，弱音信号将被强音信号所掩蔽而听不见，所以此时弱音信号可以视为冗余而不用传输。 频谱掩蔽效应相同音量下，频率低的被遮蔽；相同频率下，音量小的被遮蔽； 时域掩蔽效应时间上相邻的声音之间也有掩蔽现象，分为超前掩蔽和滞后掩蔽。产生时域掩蔽的主要原因是人的大脑处理信息需要花费一定的时间，超前掩蔽很短，只有大约5~20ms，而滞后掩蔽可以持续50~200ms。 编码模型（扩展） 常见编码格式如果说视频编码被H.264和H.265所统治，那么统治音频编码的就是AAC了。AAC全称是Advanced Audio Coding，是一个有损压缩的音频编码集。除了AAC编码外，Opus编码在即时通信领域上使用的比较广泛。 ACC规格 AAC-LC AAC-LC的全称是Advanced Audio Coding Low-Complexity，用于存储空间和计算能力有限的情况，这种类型没有使用预测和增益控制，瞬时噪声整形的阶数也比较低。 AAC-LD AAC-LD的全称是Advanced Audio Coding Low-Delay，是源于AAC-LC，结合了感知音频编码和双向通道的低延时要求，减小的算法的延时。 AAC-HE-V1 核心思想是按频谱保存，将时域转换为频域，将低频编码保存为主要部分，将高频单独放大编码保存。 AAC-HE-V2 核心思想是双声道中的声音存在某种相似性，只需存储一个声道的全部信息，然后花很少的字节用参数描述另一个声道和它不同的地方。 AAV格式 ADIF格式 ADIF是在MPEG-2中定义的，其特点是可以确定的找到这个音频数据的开始，解码必须在明确定义的开始处进行，这种格式常用在磁盘文件中。 ADTS格式 ADTS的特征是它有一个同步字的比特流，解码可以在这个流的任何位置开始，帧同步目的就是在于找出帧头在比特流中的位置，ADTS的同步字为三个字节的1111 1111 1111来表示。 LATM格式 LATM是在MPEG-4中制定的一种高效率的码流传输方式。 为什么要封装？所谓封装就是定义一个容器，将视频、音频、字幕等数据都混合封装在容器中。容器使得媒体内容的同步播放变得简单，另外容器中还会存储媒体内容的基本信息以及媒体内容的帧索引，方便快速拖动和跳转到指定位置。 常见封装格式MPEG格式后缀为（.mp4等），常用的一般是MPEG-4标准的第14部分定义的MP4文件格式。 QuickTime File Format格式后缀为 .mov ，苹果公司推出的一种视频封装格式，常用于苹果设备。 Flash Video格式后缀为 .flv，Adobe Flash延伸出来的一种视频封装格式。 硬解与软解的区别硬解码调用soc中的专门模块来进行编解码，减少了对CPU的占用，且更加省电，但是缺点是在Android手机上，由于国内产商使用的芯片参差不齐，会有很多兼容性的问题。 SOC芯片手机SOC芯片，就是在一个芯片里面集成CPU、GPU、SP、ISP、RAM内存、WIFI控制器、基带芯片、音频芯片等。 Android平台MediaCodec在Android平台的上，提供的编解码API就是MediaCodec，该API在21之前都只有Java层实现，在21后提供了C实现。MediaCodec与底层硬件的交互，是通过OpenMax来实现的。 IOS平台VideoToolBox软解码软解码是指通过CPU来对视频进行编解码处理，常用的开源库是FFmpeg，相对于硬解码来说，软解码的兼容性非常好，但是缺点也很明显，占用CPU，费电，在处理速度上会比硬解码慢。]]></content>
      <categories>
        <category>拳掌指腿（音视频）</category>
        <category>七伤拳（音视频基础）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[FFmpeg深入之ffmpeg_parse_options]]></title>
    <url>%2FFFmpeg%E6%B7%B1%E5%85%A5%E4%B9%8Bffmpeg-parse-options%2F</url>
    <content type="text"><![CDATA[简述1ffmpeg [global_options] &#123;[input_file_options] -i input_url&#125; ... &#123;[output_file_options] output_url&#125; ... 根据FFmpeg官方文档定义，FFmpeg命令由 全局参数+{输入参数+输入源}+{输出参数+输出流} 组成。其中全局参数可放在任何地方，输入参数必须放在输入源之前。 例： 1ffmpeg -i video.mp4 -i 1.png -i 1.mp3 -filter_complex "[0:v]scale=750:1334,pad=750:1334:0:0:black[source];[source][1:v]overlay=x=0:y=0[result]" -map "[result]" -map 2:a -s 540x960 -t 7 -shortest -movflags faststart -y output.mp4 那么当FFmpeg接收到上面这样一串字符命令时，是如何解析和处理的？ 123456int main(int argc, char **argv)&#123; ... /* parse options and open all input/output files */ ret = ffmpeg_parse_options(argc, argv);&#125; 在FFmpeg的main方法中可以看到，所有的解析逻辑都在ffmpeg_parse_options函数中，下面我们来一起分析下该函数 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657int ffmpeg_parse_options(int argc, char **argv)&#123; OptionParseContext octx; uint8_t error[128]; int ret; memset(&amp;octx, 0, sizeof(octx)); /* 解析输入的字符命令 */ ret = split_commandline(&amp;octx, argc, argv, options, groups, FF_ARRAY_ELEMS(groups)); if (ret &lt; 0) &#123; av_log(NULL, AV_LOG_FATAL, "Error splitting the argument list: "); goto fail; &#125; /* 应用全局参数 */ ret = parse_optgroup(NULL, &amp;octx.global_opts); if (ret &lt; 0) &#123; av_log(NULL, AV_LOG_FATAL, "Error parsing global options: "); goto fail; &#125; /* configure terminal and setup signal handlers */ term_init(); /* 打开输入文件流 */ ret = open_files(&amp;octx.groups[GROUP_INFILE], "input", open_input_file); if (ret &lt; 0) &#123; av_log(NULL, AV_LOG_FATAL, "Error opening input files: "); goto fail; &#125; /* 初始化滤镜图 */ ret = init_complex_filters(); if (ret &lt; 0) &#123; av_log(NULL, AV_LOG_FATAL, "Error initializing complex filters.\n"); goto fail; &#125; /* 打开输出文件流 */ ret = open_files(&amp;octx.groups[GROUP_OUTFILE], "output", open_output_file); if (ret &lt; 0) &#123; av_log(NULL, AV_LOG_FATAL, "Error opening output files: "); goto fail; &#125; check_filter_outputs();fail: uninit_parse_context(&amp;octx); if (ret &lt; 0) &#123; av_strerror(ret, error, sizeof(error)); av_log(NULL, AV_LOG_FATAL, "%s\n", error); &#125; return ret;&#125; 解析参数将参数解析到OptionParseContext中 123456// octx: OptionParseContext// argc, argv: main方法中接收的字符命令// options: OptionDef数组，定义了所有可用的参数// groups: OptionGroupDef数组，定义了输入和输出两种参数组// FF_ARRAY_ELEMS(groups): groups的sizeret = split_commandline(&amp;octx, argc, argv, options, groups,FF_ARRAY_ELEMS(groups)); init_parse_context123456789101112131415161718192021static void init_parse_context(OptionParseContext *octx, const OptionGroupDef *groups, int nb_groups)&#123; static const OptionGroupDef global_group = &#123; "global" &#125;; int i; memset(octx, 0, sizeof(*octx)); octx-&gt;nb_groups = nb_groups; octx-&gt;groups = av_mallocz_array(octx-&gt;nb_groups, sizeof(*octx-&gt;groups)); if (!octx-&gt;groups) exit_program(1); // 将输入参数组和输出参数组设置到 octx-&gt;groups中 for (i = 0; i &lt; octx-&gt;nb_groups; i++) octx-&gt;groups[i].group_def = &amp;groups[i]; // 将全局参数组放入到 octx-&gt;global_opts.group_def中 octx-&gt;global_opts.group_def = &amp;global_group; octx-&gt;global_opts.arg = ""; init_opts();&#125; 遍历所有参数遍历所有参数，将参数加入到OptionParseContext中对应的参数组中 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556 while (optindex &lt; argc) &#123; const char *opt = argv[optindex++], *arg; const OptionDef *po; int ret; av_log(NULL, AV_LOG_DEBUG, "Reading option '%s' ...", opt); // 过滤 --开头的参数 if (opt[0] == '-' &amp;&amp; opt[1] == '-' &amp;&amp; !opt[2]) &#123; dashdash = optindex; continue; &#125; if (opt[0] != '-' || !opt[1] || dashdash+1 == optindex) &#123; // 处理输出参数，将参数添加到输出参数组中 finish_group(octx, 0, opt); av_log(NULL, AV_LOG_DEBUG, " matched as %s.\n", groups[0].name); continue; &#125; opt++;#define GET_ARG(arg) \do &#123; \ arg = argv[optindex++]; \ if (!arg) &#123; \ av_log(NULL, AV_LOG_ERROR, "Missing argument for option '%s'.\n", opt);\ return AVERROR(EINVAL); \ &#125; \&#125; while (0) if ((ret = match_group_separator(groups, nb_groups, opt)) &gt;= 0) &#123; // 处理输入参数，加入到输入参数组中 GET_ARG(arg); finish_group(octx, ret, arg); av_log(NULL, AV_LOG_DEBUG, " matched as %s with argument '%s'.\n", groups[ret].name, arg); continue; &#125; // 处理一般参数，根据参数名称查找OptionDef对象 po = find_option(options, opt); if (po-&gt;name) &#123; if (po-&gt;flags &amp; OPT_EXIT) &#123; arg = argv[optindex++]; &#125; else if (po-&gt;flags &amp; HAS_ARG) &#123; GET_ARG(arg); &#125; else &#123; arg = "1"; &#125; // 将参数加入到octx对应的参数组中 add_opt(octx, po, opt, arg); av_log(NULL, AV_LOG_DEBUG, " matched as option '%s' (%s) with " "argument '%s'.\n", po-&gt;name, po-&gt;help, arg); continue; &#125; ... return AVERROR_OPTION_NOT_FOUND; &#125; 解析全局参数解析所有的全局参数 12345678910111213int parse_optgroup(void *optctx, OptionGroup *g)&#123; int i, ret; for (i = 0; i &lt; g-&gt;nb_opts; i++) &#123; Option *o = &amp;g-&gt;opts[i]; // 解析参数 ret = write_option(optctx, o-&gt;opt, o-&gt;key, o-&gt;val); if (ret &lt; 0) return ret; &#125; return 0;&#125; write_option该方法中用来解析所有全局参数，根据每个Options对象定义的解析方法来解析。比如说 filter_complex 参数 12&#123; "filter_complex", HAS_ARG | OPT_EXPERT, &#123; .func_arg = opt_filter_complex &#125;, "create a complex filtergraph", "graph_description" &#125;, 该Options定义了自己的func_arg，那么解析该参数就会调用 opt_filter_complex函数来解析。 123456789101112131415161718192021222324252627282930313233343536static int write_option(void *optctx, const OptionDef *po, const char *opt, const char *arg)&#123; // 根据Option定义的flag，来解析不同类型的Option if (po-&gt;flags &amp; OPT_STRING) &#123; char *str; str = av_strdup(arg); av_freep(dst); if (!str) return AVERROR(ENOMEM); *(char **)dst = str; &#125; else if (po-&gt;flags &amp; OPT_BOOL || po-&gt;flags &amp; OPT_INT) &#123; *(int *)dst = parse_number_or_die(opt, arg, OPT_INT64, INT_MIN, INT_MAX); &#125; else if (po-&gt;flags &amp; OPT_INT64) &#123; *(int64_t *)dst = parse_number_or_die(opt, arg, OPT_INT64, INT64_MIN, INT64_MAX); &#125; else if (po-&gt;flags &amp; OPT_TIME) &#123; *(int64_t *)dst = parse_time_or_die(opt, arg, 1); &#125; else if (po-&gt;flags &amp; OPT_FLOAT) &#123; *(float *)dst = parse_number_or_die(opt, arg, OPT_FLOAT, -INFINITY, INFINITY); &#125; else if (po-&gt;flags &amp; OPT_DOUBLE) &#123; *(double *)dst = parse_number_or_die(opt, arg, OPT_DOUBLE, -INFINITY, INFINITY); &#125; else if (po-&gt;u.func_arg) &#123; // 调用Options的func_arg函数来解析 int ret = po-&gt;u.func_arg(optctx, opt, arg); if (ret &lt; 0) &#123; av_log(NULL, AV_LOG_ERROR, "Failed to set value '%s' for option '%s': %s\n", arg, opt, av_err2str(ret)); return ret; &#125; &#125; if (po-&gt;flags &amp; OPT_EXIT) exit_program(0); return 0;&#125; 打开输入文件流从OptionParseContext取出输入参数组， 1ret = open_files(&amp;octx.groups[GROUP_INFILE], "input", open_input_file); open_files123456789101112131415161718192021static int open_files(OptionGroupList *l, const char *inout, int (*open_file)(OptionsContext*, const char*))&#123; int i, ret; for (i = 0; i &lt; l-&gt;nb_groups; i++) &#123; OptionGroup *g = &amp;l-&gt;groups[i]; OptionsContext o; init_options(&amp;o); o.g = g; // 解析输入参数组，和解析全局参数逻辑一样 ret = parse_optgroup(&amp;o, g); ... // 调用参数中传递的函数打开文件，g-&gt;arg就是-i后面的文件路径 ret = open_file(&amp;o, g-&gt;arg); uninit_options(&amp;o); ... return 0;&#125; open_input_file首先会处理该输入文件对应的输入参数，然后通过avformat_open_input开启流，再通过avformat_find_stream_info获取流详细信息，最后将流加入到全局输入流集合中。 关于avformat_open_input可以查看这篇文章 FFmpeg深入之avformat_open_input 关于avformat_find_stream_info可以查看这篇文章 FFmpeg深入之avformat_find_stream_info  int open_input_file(OptionsContext *o, const char *filename)&#123; InputFile *f; AVFormatContext *ic; AVInputFormat *file_iformat = NULL; int err, i, ret; int64_t timestamp; AVDictionary *unused_opts = NULL; AVDictionaryEntry *e = NULL; char * video_codec_name = NULL; char * audio_codec_name = NULL; char *subtitle_codec_name = NULL; char * data_codec_name = NULL; int scan_all_pmts_set = 0; ... // 如果输入参数中定义了解封装格式，则查找该格式是否存在 if (o-&gt;format) &#123; if (!(file_iformat = av_find_input_format(o-&gt;format))) &#123; av_log(NULL, AV_LOG_FATAL, "Unknown input format: '%s'\n", o-&gt;format); exit_program(1); &#125; &#125; if (!strcmp(filename, "-")) filename = "pipe:"; stdin_interaction &amp;= strncmp(filename, "pipe:", 5) &amp;&amp; strcmp(filename, "/dev/stdin"); /* 创建AVFormatContext */ ic = avformat_alloc_context(); ... // 设置输入音频采样率 if (o-&gt;nb_audio_sample_rate) &#123; av_dict_set_int(&amp;o-&gt;g-&gt;format_opts, "sample_rate", o-&gt;audio_sample_rate[o-&gt;nb_audio_sample_rate - 1].u.i, 0); &#125; // 设置音频声道 if (o-&gt;nb_audio_channels) &#123; /* because we set audio_channels based on both the "ac" and * "channel_layout" options, we need to check that the specified * demuxer actually has the "channels" option before setting it */ if (file_iformat &amp;&amp; file_iformat-&gt;priv_class &amp;&amp; av_opt_find(&amp;file_iformat-&gt;priv_class, "channels", NULL, 0, AV_OPT_SEARCH_FAKE_OBJ)) &#123; av_dict_set_int(&amp;o-&gt;g-&gt;format_opts, "channels", o-&gt;audio_channels[o-&gt;nb_audio_channels - 1].u.i, 0); &#125; &#125; // 设置视频帧率 if (o-&gt;nb_frame_rates) &#123; /* set the format-level framerate option; * this is important for video grabbers, e.g. x11 */ if (file_iformat &amp;&amp; file_iformat-&gt;priv_class &amp;&amp; av_opt_find(&amp;file_iformat-&gt;priv_class, "framerate", NULL, 0, AV_OPT_SEARCH_FAKE_OBJ)) &#123; av_dict_set(&amp;o-&gt;g-&gt;format_opts, "framerate", o-&gt;frame_rates[o-&gt;nb_frame_rates - 1].u.str, 0); &#125; &#125; // 设置视频帧数 if (o-&gt;nb_frame_sizes) &#123; av_dict_set(&amp;o-&gt;g-&gt;format_opts, "video_size", o-&gt;frame_sizes[o-&gt;nb_frame_sizes - 1].u.str, 0); &#125; // 设置视频像素的颜色空间格式 if (o-&gt;nb_frame_pix_fmts) av_dict_set(&amp;o-&gt;g-&gt;format_opts, "pixel_format", o-&gt;frame_pix_fmts[o-&gt;nb_frame_pix_fmts - 1].u.str, 0); // 设置 c:v c:a c:s c:d 解码格式参数 MATCH_PER_TYPE_OPT(codec_names, str, video_codec_name, ic, "v"); MATCH_PER_TYPE_OPT(codec_names, str, audio_codec_name, ic, "a"); MATCH_PER_TYPE_OPT(codec_names, str, subtitle_codec_name, ic, "s"); MATCH_PER_TYPE_OPT(codec_names, str, data_codec_name, ic, "d"); if (video_codec_name) ic-&gt;video_codec = find_codec_or_die(video_codec_name , AVMEDIA_TYPE_VIDEO , 0); if (audio_codec_name) ic-&gt;audio_codec = find_codec_or_die(audio_codec_name , AVMEDIA_TYPE_AUDIO , 0); if (subtitle_codec_name) ic-&gt;subtitle_codec = find_codec_or_die(subtitle_codec_name, AVMEDIA_TYPE_SUBTITLE, 0); if (data_codec_name) ic-&gt;data_codec = find_codec_or_die(data_codec_name , AVMEDIA_TYPE_DATA , 0); // 如果该解码格式没有开启不存在，则设置为AV_CODEC_ID_NONE ic-&gt;video_codec_id = video_codec_name ? ic-&gt;video_codec-&gt;id : AV_CODEC_ID_NONE; ic-&gt;audio_codec_id = audio_codec_name ? ic-&gt;audio_codec-&gt;id : AV_CODEC_ID_NONE; ic-&gt;subtitle_codec_id = subtitle_codec_name ? ic-&gt;subtitle_codec-&gt;id : AV_CODEC_ID_NONE; ic-&gt;data_codec_id = data_codec_name ? ic-&gt;data_codec-&gt;id : AV_CODEC_ID_NONE; ic-&gt;flags |= AVFMT_FLAG_NONBLOCK; if (o-&gt;bitexact) ic-&gt;flags |= AVFMT_FLAG_BITEXACT; ic-&gt;interrupt_callback = int_cb; if (!av_dict_get(o-&gt;g-&gt;format_opts, "scan_all_pmts", NULL, AV_DICT_MATCH_CASE)) &#123; av_dict_set(&amp;o-&gt;g-&gt;format_opts, "scan_all_pmts", "1", AV_DICT_DONT_OVERWRITE); scan_all_pmts_set = 1; &#125; // 打开文件流，读取多媒体文件头信息 err = avformat_open_input(&amp;ic, filename, file_iformat, &amp;o-&gt;g-&gt;format_opts); ... if (scan_all_pmts_set) av_dict_set(&amp;o-&gt;g-&gt;format_opts, "scan_all_pmts", NULL, AV_DICT_MATCH_CASE); remove_avoptions(&amp;o-&gt;g-&gt;format_opts, o-&gt;g-&gt;codec_opts); assert_avoptions(o-&gt;g-&gt;format_opts); /* 根据avformat_open_input开启的流信息中确定的编码格式，重新选择解码器 */ for (i = 0; i &lt; ic-&gt;nb_streams; i++) choose_decoder(o, ic, ic-&gt;streams[i]); if (find_stream_info) &#123; AVDictionary **opts = setup_find_stream_info_opts(ic, o-&gt;g-&gt;codec_opts); int orig_nb_streams = ic-&gt;nb_streams; /* 解码文件头数据获取流信息，如果不足，则解码首帧来获取信息 */ ret = avformat_find_stream_info(ic, opts); for (i = 0; i &lt; orig_nb_streams; i++) av_dict_free(&amp;opts[i]); av_freep(&amp;opts); ... &#125; ... /* 将流信息添加到全局的输入流集合中 */ add_input_streams(o, ic); // 释放资源,重置状态 ... return 0;&#125; 初始化滤镜图1234567891011121314151617181920212223242526272829303132333435363738int init_complex_filtergraph(FilterGraph *fg)&#123; AVFilterInOut *inputs, *outputs, *cur; AVFilterGraph *graph; int ret = 0; // AVFilterGraph graph = avfilter_graph_alloc(); if (!graph) return AVERROR(ENOMEM); graph-&gt;nb_threads = 1; // 解析滤镜参数 ret = avfilter_graph_parse2(graph, fg-&gt;graph_desc, &amp;inputs, &amp;outputs); if (ret &lt; 0) goto fail; for (cur = inputs; cur; cur = cur-&gt;next) init_input_filter(fg, cur); // 将解析的结果放到AVFilterGraph中，将每一个滤镜解析成 输入+滤镜+输出 for (cur = outputs; cur;) &#123; GROW_ARRAY(fg-&gt;outputs, fg-&gt;nb_outputs); fg-&gt;outputs[fg-&gt;nb_outputs - 1] = av_mallocz(sizeof(*fg-&gt;outputs[0])); if (!fg-&gt;outputs[fg-&gt;nb_outputs - 1]) exit_program(1); fg-&gt;outputs[fg-&gt;nb_outputs - 1]-&gt;graph = fg; fg-&gt;outputs[fg-&gt;nb_outputs - 1]-&gt;out_tmp = cur; fg-&gt;outputs[fg-&gt;nb_outputs - 1]-&gt;type = avfilter_pad_get_type(cur-&gt;filter_ctx-&gt;output_pads, cur-&gt;pad_idx); fg-&gt;outputs[fg-&gt;nb_outputs - 1]-&gt;name = describe_filter_link(fg, cur, 0); cur = cur-&gt;next; fg-&gt;outputs[fg-&gt;nb_outputs - 1]-&gt;out_tmp-&gt;next = NULL; &#125;fail: avfilter_inout_free(&amp;inputs); avfilter_graph_free(&amp;graph); return ret;&#125; avfilter_graph_parse21234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950int avfilter_graph_parse2(AVFilterGraph *graph, const char *filters, AVFilterInOut **inputs, AVFilterInOut **outputs)&#123; int index = 0, ret = 0; char chr = 0; AVFilterInOut *curr_inputs = NULL, *open_inputs = NULL, *open_outputs = NULL; filters += strspn(filters, WHITESPACES); if ((ret = parse_sws_flags(&amp;filters, graph)) &lt; 0) goto fail; // 循环处理所有滤镜 do &#123; AVFilterContext *filter; filters += strspn(filters, WHITESPACES); // 解析输入 0:v if ((ret = parse_inputs(&amp;filters, &amp;curr_inputs, &amp;open_outputs, graph)) &lt; 0) goto end; // 解析滤镜 scale=w:h if ((ret = parse_filter(&amp;filter, &amp;filters, graph, index, graph)) &lt; 0) goto end; if ((ret = link_filter_inouts(filter, &amp;curr_inputs, &amp;open_inputs, graph)) &lt; 0) goto end; // 解析输出 [source] if ((ret = parse_outputs(&amp;filters, &amp;curr_inputs, &amp;open_inputs, &amp;open_outputs, graph)) &lt; 0) goto end; filters += strspn(filters, WHITESPACES); chr = *filters++; if (chr == ';' &amp;&amp; curr_inputs) append_inout(&amp;open_outputs, &amp;curr_inputs); index++; &#125; while (chr == ',' || chr == ';'); ... append_inout(&amp;open_outputs, &amp;curr_inputs); *inputs = open_inputs; *outputs = open_outputs; return 0; ...&#125; 打开输出文件流open_files123456789101112131415161718192021static int open_files(OptionGroupList *l, const char *inout, int (*open_file)(OptionsContext*, const char*))&#123; int i, ret; for (i = 0; i &lt; l-&gt;nb_groups; i++) &#123; OptionGroup *g = &amp;l-&gt;groups[i]; OptionsContext o; init_options(&amp;o); o.g = g; // 解析输出参数组，和解析全局参数逻辑一样 ret = parse_optgroup(&amp;o, g); ... // 调用参数中传递的函数打开文件，g-&gt;arg就是参数最后面的输出文件路径 ret = open_file(&amp;o, g-&gt;arg); uninit_options(&amp;o); ... return 0;&#125; open_output_ int open_output_file(OptionsContext *o, const char *filename)&#123; AVFormatContext *oc; int i, j, err; OutputFile *of; OutputStream *ost; InputStream *ist; AVDictionary *unused_opts = NULL; AVDictionaryEntry *e = NULL; int format_flags = 0; ... // 创建AVFormatContext err = avformat_alloc_output_context2(&amp;oc, NULL, o-&gt;format, filename); ... /* create streams for all unlabeled output pads */ for (i = 0; i &lt; nb_filtergraphs; i++) &#123; FilterGraph *fg = filtergraphs[i]; for (j = 0; j &lt; fg-&gt;nb_outputs; j++) &#123; OutputFilter *ofilter = fg-&gt;outputs[j]; if (!ofilter-&gt;out_tmp || ofilter-&gt;out_tmp-&gt;name) continue; switch (ofilter-&gt;type) &#123; case AVMEDIA_TYPE_VIDEO: o-&gt;video_disable = 1; break; case AVMEDIA_TYPE_AUDIO: o-&gt;audio_disable = 1; break; case AVMEDIA_TYPE_SUBTITLE: o-&gt;subtitle_disable = 1; break; &#125; init_output_filter(ofilter, o, oc); &#125; &#125; // 是否有-map操作 if (!o-&gt;nb_stream_maps) &#123; char *subtitle_codec_name = NULL; /* pick the "best" stream of each type */ /* video: highest resolution */ if (!o-&gt;video_disable &amp;&amp; av_guess_codec(oc-&gt;oformat, NULL, filename, NULL, AVMEDIA_TYPE_VIDEO) != AV_CODEC_ID_NONE) &#123; int area = 0, idx = -1; // 查询解码器 int qcr = avformat_query_codec(oc-&gt;oformat, oc-&gt;oformat-&gt;video_codec, 0); // 遍历所有的输入流 for (i = 0; i &lt; nb_input_streams; i++) &#123; int new_area; ist = input_streams[i]; new_area = ist-&gt;st-&gt;codecpar-&gt;width * ist-&gt;st-&gt;codecpar-&gt;height + 100000000*!!ist-&gt;st-&gt;codec_info_nb_frames + 5000000*!!(ist-&gt;st-&gt;disposition &amp; AV_DISPOSITION_DEFAULT); if (ist-&gt;user_set_discard == AVDISCARD_ALL) continue; if((qcr!=MKTAG('A', 'P', 'I', 'C')) &amp;&amp; (ist-&gt;st-&gt;disposition &amp; AV_DISPOSITION_ATTACHED_PIC)) new_area = 1; if (ist-&gt;st-&gt;codecpar-&gt;codec_type == AVMEDIA_TYPE_VIDEO &amp;&amp; new_area &gt; area) &#123; if((qcr==MKTAG('A', 'P', 'I', 'C')) &amp;&amp; !(ist-&gt;st-&gt;disposition &amp; AV_DISPOSITION_ATTACHED_PIC)) continue; area = new_area; idx = i; &#125; &#125; // 创建一个新的视频输出流 if (idx &gt;= 0) new_video_stream(o, oc, idx); &#125; /* audio: most channels */ if (!o-&gt;audio_disable &amp;&amp; av_guess_codec(oc-&gt;oformat, NULL, filename, NULL, AVMEDIA_TYPE_AUDIO) != AV_CODEC_ID_NONE) &#123; int best_score = 0, idx = -1; for (i = 0; i &lt; nb_input_streams; i++) &#123; int score; ist = input_streams[i]; score = ist-&gt;st-&gt;codecpar-&gt;channels + 100000000*!!ist-&gt;st-&gt;codec_info_nb_frames + 5000000*!!(ist-&gt;st-&gt;disposition &amp; AV_DISPOSITION_DEFAULT); if (ist-&gt;user_set_discard == AVDISCARD_ALL) continue; if (ist-&gt;st-&gt;codecpar-&gt;codec_type == AVMEDIA_TYPE_AUDIO &amp;&amp; score &gt; best_score) &#123; best_score = score; idx = i; &#125; &#125; // 创建一个新的音频输出流 if (idx &gt;= 0) new_audio_stream(o, oc, idx); &#125; ... // 创建新的字幕和数据输出流 &#125; else &#123; // 遍历所有的map for (i = 0; i &lt; o-&gt;nb_stream_maps; i++) &#123; StreamMap *map = &amp;o-&gt;stream_maps[i]; if (map-&gt;disabled) continue; // map是一个输出的链接，类似于-map [source] if (map-&gt;linklabel) &#123; FilterGraph *fg; OutputFilter *ofilter = NULL; int j, k; // 应用滤镜 for (j = 0; j &lt; nb_filtergraphs; j++) &#123; fg = filtergraphs[j]; for (k = 0; k &lt; fg-&gt;nb_outputs; k++) &#123; AVFilterInOut *out = fg-&gt;outputs[k]-&gt;out_tmp; if (out &amp;&amp; !strcmp(out-&gt;name, map-&gt;linklabel)) &#123; ofilter = fg-&gt;outputs[k]; goto loop_end; &#125; &#125; &#125; ... // 创建音频和视频输出流，只有音频和视频流支持滤镜 init_output_filter(ofilter, o, oc); &#125; else &#123; // 类似于-map 2:a int src_idx = input_files[map-&gt;file_index]-&gt;ist_index + map-&gt;stream_index; ist = input_streams[input_files[map-&gt;file_index]-&gt;ist_index + map-&gt;stream_index]; ... // 根据当前map流的类型,创建不同类型的输出流 ost = NULL; switch (ist-&gt;st-&gt;codecpar-&gt;codec_type) &#123; case AVMEDIA_TYPE_VIDEO: ost = new_video_stream (o, oc, src_idx); break; case AVMEDIA_TYPE_AUDIO: ost = new_audio_stream (o, oc, src_idx); break; case AVMEDIA_TYPE_SUBTITLE: ost = new_subtitle_stream (o, oc, src_idx); break; case AVMEDIA_TYPE_DATA: ost = new_data_stream (o, oc, src_idx); break; case AVMEDIA_TYPE_ATTACHMENT: ost = new_attachment_stream(o, oc, src_idx); break; case AVMEDIA_TYPE_UNKNOWN: ... default: ... &#125; if (ost) ost-&gt;sync_ist = input_streams[ input_files[map-&gt;sync_file_index]-&gt;ist_index + map-&gt;sync_stream_index]; &#125; &#125; &#125; ... /* set the decoding_needed flags and create simple filtergraphs */ /* check filename in case of an image number is expected */ for (i = 0; i &lt; o-&gt;nb_metadata_map; i++) &#123; char *p; int in_file_index = strtol(o-&gt;metadata_map[i].u.str, &amp;p, 0); ... // 复制metadata copy_metadata(o-&gt;metadata_map[i].specifier, *p ? p + 1 : p, oc, in_file_index &gt;= 0 ? input_files[in_file_index]-&gt;ctx : NULL, o); &#125; /* copy chapters */ if (o-&gt;chapters_input_file &gt;= nb_input_files) &#123; if (o-&gt;chapters_input_file == INT_MAX) &#123; /* copy chapters from the first input file that has them*/ o-&gt;chapters_input_file = -1; for (i = 0; i &lt; nb_input_files; i++) if (input_files[i]-&gt;ctx-&gt;nb_chapters) &#123; o-&gt;chapters_input_file = i; break; &#125; &#125; else &#123; ... &#125; &#125; if (o-&gt;chapters_input_file &gt;= 0) copy_chapters(input_files[o-&gt;chapters_input_file], of, !o-&gt;metadata_chapters_manual); /* 复制全局metadata */ if (!o-&gt;metadata_global_manual &amp;&amp; nb_input_files)&#123; av_dict_copy(&amp;oc-&gt;metadata, input_files[0]-&gt;ctx-&gt;metadata, AV_DICT_DONT_OVERWRITE); if(o-&gt;recording_time != INT64_MAX) av_dict_set(&amp;oc-&gt;metadata, "duration", NULL, 0); av_dict_set(&amp;oc-&gt;metadata, "creation_time", NULL, 0); &#125; if (!o-&gt;metadata_streams_manual) for (i = of-&gt;ost_index; i &lt; nb_output_streams; i++) &#123; InputStream *ist; if (output_streams[i]-&gt;source_index &lt; 0) /* this is true e.g. for attached files */ continue; ist = input_streams[output_streams[i]-&gt;source_index]; av_dict_copy(&amp;output_streams[i]-&gt;st-&gt;metadata, ist-&gt;st-&gt;metadata, AV_DICT_DONT_OVERWRITE); if (!output_streams[i]-&gt;stream_copy) &#123; av_dict_set(&amp;output_streams[i]-&gt;st-&gt;metadata, "encoder", NULL, 0); &#125; &#125; /* process manually set programs */ for (i = 0; i &lt; o-&gt;nb_program; i++) &#123; const char *p = o-&gt;program[i].u.str; int progid = i+1; AVProgram *program; while(*p) &#123; const char *p2 = av_get_token(&amp;p, ":"); const char *to_dealloc = p2; char *key; if (!p2) break; if(*p) p++; key = av_get_token(&amp;p2, "="); if (!key || !*p2) &#123; av_freep(&amp;to_dealloc); av_freep(&amp;key); break; &#125; p2++; if (!strcmp(key, "program_num")) progid = strtol(p2, NULL, 0); av_freep(&amp;to_dealloc); av_freep(&amp;key); &#125; program = av_new_program(oc, progid); &#125; ... return 0;&#125;]]></content>
      <categories>
        <category>拳掌指腿（音视频）</category>
        <category>降龙十八掌（FFmpeg）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[Flutter深入之插件新API——Embedding-V2]]></title>
    <url>%2FFlutter%E6%B7%B1%E5%85%A5%E4%B9%8B%E6%8F%92%E4%BB%B6%E6%96%B0API-Embedding-V2%2F</url>
    <content type="text"><![CDATA[在Flutter1.12版本中正式将Embedding-V2API在Android平台默认开启，所有官方插件都迁移到了新的API。Embedding-V2APi的优势在于针对混合开发提供了更好的支持。 版本配置GeneratedPluginRegistrant生成内容配置，通过在AndroidManifest.xml中配置Embedding的版本来确定工具生成的GeneratedPluginRegistrant类的内容。12345&lt;!-- Don't delete the meta-data below. It is used by the Flutter tool to generate GeneratedPluginRegistrant.java --&gt;&lt;meta-data android:name="flutterEmbedding" android:value="2" /&gt; 具体判定逻辑在flutter_tools项目中12345678910111213141516171819202122232425262728293031323334 AndroidEmbeddingVersion getEmbeddingVersion() &#123; if (isModule) &#123; // A module type's Android project is used in add-to-app scenarios and // only supports the V2 embedding. return AndroidEmbeddingVersion.v2; &#125; if (appManifestFile == null || !appManifestFile.existsSync()) &#123; return AndroidEmbeddingVersion.v1; &#125; xml.XmlDocument document; try &#123; document = xml.parse(appManifestFile.readAsStringSync()); &#125; on xml.XmlParserException &#123; throwToolExit('Error parsing $appManifestFile ' 'Please ensure that the android manifest is a valid XML document and try again.'); &#125; on FileSystemException &#123; throwToolExit('Error reading $appManifestFile even though it exists. ' 'Please ensure that you have read permission to this file and try again.'); &#125; for (xml.XmlElement metaData in document.findAllElements('meta-data')) &#123; final String name = metaData.getAttribute('android:name'); if (name == 'flutterEmbedding') &#123; final String embeddingVersionString = metaData.getAttribute('android:value'); if (embeddingVersionString == '1') &#123; return AndroidEmbeddingVersion.v1; &#125; if (embeddingVersionString == '2') &#123; return AndroidEmbeddingVersion.v2; &#125; &#125; &#125; return AndroidEmbeddingVersion.v1; &#125;&#125; 创建时机变更旧API旧的创建插件的时机一般是在FlutterActivity的子类onCreate()回调时，手动调用下面的方法来创建所有的插件。 1GeneratedPluginRegistrant.registerWith(PluginRegistry registry); 新API新的API将插件与视图进行解绑，将插件与引擎进行绑定，并且在引擎创建的时候提供了自动创建插件的功能。123456789101112131415161718192021222324252627282930public FlutterEngine( @NonNull Context context, @NonNull FlutterLoader flutterLoader, @NonNull FlutterJNI flutterJNI, @Nullable String[] dartVmArgs, boolean automaticallyRegisterPlugins) &#123; this.pluginRegistry = new FlutterEnginePluginRegistry( context.getApplicationContext(), this, flutterLoader ); // 默认为true，自动创建插件 if (automaticallyRegisterPlugins) &#123; registerPlugins(); &#125;&#125;// 通过反射调用GeneratedPluginRegistrant.registerWith(FlutterEngine engine)方法private void registerPlugins() &#123; try &#123; Class&lt;?&gt; generatedPluginRegistrant = Class.forName("io.flutter.plugins.GeneratedPluginRegistrant"); Method registrationMethod = generatedPluginRegistrant.getDeclaredMethod("registerWith", FlutterEngine.class); registrationMethod.invoke(null, this); &#125; catch (Exception e) &#123; Log.w(TAG, "Tried to automatically register plugins with FlutterEngine (" + this + ") but could not find and invoke the GeneratedPluginRegistrant."); &#125;&#125; 注册对象变更旧API在旧的API中，registerWith方法接收的参数类型是PluginRegistry。PluginRegistry（io.flutter.plugin.common包）维护所有的插件，每一个想注册的插件都通过pluginKey在PluginRegistry这里创建一个Registrar对象（内部类对象，提供外部类私有属性的公有方法），通过该对象，每个插件可以获取到全局的BinaryMessenger对象（也就是NativeView），通过该对象可以在插件中创建一个MethodChannel来进行插件通信。 Registrant插件注册者，所有在pubspec.yaml中配置的插件都会自动在下面方法中生成。12345678910public final class GeneratedPluginRegistrant &#123; public static void registerWith(PluginRegistry registry) &#123; if (alreadyRegisteredWith(registry)) &#123; return; &#125; MartletVideoEditorPlugin.registerWith(registry.registrarFor("com.zhenyu.martlet/videoEditor")); &#125; ...&#125; RegistryPluginRegistry对外的实现类是FlutterActivity（io.flutter.app包），最终的实现类是FlutterPluginRegistry。 FlutterPluginRegistry对象是由NativeView创建的，创建时机在FlutterActivity（io.flutter.app包）的onCreate时。12345678910public FlutterNativeView(@NonNull Context context, boolean isBackgroundView) &#123; mContext = context; mPluginRegistry = new FlutterPluginRegistry(this, context); mFlutterJNI = new FlutterJNI(); mFlutterJNI.addIsDisplayingFlutterUiListener(flutterUiDisplayListener); this.dartExecutor = new DartExecutor(mFlutterJNI, context.getAssets()); mFlutterJNI.addEngineLifecycleListener(new EngineLifecycleListenerImpl()); attach(this, isBackgroundView); assertAttached();&#125; 在Registry中维护了一个插件map，里面包含了所有注册的插件。1private final Map&lt;String, Object&gt; mPluginMap = new LinkedHashMap&lt;&gt;(0); 注册pluginKey，获取Registrar12345678@Overridepublic Registrar registrarFor(String pluginKey) &#123; if (mPluginMap.containsKey(pluginKey)) &#123; throw new IllegalStateException("Plugin key " + pluginKey + " is already in use"); &#125; mPluginMap.put(pluginKey, null); return new FlutterRegistrar(pluginKey);&#125; RegistrarRegistrar只是一个内部类对象，提供Registry的一些私有属性暴露给外部使用，其中最重要的就是暴露BinaryMessenger对象给插件对象使用。123456789101112131415161718192021222324private class FlutterRegistrar implements Registrar &#123; private final String pluginKey; FlutterRegistrar(String pluginKey) &#123; this.pluginKey = pluginKey; &#125; @Override public Activity activity() &#123; return mActivity; &#125; @Override public BinaryMessenger messenger() &#123; return mNativeView; &#125; @Override public TextureRegistry textures() &#123; return mFlutterView; &#125; ...&#125; 新API在新API中，registerWith方法接收的参数是FlutterEngine。PluginRegistry（io.flutter.embedding.engine.plugins包）维护所有的插件，每一个想注册的插件都必须实现FlutterPlugin接口，并将自身对象添加到PluginRegistry（io.flutter.embedding.engine.plugins包）中。由PluginRegistry（io.flutter.embedding.engine.plugins包）统一操控所有插件的生命周期。 Registrant插件注册者，所有在pubspec.yaml中配置的插件都会自动在下面方法中生成。 12345public final class GeneratedPluginRegistrant &#123; public static void registerWith(@NonNull FlutterEngine flutterEngine) &#123; flutterEngine.getPlugins().add(new com.zhenyu.martlet.core_flutter.MartletVideoEditorPlugin()); &#125;&#125; Registry在FlutterEngine创建的时候，就会创建一个FlutterEnginePluginRegistry对象，来维护所有的插件，flutterEngine.getPlugins()获取的就是FlutterEnginePluginRegistry对象。123456789101112131415161718public FlutterEngine( @NonNull Context context, @NonNull FlutterLoader flutterLoader, @NonNull FlutterJNI flutterJNI, @Nullable String[] dartVmArgs, boolean automaticallyRegisterPlugins) &#123; this.pluginRegistry = new FlutterEnginePluginRegistry( context.getApplicationContext(), this, flutterLoader ); // 默认为true，自动创建插件 if (automaticallyRegisterPlugins) &#123; registerPlugins(); &#125;&#125; 在Registry中维护了一个插件map，里面包含了所有注册的插件对象的实例。1private final Map&lt;Class&lt;? extends FlutterPlugin&gt;, FlutterPlugin&gt; plugins = new HashMap&lt;&gt;(); 添加插件对象实例，触发插件的各种生命周期方法，并将插件需要的BinaryMessenger对象通过生命周期方法传递给插件。1234567891011121314151617181920212223242526272829303132333435363738394041424344454647@Overridepublic void add(@NonNull FlutterPlugin plugin) &#123; // 将插件实例添加到map中维护 plugins.put(plugin.getClass(), plugin); // 调用插件的onAttachedToEngine方法，pluginBinding参数中包含了需要的BinaryMessenger对象。 plugin.onAttachedToEngine(pluginBinding); // 如果插件实现了ActivityAware接口，触发Activity生命周期方法 if (plugin instanceof ActivityAware) &#123; ActivityAware activityAware = (ActivityAware) plugin; activityAwarePlugins.put(plugin.getClass(), activityAware); if (isAttachedToActivity()) &#123; activityAware.onAttachedToActivity(activityPluginBinding); &#125; &#125; // 如果插件实现了ServiceAware接口，触发Service生命周期方法 if (plugin instanceof ServiceAware) &#123; ServiceAware serviceAware = (ServiceAware) plugin; serviceAwarePlugins.put(plugin.getClass(), serviceAware); if (isAttachedToService()) &#123; serviceAware.onAttachedToService(servicePluginBinding); &#125; &#125; // 如果插件实现了BroadcastReceiverAware接口，触发BroadcastReceiver生命周期方法 if (plugin instanceof BroadcastReceiverAware) &#123; BroadcastReceiverAware broadcastReceiverAware = (BroadcastReceiverAware) plugin; broadcastReceiverAwarePlugins.put(plugin.getClass(), broadcastReceiverAware); if (isAttachedToBroadcastReceiver()) &#123; broadcastReceiverAware.onAttachedToBroadcastReceiver(broadcastReceiverPluginBinding); &#125; &#125; // 如果插件实现了ContentProviderAware接口，触发ContentProvider生命周期方法 if (plugin instanceof ContentProviderAware) &#123; ContentProviderAware contentProviderAware = (ContentProviderAware) plugin; contentProviderAwarePlugins.put(plugin.getClass(), contentProviderAware); if (isAttachedToContentProvider()) &#123; contentProviderAware.onAttachedToContentProvider(contentProviderPluginBinding); &#125; &#125;&#125; 插件实现变更旧API12345678910111213public class MartletVideoEditorPlugin implements MethodChannel.MethodCallHandler &#123; static MethodChannel mChannel; public static void registerWith(PluginRegistry.Registrar registrar) &#123; mChannel = new MethodChannel(registrar.messenger(), "com.zhenyu.martlet/videoEditor"); mChannel.setMethodCallHandler(this); &#125; @Override public void onMethodCall(MethodCall call, MethodChannel.Result result) &#123; &#125;&#125; 新API插件只需实现FlutterPlugin接口，然后在onAttachedToEngine方法中创建MethodChannel，并设置MethodCallHandler，在onDetachedFromEngine方法中将释放MethodChannel。1234567891011121314151617181920212223242526public class MartletVideoEditorPlugin implements FlutterPlugin, MethodCallHandler &#123; private MethodChannel mChannel; @Override public void onAttachedToEngine(@NonNull FlutterPluginBinding flutterPluginBinding) &#123; mChannel = new MethodChannel(flutterPluginBinding.getBinaryMessenger(), "com.zhenyu.martlet/videoEditor"); mChannel.setMethodCallHandler(this); &#125; // 如果插件不兼容旧API，可以忽略该方法的现实 public static void registerWith(Registrar registrar) &#123; &#125; @Override public void onMethodCall(@NonNull MethodCall call, @NonNull Result result) &#123; &#125; @Override public void onDetachedFromEngine(@NonNull FlutterPluginBinding binding) &#123; mChannel.setMethodCallHandler(null); mChannel = null; &#125;&#125;]]></content>
      <categories>
        <category>刀枪棍棒（Android）</category>
        <category>伏魔杖法（Flutter）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[SurfaceFlinger深入之Layer创建与渲染]]></title>
    <url>%2FSurfaceFlinger%E6%B7%B1%E5%85%A5%E4%B9%8BLayer%E5%88%9B%E5%BB%BA%E4%B8%8E%E6%B8%B2%E6%9F%93%2F</url>
    <content type="text"><![CDATA[一切的开始resumeActivity在Activity第一次执行resume的时候，会将DecorView添加到WindowManager中，由此开始了Activity视图显示的第一步。 1234567891011121314151617181920212223final void handleResumeActivity(IBinder token, boolean clearHide, boolean isForward, boolean reallyResume, int seq, String reason) &#123; ... final Activity a = r.activity; ... if (r.window == null &amp;&amp; !a.mFinished &amp;&amp; willBeVisible) &#123; r.window = r.activity.getWindow(); View decor = r.window.getDecorView(); decor.setVisibility(View.INVISIBLE); ViewManager wm = a.getWindowManager(); ... if (a.mVisibleFromClient) &#123; if (!a.mWindowAdded) &#123; a.mWindowAdded = true; wm.addView(decor, l); &#125; else &#123; ... &#125; &#125; ... &#125; ...&#125; WindowManager.addViewWindowManagerImpl最终会调用到WindowManagerGlobal中，在WindowManagerGlobal中会创建一个ViewRootImpl对象，每个DecorView都会有对应的ViewRootImpl对象，而一个Window中可以有多个DecorView。 ViewRootImpl负责与WMS通信，通过其维护的IWindowSession对象来与WMS进行通信。 12345678910111213141516171819202122232425public void addView(View view, ViewGroup.LayoutParams params, Display display, Window parentWindow) &#123; ... ViewRootImpl root; View panelParentView = null; synchronized (mLock) &#123; ... root = new ViewRootImpl(view.getContext(), display); view.setLayoutParams(wparams); mViews.add(view); mRoots.add(root); mParams.add(wparams); // do this last because it fires off messages to start doing things try &#123; root.setView(view, wparams, panelParentView); &#125; catch (RuntimeException e) &#123; ... throw e; &#125; &#125;&#125; ViewRootImpl.setView通过WindowSession与WMS通信，将Window对象交给WMS，WMS通过Window对象与ViewRootImpl通信。 1234567891011121314151617181920212223242526272829303132public void setView(View view, WindowManager.LayoutParams attrs, View panelParentView) &#123; synchronized (this) &#123; if (mView == null) &#123; mView = view; ... // If the application owns the surface, don't enable hardware acceleration if (mSurfaceHolder == null) &#123; // While this is supposed to enable only, it can effectively disable // the acceleration too. // 开启硬件渲染 enableHardwareAcceleration(attrs); ... &#125; ... // Schedule the first layout -before- adding to the window // manager, to make sure we do the relayout before receiving // any other events from the system. // post一次渲染请求，等待SF连接成功后调用 requestLayout(); ... try &#123; ... // 将mWindow添加到WMS上，WMS使用其与应用端进行通信，具体操作会对应到ViewRootImpl中 res = mWindowSession.addToDisplay(mWindow, mSeq, mWindowAttributes, getHostVisibility(), mDisplay.getDisplayId(), mAttachInfo.mContentInsets, mAttachInfo.mStableInsets, mAttachInfo.mOutsets, mInputChannel); &#125; ... &#125; &#125;&#125; WMS.addWindowSurfaceSession对应着C层的SurfaceComposerClient，该对象是用来与SurfaceFlinger进行通信的。 123456789101112131415161718public int addWindow(Session session, IWindow client, int seq, WindowManager.LayoutParams attrs, int viewVisibility, int displayId, Rect outContentInsets, Rect outStableInsets, Rect outOutsets, InputChannel outInputChannel) &#123; ... final WindowState win = new WindowState(this, session, client, token, parentWindow, appOp[0], seq, attrs, viewVisibility, session.mUid, session.mCanAddInternalSystemWindow); ... // 创建SurfaceSession()，保存在应用端的WindowSession中，用来与SurfaceFlinger通信 win.attach(); // WindowMap管理所有添加到WMS上的Window mWindowMap.put(client.asBinder(), win); ... win.mToken.addWindow(win); ... return res;&#125; 连接SFSurfaceSession维护了C层的SurfaceComposerClient对象 123public SurfaceSession() &#123; mNativeClient = nativeCreate(); &#125; 123456static jlong nativeCreate(JNIEnv* env, jclass clazz) &#123; // 创建SurfaceComposerClient对象 SurfaceComposerClient* client = new SurfaceComposerClient(); client-&gt;incStrong((void*)nativeCreate); return reinterpret_cast&lt;jlong&gt;(client);&#125; SurfaceComposerClient.onFirstRef在SurfaceComposerClient对象被第一次强引用的时候（引用计数器变为1），会触发该方法。在该方法里从SF中请求一个Client连接对象，通过该Client对象与SF通信。 1234567891011121314void SurfaceComposerClient::onFirstRef() &#123; sp&lt;ISurfaceComposer&gt; sm(ComposerService::getComposerService()); if (sm != 0) &#123; auto rootProducer = mParent.promote(); sp&lt;ISurfaceComposerClient&gt; conn; // 创建与SF的连接,通过返回的ISurfaceComposerClient对象与SF进行通信 conn = (rootProducer != nullptr) ? sm-&gt;createScopedConnection(rootProducer) : sm-&gt;createConnection(); if (conn != 0) &#123; mClient = conn; mStatus = NO_ERROR; &#125; &#125;&#125; SF.createConnection123sp&lt;ISurfaceComposerClient&gt; SurfaceFlinger::createConnection() &#123; return initClient(new Client(this));&#125; ViewRootImpl.doTraversal创建和渲染ViewRootImpl.relayoutWindow创建Layer与SF建立连接后，上面post的渲染请求被执行，会通知WMS进行relayout，在该方法的最后一个参数是Surface，这个Surface是在ViewRootImpl创建的时候就创建好的，目前只是一个空壳子。 12345678910111213141516final Surface mSurface = new Surface(); private int relayoutWindow(WindowManager.LayoutParams params, int viewVisibility, boolean insetsPending) throws RemoteException &#123; ... int relayoutResult = mWindowSession.relayout( mWindow, mSeq, params, (int) (mView.getMeasuredWidth() * appScale + 0.5f), (int) (mView.getMeasuredHeight() * appScale + 0.5f), viewVisibility, insetsPending ? WindowManagerGlobal.RELAYOUT_INSETS_PENDING : 0, mWinFrame, mPendingOverscanInsets, mPendingContentInsets, mPendingVisibleInsets, mPendingStableInsets, mPendingOutsets, mPendingBackDropFrame, mPendingMergedConfiguration, mSurface); ... return relayoutResult; &#125; WMS.relayoutWindowJava层的SurfaceControl对应着C层的SurfaceControl，SurfaceControl负责与SF进行通信。 1234567891011121314151617181920212223242526public int relayoutWindow(Session session, IWindow client, int seq, WindowManager.LayoutParams attrs, int requestedWidth, int requestedHeight, int viewVisibility, int flags, Rect outFrame, Rect outOverscanInsets, Rect outContentInsets, Rect outVisibleInsets, Rect outStableInsets, Rect outOutsets, Rect outBackdropFrame, MergedConfiguration mergedConfiguration, Surface outSurface) &#123; synchronized(mWindowMap) &#123; WindowState win = windowForClientLocked(session, client, false); ... if (shouldRelayout) &#123; ... try &#123; // 创建SurfaceControl对象，SurfaceControl对象负责与SF进行通信 result = createSurfaceControl(outSurface, result, win, winAnimator); &#125; catch (Exception e) &#123; ... return 0; &#125; &#125; ... &#125; return result;&#125; 1234567891011121314private int createSurfaceControl(Surface outSurface, int result, WindowState win, WindowStateAnimator winAnimator) &#123; WindowSurfaceController surfaceController; try &#123; // 创建Java层和C层SurfaceControl surfaceController = winAnimator.createSurfaceLocked(win.mAttrs.type, win.mOwnerUid); &#125; if (surfaceController != null) &#123; // 创建C层Surface，并关联Java层Surface surfaceController.getSurface(outSurface); &#125; return result;&#125; 创建SurfaceControl创建Java层SurfaceControl12345678910public SurfaceControl(SurfaceSession session, String name, int w, int h, int format, int flags, SurfaceControl parent, int windowType, int ownerUid) throws OutOfResourcesException &#123; mName = name; // 创建C层SurfaceControl mNativeObject = nativeCreate(session, name, w, h, format, flags, parent != null ? parent.mNativeObject : 0, windowType, ownerUid); &#125; 创建C层SurfaceControl1234567891011121314151617static jlong nativeCreate(JNIEnv* env, jclass clazz, jobject sessionObj, jstring nameStr, jint w, jint h, jint format, jint flags, jlong parentObject, jint windowType, jint ownerUid) &#123; // 获取到SurfaceSession对象，用来与SF通信 sp&lt;SurfaceComposerClient&gt; client(android_view_SurfaceSession_getClient(env, sessionObj)); SurfaceControl *parent = reinterpret_cast&lt;SurfaceControl*&gt;(parentObject); // 创建SurfaceControl sp&lt;SurfaceControl&gt; surface = client-&gt;createSurface( String8(name.c_str()), w, h, format, flags, parent, windowType, ownerUid); if (surface == NULL) &#123; jniThrowException(env, OutOfResourcesException, NULL); return 0; &#125; surface-&gt;incStrong((void *)nativeCreate); return reinterpret_cast&lt;jlong&gt;(surface.get());&#125; 1234567891011121314151617181920212223242526272829sp&lt;SurfaceControl&gt; SurfaceComposerClient::createSurface( const String8&amp; name, uint32_t w, uint32_t h, PixelFormat format, uint32_t flags, SurfaceControl* parent, uint32_t windowType, uint32_t ownerUid)&#123; sp&lt;SurfaceControl&gt; sur; if (mStatus == NO_ERROR) &#123; sp&lt;IBinder&gt; handle; sp&lt;IBinder&gt; parentHandle; sp&lt;IGraphicBufferProducer&gt; gbp; if (parent != nullptr) &#123; parentHandle = parent-&gt;getHandle(); &#125; // 请求SF创建Layer，并返回该Layer的IGraphicBufferProducer status_t err = mClient-&gt;createSurface(name, w, h, format, flags, parentHandle, windowType, ownerUid, &amp;handle, &amp;gbp); if (err == NO_ERROR) &#123; // 创建SurfaceControl，gbp对象会交给Surface对象，Surface对象通过gbp与Layer进行Buffer的通信 sur = new SurfaceControl(this, handle, gbp); &#125; &#125; return sur;&#125; SF.createLayer在SurfaceFlinger中，Surface对应的概念叫Layer。 1234567891011121314151617181920212223242526272829303132status_t SurfaceFlinger::createLayer( const String8&amp; name, const sp&lt;Client&gt;&amp; client, uint32_t w, uint32_t h, PixelFormat format, uint32_t flags, uint32_t windowType, uint32_t ownerUid, sp&lt;IBinder&gt;* handle, sp&lt;IGraphicBufferProducer&gt;* gbp, sp&lt;Layer&gt;* parent)&#123; sp&lt;Layer&gt; layer; String8 uniqueName = getUniqueLayerName(name); switch (flags &amp; ISurfaceComposerClient::eFXSurfaceMask) &#123; case ISurfaceComposerClient::eFXSurfaceNormal: result = createNormalLayer(client, uniqueName, w, h, flags, format, handle, gbp, &amp;layer); break; case ISurfaceComposerClient::eFXSurfaceDim: result = createDimLayer(client, uniqueName, w, h, flags, handle, gbp, &amp;layer); break; default: result = BAD_VALUE; break; &#125; ... result = addClientLayer(client, *handle, *gbp, layer, *parent); ... return result;&#125; 1234567891011121314151617181920void Layer::onFirstRef() &#123; // Creates a custom BufferQueue for SurfaceFlingerConsumer to use sp&lt;IGraphicBufferProducer&gt; producer; sp&lt;IGraphicBufferConsumer&gt; consumer; BufferQueue::createBufferQueue(&amp;producer, &amp;consumer, true); // mProducer就是交给Surface中维护的gbp对象，所以Surface就是BufferQueue的生产者 mProducer = new MonitoredProducer(producer, mFlinger, this); // 而SurfaceFlinger是BufferQueue的消费者 mSurfaceFlingerConsumer = new SurfaceFlingerConsumer(consumer, mTextureName, this); mSurfaceFlingerConsumer-&gt;setConsumerUsageBits(getEffectiveUsage(0)); mSurfaceFlingerConsumer-&gt;setContentsChangedListener(this); mSurfaceFlingerConsumer-&gt;setName(mName); if (mFlinger-&gt;isLayerTripleBufferingDisabled()) &#123; mProducer-&gt;setMaxDequeuedBufferCount(2); &#125; const sp&lt;const DisplayDevice&gt; hw(mFlinger-&gt;getDefaultDisplayDevice()); updateTransformHint(hw);&#125; 创建SurfaceSurface.copyFrom通过SurfaceControl创建C层的Surface，并关联到Java层Surface中。 最终创建完成后，SurfaceControl用SurfaceComposerClient与SurfaceFlinger通信。Surface用IGraphicBufferProducer与Layer进行通信。SurfaceControl由WindowManager来控制，而Surface由ViewRootImpl来控制。 1234567891011121314public void copyFrom(SurfaceControl other) &#123; ... long surfaceControlPtr = other.mNativeObject; long newNativeObject = nativeGetFromSurfaceControl(surfaceControlPtr); synchronized (mLock) &#123; if (mNativeObject != 0) &#123; nativeRelease(mNativeObject); &#125; // 关联C层Surface setNativeObjectLocked(newNativeObject); &#125;&#125; 12345678910static jlong nativeGetFromSurfaceControl(JNIEnv* env, jclass clazz, jlong surfaceControlNativeObj) &#123; // 获取SurfaceControl对象 sp&lt;SurfaceControl&gt; ctrl(reinterpret_cast&lt;SurfaceControl *&gt;(surfaceControlNativeObj)); sp&lt;Surface&gt; surface(ctrl-&gt;getSurface()); if (surface != NULL) &#123; surface-&gt;incStrong(&amp;sRefBaseOwner); &#125; return reinterpret_cast&lt;jlong&gt;(surface.get());&#125; 123456789sp&lt;Surface&gt; SurfaceControl::generateSurfaceLocked() const&#123; // This surface is always consumed by SurfaceFlinger, so the // producerControlledByApp value doesn't matter; using false. // 将Layer中的生产者对象gbp交给Surface来维护，Surface通过该对象来与Layer通信。 mSurfaceData = new Surface(mGraphicBufferProducer, false); return mSurfaceData;&#125; ViewRootImpl.performDraw开始渲染在setView时，就会根据window的属性来判断是否开启了硬件渲染，如果开启了则会创建ThreadedRenderer对象。 12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455565758596061626364656667686970717273747576777879808182private boolean draw(boolean fullRedrawNeeded) &#123; Surface surface = mSurface; ... final Rect dirty = mDirty; ... if (!dirty.isEmpty() || mIsAnimating || accessibilityFocusDirty) &#123; // 硬件渲染 if (mAttachInfo.mThreadedRenderer != null &amp;&amp; mAttachInfo.mThreadedRenderer.isEnabled()) &#123; // If accessibility focus moved, always invalidate the root. boolean invalidateRoot = accessibilityFocusDirty || mInvalidateRootRequested; mInvalidateRootRequested = false; // Draw with hardware renderer. mIsAnimating = false; if (mHardwareYOffset != yOffset || mHardwareXOffset != xOffset) &#123; mHardwareYOffset = yOffset; mHardwareXOffset = xOffset; invalidateRoot = true; &#125; if (invalidateRoot) &#123; mAttachInfo.mThreadedRenderer.invalidateRoot(); &#125; dirty.setEmpty(); // Stage the content drawn size now. It will be transferred to the renderer // shortly before the draw commands get send to the renderer. final boolean updated = updateContentDrawBounds(); ... if (updated) &#123; requestDrawWindow(); &#125; useAsyncReport = true; mAttachInfo.mThreadedRenderer.draw(mView, mAttachInfo, this); &#125; else &#123; // 软件渲染 // If we get here with a disabled &amp; requested hardware renderer, something went // wrong (an invalidate posted right before we destroyed the hardware surface // for instance) so we should just bail out. Locking the surface with software // rendering at this point would lock it forever and prevent hardware renderer // from doing its job when it comes back. // Before we request a new frame we must however attempt to reinitiliaze the // hardware renderer if it's in requested state. This would happen after an // eglTerminate() for instance. if (mAttachInfo.mThreadedRenderer != null &amp;&amp; !mAttachInfo.mThreadedRenderer.isEnabled() &amp;&amp; mAttachInfo.mThreadedRenderer.isRequested() &amp;&amp; mSurface.isValid()) &#123; // 在ThreadedRenderer初始化之前，其isEnabled方法都返回false。 // 所以在ThreadedRenderer.isRequested为true的时候，尝试初始化ThreadedRenderer，重新开始一次绘制。 try &#123; mAttachInfo.mThreadedRenderer.initializeIfNeeded( mWidth, mHeight, mAttachInfo, mSurface, surfaceInsets); &#125; catch (OutOfResourcesException e) &#123; handleOutOfResourcesException(e); return false; &#125; mFullRedrawNeeded = true; scheduleTraversals(); return false; &#125; if (!drawSoftware(surface, mAttachInfo, xOffset, yOffset, scalingRequired, dirty, surfaceInsets)) &#123; return false; &#125; &#125; &#125; if (animating) &#123; mFullRedrawNeeded = true; scheduleTraversals(); &#125; return useAsyncReport;&#125; ThreadedRenderer.draw硬件渲染1234567891011121314151617181920212223242526272829303132void draw(View view, AttachInfo attachInfo, DrawCallbacks callbacks) &#123; final Choreographer choreographer = attachInfo.mViewRootImpl.mChoreographer; choreographer.mFrameInfo.markDrawStart(); updateRootDisplayList(view, callbacks); // register animating rendernodes which started animating prior to renderer // creation, which is typical for animators started prior to first draw if (attachInfo.mPendingAnimatingRenderNodes != null) &#123; final int count = attachInfo.mPendingAnimatingRenderNodes.size(); for (int i = 0; i &lt; count; i++) &#123; registerAnimatingRenderNode( attachInfo.mPendingAnimatingRenderNodes.get(i)); &#125; attachInfo.mPendingAnimatingRenderNodes.clear(); // We don't need this anymore as subsequent calls to // ViewRootImpl#attachRenderNodeAnimator will go directly to us. attachInfo.mPendingAnimatingRenderNodes = null; &#125; int syncResult = syncAndDrawFrame(choreographer.mFrameInfo); if ((syncResult &amp; SYNC_LOST_SURFACE_REWARD_IF_FOUND) != 0) &#123; setEnabled(false); attachInfo.mViewRootImpl.mSurface.release(); // Invalidate since we failed to draw. This should fetch a Surface // if it is still needed or do nothing if we are no longer drawing attachInfo.mViewRootImpl.invalidate(); &#125; if ((syncResult &amp; SYNC_REDRAW_REQUESTED) != 0) &#123; attachInfo.mViewRootImpl.invalidate(); &#125;&#125; 123456789101112131415161718192021222324252627282930313233private void updateRootDisplayList(View view, DrawCallbacks callbacks) &#123; Trace.traceBegin(Trace.TRACE_TAG_VIEW, "Record View#draw()"); updateViewTreeDisplayList(view); // Consume and set the frame callback after we dispatch draw to the view above, but before // onPostDraw below which may reset the callback for the next frame. This ensures that // updates to the frame callback during scroll handling will also apply in this frame. final FrameDrawingCallback callback = mNextRtFrameCallback; mNextRtFrameCallback = null; if (callback != null) &#123; setFrameCallback(callback); &#125; if (mRootNodeNeedsUpdate || !mRootNode.hasDisplayList()) &#123; RecordingCanvas canvas = mRootNode.beginRecording(mSurfaceWidth, mSurfaceHeight); try &#123; final int saveCount = canvas.save(); canvas.translate(mInsetLeft, mInsetTop); callbacks.onPreDraw(canvas); canvas.enableZ(); canvas.drawRenderNode(view.updateDisplayListIfDirty()); canvas.disableZ(); callbacks.onPostDraw(canvas); canvas.restoreToCount(saveCount); mRootNodeNeedsUpdate = false; &#125; finally &#123; mRootNode.endRecording(); &#125; &#125; Trace.traceEnd(Trace.TRACE_TAG_VIEW);&#125; syncAndDrawFrameCanvasContext.createThreadedRenderer创建的时候，会对应创建一个C层的RenderProxy对象，在RenderProxy对象中会维护一个CanvasContext的上下文对象。 会根据renderType创建基于OpenGL-ES的Pipeline或者基于Vulkan的Pipeline。 1234567891011121314151617CanvasContext* CanvasContext::create(RenderThread&amp; thread, bool translucent, RenderNode* rootRenderNode, IContextFactory* contextFactory) &#123; auto renderType = Properties::getRenderPipelineType(); switch (renderType) &#123; case RenderPipelineType::SkiaGL: return new CanvasContext(thread, translucent, rootRenderNode, contextFactory, std::make_unique&lt;skiapipeline::SkiaOpenGLPipeline&gt;(thread)); case RenderPipelineType::SkiaVulkan: return new CanvasContext(thread, translucent, rootRenderNode, contextFactory, std::make_unique&lt;skiapipeline::SkiaVulkanPipeline&gt;(thread)); default: LOG_ALWAYS_FATAL("canvas context type %d not supported", (int32_t)renderType); break; &#125; return nullptr;&#125; CanvasContext.setSurfaceThreadedRenderer初始化的时候，会将ViewRootImpl中的Surface对象传递给CanvasContext。 1234567891011121314151617void CanvasContext::setSurface(sp&lt;Surface&gt;&amp;&amp; surface) &#123; if (surface) &#123; // 创建一个Surface的包装类 mNativeSurface = new ReliableSurface&#123;std::move(surface)&#125;; // TODO: Fix error handling &amp; re-shorten timeout mNativeSurface-&gt;setDequeueTimeout(4000_ms); &#125; else &#123; mNativeSurface = nullptr; &#125; ... // 是否开启了广色域RGB色彩空间 ColorMode colorMode = mWideColorGamut ? ColorMode::WideColorGamut : ColorMode::SRGB; bool hasSurface = mRenderPipeline-&gt;setSurface(mNativeSurface.get(), mSwapBehavior, colorMode, mRenderAheadCapacity); ...&#125; RenderPipeline就是上面根据RenderType创建的pipeline对象。在OpenGLPileline中，初始化EglContext，然后根据Surface创建对应的EglSurface。 1234567891011121314151617181920212223242526272829bool SkiaOpenGLPipeline::setSurface(ANativeWindow* surface, SwapBehavior swapBehavior, ColorMode colorMode, uint32_t extraBuffers) &#123; if (mEglSurface != EGL_NO_SURFACE) &#123; mEglManager.destroySurface(mEglSurface); mEglSurface = EGL_NO_SURFACE; &#125; setSurfaceColorProperties(colorMode); if (surface) &#123; // 初始化EglContext mRenderThread.requireGlContext(); // 创建EglSurface auto newSurface = mEglManager.createSurface(surface, colorMode, mSurfaceColorSpace); if (!newSurface) &#123; return false; &#125; mEglSurface = newSurface.unwrap(); &#125; if (mEglSurface != EGL_NO_SURFACE) &#123; const bool preserveBuffer = (swapBehavior != SwapBehavior::kSwap_discardBuffer); mBufferPreserved = mEglManager.setPreserveBuffer(mEglSurface, preserveBuffer); setBufferCount(surface, extraBuffers); return true; &#125; return false;&#125; 初始化EGL 1234567891011121314151617181920212223242526272829303132333435363738void EglManager::initialize() &#123; ... // 获取EglDisplay mEglDisplay = eglGetDisplay(EGL_DEFAULT_DISPLAY); LOG_ALWAYS_FATAL_IF(mEglDisplay == EGL_NO_DISPLAY, "Failed to get EGL_DEFAULT_DISPLAY! err=%s", eglErrorString()); EGLint major, minor; // 初始化EGL LOG_ALWAYS_FATAL_IF(eglInitialize(mEglDisplay, &amp;major, &amp;minor) == EGL_FALSE, "Failed to initialize display %p! err=%s", mEglDisplay, eglErrorString()); initExtensions(); // Now that extensions are loaded, pick a swap behavior if (Properties::enablePartialUpdates) &#123; // An Adreno driver bug is causing rendering problems for SkiaGL with // buffer age swap behavior (b/31957043). To temporarily workaround, // we will use preserved swap behavior. if (Properties::useBufferAge &amp;&amp; EglExtensions.bufferAge) &#123; mSwapBehavior = SwapBehavior::BufferAge; &#125; else &#123; mSwapBehavior = SwapBehavior::Preserved; &#125; &#125; // 加载EglConfig loadConfigs(); // 创建EglContext createContext(); // 创建用于离屏渲染的surface，pbuffer_surface。 // OpenGL-ES的surface一共有三种 window_surface、pbuffer_surface、pixmap_surface。EGL 和 OpenGL ES 支持两种绘制模式,back buffer和single buffer,window_surface和pbuffer_surface都是使用的back buffer,顾名思义,也就是一块GPU中的 buffer,当绘制完毕的时候,由于window_surface与nativewindow有关联,那么可以使用eglswapbuffer 将其转移到nativewindow上进行显示。而pbuffer_surface与nativewindow没有关联,也就无法显示。 createPBufferSurface(); // 将mPBufferSurface与EglDisplay与EglContext进行关联。在当前线程中激活mPBufferSurface。 makeCurrent(mPBufferSurface, nullptr, /* force */ true); ... mHasWideColorGamutSupport = EglExtensions.glColorSpace &amp;&amp; hasWideColorSpaceExtension &amp;&amp; mEglConfigWideGamut != EGL_NO_CONFIG_KHR;&#125; 创建EglSurface 1234567891011121314151617Result&lt;EGLSurface, EGLint&gt; EglManager::createSurface(EGLNativeWindowType window, ColorMode colorMode, sk_sp&lt;SkColorSpace&gt; colorSpace) &#123; bool wideColorGamut = colorMode == ColorMode::WideColorGamut &amp;&amp; mHasWideColorGamutSupport &amp;&amp; EglExtensions.noConfigContext; EGLint attribs[] = &#123;EGL_NONE, EGL_NONE, EGL_NONE&#125;; ... // 根据Surface创建OpenGL-ES的surface，类型为window_surface EGLSurface surface = eglCreateWindowSurface( mEglDisplay, wideColorGamut ? mEglConfigWideGamut : mEglConfig, window, attribs); ... return surface;&#125; CanvasContext.draw12345678910111213141516171819void CanvasContext::draw() &#123; SkRect dirty; ... Frame frame = mRenderPipeline-&gt;getFrame(); setPresentTime(); SkRect windowDirty = computeDirtyRect(frame, &amp;dirty); // 绘制缓冲区 bool drew = mRenderPipeline-&gt;draw(frame, windowDirty, dirty, mLightGeometry, &amp;mLayerUpdateQueue, mContentDrawBounds, mOpaque, mLightInfo, mRenderNodes, &amp;(profiler())); ... bool requireSwap = false; // 将EglSurface中的缓冲区交换给Surface，由SurfaceFlinger消费。 bool didSwap = mRenderPipeline-&gt;swapBuffers(frame, drew, windowDirty, mCurrentFrameInfo, &amp;requireSwap); ...&#125; SkiaOpenGLPipeline.draw 在draw方法调用之前，会调用makeCurrent方法，将上面创建的EglSurface激活。后续所有的GL操作都将作用在该EglSurface上。 12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152bool SkiaOpenGLPipeline::draw(const Frame&amp; frame, const SkRect&amp; screenDirty, const SkRect&amp; dirty, const LightGeometry&amp; lightGeometry, LayerUpdateQueue* layerUpdateQueue, const Rect&amp; contentDrawBounds, bool opaque, const LightInfo&amp; lightInfo, const std::vector&lt;sp&lt;RenderNode&gt;&gt;&amp; renderNodes, FrameInfoVisualizer* profiler) &#123; mEglManager.damageFrame(frame, dirty); SkColorType colorType = getSurfaceColorType(); // setup surface for fbo0 GrGLFramebufferInfo fboInfo; fboInfo.fFBOID = 0; if (colorType == kRGBA_F16_SkColorType) &#123; fboInfo.fFormat = GL_RGBA16F; &#125; else if (colorType == kN32_SkColorType) &#123; // Note: The default preference of pixel format is RGBA_8888, when other // pixel format is available, we should branch out and do more check. fboInfo.fFormat = GL_RGBA8; &#125; else &#123; LOG_ALWAYS_FATAL("Unsupported color type."); &#125; GrBackendRenderTarget backendRT(frame.width(), frame.height(), 0, STENCIL_BUFFER_SIZE, fboInfo); SkSurfaceProps props(0, kUnknown_SkPixelGeometry); SkASSERT(mRenderThread.getGrContext() != nullptr); sk_sp&lt;SkSurface&gt; surface(SkSurface::MakeFromBackendRenderTarget( mRenderThread.getGrContext(), backendRT, this-&gt;getSurfaceOrigin(), colorType, mSurfaceColorSpace, &amp;props)); SkiaPipeline::updateLighting(lightGeometry, lightInfo); renderFrame(*layerUpdateQueue, dirty, renderNodes, opaque, contentDrawBounds, surface, SkMatrix::I()); layerUpdateQueue-&gt;clear(); // Draw visual debugging features if (CC_UNLIKELY(Properties::showDirtyRegions || ProfileType::None != Properties::getProfileType())) &#123; SkCanvas* profileCanvas = surface-&gt;getCanvas(); SkiaProfileRenderer profileRenderer(profileCanvas); profiler-&gt;draw(profileRenderer); profileCanvas-&gt;flush(); &#125; // Log memory statistics if (CC_UNLIKELY(Properties::debugLevel != kDebugDisabled)) &#123; dumpResourceCacheUsage(); &#125; return true;&#125;]]></content>
      <categories>
        <category>刀枪棍棒（Android）</category>
        <category>五虎断门刀（Android原生）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[Android图形架构总览]]></title>
    <url>%2FAndroid%E5%9B%BE%E5%BD%A2%E6%9E%B6%E6%9E%84%E6%80%BB%E8%A7%88%2F</url>
    <content type="text"><![CDATA[图形渲染流程​ 在Android中，所有的渲染都是将内容绘制到对应的Surface上，Surface作为BufferQueue的生产方，每当发生入队操作时，都会通知BufferQueue创建者进行消费，也就是SurfaceFlinger，由SurfaceFlinger进行合并，然后发送到显示器进行显示。 下面这张图描述了关键组件的工作流程: 当前一个可见的界面被创建时，会通过WindowManager去创建一个Window对象，而每一个Window对象中都会包含一个Surface对象，该对象由WindowManager向SurfaceFlinger请求创建。Surface中包含一个BufferQueue，它由SurfaceFlinger向HAL层调用Gralloc对其进行内存分配。 应用获取到该Window对象后，通过Surface对象创建EGLSurface对象，然后在View的draw方法中使用Canvas进行绘制，此时使用的Canvas是一个OpenGL ES Canvas，也就是会通过OpenGL ES对View进行绘制，GLES渲染完成后，通过EGL的swapBuffers方法将绘制的缓冲区加入到EGLSurface连接的Surface所拥有的BufferQueue上。 SurfaceFlinger接收HAL层发送来的VSYNC事件进行刷新，SurfaceFlinger从每个Surface的BufferQueue中获取新的缓冲区，释放掉其之前的缓冲区，如果获取不到新的，则继续使用之前的缓冲区。 将所有要显示的Surface传入到HAL层进行查询，哪些需要进行合并，然后根据返回的标记，对需要进行合并的Surface进行合并（使用OpenGL ES将两个缓冲区合并到第三个缓冲区中），然后将处理后的所有Surface交给显示器，由显示器进行叠加层显示。参考下图 BufferQueue​ BufferQueue用来连接缓冲区生产方和缓冲区消费方。 ​ 生产方向BufferQueue请求一个可用的缓冲区（dequeueBuffer），然后向该缓冲区中写入数据后将其返回到队列中（queueBuffer），消费方从队列中获取该缓冲区并使用它（acquireBuffer），消费方使用完毕后将该缓冲区释放并重新返回到队列中（releaseBuffer）。 SurfaceFlinger​ SurfaceFlinger负责创建Surface，并消费其创建的Surface。应用想要显示一个界面，都必须通过WindowManager向SurfaceFlinger请求一个Surface，然后将显示的内容交给Surface传递给SurfaceFlinger。 ​ SurfaceFlinger仅仅在显示器刷新的时候才会进行处理，而不是应用更新了Surface中的缓冲队列就立马处理，这样可以减少内存的使用。显示器的刷新是通过VSYNC信号发送到SurfaceFlinger上的。 ​ 当SurfaceFlinger接收到VSYNC信号，SurfaceFlinger会遍历所有可见Surface，获取每个Surface最新的缓冲区，如果获取到了释放之前的缓冲区，如果没有获取到最新的缓冲区，则继续使用之前的缓冲区。如果该Surface自始至终都没有过缓冲区，则忽略该Surface。 ​ SurfaceFlinger收集了所有可见Surface的最新缓冲区后，会询问HWC层硬件将如果处理合成，然后将HWC层标记为SurfaceFlinger合成的全部合成为一个缓冲区，最后将剩下的所有缓冲区传递给HWC层进行显示。 WindowManager​ WindowManager控制Window对象，Window是View对象的容器。WindowManager监视生命周期，输入和焦点事件，屏幕方向，过渡，动画，位置，变换，z顺序以及窗口的许多其他方面。WindowManager将所有窗口元数据发送到SurfaceFlinger，以便SurfaceFlinger可以使用该数据进行合成、显示。 屏幕叠加层​ 上面说到过SurfaceFlinger在收集了所有可见Surface的最新缓冲区后，需要询问HWC层，那是因为显示器一般都有叠加层支持，也就是显示器并不是只能显示一个缓冲区，它可以同时显示多个缓冲区，可以从不同的缓冲区读取屏幕上不同位置的数据。这样做可以非常显著的提高效率，避免了SurfaceFlinger在不停的合并（SurfaceFlinger的合并是通过GPU来合成，这会抢占应用使用GPU渲染的时间），只有在Surface的个数大于叠加层的个数的时候才会由SurfaceFlinger来合并。 所以SurfaceFlinger维护的Surface的数量会对能耗和性能产生很大影响。 叠加层最少有4个： 状态栏 系统导航栏 应用 壁纸 Surface​ Surface是一个接口，维护了一个对BufferQueue的连接，用于将缓冲区从生产方流动都消费方。 向Surface中绘制有两种方式: GPU渲染一般使用OpenGL ES或者Vulkan渲染，View的draw方法使用的就是这种方式 CPU渲染使用lockCanvas或者new Canvas，这种方式都是使用CPU渲染 使用lockCanvas时需要注意，一旦使用CPU渲染的Canvas向Surface中进行了绘制，那么CPU渲染程序会连接到Surface中的BufferQueue上，直到Surface被销毁才会断开连接，因为连接无法断开。所以此后该Surface将无法接受GPU渲染程序的连接。（新版本中使用lockHardwareCanvas方法可以返回一个使用GPU进行渲染的Canvas） SurfaceView​ SurfaceView = Surface + 特殊的View ​ SurfaceView在View体系中只是一个透明的占位符，与View本身的渲染毫无关系，是独立的。SurfaceView在即将显示的时候会去SurfaceFlinger那请求一个单独的Surface，通过SurfaceHolder异步获取到Surface后，对该Surface进行渲染。SurfaceView的Surface在SurfaceFlinger中默认是放置在应用界面Surface的后面。 ​ 采用单独Surface对性能会有很大的提升，因为缓冲区将直接由SurfaceFlinger进行合并然后发送给显示器，不受应用本身绘制的影响，也不会导致应用重绘。但是缺点也很明显，SurfaceView并不是一个真正的View，对它进行大小位置的变动会非常麻烦，因为每次变动都需要通知SurfaceFlinger来改变Surface的大小位置。 ​ 在使用的过程中一般都会在异步线程中对SurfaceView进行渲染。 GLSurfaceView​ GLSurfaceView = SurfaceView + EGL ​ GLSurfaceView会创建一个单独的渲染线程（因为GLES的状态机是和线程绑定的），然后会初始化EGL上下文，封装Render作为GLES的生命回调。 SurfaceTexture​ SurfaceTexture = Consumer + GLES外部纹理 ​ SurfaceTexture拥有一个BufferQueue，可以用于缓冲区的流动。将SurfaceTexture发送给生产方（例如Camera），生产方会将缓冲区写入到队列中，通过SurfaceTexture的onFrameAvailable方法可以获得通知。 ​ SurfaceTexture的另一个特点是其在创建BufferQueue的时候，会添加GRALLOC_USAGE_HW_TEXTURE标记，这样gralloc创建的缓冲区就可以被GLES使用。所以SurfaceTexture可以绑定一个OES纹理，通过updateTexImage方法可以从BufferQueue中获取新的缓冲区给OES纹理使用。 ​ BufferQueue传递的不仅仅只是缓冲区数据，还有一些附加参数，比如缓冲区的时间戳、转换矩阵。获取附加参数一般在updateTexImage方法后，通过getTransformMatrix方法可以获取前面出队的缓冲区的转换矩阵，之所以传递转换矩阵，而不是将数据在传递前就转换，是因为在真正使用的过程中，数据可能会经过多次流动，会产生多次转换，传递转换矩阵可以使多次转换合并，降低开销；通过getTimestamp获取该缓冲区生产的时间戳，采用生产的时间戳而不是用消费的时间戳是因为有些场景（比如录像）对时间非常敏感。 SurfaceTexture可以认为是GLConsumer，是一个BufferQueue的所有方和消费方；通过SurfaceTexture创建Surface对象，实际上是创建了一个BufferQueue的生产方。 TextureViewTextureView = SurfaceTexture + View TextureView是一个真正意义上的View，受应用绘制体系控制，不是独立的。TextureView内部会创建一个SurfaceTexture，并将它交给应用的View体系，然后对SurfaceTexture设置onFrameAvailable监听，当BufferQueue中有变动时，先通过updateTexImage方法从SurfaceTexture中获取到最新的缓冲区，然后调用invalidate方法通知应用的View体系将缓冲区合并到应用本身的Surface上。 与SurfaceView相比，TextureView是一个真正的View，对它进行大小位置的变动会很简单高效。但是缺点也很明显，需要先将TextureView上的缓冲区合并到应用的缓冲区（会对TextureView上层的View造成重绘），然后再由SurfaceFlinger对所有Surface进行合并，这样TextureView的内容实际上是被合并了两次，相比SurfaceView，性能消耗比较大。]]></content>
      <categories>
        <category>刀枪棍棒（Android）</category>
        <category>五虎断门刀（Android原生）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[FFmpeg深入之avformat_open_input]]></title>
    <url>%2FFFmpeg%E6%B7%B1%E5%85%A5%E4%B9%8Bavformat-open-input%2F</url>
    <content type="text"><![CDATA[1234567891011121314# libavformat/avformat.h/** * 读取多媒体文件头信息 * 此时并未开始解码，开启的流通过avformat_close_input()关闭。 * * @param ps AVFormatContext (通过avformat_alloc_context创建)，可以传NULL。 * @param url 多媒体URL * @param fmt 指定解封装的格式，传NULL时将自动检测. * @param options 解封装参数自定义，可以传NULL * * @return 0 成功 */int avformat_open_input(AVFormatContext **ps, const char *url, AVInputFormat *fmt, AVDictionary **options); 方法调用流程图 avformat_open_# libavformat/utils.cint avformat_open_input(AVFormatContext **ps, const char *filename, AVInputFormat *fmt, AVDictionary **options)&#123; AVFormatContext *s = *ps; int i, ret = 0; AVDictionary *tmp = NULL; AVDictionary *tmp2 = NULL; ID3v2ExtraMeta *id3v2_extra_meta = NULL; // 传入的context为空则初始化 if (!s &amp;&amp; !(s = avformat_alloc_context())) return AVERROR(ENOMEM); if (!s-&gt;av_class) &#123; av_log(NULL, AV_LOG_ERROR, "Input context has not been properly allocated by avformat_alloc_context() and is not NULL either\n"); return AVERROR(EINVAL); &#125; if (fmt) s-&gt;iformat = fmt; // 指定解封装格式 if (options) // 自定义解封装参数 av_dict_copy(&amp;tmp, *options, 0); // 自定义IO读取类，通过提前创建Context，赋值其pb字段 if (s-&gt;pb) // must be before any goto fail s-&gt;flags |= AVFMT_FLAG_CUSTOM_IO; if ((ret = av_opt_set_dict(s, &amp;tmp)) &lt; 0) goto fail; av_strlcpy(s-&gt;filename, filename ? filename : "", sizeof(s-&gt;filename)); // 打开多媒体文件流，并进行解封装格式的探测 if ((ret = init_input(s, filename, &amp;tmp)) &lt; 0) goto fail; s-&gt;probe_score = ret; if (!s-&gt;protocol_whitelist &amp;&amp; s-&gt;pb &amp;&amp; s-&gt;pb-&gt;protocol_whitelist) &#123; s-&gt;protocol_whitelist = av_strdup(s-&gt;pb-&gt;protocol_whitelist); if (!s-&gt;protocol_whitelist) &#123; ret = AVERROR(ENOMEM); goto fail; &#125; &#125; if (!s-&gt;protocol_blacklist &amp;&amp; s-&gt;pb &amp;&amp; s-&gt;pb-&gt;protocol_blacklist) &#123; s-&gt;protocol_blacklist = av_strdup(s-&gt;pb-&gt;protocol_blacklist); if (!s-&gt;protocol_blacklist) &#123; ret = AVERROR(ENOMEM); goto fail; &#125; &#125; // URL协议判定 if (s-&gt;format_whitelist &amp;&amp; av_match_list(s-&gt;iformat-&gt;name, s-&gt;format_whitelist, ',') &lt;= 0) &#123; av_log(s, AV_LOG_ERROR, "Format not on whitelist \'%s\'\n", s-&gt;format_whitelist); ret = AVERROR(EINVAL); goto fail; &#125; avio_skip(s-&gt;pb, s-&gt;skip_initial_bytes); /* Check filename in case an image number is expected. */ if (s-&gt;iformat-&gt;flags &amp; AVFMT_NEEDNUMBER) &#123; if (!av_filename_number_test(filename)) &#123; ret = AVERROR(EINVAL); goto fail; &#125; &#125; s-&gt;duration = s-&gt;start_time = AV_NOPTS_VALUE; // 根据解封装器中定义的Context大小和AVClass对象来初始化 if (s-&gt;iformat-&gt;priv_data_size &gt; 0) &#123; if (!(s-&gt;priv_data = av_mallocz(s-&gt;iformat-&gt;priv_data_size))) &#123; ret = AVERROR(ENOMEM); goto fail; &#125; if (s-&gt;iformat-&gt;priv_class) &#123; *(const AVClass **) s-&gt;priv_data = s-&gt;iformat-&gt;priv_class; av_opt_set_defaults(s-&gt;priv_data); if ((ret = av_opt_set_dict(s-&gt;priv_data, &amp;tmp)) &lt; 0) goto fail; &#125; &#125; /* e.g. AVFMT_NOFILE formats will not have a AVIOContext */ if (s-&gt;pb) ff_id3v2_read_dict(s-&gt;pb, &amp;s-&gt;internal-&gt;id3v2_meta, ID3v2_DEFAULT_MAGIC, &amp;id3v2_extra_meta); if (!(s-&gt;flags&amp;AVFMT_FLAG_PRIV_OPT)) &#123; // 读取媒体文件的头信息，创建对应的音视频流 if (s-&gt;iformat-&gt;read_header2) &#123; if (options) av_dict_copy(&amp;tmp2, *options, 0); if ((ret = s-&gt;iformat-&gt;read_header2(s, &amp;tmp2)) &lt; 0) goto fail; &#125; else if (s-&gt;iformat-&gt;read_header &amp;&amp; (ret = s-&gt;iformat-&gt;read_header(s)) &lt; 0) goto fail; &#125; if (!s-&gt;metadata) &#123; s-&gt;metadata = s-&gt;internal-&gt;id3v2_meta; s-&gt;internal-&gt;id3v2_meta = NULL; &#125; else if (s-&gt;internal-&gt;id3v2_meta) &#123; int level = AV_LOG_WARNING; if (s-&gt;error_recognition &amp; AV_EF_COMPLIANT) level = AV_LOG_ERROR; av_log(s, level, "Discarding ID3 tags because more suitable tags were found.\n"); av_dict_free(&amp;s-&gt;internal-&gt;id3v2_meta); if (s-&gt;error_recognition &amp; AV_EF_EXPLODE) return AVERROR_INVALIDDATA; &#125; if (id3v2_extra_meta) &#123; if (!strcmp(s-&gt;iformat-&gt;name, "mp3") || !strcmp(s-&gt;iformat-&gt;name, "aac") || !strcmp(s-&gt;iformat-&gt;name, "tta")) &#123; if ((ret = ff_id3v2_parse_apic(s, &amp;id3v2_extra_meta)) &lt; 0) goto fail; if ((ret = ff_id3v2_parse_chapters(s, &amp;id3v2_extra_meta)) &lt; 0) goto fail; &#125; else av_log(s, AV_LOG_DEBUG, "demuxer does not support additional id3 data, skipping\n"); &#125; ff_id3v2_free_extra_meta(&amp;id3v2_extra_meta); if ((ret = avformat_queue_attached_pictures(s)) &lt; 0) goto fail; if (!(s-&gt;flags&amp;AVFMT_FLAG_PRIV_OPT) &amp;&amp; s-&gt;pb &amp;&amp; !s-&gt;internal-&gt;data_offset) s-&gt;internal-&gt;data_offset = avio_tell(s-&gt;pb); s-&gt;internal-&gt;raw_packet_buffer_remaining_size = RAW_PACKET_BUFFER_SIZE; update_stream_avctx(s); for (i = 0; i &lt; s-&gt;nb_streams; i++) s-&gt;streams[i]-&gt;internal-&gt;orig_codec_id = s-&gt;streams[i]-&gt;codecpar-&gt;codec_id; if (options) &#123; av_dict_free(options); *options = tmp; av_dict_free(&amp;tmp2); &#125; *ps = s; return 0;fail: ff_id3v2_free_extra_meta(&amp;id3v2_extra_meta); av_dict_free(&amp;tmp); av_dict_free(&amp;tmp2); if (s-&gt;pb &amp;&amp; !(s-&gt;flags &amp; AVFMT_FLAG_CUSTOM_IO)) avio_closep(&amp;s-&gt;pb); avformat_free_context(s); *ps = NULL; return ret;&#125; init_input通过将所有注册的解封装器对输入的媒体流进行一一解析，得出各自的分数，最终选取分数最高的解封装器作为该媒体的解封装器。 1234567891011121314151617181920212223242526272829303132333435# libavformat/utils.c/* 打开输入的多媒体，预测解封装格式 */static int init_input(AVFormatContext *s, const char *filename, AVDictionary **options)&#123; int ret; AVProbeData pd = &#123; filename, NULL, 0 &#125;; // 最低分数线 25分 int score = AVPROBE_SCORE_RETRY; // 如果自定义了IO读取 if (s-&gt;pb) &#123; s-&gt;flags |= AVFMT_FLAG_CUSTOM_IO; // 如果已经指定了解封装格式，则直接return 0，没有则调用av_probe_input_buffer2探测 if (!s-&gt;iformat) return av_probe_input_buffer2(s-&gt;pb, &amp;s-&gt;iformat, filename, s, 0, s-&gt;format_probesize); else if (s-&gt;iformat-&gt;flags &amp; AVFMT_NOFILE) av_log(s, AV_LOG_WARNING, "Custom AVIOContext makes no sense and " "will be ignored with AVFMT_NOFILE format.\n"); return 0; &#125; // 如果已经指定了解封装格式，则直接返回25分，没有则调用av_probe_input_format2探测 if ((s-&gt;iformat &amp;&amp; s-&gt;iformat-&gt;flags &amp; AVFMT_NOFILE) || (!s-&gt;iformat &amp;&amp; (s-&gt;iformat = av_probe_input_format2(&amp;pd, 0, &amp;score)))) return score; if ((ret = s-&gt;io_open(s, &amp;s-&gt;pb, filename, AVIO_FLAG_READ | s-&gt;avio_flags, options)) &lt; 0) return ret; if (s-&gt;iformat) return 0; return av_probe_input_buffer2(s-&gt;pb, &amp;s-&gt;iformat, filename, s, 0, s-&gt;format_probesize);&#125; av_probe_input_format212345678910111213# libavformat/format.cAVInputFormat *av_probe_input_format2(AVProbeData *pd, int is_opened, int *score_max)&#123; int score_ret; AVInputFormat *fmt = av_probe_input_format3(pd, is_opened, &amp;score_ret); // 将传进来的25分作为一个阈值，超过才可以返回 if (score_ret &gt; *score_max) &#123; *score_max = score_ret; return fmt; &#125; else return NULL;&#125; av_probe_input_format3以mp4文件为例，第一次进入时由于没有开启IO，所有此时read_probe得分应该为0，mime_type也为空得分也是0，mov解封装器定义了read_probe所以不会去对比扩展名，最终第一次执行整个函数返回0分。 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384858687# libavformat/format.cAVInputFormat *av_probe_input_format3(AVProbeData *pd, int is_opened, int *score_ret)&#123; AVProbeData lpd = *pd; AVInputFormat *fmt1 = NULL, *fmt; int score, score_max = 0; const static uint8_t zerobuffer[AVPROBE_PADDING_SIZE]; // 音频ID3处理，默认NO_ID3 enum nodat &#123; NO_ID3, ID3_ALMOST_GREATER_PROBE, ID3_GREATER_PROBE, ID3_GREATER_MAX_PROBE, &#125; nodat = NO_ID3; // 第一次进来为空，赋值一个空的32个字节的buffer if (!lpd.buf) lpd.buf = (unsigned char *) zerobuffer; // 判断是否是ID3v2 if (lpd.buf_size &gt; 10 &amp;&amp; ff_id3v2_match(lpd.buf, ID3v2_DEFAULT_MAGIC)) &#123; int id3len = ff_id3v2_tag_len(lpd.buf); if (lpd.buf_size &gt; id3len + 16) &#123; if (lpd.buf_size &lt; 2LL*id3len + 16) nodat = ID3_ALMOST_GREATER_PROBE; lpd.buf += id3len; lpd.buf_size -= id3len; &#125; else if (id3len &gt;= PROBE_BUF_MAX) &#123; nodat = ID3_GREATER_MAX_PROBE; &#125; else nodat = ID3_GREATER_PROBE; &#125; fmt = NULL; // 循环遍历所有的解封装器 while ((fmt1 = av_iformat_next(fmt1))) &#123; // 如果流没有打开，则只有没有AVFMT_NOFILE标记的解封装器才可以尝试（image2解封装器例外） // 如果流已经打开，则只有有AVFMT_NOFILE标记的解封装器才可以尝试（image2解封装器例外） if (!is_opened == !(fmt1-&gt;flags &amp; AVFMT_NOFILE) &amp;&amp; strcmp(fmt1-&gt;name, "image2")) continue; score = 0; if (fmt1-&gt;read_probe) &#123; // 调用解封装器的read_probe方法进行探测 score = fmt1-&gt;read_probe(&amp;lpd); if (score) av_log(NULL, AV_LOG_TRACE, "Probing %s score:%d size:%d\n", fmt1-&gt;name, score, lpd.buf_size); // 比较当前解封装器注册的扩展后缀中是否包含媒体URL的后缀名 if (fmt1-&gt;extensions &amp;&amp; av_match_ext(lpd.filename, fmt1-&gt;extensions)) &#123; switch (nodat) &#123; case NO_ID3: score = FFMAX(score, 1); break; case ID3_GREATER_PROBE: case ID3_ALMOST_GREATER_PROBE: score = FFMAX(score, AVPROBE_SCORE_EXTENSION / 2 - 1); break; case ID3_GREATER_MAX_PROBE: score = FFMAX(score, AVPROBE_SCORE_EXTENSION); break; &#125; &#125; &#125; else if (fmt1-&gt;extensions) &#123; // 如果当前解封装器没有提供read_probe方法，则比对后缀名 if (av_match_ext(lpd.filename, fmt1-&gt;extensions)) score = AVPROBE_SCORE_EXTENSION; // 后缀名匹配得50分 &#125; // 比较当前解封装器注册的mime_type中是否包含当前媒体文件的mime_type if (av_match_name(lpd.mime_type, fmt1-&gt;mime_type)) &#123; if (AVPROBE_SCORE_MIME &gt; score) &#123; av_log(NULL, AV_LOG_DEBUG, "Probing %s score:%d increased to %d due to MIME type\n", fmt1-&gt;name, score, AVPROBE_SCORE_MIME); score = AVPROBE_SCORE_MIME; // mime_type相同得75分 &#125; &#125; // 记录最大分数的解封装器 if (score &gt; score_max) &#123; score_max = score; fmt = fmt1; &#125; else if (score == score_max) fmt = NULL; &#125; if (nodat == ID3_GREATER_PROBE) score_max = FFMIN(AVPROBE_SCORE_EXTENSION / 2 - 1, score_max); // 返回最大分数 *score_ret = score_max; // 返回探测的解封装器 return fmt;&#125; read_probe以mp4为例，则会进入mov的解封装器中处理 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384858687# libavformat/mov.cstatic int mov_probe(AVProbeData *p)&#123; int64_t offset; uint32_t tag; int score = 0; int moov_offset = -1; /* check file header */ offset = 0; for (;;) &#123; /* 如果buffer的大小不足，则退出*/ if ((offset + 8) &gt; (unsigned int)p-&gt;buf_size) break; // 读取当前offset后第二个int，获取当前box的name tag = AV_RL32(p-&gt;buf + offset + 4); switch(tag) &#123; /* check for obvious tags */ case MKTAG('m','o','o','v'): // 记录moovbox name的起始位置 moov_offset = offset + 4; case MKTAG('m','d','a','t'): case MKTAG('p','n','o','t'): /* detect movs with preview pics like ew.mov and april.mov */ case MKTAG('u','d','t','a'): /* Packet Video PVAuthor adds this and a lot of more junk */ case MKTAG('f','t','y','p'): // 如果当前box的size小于8个字节且不等于1个字节 // 又或者解析的最后一个box长度小于12字节 // 又或者解析的最后一个box前8个字节都是0 // 则返回当前分数和50分中最大的分数。 if (AV_RB32(p-&gt;buf+offset) &lt; 8 &amp;&amp; (AV_RB32(p-&gt;buf+offset) != 1 || offset + 12 &gt; (unsigned int)p-&gt;buf_size || AV_RB64(p-&gt;buf+offset + 8) == 0)) &#123; score = FFMAX(score, AVPROBE_SCORE_EXTENSION); &#125; else if (tag == MKTAG('f','t','y','p') &amp;&amp; ( AV_RL32(p-&gt;buf + offset + 8) == MKTAG('j','p','2',' ') || AV_RL32(p-&gt;buf + offset + 8) == MKTAG('j','p','x',' ') )) &#123; // 如果major_brand是jp2、jpx(JPEG 2000),则直接返回5分 score = FFMAX(score, 5); &#125; else &#123; score = AVPROBE_SCORE_MAX; // 否则给100分 &#125; // 将offset定位到下一个box的起始位置 offset = FFMAX(4, AV_RB32(p-&gt;buf+offset)) + offset; break; /* those are more common words, so rate then a bit less */ case MKTAG('e','d','i','w'): /* xdcam files have reverted first tags */ case MKTAG('w','i','d','e'): case MKTAG('f','r','e','e'): case MKTAG('j','u','n','k'): case MKTAG('p','i','c','t'): score = FFMAX(score, AVPROBE_SCORE_MAX - 5); offset = FFMAX(4, AV_RB32(p-&gt;buf+offset)) + offset; break; case MKTAG(0x82,0x82,0x7f,0x7d): case MKTAG('s','k','i','p'): case MKTAG('u','u','i','d'): case MKTAG('p','r','f','l'): /* if we only find those cause probedata is too small at least rate them */ score = FFMAX(score, AVPROBE_SCORE_EXTENSION); offset = FFMAX(4, AV_RB32(p-&gt;buf+offset)) + offset; break; default: offset = FFMAX(4, AV_RB32(p-&gt;buf+offset)) + offset; &#125; &#125; if(score &gt; AVPROBE_SCORE_MAX - 50 &amp;&amp; moov_offset != -1) &#123; // 检测是否是MPEG-PS封装格式 offset = moov_offset; while(offset &lt; (p-&gt;buf_size - 16))&#123; // 读取moovbox，检测到MPEG-PS格式则返回5分 if(AV_RL32(p-&gt;buf + offset ) == MKTAG('h','d','l','r') &amp;&amp; AV_RL32(p-&gt;buf + offset + 8) == MKTAG('m','h','l','r') &amp;&amp; AV_RL32(p-&gt;buf + offset + 12) == MKTAG('M','P','E','G'))&#123; av_log(NULL, AV_LOG_WARNING, "Found media data tag MPEG indicating this is a MOV-packed MPEG-PS.\n"); return 5; &#125;else // 每次移动2个字节，不断循环检测 offset+=2; &#125; &#125; return score;&#125; io_open123456789101112131415161718192021222324static int io_open_default(AVFormatContext *s, AVIOContext **pb, const char *url, int flags, AVDictionary **options)&#123; int loglevel; if (!strcmp(url, s-&gt;filename) || s-&gt;iformat &amp;&amp; !strcmp(s-&gt;iformat-&gt;name, "image2") || s-&gt;oformat &amp;&amp; !strcmp(s-&gt;oformat-&gt;name, "image2") ) &#123; loglevel = AV_LOG_DEBUG; &#125; else loglevel = AV_LOG_INFO; av_log(s, loglevel, "Opening \'%s\' for %s\n", url, flags &amp; AVIO_FLAG_WRITE ? "writing" : "reading");#if FF_API_OLD_OPEN_CALLBACKSFF_DISABLE_DEPRECATION_WARNINGS if (s-&gt;open_cb) return s-&gt;open_cb(s, pb, url, flags, &amp;s-&gt;interrupt_callback, options);FF_ENABLE_DEPRECATION_WARNINGS#endif return ffio_open_whitelist(pb, url, flags, &amp;s-&gt;interrupt_callback, options, s-&gt;protocol_whitelist, s-&gt;protocol_blacklist);&#125; ffurl_open_whitelist123456789101112131415161718192021222324252627282930313233343536373839404142434445464748int ffurl_open_whitelist(URLContext **puc, const char *filename, int flags, const AVIOInterruptCB *int_cb, AVDictionary **options, const char *whitelist, const char* blacklist, URLContext *parent)&#123; AVDictionary *tmp_opts = NULL; AVDictionaryEntry *e; // 解析URL，找到匹配的协议解析类，保存在puc-&gt;prot中 int ret = ffurl_alloc(puc, filename, flags, int_cb); if (ret &lt; 0) return ret; if (parent) av_opt_copy(*puc, parent); if (options &amp;&amp; (ret = av_opt_set_dict(*puc, options)) &lt; 0) goto fail; if (options &amp;&amp; (*puc)-&gt;prot-&gt;priv_data_class &amp;&amp; (ret = av_opt_set_dict((*puc)-&gt;priv_data, options)) &lt; 0) goto fail; if (!options) options = &amp;tmp_opts; av_assert0(!whitelist || !(e=av_dict_get(*options, "protocol_whitelist", NULL, 0)) || !strcmp(whitelist, e-&gt;value)); av_assert0(!blacklist || !(e=av_dict_get(*options, "protocol_blacklist", NULL, 0)) || !strcmp(blacklist, e-&gt;value)); if ((ret = av_dict_set(options, "protocol_whitelist", whitelist, 0)) &lt; 0) goto fail; if ((ret = av_dict_set(options, "protocol_blacklist", blacklist, 0)) &lt; 0) goto fail; if ((ret = av_opt_set_dict(*puc, options)) &lt; 0) goto fail; // 打开协议链接 ret = ffurl_connect(*puc, options); if (!ret) return 0;fail: ffurl_close(*puc); *puc = NULL; return ret;&#125; ffurl_connect1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859int ffurl_connect(URLContext *uc, AVDictionary **options)&#123; int err; AVDictionary *tmp_opts = NULL; AVDictionaryEntry *e; if (!options) options = &amp;tmp_opts; // Check that URLContext was initialized correctly and lists are matching if set av_assert0(!(e=av_dict_get(*options, "protocol_whitelist", NULL, 0)) || (uc-&gt;protocol_whitelist &amp;&amp; !strcmp(uc-&gt;protocol_whitelist, e-&gt;value))); av_assert0(!(e=av_dict_get(*options, "protocol_blacklist", NULL, 0)) || (uc-&gt;protocol_blacklist &amp;&amp; !strcmp(uc-&gt;protocol_blacklist, e-&gt;value))); // 协议检查 if (uc-&gt;protocol_whitelist &amp;&amp; av_match_list(uc-&gt;prot-&gt;name, uc-&gt;protocol_whitelist, ',') &lt;= 0) &#123; av_log(uc, AV_LOG_ERROR, "Protocol '%s' not on whitelist '%s'!\n", uc-&gt;prot-&gt;name, uc-&gt;protocol_whitelist); return AVERROR(EINVAL); &#125; if (uc-&gt;protocol_blacklist &amp;&amp; av_match_list(uc-&gt;prot-&gt;name, uc-&gt;protocol_blacklist, ',') &gt; 0) &#123; av_log(uc, AV_LOG_ERROR, "Protocol '%s' on blacklist '%s'!\n", uc-&gt;prot-&gt;name, uc-&gt;protocol_blacklist); return AVERROR(EINVAL); &#125; if (!uc-&gt;protocol_whitelist &amp;&amp; uc-&gt;prot-&gt;default_whitelist) &#123; av_log(uc, AV_LOG_DEBUG, "Setting default whitelist '%s'\n", uc-&gt;prot-&gt;default_whitelist); uc-&gt;protocol_whitelist = av_strdup(uc-&gt;prot-&gt;default_whitelist); if (!uc-&gt;protocol_whitelist) &#123; return AVERROR(ENOMEM); &#125; &#125; else if (!uc-&gt;protocol_whitelist) av_log(uc, AV_LOG_DEBUG, "No default whitelist set\n"); // This should be an error once all declare a default whitelist if ((err = av_dict_set(options, "protocol_whitelist", uc-&gt;protocol_whitelist, 0)) &lt; 0) return err; if ((err = av_dict_set(options, "protocol_blacklist", uc-&gt;protocol_blacklist, 0)) &lt; 0) return err; // 打开链接,比如HTTP协议的URL，调用的就是HTTP解析类中的url_open方法 err = uc-&gt;prot-&gt;url_open2 ? uc-&gt;prot-&gt;url_open2(uc, uc-&gt;filename, uc-&gt;flags, options) : uc-&gt;prot-&gt;url_open(uc, uc-&gt;filename, uc-&gt;flags); av_dict_set(options, "protocol_whitelist", NULL, 0); av_dict_set(options, "protocol_blacklist", NULL, 0); if (err) return err; uc-&gt;is_connected = 1; /* We must be careful here as ffurl_seek() could be slow, * for example for http */ if ((uc-&gt;flags &amp; AVIO_FLAG_WRITE) || !strcmp(uc-&gt;prot-&gt;name, "file")) if (!uc-&gt;is_streamed &amp;&amp; ffurl_seek(uc, 0, SEEK_SET) &lt; 0) uc-&gt;is_streamed = 1; return 0;&#125; http_open123456789101112131415161718192021222324252627282930313233343536373839404142434445static int http_open(URLContext *h, const char *uri, int flags, AVDictionary **options)&#123; HTTPContext *s = h-&gt;priv_data; int ret; s-&gt;app_ctx = (AVApplicationContext *)(intptr_t)s-&gt;app_ctx_intptr; if( s-&gt;seekable == 1 ) h-&gt;is_streamed = 0; else h-&gt;is_streamed = 1; s-&gt;filesize = UINT64_MAX; s-&gt;location = av_strdup(uri); if (!s-&gt;location) return AVERROR(ENOMEM); if (options) av_dict_copy(&amp;s-&gt;chained_options, *options, 0); if (s-&gt;headers) &#123; int len = strlen(s-&gt;headers); if (len &lt; 2 || strcmp("\r\n", s-&gt;headers + len - 2)) &#123; av_log(h, AV_LOG_WARNING, "No trailing CRLF found in HTTP header.\n"); ret = av_reallocp(&amp;s-&gt;headers, len + 3); if (ret &lt; 0) return ret; s-&gt;headers[len] = '\r'; s-&gt;headers[len + 1] = '\n'; s-&gt;headers[len + 2] = '\0'; &#125; &#125; if (s-&gt;listen) &#123; return http_listen(h, uri, flags, options); &#125; av_application_will_http_open(s-&gt;app_ctx, (void*)h, uri); // 发送HTTP请求，读取response ret = http_open_cnx(h, options); av_application_did_http_open(s-&gt;app_ctx, (void*)h, uri, ret, s-&gt;http_code, s-&gt;filesize); if (ret &lt; 0) av_dict_free(&amp;s-&gt;chained_options); return ret;&#125; av_probe_input_buffer2123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384858687888990919293949596979899100101102103# libavformat/format.cint av_probe_input_buffer2(AVIOContext *pb, AVInputFormat **fmt, const char *filename, void *logctx, unsigned int offset, unsigned int max_probe_size)&#123; AVProbeData pd = &#123; filename ? filename : "" &#125;; uint8_t *buf = NULL; int ret = 0, probe_size, buf_offset = 0; int score = 0; int ret2; // 默认最大探测的buffer大小为1 &lt;&lt; 20，大概1048576字节，1M左右 // 外部可通过format_probesize字典参数来改变 if (!max_probe_size) max_probe_size = PROBE_BUF_MAX; else if (max_probe_size &lt; PROBE_BUF_MIN) &#123; // 自定义的最大探测大小不能小于最小的2048个字节 av_log(logctx, AV_LOG_ERROR, "Specified probe size value %u cannot be &lt; %u\n", max_probe_size, PROBE_BUF_MIN); return AVERROR(EINVAL); &#125; if (offset &gt;= max_probe_size) return AVERROR(EINVAL); if (pb-&gt;av_class) &#123; uint8_t *mime_type_opt = NULL; char *semi; av_opt_get(pb, "mime_type", AV_OPT_SEARCH_CHILDREN, &amp;mime_type_opt); pd.mime_type = (const char *)mime_type_opt; semi = pd.mime_type ? strchr(pd.mime_type, ';') : NULL; if (semi) &#123; *semi = '\0'; &#125; &#125;#if 0 if (!*fmt &amp;&amp; pb-&gt;av_class &amp;&amp; av_opt_get(pb, "mime_type", AV_OPT_SEARCH_CHILDREN, &amp;mime_type) &gt;= 0 &amp;&amp; mime_type) &#123; if (!av_strcasecmp(mime_type, "audio/aacp")) &#123; *fmt = av_find_input_format("aac"); &#125; av_freep(&amp;mime_type); &#125;#endif // 从最小探测大小开始，一直到最大探测大小为止，每次探测的大小都是上一次的两倍，直到探测成功。 for (probe_size = PROBE_BUF_MIN; probe_size &lt;= max_probe_size &amp;&amp; !*fmt; // 探测大小x2 probe_size = FFMIN(probe_size &lt;&lt; 1, FFMAX(max_probe_size, probe_size + 1))) &#123; // 如果探测已经到了最大值，则清空之前探测的分数 score = probe_size &lt; max_probe_size ? AVPROBE_SCORE_RETRY : 0; // 从IO中读取probe_size个字节的数据，用于封装格式探测 if ((ret = av_reallocp(&amp;buf, probe_size + AVPROBE_PADDING_SIZE)) &lt; 0) goto fail; if ((ret = avio_read(pb, buf + buf_offset, probe_size - buf_offset)) &lt; 0) &#123; /* Fail if error was not end of file, otherwise, lower score. */ if (ret != AVERROR_EOF) goto fail; score = 0; ret = 0; /* error was end of file, nothing read */ &#125; buf_offset += ret; if (buf_offset &lt; offset) continue; pd.buf_size = buf_offset - offset; pd.buf = &amp;buf[offset]; memset(pd.buf + pd.buf_size, 0, AVPROBE_PADDING_SIZE); // 再次探测封装格式，此时IO已经打开。 *fmt = av_probe_input_format2(&amp;pd, 1, &amp;score); if (*fmt) &#123; /* This can only be true in the last iteration. */ if (score &lt;= AVPROBE_SCORE_RETRY) &#123; av_log(logctx, AV_LOG_WARNING, "Format %s detected only with low score of %d, " "misdetection possible!\n", (*fmt)-&gt;name, score); &#125; else av_log(logctx, AV_LOG_DEBUG, "Format %s probed with size=%d and score=%d\n", (*fmt)-&gt;name, probe_size, score);#if 0 FILE *f = fopen("probestat.tmp", "ab"); fprintf(f, "probe_size:%d format:%s score:%d filename:%s\n", probe_size, (*fmt)-&gt;name, score, filename); fclose(f);#endif &#125; &#125; if (!*fmt) ret = AVERROR_INVALIDDATA;fail: // 将读取过的字节回退 ret2 = ffio_rewind_with_probe_data(pb, &amp;buf, buf_offset); if (ret &gt;= 0) ret = ret2; av_freep(&amp;pd.mime_type); return ret &lt; 0 ? ret : score;&#125; read_header获取到解封装器后，调用解封装器的read_header方法，读取媒体的文件头，进行相关初始化工作。对于mp4来为，其解封装器是mov。 # libavformat/mov.cstatic int mov_read_header(AVFormatContext *s)&#123; MOVContext *mov = s-&gt;priv_data; AVIOContext *pb = s-&gt;pb; int j, err; MOVAtom atom = &#123; AV_RL32("root") &#125;; int i; if (mov-&gt;decryption_key_len != 0 &amp;&amp; mov-&gt;decryption_key_len != AES_CTR_KEY_SIZE) &#123; av_log(s, AV_LOG_ERROR, "Invalid decryption key len %d expected %d\n", mov-&gt;decryption_key_len, AES_CTR_KEY_SIZE); return AVERROR(EINVAL); &#125; mov-&gt;fc = s; mov-&gt;trak_index = -1; /* .mov and .mp4 aren't streamable anyway (only progressive download if moov is before mdat) */ if (pb-&gt;seekable &amp; AVIO_SEEKABLE_NORMAL) atom.size = avio_size(pb); else atom.size = INT64_MAX; /* check MOV header */ do &#123; if (mov-&gt;moov_retry) avio_seek(pb, 0, SEEK_SET); if ((err = mov_read_default(mov, pb, atom)) &lt; 0) &#123; av_log(s, AV_LOG_ERROR, "error reading header\n"); mov_read_close(s); return err; &#125; &#125; while ((pb-&gt;seekable &amp; AVIO_SEEKABLE_NORMAL) &amp;&amp; !mov-&gt;found_moov &amp;&amp; !mov-&gt;moov_retry++); // 如果没有找到moov box if (!mov-&gt;found_moov) &#123; av_log(s, AV_LOG_ERROR, "moov atom not found\n"); mov_read_close(s); return AVERROR_INVALIDDATA; &#125; av_log(mov-&gt;fc, AV_LOG_TRACE, "on_parse_exit_offset=%"PRId64"\n", avio_tell(pb)); if (pb-&gt;seekable &amp; AVIO_SEEKABLE_NORMAL) &#123; if (mov-&gt;nb_chapter_tracks &gt; 0 &amp;&amp; !mov-&gt;ignore_chapters) mov_read_chapters(s); for (i = 0; i &lt; s-&gt;nb_streams; i++) if (s-&gt;streams[i]-&gt;codecpar-&gt;codec_tag == AV_RL32("tmcd")) &#123; mov_read_timecode_track(s, s-&gt;streams[i]); &#125; else if (s-&gt;streams[i]-&gt;codecpar-&gt;codec_tag == AV_RL32("rtmd")) &#123; mov_read_rtmd_track(s, s-&gt;streams[i]); &#125; &#125; /* copy timecode metadata from tmcd tracks to the related video streams */ for (i = 0; i &lt; s-&gt;nb_streams; i++) &#123; AVStream *st = s-&gt;streams[i]; MOVStreamContext *sc = st-&gt;priv_data; if (sc-&gt;timecode_track &gt; 0) &#123; AVDictionaryEntry *tcr; int tmcd_st_id = -1; for (j = 0; j &lt; s-&gt;nb_streams; j++) if (s-&gt;streams[j]-&gt;id == sc-&gt;timecode_track) tmcd_st_id = j; if (tmcd_st_id &lt; 0 || tmcd_st_id == i) continue; tcr = av_dict_get(s-&gt;streams[tmcd_st_id]-&gt;metadata, "timecode", NULL, 0); if (tcr) av_dict_set(&amp;st-&gt;metadata, "timecode", tcr-&gt;value, 0); &#125; &#125; export_orphan_timecode(s); for (i = 0; i &lt; s-&gt;nb_streams; i++) &#123; AVStream *st = s-&gt;streams[i]; MOVStreamContext *sc = st-&gt;priv_data; fix_timescale(mov, sc); if(st-&gt;codecpar-&gt;codec_type == AVMEDIA_TYPE_AUDIO &amp;&amp; st-&gt;codecpar-&gt;codec_id == AV_CODEC_ID_AAC) &#123; st-&gt;skip_samples = sc-&gt;start_pad; &#125; if (st-&gt;codecpar-&gt;codec_type == AVMEDIA_TYPE_VIDEO &amp;&amp; sc-&gt;nb_frames_for_fps &gt; 0 &amp;&amp; sc-&gt;duration_for_fps &gt; 0) av_reduce(&amp;st-&gt;avg_frame_rate.num, &amp;st-&gt;avg_frame_rate.den, sc-&gt;time_scale*(int64_t)sc-&gt;nb_frames_for_fps, sc-&gt;duration_for_fps, INT_MAX); if (st-&gt;codecpar-&gt;codec_type == AVMEDIA_TYPE_SUBTITLE) &#123; if (st-&gt;codecpar-&gt;width &lt;= 0 || st-&gt;codecpar-&gt;height &lt;= 0) &#123; st-&gt;codecpar-&gt;width = sc-&gt;width; st-&gt;codecpar-&gt;height = sc-&gt;height; &#125; if (st-&gt;codecpar-&gt;codec_id == AV_CODEC_ID_DVD_SUBTITLE) &#123; if ((err = mov_rewrite_dvd_sub_extradata(st)) &lt; 0) return err; &#125; &#125; if (mov-&gt;handbrake_version &amp;&amp; mov-&gt;handbrake_version &lt;= 1000000*0 + 1000*10 + 2 &amp;&amp; // 0.10.2 st-&gt;codecpar-&gt;codec_id == AV_CODEC_ID_MP3 ) &#123; av_log(s, AV_LOG_VERBOSE, "Forcing full parsing for mp3 stream\n"); st-&gt;need_parsing = AVSTREAM_PARSE_FULL; &#125; &#125; if (mov-&gt;trex_data) &#123; for (i = 0; i &lt; s-&gt;nb_streams; i++) &#123; AVStream *st = s-&gt;streams[i]; MOVStreamContext *sc = st-&gt;priv_data; if (st-&gt;duration &gt; 0) &#123; if (sc-&gt;data_size &gt; INT64_MAX / sc-&gt;time_scale / 8) &#123; av_log(s, AV_LOG_ERROR, "Overflow during bit rate calculation %"PRId64" * 8 * %d\n", sc-&gt;data_size, sc-&gt;time_scale); mov_read_close(s); return AVERROR_INVALIDDATA; &#125; st-&gt;codecpar-&gt;bit_rate = sc-&gt;data_size * 8 * sc-&gt;time_scale / st-&gt;duration; &#125; &#125; &#125; if (mov-&gt;use_mfra_for &gt; 0) &#123; for (i = 0; i &lt; s-&gt;nb_streams; i++) &#123; AVStream *st = s-&gt;streams[i]; MOVStreamContext *sc = st-&gt;priv_data; if (sc-&gt;duration_for_fps &gt; 0) &#123; if (sc-&gt;data_size &gt; INT64_MAX / sc-&gt;time_scale / 8) &#123; av_log(s, AV_LOG_ERROR, "Overflow during bit rate calculation %"PRId64" * 8 * %d\n", sc-&gt;data_size, sc-&gt;time_scale); mov_read_close(s); return AVERROR_INVALIDDATA; &#125; st-&gt;codecpar-&gt;bit_rate = sc-&gt;data_size * 8 * sc-&gt;time_scale / sc-&gt;duration_for_fps; &#125; &#125; &#125; for (i = 0; i &lt; mov-&gt;bitrates_count &amp;&amp; i &lt; s-&gt;nb_streams; i++) &#123; if (mov-&gt;bitrates[i]) &#123; s-&gt;streams[i]-&gt;codecpar-&gt;bit_rate = mov-&gt;bitrates[i]; &#125; &#125; ff_rfps_calculate(s); for (i = 0; i &lt; s-&gt;nb_streams; i++) &#123; AVStream *st = s-&gt;streams[i]; MOVStreamContext *sc = st-&gt;priv_data; switch (st-&gt;codecpar-&gt;codec_type) &#123; case AVMEDIA_TYPE_AUDIO: err = ff_replaygain_export(st, s-&gt;metadata); if (err &lt; 0) &#123; mov_read_close(s); return err; &#125; break; case AVMEDIA_TYPE_VIDEO: if (sc-&gt;display_matrix) &#123; err = av_stream_add_side_data(st, AV_PKT_DATA_DISPLAYMATRIX, (uint8_t*)sc-&gt;display_matrix, sizeof(int32_t) * 9); if (err &lt; 0) return err; sc-&gt;display_matrix = NULL; &#125; if (sc-&gt;stereo3d) &#123; err = av_stream_add_side_data(st, AV_PKT_DATA_STEREO3D, (uint8_t *)sc-&gt;stereo3d, sizeof(*sc-&gt;stereo3d)); if (err &lt; 0) return err; sc-&gt;stereo3d = NULL; &#125; if (sc-&gt;spherical) &#123; err = av_stream_add_side_data(st, AV_PKT_DATA_SPHERICAL, (uint8_t *)sc-&gt;spherical, sc-&gt;spherical_size); if (err &lt; 0) return err; sc-&gt;spherical = NULL; &#125; if (sc-&gt;mastering) &#123; err = av_stream_add_side_data(st, AV_PKT_DATA_MASTERING_DISPLAY_METADATA, (uint8_t *)sc-&gt;mastering, sizeof(*sc-&gt;mastering)); if (err &lt; 0) return err; sc-&gt;mastering = NULL; &#125; if (sc-&gt;coll) &#123; err = av_stream_add_side_data(st, AV_PKT_DATA_CONTENT_LIGHT_LEVEL, (uint8_t *)sc-&gt;coll, sc-&gt;coll_size); if (err &lt; 0) return err; sc-&gt;coll = NULL; &#125; break; &#125; &#125; ff_configure_buffers_for_index(s, AV_TIME_BASE); for (i = 0; i &lt; mov-&gt;fragment_index_count; i++) &#123; MOVFragmentIndex *idx = mov-&gt;fragment_index_data[i]; for (j = 0; j &lt; idx-&gt;item_count; j++) if (idx-&gt;items[j].moof_offset &lt;= mov-&gt;fragment.moof_offset) idx-&gt;items[j].headers_read = 1; &#125; return 0;&#125; mov_read_ int mov_read_default(MOVContext *c, AVIOContext *pb, MOVAtom atom)&#123; int64_t total_size = 0; MOVAtom a; int i; if (c-&gt;atom_depth &gt; 10) &#123; av_log(c-&gt;fc, AV_LOG_ERROR, "Atoms too deeply nested\n"); return AVERROR_INVALIDDATA; &#125; c-&gt;atom_depth ++; if (atom.size &lt; 0) atom.size = INT64_MAX; // 流没有结束则不断循环 while (total_size &lt;= atom.size - 8 &amp;&amp; !avio_feof(pb)) &#123; int (*parse)(MOVContext*, AVIOContext*, MOVAtom) = NULL; a.size = atom.size; a.type=0; if (atom.size &gt;= 8) &#123; // 读取当前box的1-4字节，获取box的长度 a.size = avio_rb32(pb); // 读取当前box的4-8字节，获取box的类型 a.type = avio_rl32(pb); if (a.type == MKTAG('f','r','e','e') &amp;&amp; a.size &gt;= 8 &amp;&amp; c-&gt;fc-&gt;strict_std_compliance &lt; FF_COMPLIANCE_STRICT &amp;&amp; c-&gt;moov_retry) &#123; uint8_t buf[8]; uint32_t *type = (uint32_t *)buf + 1; if (avio_read(pb, buf, 8) != 8) return AVERROR_INVALIDDATA; avio_seek(pb, -8, SEEK_CUR); if (*type == MKTAG('m','v','h','d') || *type == MKTAG('c','m','o','v')) &#123; av_log(c-&gt;fc, AV_LOG_ERROR, "Detected moov in a free atom.\n"); a.type = MKTAG('m','o','o','v'); &#125; &#125; if (atom.type != MKTAG('r','o','o','t') &amp;&amp; atom.type != MKTAG('m','o','o','v')) &#123; if (a.type == MKTAG('t','r','a','k') || a.type == MKTAG('m','d','a','t')) &#123; av_log(c-&gt;fc, AV_LOG_ERROR, "Broken file, trak/mdat not at top-level\n"); avio_skip(pb, -8); c-&gt;atom_depth --; return 0; &#125; &#125; total_size += 8; // 对于box比较长的其长度是用8个字节来存储的 if (a.size == 1 &amp;&amp; total_size + 8 &lt;= atom.size) &#123; /* 64 bit extended size */ a.size = avio_rb64(pb) - 8; total_size += 8; &#125; &#125; av_log(c-&gt;fc, AV_LOG_TRACE, "type:'%s' parent:'%s' sz: %"PRId64" %"PRId64" %"PRId64"\n", av_fourcc2str(a.type), av_fourcc2str(atom.type), a.size, total_size, atom.size); if (a.size == 0) &#123; a.size = atom.size - total_size + 8; &#125; a.size -= 8; if (a.size &lt; 0) break; a.size = FFMIN(a.size, atom.size - total_size); // 根据box的类型，找到对应的处理函数 for (i = 0; mov_default_parse_table[i].type; i++) if (mov_default_parse_table[i].type == a.type) &#123; parse = mov_default_parse_table[i].parse; break; &#125; // container is user data if (!parse &amp;&amp; (atom.type == MKTAG('u','d','t','a') || atom.type == MKTAG('i','l','s','t'))) parse = mov_read_udta_string; // Supports parsing the QuickTime Metadata Keys. // https://developer.apple.com/library/mac/documentation/QuickTime/QTFF/Metadata/Metadata.html if (!parse &amp;&amp; c-&gt;found_hdlr_mdta &amp;&amp; atom.type == MKTAG('m','e','t','a') &amp;&amp; a.type == MKTAG('k','e','y','s')) &#123; parse = mov_read_keys; &#125; if (!parse) &#123; /* skip leaf atoms data */ avio_skip(pb, a.size); &#125; else &#123; int64_t start_pos = avio_tell(pb); int64_t left; // 处理该box int err = parse(c, pb, a); if (err &lt; 0) &#123; c-&gt;atom_depth --; return err; &#125; if (c-&gt;found_moov &amp;&amp; c-&gt;found_mdat &amp;&amp; ((!(pb-&gt;seekable &amp; AVIO_SEEKABLE_NORMAL) || c-&gt;fc-&gt;flags &amp; AVFMT_FLAG_IGNIDX || c-&gt;fragment_index_complete) || start_pos + a.size == avio_size(pb))) &#123; if (!(pb-&gt;seekable &amp; AVIO_SEEKABLE_NORMAL) || c-&gt;fc-&gt;flags &amp; AVFMT_FLAG_IGNIDX || c-&gt;fragment_index_complete) c-&gt;next_root_atom = start_pos + a.size; c-&gt;atom_depth --; return 0; &#125; left = a.size - avio_tell(pb) + start_pos; if (left &gt; 0) /* skip garbage at atom end */ avio_skip(pb, left); else if (left &lt; 0) &#123; av_log(c-&gt;fc, AV_LOG_WARNING, "overread end of atom '%.4s' by %"PRId64" bytes\n", (char*)&amp;a.type, -left); avio_seek(pb, left, SEEK_CUR); &#125; &#125; total_size += a.size; &#125; if (total_size &lt; atom.size &amp;&amp; atom.size &lt; 0x7ffff) avio_skip(pb, atom.size - total_size); c-&gt;atom_depth --; return 0;&#125;]]></content>
      <categories>
        <category>拳掌指腿（音视频）</category>
        <category>降龙十八掌（FFmpeg）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[了解cmake]]></title>
    <url>%2F%E4%BA%86%E8%A7%A3cmake%2F</url>
    <content type="text"><![CDATA[什么是cmakecmake是一个跨平台的构建工具，主要是用来解决不同平台上Makefile不同的问题，通过编写CMakeLists.txt文件，然后通过cmake命令自动生成对应平台的Makefile。 编写CMakeLists12345678910111213141516171819202122232425262728293031323334// 指定运行此配置文件所需的 CMake 的最低版本cmake_minimum_required(VERSION 3.4.1)// 该指令的主要作用就是将指定的源文件生成链接文件，然后添加到工程中去。// native-lib表示生成的链接文件的名字。// SHARED表示生成的文件类型：STATIC类型在链接其它目标的时候使用。SHARED库会被动态链接，在运行时会被加载。MODULE库是一种不会被链接到其它目标中的插件，但是可能会在运行时使用dlopen-系列的函数。// native-lib.cpp 表示源文件，可以有多个。add_library(native-lib SHARED native-lib.cpp)// 查找一个库，log库已经包含在NDK中。// log-lib用来存储查找结果// log 表示查找库的名字find_library(log-lib log)// 为了确保CMake可以在编译时定位您的标头文件，使用该命令来指定包含标头文件的路径include_directories($&#123;openCVPath&#125;/native/jni/include)// set用来设置一些变量 // CMAKE_CURRENT_SOURCE_DIR 表示当前CMakeLists所在的目录set(openCVPath $&#123;CMAKE_SOURCE_DIR&#125;/../../../../opencv-sdk )// 使用static来添加依赖库，IMPORTED表示您只希望将库导入到项目中,// 然后需要通过set_target_properties来指定库的路径add_library(lib_opencv STATIC IMPORTED)// lib_opencv就是上面导入的库的变量// PROPERTIES 表示设置属性// IMPORTED_LOCATION 设置导入库的位置set_target_properties(lib_opencv PROPERTIES IMPORTED_LOCATION $&#123;openCVPath&#125;/native/libs/$&#123;ANDROID_ABI&#125;/libopencv_java4.so)// 将预构建库关联到您自己的原生库// native-lib库就是原生库，将上面查找的系统库和添加的依赖库都链接到目标库上。target_link_libraries(native-lib $&#123;log-lib&#125; lib_opencv) 运行cmake运行cmake只需要执行cmake命令，后面跟上CMakeLists所在的目录位置，运行后在当前目录中生成Makefile。1cmake . 然后再在当前目录执行make命令，就可以生成对应的项目编译文件。]]></content>
      <categories>
        <category>内功心法（CS基础）</category>
        <category>北冥神功（编程语言）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[Flutter深入之flutter-build-bundle命令如何编译Dart?]]></title>
    <url>%2FFlutter%E6%B7%B1%E5%85%A5%E4%B9%8Bflutter-build-bundle%E5%91%BD%E4%BB%A4%E5%A6%82%E4%BD%95%E7%BC%96%E8%AF%91Dart%2F</url>
    <content type="text"><![CDATA[开篇上一篇我们讲到，在flutterTask中会调用flutter build bundle命令来编译dart代码，生成dart资源。那么build bundle命令是如何编译dart代码的？编译后生成了哪些资源？这些资源都是些什么？ app.dill : 这就是dart代码编译后的二级制文件 Frontend_server.d : 这里面放的是frontend_server.dart.snapshot的绝对路径，使用该snapshot来编译dart代码生成上面的app.dill ​ flutter/bin/cache/artifacts/engine/darwin-x64/frontend_server.dart.snapshot snapshot_blob.bin.d : 这里面放的是所有参与编译的dart文件的绝对路径的集合，包括项目的代码和flutterSdk的代码以及pub库中的三方代码。 snapshot_blob.bin.d.fingerprint : 这里面放的是snapshot_blob.bin.d中的所有文件的绝对路径以及每个文件所对应的md5值。使用这个md5来判断该文件是否有修改。在每次编译的时候会判断，如果没有文件修改，则直接跳过编译。 编译Dart资源flutter build bundle命令的实现在BuildBundleCommand类中。 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384858687888990919293949596979899Future&lt;void&gt; build(&#123; TargetPlatform platform, BuildMode buildMode, String mainPath = defaultMainPath, String manifestPath = defaultManifestPath, String snapshotPath, String applicationKernelFilePath, String depfilePath, String privateKeyPath = defaultPrivateKeyPath, String assetDirPath, String packagesPath, bool precompiledSnapshot = false, bool reportLicensedPackages = false, bool trackWidgetCreation = false, String compilationTraceFilePath, bool buildHotUpdate = false, List&lt;String&gt; extraFrontEndOptions = const &lt;String&gt;[], List&lt;String&gt; extraGenSnapshotOptions = const &lt;String&gt;[], List&lt;String&gt; fileSystemRoots, String fileSystemScheme,&#125;) async &#123; snapshotPath ??= defaultSnapshotPath; // 通过--depfile 参数可自定义，默认是build/snapshot_blob.bin.d文件 // 该文件就是用来记录项目中参与编译的文件集合 depfilePath ??= defaultDepfilePath; // 通过--asset-dir 参数可自定义，默认在当前目录下 // 该目录中文件就是flutter的产物，最终合并到apk中的flutter_assets资源 assetDirPath ??= getAssetBuildDirectory(); // .packages目录 packagesPath ??= fs.path.absolute(PackageMap.globalPackagesPath); // app.dill dart代码编译后的二级制文件 applicationKernelFilePath ??= defaultApplicationKernelPath; DevFSContent kernelContent; if (!precompiledSnapshot) &#123; // debug模式下，默认不开启预编译 precompiledSnapshot=false if ((extraFrontEndOptions != null) &amp;&amp; extraFrontEndOptions.isNotEmpty) printTrace('Extra front-end options: $extraFrontEndOptions'); ensureDirectoryExists(applicationKernelFilePath); // 编译dart代码，生成app.dill 和 snapshot_blob.bin.d 以及 snapshot_blob.bin.d.fingerprint final CompilerOutput compilerOutput = await kernelCompiler.compile( sdkRoot: artifacts.getArtifactPath(Artifact.flutterPatchedSdkPath), incrementalCompilerByteStorePath: compilationTraceFilePath != null ? null : fs.path.absolute(getIncrementalCompilerByteStoreDirectory()), mainPath: fs.file(mainPath).absolute.path, outputFilePath: applicationKernelFilePath, depFilePath: depfilePath, trackWidgetCreation: trackWidgetCreation, extraFrontEndOptions: extraFrontEndOptions, fileSystemRoots: fileSystemRoots, fileSystemScheme: fileSystemScheme, packagesPath: packagesPath, linkPlatformKernelIn: compilationTraceFilePath != null, ); if (compilerOutput?.outputFilename == null) &#123; throwToolExit('Compiler failed on $mainPath'); &#125; kernelContent = DevFSFileContent(fs.file(compilerOutput.outputFilename)); // 生成 frontend_server.d文件，向文件中写入frontendServerSnapshotForEngineDartSdk的路径 await fs.directory(getBuildDirectory()).childFile('frontend_server.d') .writeAsString('frontend_server.d: $&#123;artifacts.getArtifactPath(Artifact.frontendServerSnapshotForEngineDartSdk)&#125;\n'); if (compilationTraceFilePath != null) &#123; // final JITSnapshotter snapshotter = JITSnapshotter(); final int snapshotExitCode = await snapshotter.build( platform: platform, buildMode: buildMode, mainPath: applicationKernelFilePath, outputPath: getBuildDirectory(), packagesPath: packagesPath, compilationTraceFilePath: compilationTraceFilePath, extraGenSnapshotOptions: extraGenSnapshotOptions, buildHotUpdate: buildHotUpdate, ); if (snapshotExitCode != 0) &#123; throwToolExit('Snapshotting exited with non-zero exit code: $snapshotExitCode'); &#125; &#125; &#125; // 生成 flutter_assets final AssetBundle assets = await buildAssets( manifestPath: manifestPath, assetDirPath: assetDirPath, packagesPath: packagesPath, reportLicensedPackages: reportLicensedPackages, ); if (assets == null) throwToolExit('Error building assets', exitCode: 1); await assemble( buildMode: buildMode, assetBundle: assets, kernelContent: kernelContent, privateKeyPath: privateKeyPath, assetDirPath: assetDirPath, compilationTraceFilePath: compilationTraceFilePath, );&#125; 编译Dart代码12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455565758596061626364656667686970717273747576777879808182838485868788class KernelCompiler &#123; const KernelCompiler(); Future&lt;CompilerOutput&gt; compile(&#123; String sdkRoot, String mainPath, String outputFilePath, String depFilePath, bool linkPlatformKernelIn = false, bool aot = false, bool trackWidgetCreation = false, List&lt;String&gt; extraFrontEndOptions, String incrementalCompilerByteStorePath, String packagesPath, List&lt;String&gt; fileSystemRoots, String fileSystemScheme, bool targetProductVm = false, &#125;) async &#123; // flutter/bin/cache/artifacts/engine/darwin-x64/frontend_server.dart.snapshot // 获取上面的snapshot final String frontendServer = artifacts.getArtifactPath( Artifact.frontendServerSnapshotForEngineDartSdk ); Fingerprinter fingerprinter; // 如果snapshot_blob.bin.d文件不为空，则说明有编译缓存 if (depFilePath != null) &#123; // 判断与上次编译对比，是否有文件的md5改变 fingerprinter = Fingerprinter( // snapshot_blob.bin.d.fingerprint文件 fingerprintPath: '$depFilePath.fingerprint', // main.dart paths: &lt;String&gt;[mainPath], properties: &lt;String, String&gt;&#123; 'entryPoint': mainPath, 'trackWidgetCreation': trackWidgetCreation.toString(), 'linkPlatformKernelIn': linkPlatformKernelIn.toString(), &#125;, // snapshot_blob.bin.d中的文件集合 depfilePaths: &lt;String&gt;[depFilePath], pathFilter: (String path) =&gt; !path.startsWith('/b/build/slave/'), ); // 判断是否有文件改动，如果没有，则直接返回。 if (await fingerprinter.doesFingerprintMatch()) &#123; printTrace('Skipping kernel compilation. Fingerprint match.'); return CompilerOutput(outputFilePath, 0); &#125; &#125; // 如果没有上次编译缓存，或者文件有改变，Fingerprinter不匹配，则使用dart重新编译 ... // dart frontend_server.dart.snapshot --sdk-root flutter_patched_sdk/ --strong --target=flutter --no-link-platform --incremental --packages .packages --output-dill app.dill --depfile snapshot_blob.bin.d --filesystem-scheme --debug main.dart final List&lt;String&gt; command = &lt;String&gt;[ engineDartPath, frontendServer, '--sdk-root', sdkRoot, '--strong', '--target=flutter', ]; // 参数拼装 ... final Process server = await processManager .start(command) .catchError((dynamic error, StackTrace stack) &#123; printError('Failed to start frontend server $error, $stack'); &#125;); final _StdoutHandler _stdoutHandler = _StdoutHandler(); server.stderr .transform(utf8.decoder) .listen((String message) &#123; printError(message); &#125;); server.stdout .transform(utf8.decoder) .transform(const LineSplitter()) .listen(_stdoutHandler.handler); final int exitCode = await server.exitCode; if (exitCode == 0) &#123; if (fingerprinter != null) &#123; // 更新snapshot_blob.bin.d.fingerprint文件 await fingerprinter.writeFingerprint(); &#125; return _stdoutHandler.compilerOutput.future; &#125; return null; &#125;&#125; Fingerprint对比12345678910111213141516171819202122232425Future&lt;bool&gt; doesFingerprintMatch() async &#123; try &#123; // 获取到当前的 snapshot_blob.bin.d.fingerprint文件 final File fingerprintFile = fs.file(fingerprintPath); if (!fingerprintFile.existsSync()) return false; if (!_depfilePaths.every(fs.isFileSync)) return false; final List&lt;String&gt; paths = await _getPaths(); if (!paths.every(fs.isFileSync)) return false; // 读取上一次的snapshot_blob.bin.d.fingerprint文件，构建一个老的Fingerprint对象 final Fingerprint oldFingerprint = Fingerprint.fromJson(await fingerprintFile.readAsString()); // 使用上一次的snapshot_blob.bin.d文件中的文件集合，构建一个新的Fingerprint对象，就是重新计算这些文件的md5值，此处并没有重新去收集更新snapshot_blob.bin.d文件中的文件集合，依旧用的是上一次的。如果本次有dart文件新建，但是并没有修改其它已经存在的文件，则该新建的dart文件不会被编译。 final Fingerprint newFingerprint = await buildFingerprint(); // 对比两次的文件集合中的每个文件的md5是否一样 return oldFingerprint == newFingerprint; &#125; catch (e) &#123; // Log exception and continue, fingerprinting is only a performance improvement. printTrace('Fingerprint check error: $e'); &#125; return false;&#125; 生成FlutteerAsset1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859606162636465666768697071727374757677787980818283848586878889 @override Future&lt;int&gt; build(&#123; String manifestPath = defaultManifestPath, String assetDirPath, String packagesPath, bool includeDefaultFonts = true, bool reportLicensedPackages = false &#125;) async &#123; // flutter_assets生成目录，通过--asset-dir参数来设置 assetDirPath ??= getAssetBuildDirectory(); // .package目录 packagesPath ??= fs.path.absolute(PackageMap.globalPackagesPath); FlutterManifest flutterManifest; try &#123; // manifestPath 就是 yaml文件 flutterManifest = await FlutterManifest.createFromPath(manifestPath); &#125; catch (e) &#123; printStatus('Error detected in pubspec.yaml:', emphasis: true); printError('$e'); return 1; &#125; ...// 处理yaml中配置的图片和字体资源 final Map&lt;_Asset, List&lt;_Asset&gt;&gt; assetVariants = _parseAssets( packageMap, flutterManifest, assetBasePath, excludeDirs: &lt;String&gt;[assetDirPath, getBuildDirectory()] ); if (assetVariants == null) return 1; final List&lt;Map&lt;String, dynamic&gt;&gt; fonts = _parseFonts( flutterManifest, includeDefaultFonts, packageMap, ); // 添加所有package(SDK中的，三方的，项目中的)资源和字体 for (String packageName in packageMap.map.keys) &#123; final Uri package = packageMap.map[packageName]; if (package != null &amp;&amp; package.scheme == 'file') &#123; // 得到该package的yaml文件 final String packageManifestPath = fs.path.fromUri(package.resolve('../pubspec.yaml')); final FlutterManifest packageFlutterManifest = await FlutterManifest.createFromPath(packageManifestPath); if (packageFlutterManifest == null) continue; // 跳过自身，因为在上面已经处理过自身的资源了 if (packageFlutterManifest.appName == flutterManifest.appName) continue; final String packageBasePath = fs.path.dirname(packageManifestPath); // 处理yaml文件中定义的资源和字体 final Map&lt;_Asset, List&lt;_Asset&gt;&gt; packageAssets = _parseAssets( packageMap, packageFlutterManifest, packageBasePath, packageName: packageName, ); if (packageAssets == null) return 1; assetVariants.addAll(packageAssets); fonts.addAll(_parseFonts( packageFlutterManifest, includeDefaultFonts, packageMap, packageName: packageName, )); &#125; &#125;... final List&lt;_Asset&gt; materialAssets = &lt;_Asset&gt;[]; // 如果使用的是MaterialDesign，则添加MaterialDesign的默认字体 // 该字体定义在 packages/flutter_tools/schema/material_fonts.yaml if (flutterManifest.usesMaterialDesign &amp;&amp; includeDefaultFonts) &#123; materialAssets.addAll(_getMaterialAssets(_fontSetMaterial)); &#125; ...// AssetManifest.json entries[_assetManifestJson] = _createAssetManifest(assetVariants);// FontManifest.json entries[_fontManifestJson] = DevFSStringContent(json.encode(fonts));// LICENSE entries[_license] = await _obtainLicenses(packageMap, assetBasePath, reportPackages: reportLicensedPackages); return 0; &#125; 处理yaml中定义的资源和字体 12345678910111213141516171819202122232425262728293031323334353637383940414243Map&lt;_Asset, List&lt;_Asset&gt;&gt; _parseAssets( PackageMap packageMap, FlutterManifest flutterManifest, String assetBase, &#123; List&lt;String&gt; excludeDirs = const &lt;String&gt;[], String packageName&#125;) &#123; final Map&lt;_Asset, List&lt;_Asset&gt;&gt; result = &lt;_Asset, List&lt;_Asset&gt;&gt;&#123;&#125;; final _AssetDirectoryCache cache = _AssetDirectoryCache(excludeDirs); // 处理yaml中配置的assets(图片之类的资源) for (Uri assetUri in flutterManifest.assets) &#123; if (assetUri.toString().endsWith('/')) &#123; _parseAssetsFromFolder(packageMap, flutterManifest, assetBase, cache, result, assetUri, excludeDirs: excludeDirs, packageName: packageName); &#125; else &#123; _parseAssetFromFile(packageMap, flutterManifest, assetBase, cache, result, assetUri, excludeDirs: excludeDirs, packageName: packageName); &#125; &#125; // 处理yaml中配置的字体 for (Font font in flutterManifest.fonts) &#123; for (FontAsset fontAsset in font.fontAssets) &#123; final _Asset baseAsset = _resolveAsset( packageMap, assetBase, fontAsset.assetUri, packageName, ); if (!baseAsset.assetFileExists) &#123; printError('Error: unable to locate asset entry in pubspec.yaml: "$&#123;fontAsset.assetUri&#125;".'); return null; &#125; result[baseAsset] = &lt;_Asset&gt;[]; &#125; &#125; return result;&#125; 输出Assets12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152Future&lt;void&gt; assemble(&#123; BuildMode buildMode, AssetBundle assetBundle, DevFSContent kernelContent, File dylibFile, String privateKeyPath = defaultPrivateKeyPath, String assetDirPath, String compilationTraceFilePath,&#125;) async &#123; assetDirPath ??= getAssetBuildDirectory(); printTrace('Building bundle'); final Map&lt;String, DevFSContent&gt; assetEntries = Map&lt;String, DevFSContent&gt;.from(assetBundle.entries); if (kernelContent != null) &#123; if (compilationTraceFilePath != null) &#123; final String vmSnapshotData = artifacts.getArtifactPath(Artifact.vmSnapshotData); final String isolateSnapshotData = fs.path.join(getBuildDirectory(), _kIsolateSnapshotData); final String isolateSnapshotInstr = fs.path.join(getBuildDirectory(), _kIsolateSnapshotInstr); // 拷贝 engine/darwin-x64/vm_isolate_snapshot.bin // 复制 flutter-assets/vm_snapshot_data assetEntries[_kVMSnapshotData] = DevFSFileContent(fs.file(vmSnapshotData)); // 拷贝 engine/darwin-x64/isolate_snapshot.bin // 复制 flutter-assets/isolate_snapshot_data assetEntries[_kIsolateSnapshotData] = DevFSFileContent(fs.file(isolateSnapshotData)); // 拷贝 build/isolate_snapshot_instr，项目编译生成的指令集 // 复制 flutter-assets/isolate_snapshot_instr，release模式下使用 assetEntries[_kIsolateSnapshotInstr] = DevFSFileContent(fs.file(isolateSnapshotInstr)); &#125; else &#123; final String platformKernelDill = artifacts.getArtifactPath(Artifact.platformKernelDill); final String vmSnapshotData = artifacts.getArtifactPath(Artifact.vmSnapshotData, null, buildMode); final String isolateSnapshotData = artifacts.getArtifactPath(Artifact.isolateSnapshotData, null, buildMode); // 拷贝 build/app.dill 项目编译生成的二进制文件,debug模式下使用 // 复制 flutter-assets/kernel_blob.bin assetEntries[_kKernelKey] = kernelContent; // 拷贝 engine/common/flutter_patched_sdk/platform_strong.dill // 复制 flutter-assets/platform_strong.dill assetEntries[_kPlatformKernelKey] = DevFSFileContent(fs.file(platformKernelDill)); // 拷贝 engine/darwin-x64/vm_isolate_snapshot.bin // 复制 flutter-assets/vm_snapshot_data assetEntries[_kVMSnapshotData] = DevFSFileContent(fs.file(vmSnapshotData)); // 拷贝 engine/darwin-x64/isolate_snapshot.bin // 复制 flutter-assets/isolate_snapshot_data assetEntries[_kIsolateSnapshotData] = DevFSFileContent(fs.file(isolateSnapshotData)); &#125; &#125; // 将上面的文件写入到指定目录 await writeBundle(fs.directory(assetDirPath), assetEntries); printTrace('Wrote $assetDirPath');&#125; 123456build bundle --suppress-analytics --target /Volumes/zhenyu/zhenyu/flutter_2/app_1/lib/main.dart --filesystem-scheme org-dartlang-root --depfile /Volumes/zhenyu/zhenyu/flutter_2/app_1/build/app/intermediates/flutter/debug/snapshot_blob.bin.d --asset-dir /Volumes/zhenyu/zhenyu/flutter_2/app_1/build/app/intermediates/flutter/debug/flutter_assets --debugdart /Volumes/zhenyu/zhenyu/project/flutter/flutter/bin/cache/artifacts/engine/darwin-x64/frontend_server.dart.snapshot --sdk-root /Volumes/zhenyu/zhenyu/project/flutter/flutter/bin/cache/artifacts/engine/common/flutter_patched_sdk/ --strong --target=flutter --no-link-platform --incremental --packages /Volumes/zhenyu/zhenyu/flutter_2/app_1/.packages --output-dill /Volumes/zhenyu/zhenyu/flutter_2/app_1/build/app.dill --depfile /Volumes/zhenyu/zhenyu/flutter_2/app_1/build/snapshot_blob.bin.d --filesystem-scheme --debug /Volumes/zhenyu/zhenyu/flutter_2/app_1/lib/main.dart]]></content>
      <categories>
        <category>刀枪棍棒（Android）</category>
        <category>伏魔杖法（Flutter）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[Flutter深入之flutter初始化]]></title>
    <url>%2FFlutter%E6%B7%B1%E5%85%A5%E4%B9%8Bflutter%E5%88%9D%E5%A7%8B%E5%8C%96%2F</url>
    <content type="text"><![CDATA[开篇前面两篇我们讲到了Flutter应用如何编译，并将编译结果如何放入APK中。那么当应用启动时，又是如何执行到main.dart中的main方法呢？Flutter的绘制内容又是如何被显示的呢？ FlutterApplication在应用启动的Application中会执行一些初始化操作，比如初始化一些路径文件夹等配置，搬移flutter资源，最为重要的是安装flutter.so。在前面的编译讲解中，我们知道该so就是flutter-engine。 1234567891011121314151617181920# FlutterMain.javapublic static void startInitialization(Context applicationContext, Settings settings) &#123; ... sSettings = settings; long initStartTimestampMillis = SystemClock.uptimeMillis(); // 初始化路径等配置 initConfig(applicationContext); // 检测aot initAot(applicationContext); // 搬移flutter资源 initResources(applicationContext); // 加载so System.loadLibrary("flutter"); // initTimeMicros是java层初始化loadLibrary所花费的时间。 // 因为so库中需要记录flutter开始的时间，但是在加载so库之前， // 是无法调用so中的api，所以将加载so库所花费的时间传下去，然后so库中使用当前时间回退该花费时间，就是应用启动的真正时间。 long initTimeMillis = SystemClock.uptimeMillis() - initStartTimestampMillis; nativeRecordStartTimestamp(initTimeMillis); &#125; 123456789# flutter_main.ccstatic void RecordStartTimestamp(JNIEnv* env, jclass jcaller, jlong initTimeMillis) &#123; int64_t initTimeMicros = static_cast&lt;int64_t&gt;(initTimeMillis) * static_cast&lt;int64_t&gt;(1000); // initTimeMicros是java层初始化所花费的时间，使用当前时间回退该花费时间，就是应用启动的真正时间。 blink::engine_main_enter_ts = Dart_TimelineGetMicros() - initTimeMicros;&#125; 在加载so库完成后，会触发JNI回调JNI_OnLoad，开始动态注册JNI方法。 12345678910111213141516171819202122# library_loader.ccJNIEXPORT jint JNI_OnLoad(JavaVM* vm, void* reserved) &#123; // 将javaVM保存成全局变量 fml::jni::InitJavaVM(vm); // 获取JNIEnv,通过JNIEnv.RegisterNatives来动态注册 JNIEnv* env = fml::jni::AttachCurrentThread(); bool result = false; // 注册FlutterMain.java中的native方法 result = shell::FlutterMain::Register(env); FML_CHECK(result); // 注册FlutterCallbackInformation.java、FlutterView.java、FlutterNativeView.java中的native方法 result = shell::PlatformViewAndroid::Register(env); FML_CHECK(result); // 注册VsyncWaiter.java中的native方法 result = shell::VsyncWaiterAndroid::Register(env); FML_CHECK(result); return JNI_VERSION_1_4;&#125; FlutterActivity一个Flutter应用是依附在一个Android的View上的，也就是FlutterView，而FlutterActivity会将FlutterView添加到自己的布局上。 FlutterView是一个SurfaceView 1234567891011121314151617181920212223242526272829303132333435363738394041# FlutterActivityDelegate.java@Override public void onCreate(Bundle savedInstanceState) &#123; // 通过flutter run启动时，是通过 shell am start 来启动，在启动时候会设置一些参数，这些参数会放入到Activity的intent中 String[] args = getArgsFromIntent(activity.getIntent()); // 调用JNI方法nativeInit去初始化 FlutterMain.ensureInitializationComplete(activity.getApplicationContext(), args); // 默认为null，可以在Activity中重写createFlutterView方法，实现自己的FlutterView flutterView = viewFactory.createFlutterView(activity); if (flutterView == null) &#123; // 默认为null，可以在Activity中重写createFlutterNativeView方法，实现自己的FlutterNativeView FlutterNativeView nativeView = viewFactory.createFlutterNativeView(); // 创建FlutterView flutterView = new FlutterView(activity, null, nativeView); flutterView.setLayoutParams(matchParent); // 将FlutterView放入Activity中 activity.setContentView(flutterView); // 创建一个view，背景色设置为windowBackground中的颜色，作为Flutter项目启动之前的视图 launchView = createLaunchView(); if (launchView != null) &#123; // 添加到Activity中， // 并设置FirstFrameListener监听，当Surface创建完成时，移除这个view addLaunchView(); &#125; &#125; // 如果是通过flutter run启动的，则从intent中解析bundle参数，直接运行 if (loadIntent(activity.getIntent())) &#123; return; &#125; if (!flutterView.getFlutterNativeView().isApplicationRunning()) &#123; // 获取flutter项目地址，默认是flutter_assets目录 String appBundlePath = FlutterMain.findAppBundlePath(activity.getApplicationContext()); if (appBundlePath != null) &#123; FlutterRunArguments arguments = new FlutterRunArguments(); arguments.bundlePath = appBundlePath; arguments.entrypoint = "main"; // 运行flutter项目 flutterView.runFromBundle(arguments); &#125; &#125; &#125; nativeInit12345678910111213141516171819202122232425262728# flutter_main.ccvoid FlutterMain::Init(JNIEnv* env, jclass clazz, jobject context, jobjectArray jargs, jstring bundlePath, jstring appStoragePath, jstring engineCachesPath) &#123; // 处理启动命令参数 auto command_line = fml::CommandLineFromIterators(args.begin(), args.end()); auto settings = SettingsFromCommandLine(command_line); // 默认是flutter_assets目录，可以通过在manifest中meta里面使用PUBLIC_FLUTTER_ASSETS_DIR_KEY来设置 settings.assets_path = fml::jni::JavaStringToString(env, bundlePath); ... // 如果是debug模式 if (!blink::DartVM::IsRunningPrecompiledCode()) &#123; // kernel_blob.bin文件就是dart工程编译后生成的二进制文件 auto application_kernel_path = fml::paths::JoinPaths(&#123;settings.assets_path, "kernel_blob.bin"&#125;); if (fml::IsFile(application_kernel_path)) &#123; settings.application_kernel_asset = application_kernel_path; &#125; &#125; ... // 创建FlutterMain g_flutter_main.reset(new FlutterMain(std::move(settings)));&#125; 创建FlutterViewFlutterView与Flutter代码进行通信则是通过FlutterNativeView来进行 12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455565758596061626364656667686970717273# FlutterView.java public FlutterView(Context context, AttributeSet attrs, FlutterNativeView nativeView) &#123; super(context, attrs); // 是否使用skia渲染，只有在模拟器上并且开启了才会使用，默认情况下，在真机上会优先使用OpenGL或者Vulkan // 在flutter run时可以加上参数--enable-software-rendering来开启 mIsSoftwareRenderingEnabled = nativeGetIsSoftwareRenderingEnabled(); // 监听系统级别的Activity过渡动画属性是否开启，随时改变 mAnimationScaleObserver = new AnimationScaleObserver(new Handler()); mMetrics = new ViewportMetrics(); mMetrics.devicePixelRatio = context.getResources().getDisplayMetrics().density; setFocusable(true); setFocusableInTouchMode(true); Activity activity = (Activity) getContext(); if (nativeView == null) &#123; // 创建默认的FlutterNativeView mNativeView = new FlutterNativeView(activity.getApplicationContext()); &#125; else &#123; mNativeView = nativeView; &#125; // 在PlatformViewsController中注册了一个MethodChannel插件，名字为flutter/platform_views mNativeView.attachViewAndActivity(this, activity); mSurfaceCallback = new SurfaceHolder.Callback() &#123; @Override public void surfaceCreated(SurfaceHolder holder) &#123; assertAttached(); // 通知native层 surface创建 // mNativeView.get()得到的就是nativeAttach返回的AndroidShellHolder对象指针 nativeSurfaceCreated(mNativeView.get(), holder.getSurface()); &#125; @Override public void surfaceChanged(SurfaceHolder holder, int format, int width, int height) &#123; assertAttached(); // 通知native层 surface改变，需重新创建 nativeSurfaceChanged(mNativeView.get(), width, height); &#125; @Override public void surfaceDestroyed(SurfaceHolder holder) &#123; assertAttached(); // 通知native层 surface销毁 nativeSurfaceDestroyed(mNativeView.get()); &#125; &#125;; getHolder().addCallback(mSurfaceCallback); mAccessibilityManager = (AccessibilityManager) getContext().getSystemService(Context.ACCESSIBILITY_SERVICE); mActivityLifecycleListeners = new ArrayList&lt;&gt;(); mFirstFrameListeners = new ArrayList&lt;&gt;(); // 创建各种插件调用，这些插件已经在engine.cc中和system_channels.dart实现， // 通过这些插件，可以调用Flutter Engine中的指定方法 mFlutterLocalizationChannel = new MethodChannel(this, "flutter/localization", JSONMethodCodec.INSTANCE); mFlutterNavigationChannel = new MethodChannel(this, "flutter/navigation", JSONMethodCodec.INSTANCE); mFlutterKeyEventChannel = new BasicMessageChannel&lt;&gt;(this, "flutter/keyevent", JSONMessageCodec.INSTANCE); mFlutterLifecycleChannel = new BasicMessageChannel&lt;&gt;(this, "flutter/lifecycle", StringCodec.INSTANCE); mFlutterSystemChannel = new BasicMessageChannel&lt;&gt;(this, "flutter/system", JSONMessageCodec.INSTANCE); mFlutterSettingsChannel = new BasicMessageChannel&lt;&gt;(this, "flutter/settings", JSONMessageCodec.INSTANCE); // 创建插件flutter/platform，提供给dart中使用 PlatformPlugin platformPlugin = new PlatformPlugin(activity); MethodChannel flutterPlatformChannel = new MethodChannel(this, "flutter/platform", JSONMethodCodec.INSTANCE); flutterPlatformChannel.setMethodCallHandler(platformPlugin); addActivityLifecycleListener(platformPlugin); mImm = (InputMethodManager) getContext().getSystemService(Context.INPUT_METHOD_SERVICE); mTextInputPlugin = new TextInputPlugin(this); // 将当前的locale信息设置给dart setLocale(getResources().getConfiguration().locale); // 将系统字体缩放和时间格式设置给dart setUserSettings(); &#125; nativeSurfaceCreated12345678910static void SurfaceCreated(JNIEnv* env, jobject jcaller, jlong shell_holder, jobject jsurface) &#123; fml::jni::ScopedJavaLocalFrame scoped_local_reference_frame(env); // 通过ndk获取Android中surface对象对应的NativeWindow对象，将其放入AndroidNativeWindow中维护。 auto window = fml::MakeRefCounted&lt;AndroidNativeWindow&gt;( ANativeWindow_fromSurface(env, jsurface)); ANDROID_SHELL_HOLDER-&gt;GetPlatformView()-&gt;NotifyCreated(std::move(window));&#125; 1234567891011void PlatformViewAndroid::NotifyCreated( fml::RefPtr&lt;AndroidNativeWindow&gt; native_window) &#123; if (android_surface_) &#123; // 通过jni调用上层FlutterNativeView.onFirstFrame方法，执行上层注册的FirstFrameListener; InstallFirstFrameCallback(); // 给AndroidSurface设置window // 比如AndroidSurfaceGL实现就会通过eglCreateWindowSurface来创建一个基于该window的surface android_surface_-&gt;SetNativeWindow(native_window); &#125; PlatformView::NotifyCreated();&#125; nativeSurfaceChanged12345678static void SurfaceChanged(JNIEnv* env, jobject jcaller, jlong shell_holder, jint width, jint height) &#123; ANDROID_SHELL_HOLDER-&gt;GetPlatformView()-&gt;NotifyChanged( SkISize::Make(width, height));&#125; 123456789101112131415161718void PlatformViewAndroid::NotifyChanged(const SkISize&amp; size) &#123; if (!android_surface_) &#123; return; &#125; fml::AutoResetWaitableEvent latch; fml::TaskRunner::RunNowOrPostTask( task_runners_.GetGPUTaskRunner(), // // 在GPU线程中执行，通知AndroidSurface对象去重新创建Surface // 比如AndroidSurfaceGL就会 // 先通过eglMakeCurrent EGL_NO_SURFACE 来解除绑定 // 再通过eglDestroySurface销毁当前surface， // 然后再调用eglCreateWindowSurface重新创建一个surface [&amp;latch, surface = android_surface_.get(), size]() &#123; surface-&gt;OnScreenSurfaceResize(size); latch.Signal(); &#125;); latch.Wait();&#125; nativeSurfaceDestroyed123static void SurfaceDestroyed(JNIEnv* env, jobject jcaller, jlong shell_holder) &#123; ANDROID_SHELL_HOLDER-&gt;GetPlatformView()-&gt;NotifyDestroyed();&#125; 123456789void PlatformViewAndroid::NotifyDestroyed() &#123; PlatformView::NotifyDestroyed(); if (android_surface_) &#123; // 直接在当前线程通知AndroidSurface销毁Surface // 比如AndroidSurfaceGL // 会调用eglMakeCurrent EGL_NO_SURFACE 来解除绑定 android_surface_-&gt;TeardownOnScreenContext(); &#125;&#125; 创建FlutterNativeViewFlutterNativeView是一个与Flutter异步通信的工具，在FlutterNativeView创建的时候，通过nativeAttach将对象实例传给native层。 具体通信就是FlutterNativeView通过jni与c++通信，然后c++通过其封装的Tonic库调用dart代码；dart代码通过native方法与c++通信，然后c++通过attach中传入的Java对象实例，以及JNIEnv对象，调用Java对象的方法。从而实现了Java与Dart的互相调用。 nativeAttacch123456789# FlutterNativeView.javapublic FlutterNativeView(Context context, boolean isBackgroundView) &#123; mContext = context; // 用来注册plugin mPluginRegistry = new FlutterPluginRegistry(this, context); attach(this, isBackgroundView); assertAttached(); mMessageHandlers = new HashMap&lt;&gt;();&#125; 12345678910111213141516# platform_view_android_jni.ccstatic jlong Attach(JNIEnv* env, jclass clazz, jobject flutterView, jboolean is_background_view) &#123; // 使用FlutterNativeView实例引用创建一个全局弱引用保存 fml::jni::JavaObjectWeakGlobalRef java_object(env, flutterView); // 创建AndroidShellHolder对象，并返回其指针 auto shell_holder = std::make_unique&lt;AndroidShellHolder&gt;( FlutterMain::Get().GetSettings(), java_object, is_background_view); if (shell_holder-&gt;IsValid()) &#123; return reinterpret_cast&lt;jlong&gt;(shell_holder.release()); &#125; else &#123; return 0; &#125;&#125; 创建AndroidShellHolder在engine中有四种线程，分别是paltform、ui、gpu、io。其中会把当前初始化的线程作为platform线程，并维护一个当前线程的Android Loop对象。每一个线程对象中都有一个TaskRunner对象，用来执行操作任务。 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384858687888990919293949596979899100101102103104105106# android_shell_holder.ccAndroidShellHolder::AndroidShellHolder( blink::Settings settings, fml::jni::JavaObjectWeakGlobalRef java_object, bool is_background_view) : settings_(std::move(settings)), java_object_(java_object) &#123; ... if (is_background_view) &#123; // 如果是后台视图，则只创建ui线程 thread_host_ = &#123;thread_label, ThreadHost::Type::UI&#125;; &#125; else &#123; // 默认创建的FlutterNativeView，is_background_view = false // 则创建ui线程，gpu线程，io线程 thread_host_ = &#123;thread_label, ThreadHost::Type::UI | ThreadHost::Type::GPU | ThreadHost::Type::IO&#125;; &#125; ... fml::WeakPtr&lt;PlatformViewAndroid&gt; weak_platform_view; // 通过模板函数一个回调，在回调中创建PlatFormViewAndroid Shell::CreateCallback&lt;PlatformView&gt; on_create_platform_view = [is_background_view, java_object, &amp;weak_platform_view](Shell&amp; shell) &#123; // 创建PlatformViewAndroid std::unique_ptr&lt;PlatformViewAndroid&gt; platform_view_android; // 如果是后台视图，则一定不使用软件渲染;否则就看配置中是否开启 if (is_background_view) &#123; platform_view_android = std::make_unique&lt;PlatformViewAndroid&gt;( shell, // delegate shell.GetTaskRunners(), // task runners java_object // java object handle for JNI interop ); &#125; else &#123; platform_view_android = std::make_unique&lt;PlatformViewAndroid&gt;( shell, // delegate shell.GetTaskRunners(), // task runners java_object, // java object handle for JNI interop shell.GetSettings() .enable_software_rendering // use software rendering ); &#125; weak_platform_view = platform_view_android-&gt;GetWeakPtr(); return platform_view_android; &#125;; // 通过模板函数创建一个回调，在回调中创建Rasterizer Shell::CreateCallback&lt;Rasterizer&gt; on_create_rasterizer = [](Shell&amp; shell) &#123; return std::make_unique&lt;Rasterizer&gt;(shell.GetTaskRunners()); &#125;; // 创建当前线程的messageLoop，并创建一个taskRunner // 在MessageLoop和TaskRunner中都维护了一个Loop对象，该对象是根据平台来获取。对于Android来说，则是通过ndk从Android系统中获取当前线程中的Android Loop对象。如果当前线程不是主线程，没有初始化loop，则会通过ndk为当前线程创建一个loop，返回新创建的loop。 fml::MessageLoop::EnsureInitializedForCurrentThread(); fml::RefPtr&lt;fml::TaskRunner&gt; gpu_runner; fml::RefPtr&lt;fml::TaskRunner&gt; ui_runner; fml::RefPtr&lt;fml::TaskRunner&gt; io_runner; // 上面创建的taskRunner作为paltform_runner，所以platform_runner中维护的是Android当前线程的loop对象。 fml::RefPtr&lt;fml::TaskRunner&gt; platform_runner = fml::MessageLoop::GetCurrent().GetTaskRunner(); if (is_background_view) &#123; // 如果是后台视图，则只有一个ui线程，所以四大TaskRunner都等于UITaskRunner auto single_task_runner = thread_host_.ui_thread-&gt;GetTaskRunner(); gpu_runner = single_task_runner; ui_runner = single_task_runner; io_runner = single_task_runner; &#125; else &#123; // 拿到其它三个线程的三个TaskRunner gpu_runner = thread_host_.gpu_thread-&gt;GetTaskRunner(); ui_runner = thread_host_.ui_thread-&gt;GetTaskRunner(); io_runner = thread_host_.io_thread-&gt;GetTaskRunner(); &#125; // 使用四大TaskRunner创建TaskRunners对象 blink::TaskRunners task_runners(thread_label, // label platform_runner, // platform gpu_runner, // gpu ui_runner, // ui io_runner // io ); // 创建Shell对象 shell_ = Shell::Create(task_runners, // task runners settings_, // settings on_create_platform_view, // platform view create callback on_create_rasterizer // rasterizer create callback ); platform_view_ = weak_platform_view; FML_DCHECK(platform_view_); is_valid_ = shell_ != nullptr; if (is_valid_) &#123; task_runners.GetGPUTaskRunner()-&gt;PostTask([]() &#123; // 在Android中线程优先级-8是最高级别的，所以GPU线程的级别稍微低一点，设为-5 // 详情请看 Proces.THREAD_PRIORITY_URGENT_DISPLAY if (::setpriority(PRIO_PROCESS, gettid(), -5) != 0) &#123; // 如果失败，则改成-2 if (::setpriority(PRIO_PROCESS, gettid(), -2) != 0) &#123; FML_LOG(ERROR) &lt;&lt; "Failed to set GPU task runner priority"; &#125; &#125; &#125;); task_runners.GetUITaskRunner()-&gt;PostTask([]() &#123; // 将UI线程的优先级设为-1 if (::setpriority(PRIO_PROCESS, gettid(), -1) != 0) &#123; FML_LOG(ERROR) &lt;&lt; "Failed to set UI task runner priority"; &#125; &#125;); &#125;&#125; 创建Shell1234567891011121314151617181920# shell.ccstd::unique_ptr&lt;Shell&gt; Shell::Create( blink::TaskRunners task_runners, blink::Settings settings, Shell::CreateCallback&lt;PlatformView&gt; on_create_platform_view, Shell::CreateCallback&lt;Rasterizer&gt; on_create_rasterizer) &#123; // 初始化Skia库、初始化ICU库 PerformInitializationTasks(settings); // 创建DartVM auto vm = blink::DartVM::ForProcess(settings); FML_CHECK(vm) &lt;&lt; "Must be able to initialize the VM."; // 在platform线程中创建Shell return Shell::Create(std::move(task_runners), // std::move(settings), // vm-&gt;GetIsolateSnapshot(), // blink::DartSnapshot::Empty(), // std::move(on_create_platform_view), // std::move(on_create_rasterizer) // );&#125; 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859606162636465666768697071727374757677787980818283# shell.ccstd::unique_ptr&lt;Shell&gt; Shell::CreateShellOnPlatformThread( blink::TaskRunners task_runners, blink::Settings settings, fml::RefPtr&lt;blink::DartSnapshot&gt; isolate_snapshot, fml::RefPtr&lt;blink::DartSnapshot&gt; shared_snapshot, Shell::CreateCallback&lt;PlatformView&gt; on_create_platform_view, Shell::CreateCallback&lt;Rasterizer&gt; on_create_rasterizer) &#123; if (!task_runners.IsValid()) &#123; return nullptr; &#125; // 创建Shell对象，并安装提供的服务的协议处理 // _flutter.screenshot 、 _flutter.screenshotSkp 、_flutter.runInView // _flutter.flushUIThreadTasks 、 _flutter.setAssetBundlePath auto shell = std::unique_ptr&lt;Shell&gt;(new Shell(task_runners, settings)); // 在platform线程创建PlatformView // 执行回调，创建PlatFormViewAndroid对象，因为各个平台不同，所以使用回调来创建不同对象 auto platform_view = on_create_platform_view(*shell.get()); // 创建一个垂直同步工具，在Android层进行doFrame时，执行动画等操作。 // 具体实现是在c++层，调用该工具的AwaitVSync方法， // 会通过jni调用上层VsyncWaiter.asyncWaitForVsync方法， // 该方法会添加一个回调Choreographer.getInstance().postFrameCallback(); // 在回调doFrame时，会执行nativeOnVsync方法，执行该同步工具的OnNativeVsync方法 auto vsync_waiter = platform_view-&gt;CreateVSyncWaiter(); if (!vsync_waiter) &#123; return nullptr; &#125; // 在IO线程创建一个IOManager auto io_task_runner = shell-&gt;GetTaskRunners().GetIOTaskRunner(); fml::TaskRunner::RunNowOrPostTask( io_task_runner, ... () &#123; io_manager = std::make_unique&lt;IOManager&gt;( platform_view-&gt;CreateResourceContext(), io_task_runner); ... &#125;); // 在GPU线程创建一个Rasterizer fml::TaskRunner::RunNowOrPostTask( task_runners.GetGPUTaskRunner(), ...() &#123; // 执行回调，创建Rasterizer对象 if (auto new_rasterizer = on_create_rasterizer(*shell)) &#123; rasterizer = std::move(new_rasterizer); &#125; &#125;); // 在UI线程中创建Engine fml::TaskRunner::RunNowOrPostTask( shell-&gt;GetTaskRunners().GetUITaskRunner(), fml::MakeCopyable(...() mutable &#123; const auto&amp; task_runners = shell-&gt;GetTaskRunners(); // 创建animator，并持有上面创建的垂直同步工具 auto animator = std::make_unique&lt;Animator&gt;(*shell, task_runners, std::move(vsync_waiter)); // 创建engine engine = std::make_unique&lt;Engine&gt;(*shell, // shell-&gt;GetDartVM(), // std::move(isolate_snapshot), // std::move(shared_snapshot), // task_runners, // shell-&gt;GetSettings(), // std::move(animator), // std::move(resource_context), // std::move(unref_queue) // ); &#125;)); // 启动 if (!shell-&gt;Setup(std::move(platform_view), // std::move(engine), // std::move(rasterizer), // std::move(io_manager)) // ) &#123; return nullptr; &#125; return shell;&#125; 创建DartVM12345678910111213141516171819202122232425262728293031323334353637383940fml::RefPtr&lt;DartVM&gt; DartVM::ForProcess( Settings settings, fml::RefPtr&lt;DartSnapshot&gt; vm_snapshot, fml::RefPtr&lt;DartSnapshot&gt; isolate_snapshot, fml::RefPtr&lt;DartSnapshot&gt; shared_snapshot) &#123; std::lock_guard&lt;std::mutex&gt; lock(gVMMutex); std::call_once(gVMInitialization, [settings, // vm_snapshot, // isolate_snapshot, // shared_snapshot // ]() mutable &#123; if (!vm_snapshot) &#123; // 加载vm_snapshot_instr文件和vm_snapshot_data文件，创建DartSnapshot对象 vm_snapshot = DartSnapshot::VMSnapshotFromSettings(settings); &#125; if (!(vm_snapshot &amp;&amp; vm_snapshot-&gt;IsValid())) &#123; FML_LOG(ERROR) &lt;&lt; "VM snapshot must be valid."; return; &#125; if (!isolate_snapshot) &#123; // 加载isolate_snapshot_data和isolate_snapshot_instr,创建DartSnapshot对象 isolate_snapshot = DartSnapshot::IsolateSnapshotFromSettings(settings); &#125; if (!(isolate_snapshot &amp;&amp; isolate_snapshot-&gt;IsValid())) &#123; FML_LOG(ERROR) &lt;&lt; "Isolate snapshot must be valid."; return; &#125; if (!shared_snapshot) &#123; // 使用空文件创建一个DartSnapshot对象 shared_snapshot = DartSnapshot::Empty(); &#125; // 创建DartVM gVM = fml::MakeRefCounted&lt;DartVM&gt;(settings, // std::move(vm_snapshot), // std::move(isolate_snapshot), // std::move(shared_snapshot) // ); &#125;); return gVM;&#125; 创建AndroidSurface在创建PatformViewANdroid的时候，会根据use_software_rendering来创建AndroidSurface来处理真正的绘制 123456789101112PlatformViewAndroid::PlatformViewAndroid( PlatformView::Delegate&amp; delegate, blink::TaskRunners task_runners, fml::jni::JavaObjectWeakGlobalRef java_object, bool use_software_rendering) : PlatformView(delegate, std::move(task_runners)), java_object_(java_object), android_surface_(AndroidSurface::Create(use_software_rendering)) &#123; FML_CHECK(android_surface_) &lt;&lt; "Could not create an OpenGL, Vulkan or Software surface to setup " "rendering.";&#125; 如果use_software_rendering=true，则说明开启软件渲染，则会创建AndroidSurfaceSoftware对象，该对象中使用skia库来绘制。而如果没有开启，则如果系统支持valkan，则就创建AndroidSurfaceVulkan，如果不支持则只能使用AndroidSurfaceGL。 12345678910111213141516std::unique_ptr&lt;AndroidSurface&gt; AndroidSurface::Create( bool use_software_rendering) &#123; if (use_software_rendering) &#123; auto software_surface = std::make_unique&lt;AndroidSurfaceSoftware&gt;(); return software_surface-&gt;IsValid() ? std::move(software_surface) : nullptr; &#125;#if SHELL_ENABLE_VULKAN auto vulkan_surface = std::make_unique&lt;AndroidSurfaceVulkan&gt;(); return vulkan_surface-&gt;IsValid() ? std::move(vulkan_surface) : nullptr;#else // SHELL_ENABLE_VULKAN auto gl_surface = std::make_unique&lt;AndroidSurfaceGL&gt;(); return gl_surface-&gt;IsOffscreenContextValid() ? std::move(gl_surface) : nullptr;#endif // SHELL_ENABLE_VULKAN&#125; 启动BundlenativeRunBundleAndSnapshotFromLibrary12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455565758596061# platform_view_android_jni.ccstatic void RunBundleAndSnapshotFromLibrary(JNIEnv* env, jobject jcaller, jlong shell_holder, jstring jbundlepath, jstring jdefaultPath, jstring jEntrypoint, jstring jLibraryUrl, jobject jAssetManager) &#123; auto asset_manager = fml::MakeRefCounted&lt;blink::AssetManager&gt;(); // flutter代码目录，默认是flutter_assets const auto bundlepath = fml::jni::JavaStringToString(env, jbundlepath); if (bundlepath.size() &gt; 0) &#123; const auto file_ext_index = bundlepath.rfind("."); if (bundlepath.substr(file_ext_index) == ".zip") &#123; // 如果路径是个zip文件，创建一个ZipAssetStore对象 asset_manager-&gt;PushBack( std::make_unique&lt;blink::ZipAssetStore&gt;(bundlepath)); &#125; else &#123; // 如果路径是个目录，创建一个DirectoryAssetBundle对象 asset_manager-&gt;PushBack( std::make_unique&lt;blink::DirectoryAssetBundle&gt;(fml::OpenDirectory( bundlepath.c_str(), false, fml::FilePermission::kRead))); &#125; // 从该路径推到出APK的Assets路径 const auto last_slash_index = bundlepath.rfind("/", bundlepath.size()); if (last_slash_index != std::string::npos) &#123; auto apk_asset_dir = bundlepath.substr( last_slash_index + 1, bundlepath.size() - last_slash_index); // 创建一个APKAssetProvider对象 asset_manager-&gt;PushBack(std::make_unique&lt;blink::APKAssetProvider&gt;( env, // jni environment jAssetManager, // asset manager std::move(apk_asset_dir)) // apk asset dir ); &#125; &#125; ... auto isolate_configuration = CreateIsolateConfiguration(*asset_manager); ... RunConfiguration config(std::move(isolate_configuration), std::move(asset_manager)); &#123; auto entrypoint = fml::jni::JavaStringToString(env, jEntrypoint); auto libraryUrl = fml::jni::JavaStringToString(env, jLibraryUrl); if ((entrypoint.size() &gt; 0) &amp;&amp; (libraryUrl.size() &gt; 0)) &#123; config.SetEntrypointAndLibrary(std::move(entrypoint), std::move(libraryUrl)); &#125; else if (entrypoint.size() &gt; 0) &#123; // 设置进入点，默认为main config.SetEntrypoint(std::move(entrypoint)); &#125; &#125; // 启动 ANDROID_SHELL_HOLDER-&gt;Launch(std::move(config));&#125; Launch最终调用dart运行时来加载dart代码，然后通过Tonic库来执行dart代码中的entrypoint方法，也就是main方法 123456789101112void AndroidShellHolder::Launch(RunConfiguration config) &#123; shell_-&gt;GetTaskRunners().GetUITaskRunner()-&gt;PostTask( fml::MakeCopyable([engine = shell_-&gt;GetEngine(), // config = std::move(config) // ]() mutable &#123; // 真正是在UI线程中启动 if (!engine || engine-&gt;Run(std::move(config)) == shell::Engine::RunStatus::Failure) &#123; &#125; else &#123; &#125; &#125;));&#125; main方法执行最终在main方法中通过runApp来进行视图绘制显示。]]></content>
      <categories>
        <category>刀枪棍棒（Android）</category>
        <category>伏魔杖法（Flutter）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[Flutter深入之flutter run命令究竟做了什么?]]></title>
    <url>%2FFlutter%E6%B7%B1%E5%85%A5%E4%B9%8Bflutter-run%E5%91%BD%E4%BB%A4%E7%A9%B6%E7%AB%9F%E5%81%9A%E4%BA%86%E4%BB%80%E4%B9%88%2F</url>
    <content type="text"><![CDATA[开篇当我们创建一个Flutter App项目后，在当前项目路径下运行命令flutter run，就可以编译生成一个APK，并且将APK安装到模拟器中并启动。那么Flutter究竟是如何编译Dart资源的？又是如何将Dart资源放入到APK中？ 接下来让我们慢慢跟着代码来分析flutter run 命令的执行过程。 flutter命令在Mac上，flutter命令是指 ${flutterSdk}/bin/flutter 这个shell程序。 1234567891011121314151617181920... update逻辑...PROG_NAME="$(path_uri "$(follow_links "$BASH_SOURCE")")"BIN_DIR="$(cd "$&#123;PROG_NAME%/*&#125;" ; pwd -P)"export FLUTTER_ROOT="$(cd "$&#123;BIN_DIR&#125;/.." ; pwd -P)"FLUTTER_TOOLS_DIR="$FLUTTER_ROOT/packages/flutter_tools"SNAPSHOT_PATH="$FLUTTER_ROOT/bin/cache/flutter_tools.snapshot"STAMP_PATH="$FLUTTER_ROOT/bin/cache/flutter_tools.stamp"SCRIPT_PATH="$FLUTTER_TOOLS_DIR/bin/flutter_tools.dart"DART_SDK_PATH="$FLUTTER_ROOT/bin/cache/dart-sdk"DART="$DART_SDK_PATH/bin/dart"PUB="$DART_SDK_PATH/bin/pub"// 实际上抛除更新逻辑，flutter的这个shell就是直接执行了dart命令// $FLUTTER_TOOL_ARGS 这个参数可以无视// $SNAPSHOT_PATH 这个指的就是$FLUTTER_ROOT/bin/cache/flutter_tools.snapshot这个snapshot// $@ 这个就是flutter 后面跟的参数"$DART" $FLUTTER_TOOL_ARGS "$SNAPSHOT_PATH" "$@" 我们可以看到，实际上flutter run 命令，执行的就是 dart flutter_tools.snapshot run flutter_tools项目flutter_toools.snapshot实际上就是${flutterSdk}/packages/flutter_tools这个项目编译生成的snapshot文件。所以flutter run 实际上就是使用dart来执行了这个dart项目的main方法，run就是参数。 我们可以将上面shell中的snapshot地址修改成本地flutter_tools项目地址，这样运行flutter命令走的就是本地项目代码，可以用于调试修改源码。 123将这一行 "$DART" $FLUTTER_TOOL_ARGS "$SNAPSHOT_PATH" "$@"修改成 "$DART" $FLUTTER_TOOL_ARGS "$FLUTTER_ROOT/packages/flutter_tools/bin/flutter_tools.dart" "$@" 执行main方法flutter_tools项目的main方法定义在 bin/flutter_tools.dart中。 12345678void main(List&lt;String&gt; args) &#123; if (args != null) &#123; for (String arg in args) &#123; print('参数 == $arg'); &#125; &#125; executable.main(args);&#125; 创建所有命令的对应对象再看看 lib/executable.dart，在这里会预先创建好每一种命令对应的对象，然后后面会根据解析的参数来选择指定的命令对象来执行。 123456789101112131415161718192021222324252627282930313233343536Future&lt;Null&gt; main(List&lt;String&gt; args) async &#123; ... await runner.run(args, &lt;FlutterCommand&gt;[ AnalyzeCommand(verboseHelp: verboseHelp), AttachCommand(verboseHelp: verboseHelp), BuildCommand(verboseHelp: verboseHelp), ChannelCommand(verboseHelp: verboseHelp), CleanCommand(), ConfigCommand(verboseHelp: verboseHelp), CreateCommand(), DaemonCommand(hidden: !verboseHelp), DevicesCommand(), DoctorCommand(verbose: verbose), DriveCommand(), EmulatorsCommand(), FormatCommand(), FuchsiaReloadCommand(), IdeConfigCommand(hidden: !verboseHelp), InjectPluginsCommand(hidden: !verboseHelp), InstallCommand(), LogsCommand(), MakeHostAppEditableCommand(), PackagesCommand(), PrecacheCommand(), RunCommand(verboseHelp: verboseHelp), ScreenshotCommand(), ShellCompletionCommand(), StopCommand(), TestCommand(verboseHelp: verboseHelp), TraceCommand(), UpdatePackagesCommand(hidden: !verboseHelp), UpgradeCommand(), ], verbose: verbose, muteCommandLogging: muteCommandLogging, verboseHelp: verboseHelp);&#125; 执行参数中指定的命令对象在runner.run中会通过runZoned创建一个新的分区，在新分区中运行，最终会在command_runner.dart的runCommand方法中解析参数，选定指定的命令对象来执行。 1234567891011121314151617181920212223Future&lt;T&gt; runCommand(ArgResults topLevelResults) async &#123; // 解析后的参数对象 也就是run参数 var argResults = topLevelResults; var commands = _commands; // 上一步创建的所有命令对应的对象 Command command; var commandString = executableName; while (commands.isNotEmpty) &#123; ... // Step into the command. argResults = argResults.command; // argResults.name 就是 run,所以此处取出的就是RunCommand对象 command = commands[argResults.name]; command._globalResults = topLevelResults; command._argResults = argResults; commands = command._subcommands; commandString += " $&#123;argResults.name&#125;"; ... &#125; ... // 执行 RunCommand.run() return (await command.run()) as T;&#125; 执行Run命令上面的command.run方法实际上执行的是其父类的run方法，最终会执行到runCommand方法，该方法由子类实现。所以我们主要关注RunCommand.runCommand方法。 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859606162636465666768697071727374 @override Future&lt;FlutterCommandResult&gt; runCommand() async &#123; Cache.releaseLockEarly(); // debug模式会默认开启热加载模式，如果不想要可以使用参数 --no-hot来关闭 final bool hotMode = shouldUseHotMode(); ... // 所有支持的flutter设备 final List&lt;FlutterDevice&gt; flutterDevices = devices.map((Device device) &#123; return FlutterDevice( device, trackWidgetCreation: argResults['track-widget-creation'], dillOutputPath: argResults['output-dill'], fileSystemRoots: argResults['filesystem-root'], fileSystemScheme: argResults['filesystem-scheme'], ); &#125;).toList(); ResidentRunner runner; final String applicationBinaryPath = argResults['use-application-binary']; if (hotMode) &#123; // 创建热加载模式的HotRunner runner = HotRunner( flutterDevices, target: targetFile, debuggingOptions: _createDebuggingOptions(), benchmarkMode: argResults['benchmark'], applicationBinary: applicationBinaryPath == null ? null : fs.file(applicationBinaryPath), projectRootPath: argResults['project-root'], packagesFilePath: globalResults['packages'], dillOutputPath: argResults['output-dill'], stayResident: stayResident, ipv6: ipv6, ); &#125; else &#123; runner = ColdRunner( flutterDevices, target: targetFile, debuggingOptions: _createDebuggingOptions(), traceStartup: traceStartup, applicationBinary: applicationBinaryPath == null ? null : fs.file(applicationBinaryPath), stayResident: stayResident, ipv6: ipv6, ); &#125; ... // HotRunner.run 以热加载模式运行 final int result = await runner.run( appStartedCompleter: appStartedTimeRecorder, route: route, shouldBuild: !runningWithPrebuiltApplication &amp;&amp; argResults['build'], ); if (result != 0) throwToolExit(null, exitCode: result); return FlutterCommandResult( ExitStatus.success, timingLabelParts: &lt;String&gt;[ hotMode ? 'hot' : 'cold', getModeName(getBuildMode()), devices.length == 1 ? getNameForTargetPlatform(await devices[0].targetPlatform) : 'multiple', devices.length == 1 &amp;&amp; await devices[0].isLocalEmulator ? 'emulator' : null ], endTimeOverride: appStartedTime, ); &#125; 热加载模式运行123456789101112131415161718192021222324252627282930313233 @override Future&lt;int&gt; run(&#123; Completer&lt;DebugConnectionInfo&gt; connectionInfoCompleter, Completer&lt;void&gt; appStartedCompleter, String route, bool shouldBuild = true &#125;) async &#123; ... // 执行 pub get 去更新flutter项目依赖 if (!await _refreshDartDependencies()) &#123; // Some kind of source level error or missing file in the Dart code. return 1; &#125; firstBuildTime = DateTime.now(); for (FlutterDevice device in flutterDevices) &#123; // 通过设备对象去运行 final int result = await device.runHot( hotRunner: this, route: route, shouldBuild: shouldBuild, ); if (result != 0) &#123; return result; &#125; &#125;// 与设备进行socket连接，用于热加载 return attach( connectionInfoCompleter: connectionInfoCompleter, appStartedCompleter: appStartedCompleter ); &#125; 123456789101112131415161718192021222324252627282930313233343536373839404142Future&lt;int&gt; runHot(&#123; HotRunner hotRunner, String route, bool shouldBuild, &#125;) async &#123; final bool prebuiltMode = hotRunner.applicationBinary != null; // 构建模式 debug final String modeName = hotRunner.debuggingOptions.buildInfo.modeName;// 设备架构 final TargetPlatform targetPlatform = await device.targetPlatform; // 获取应用对象 package = await getApplicationPackageForPlatform( targetPlatform, applicationBinary: hotRunner.applicationBinary );... // Start the application. final bool hasDirtyDependencies = hotRunner.hasDirtyDependencies(this); // 启动App，不同的平台有不同的实现，在Android平台，是由AndroidDevice来实现 final Future&lt;LaunchResult&gt; futureResult = device.startApp( package, mainPath: hotRunner.mainPath, debuggingOptions: hotRunner.debuggingOptions, platformArgs: platformArgs, route: route, prebuiltApplication: prebuiltMode, applicationNeedsRebuild: shouldBuild || hasDirtyDependencies, usesTerminalUi: hotRunner.usesTerminalUI, ipv6: hotRunner.ipv6, ); final LaunchResult result = await futureResult; if (!result.started) &#123; printError('Error launching application on $&#123;device.name&#125;.'); await stopEchoingDeviceLog(); return 2; &#125; observatoryUris = &lt;Uri&gt;[result.observatoryUri]; return 0; &#125; 创建应用对象具体的编译运行逻辑根据不同平台设备的特性不同，由不同的对象去执行 12345678910111213141516171819202122232425262728Future&lt;ApplicationPackage&gt; getApplicationPackageForPlatform( TargetPlatform platform, &#123;File applicationBinary&#125;) async &#123; switch (platform) &#123; case TargetPlatform.android_arm: case TargetPlatform.android_arm64: case TargetPlatform.android_x64: case TargetPlatform.android_x86: return applicationBinary == null // FlutterProject.current()的到的是当前目录， // FlutterProject.current()).android 得到的就是当前flutter项目中的android子项目 ? await AndroidApk.fromAndroidProject((await FlutterProject.current()).android) : AndroidApk.fromApk(applicationBinary); case TargetPlatform.ios: return applicationBinary == null ? IOSApp.fromIosProject((await FlutterProject.current()).ios) : IOSApp.fromPrebuiltApp(applicationBinary); case TargetPlatform.tester: return FlutterTesterApp.fromCurrentDirectory(); case TargetPlatform.darwin_x64: case TargetPlatform.linux_x64: case TargetPlatform.windows_x64: case TargetPlatform.fuchsia: return null; &#125; assert(platform != null); return null;&#125; 创建Android应用对象获取到Apk文件以及包名和启动的Activity名字来构建一个AndroidApk对象返回。 12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455 /// Creates a new AndroidApk based on the information in the Android manifest. static Future&lt;AndroidApk&gt; fromAndroidProject(AndroidProject androidProject) async &#123; File apkFile; if (androidProject.isUsingGradle) &#123; // 如果是gradle项目，获取apk文件 // 就是在此处执行 Initializing gradle 和 Resolving dependencies // 该方法返回build目录中的apk文件路径 apkFile = await getGradleAppOut(androidProject); if (apkFile.existsSync()) &#123; // 如果Apk文件已经存在过了，则直接读取Apk文件，从中获取packageName和launchableActivityName // 通过 packageName和launchableActivityName和Apk文件 创建一个AndroidApk对象返回 return AndroidApk.fromApk(apkFile); &#125; &#125; else &#123; apkFile = fs.file(fs.path.join(getAndroidBuildDirectory(), 'app.apk')); &#125;// 如果build目录中没有Apk文件，则读取manifest，获取packageName和launchableActivityName final File manifest = androidProject.appManifestFile; if (!manifest.existsSync()) return null; final String manifestString = manifest.readAsStringSync(); final xml.XmlDocument document = xml.parse(manifestString); final Iterable&lt;xml.XmlElement&gt; manifests = document.findElements('manifest'); if (manifests.isEmpty) return null; // 获取packageName final String packageId = manifests.first.getAttribute('package'); String launchActivity; // 获取launchableActivityName for (xml.XmlElement category in document.findAllElements('category')) &#123; if (category.getAttribute('android:name') == 'android.intent.category.LAUNCHER') &#123; final xml.XmlElement activity = category.parent.parent; final String enabled = activity.getAttribute('android:enabled'); if (enabled == null || enabled == 'true') &#123; final String activityName = activity.getAttribute('android:name'); launchActivity = '$packageId/$activityName'; break; &#125; &#125; &#125; if (packageId == null || launchActivity == null) return null; return AndroidApk( id: packageId, file: apkFile, launchActivity: launchActivity ); &#125; Initializing gradle在第一次执行gradlew命令时，会初始化gradlew命令，也就是获取gradlew命令全路径，并且通过 gradlew -v 来验证gradlew可以正常运行。 1234567891011Future&lt;String&gt; _initializeGradle(FlutterProject project) async &#123; final Directory android = project.android.hostAppGradleRoot; final Status status = logger.startProgress('Initializing gradle...', expectSlowOperation: true); // 获取gradlew命令文件 String gradle = _locateGradlewExecutable(android); ... // 执行 gradlew -v 来验证 await runCheckedAsync(&lt;String&gt;[gradle, '-v'], environment: _gradleEnv); status.stop(); return gradle;&#125; Resolving dependencies1234567891011121314151617181920212223242526Future&lt;GradleProject&gt; _readGradleProject() async &#123; // 当前flutter项目 final FlutterProject flutterProject = await FlutterProject.current(); // 获取gradlew命令 final String gradle = await _ensureGradle(flutterProject); // 解析local.properties中的配置 updateLocalProperties(project: flutterProject); final Status status = logger.startProgress('Resolving dependencies...', expectSlowOperation: true); GradleProject project; try &#123; // 运行 gradlew app:properties 命令，获取所有的项目中所有的properties final RunResult runResult = await runCheckedAsync( &lt;String&gt;[gradle, 'app:properties'], workingDirectory: flutterProject.android.hostAppGradleRoot.path, environment: _gradleEnv, ); final String properties = runResult.stdout.trim(); // 提取出 build目录和 build type 和 flavors // 根据这三个属性创建GradleProject对象 project = GradleProject.fromAppProperties(properties); &#125; catch (exception) &#123; ... &#125; status.stop(); return project;&#125; 启动应用1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859606162636465666768 @override Future&lt;LaunchResult&gt; startApp( ApplicationPackage package, &#123; String mainPath, String route, DebuggingOptions debuggingOptions, Map&lt;String, dynamic&gt; platformArgs, bool prebuiltApplication = false, bool applicationNeedsRebuild = false, bool usesTerminalUi = true, bool ipv6 = false, &#125;) async &#123; // 检查版本和架构是否支持... if (!prebuiltApplication) &#123; printTrace('Building APK'); final FlutterProject project = await FlutterProject.current(); // assembleDebug 构建APK await buildApk( project: project, target: mainPath, buildInfo: buildInfo, ); // Package has been built, so we can get the updated application ID and // activity name from the .apk. package = await AndroidApk.fromAndroidProject(project.android); &#125; printTrace("Stopping app '$&#123;package.name&#125;' on $name."); await stopApp(package);// 运行adb install 命令来安装上面生成的Apk文件 if (!await _installLatestApp(package)) return LaunchResult.failed(); ... List&lt;String&gt; cmd;// 启动Activity cmd = adbCommandForDevice(&lt;String&gt;[ 'shell', 'am', 'start', '-a', 'android.intent.action.RUN', '-f', '0x20000000', // FLAG_ACTIVITY_SINGLE_TOP '--ez', 'enable-background-compilation', 'true', '--ez', 'enable-dart-profiling', 'true', ]);... cmd.add(apk.launchActivity); final String result = (await runCheckedAsync(cmd)).stdout; // This invocation returns 0 even when it fails. if (result.contains('Error: ')) &#123; printError(result.trim()); return LaunchResult.failed(); &#125;... try &#123; Uri observatoryUri; if (debuggingOptions.buildInfo.isDebug || debuggingOptions.buildInfo.isProfile) &#123; observatoryUri = await observatoryDiscovery.uri; &#125; return LaunchResult.succeeded(observatoryUri: observatoryUri); &#125; catch (error) &#123; printError('Error waiting for a debug connection: $error'); return LaunchResult.failed(); &#125; finally &#123; await observatoryDiscovery.cancel(); &#125; &#125; 构建Android ApkassembleTask123456789101112131415161718192021222324252627282930313233343536373839404142Future&lt;Null&gt; _buildGradleProjectV2( FlutterProject flutterProject, String gradle, BuildInfo buildInfo, String target) async &#123; final GradleProject project = await _gradleProject(); // 根据build信息，获取assembleDebug任务名称 final String assembleTask = project.assembleTaskFor(buildInfo); ... final Status status = logger.startProgress( "Gradle task '$assembleTask'...", expectSlowOperation: true, multilineOutput: true, ); // gradlew 路径 final String gradlePath = fs.file(gradle).absolute.path; // 组装参数 final List&lt;String&gt; command = &lt;String&gt;[gradlePath]; ... command.add(assembleTask); // 运行 gradlew -args assembleDebug final int exitCode = await runCommandAndStreamOutput( command, workingDirectory: flutterProject.android.hostAppGradleRoot.path, allowReentrantFlutter: true, environment: _gradleEnv, filter: logger.isVerbose ? null : ndkMessageFilter, ); status.stop(); if (exitCode != 0) throwToolExit('Gradle task $assembleTask failed with exit code $exitCode', exitCode: exitCode); // 找到生成的Apk文件 final File apkFile = _findApkFile(project, buildInfo); ... apkFile.copySync(project.apkDirectory.childFile('app.apk').path); printTrace('calculateSha: $&#123;project.apkDirectory&#125;/app.apk'); final File apkShaFile = project.apkDirectory.childFile('app.apk.sha1'); apkShaFile.writeAsStringSync(calculateSha(apkFile)); ...&#125; flutter.gradle我们可以看到在flutter_tools中，只是简单的执行了一下assemble任务，并没有对Dart资源进行编译和处理，那么具体的逻辑，对于Android项目来说，是在gradle/flutter.gradle中。 在flutter.gradle中，创建了flutterPlugin。 1apply plugin: FlutterPlugin 12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455565758596061626364656667686970717273747576777879 @Overridevoid apply(Project project) &#123; // Add custom build types ... // 找到flutter命令 flutterExecutable = Paths.get(flutterRoot.absolutePath, "bin", flutterExecutableName).toFile(); if (project.hasProperty('localEngineOut')) &#123; // 配置了本地自己编译的引擎目录 ... &#125; else &#123; // 找到flutter engine目录，这里面有已经编译好的所有平台架构所有build type的flutter engine。 // Android平台提供的是一个jar包，flutter.jar，在这个jar包中包含了engine so库，和Java Api库。 Path baseEnginePath = Paths.get(flutterRoot.absolutePath, "bin", "cache", "artifacts", "engine") // 根据配置的平台架构来选择对应的flutter.jar,默认是arm架构 String targetArch = 'arm' if (project.hasProperty('target-platform') &amp;&amp; project.property('target-platform') == 'android-arm64') &#123; targetArch = 'arm64' &#125; // 获取对应架构下所有build type的flutter.jar debugFlutterJar = baseEnginePath.resolve("android-$&#123;targetArch&#125;").resolve("flutter.jar").toFile() profileFlutterJar = baseEnginePath.resolve("android-$&#123;targetArch&#125;-profile").resolve("flutter.jar").toFile() releaseFlutterJar = baseEnginePath.resolve("android-$&#123;targetArch&#125;-release").resolve("flutter.jar").toFile() dynamicProfileFlutterJar = baseEnginePath.resolve("android-$&#123;targetArch&#125;-dynamic-profile").resolve("flutter.jar").toFile() dynamicReleaseFlutterJar = baseEnginePath.resolve("android-$&#123;targetArch&#125;-dynamic-release").resolve("flutter.jar").toFile() if (!debugFlutterJar.isFile()) &#123; project.exec &#123; executable flutterExecutable.absolutePath args "--suppress-analytics" args "precache" &#125; if (!debugFlutterJar.isFile()) &#123; throw new GradleException("Unable to find flutter.jar in SDK: $&#123;debugFlutterJar&#125;") &#125; &#125; // 对于 x86架构，只在debug类型下才会添加，所以单独创建了一个task，专门用来将x86、x86_64两种架构的so库加入到apk中。 flutterX86Jar = project.file("$&#123;project.buildDir&#125;/$&#123;AndroidProject.FD_INTERMEDIATES&#125;/flutter/flutter-x86.jar") Task flutterX86JarTask = project.tasks.create("$&#123;flutterBuildPrefix&#125;X86Jar", Jar) &#123; destinationDir flutterX86Jar.parentFile archiveName flutterX86Jar.name from("$&#123;flutterRoot&#125;/bin/cache/artifacts/engine/android-x86/libflutter.so") &#123; into "lib/x86" &#125; from("$&#123;flutterRoot&#125;/bin/cache/artifacts/engine/android-x64/libflutter.so") &#123; into "lib/x86_64" &#125; &#125; // 将flutter.jar添加到项目的依赖列表中，打包就会自动打入到apk中。 project.android.buildTypes.each &#123; addFlutterJarApiDependency(project, it, flutterX86JarTask) &#125; project.android.buildTypes.whenObjectAdded &#123; addFlutterJarApiDependency(project, it, flutterX86JarTask) &#125; &#125;// flutter配置 project.extensions.create("flutter", FlutterExtension) // 添加 flutterTask，具体编译操作就在这个task里面 project.afterEvaluate this.&amp;addFlutterTask // 对于 flutter plugin的处理 File pluginsFile = new File(project.projectDir.parentFile.parentFile, '.flutter-plugins') Properties plugins = readPropertiesIfExist(pluginsFile) plugins.each &#123; name, _ -&gt; def pluginProject = project.rootProject.findProject(":$name") if (pluginProject != null) &#123; project.dependencies &#123; if (project.getConfigurations().findByName("implementation")) &#123; implementation pluginProject &#125; else &#123; compile pluginProject &#125; &#125; pluginProject.afterEvaluate this.&amp;addFlutterJarCompileOnlyDependency &#125; else &#123; project.logger.error("Plugin project :$name not found. Please update settings.gradle.") &#125; &#125;&#125; 在flutterPlugin中创建了flutterTask 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859606162636465 private void addFlutterTask(Project project) &#123;// 设置 target String target = project.flutter.target if (target == null) &#123; target = 'lib/main.dart' &#125; if (project.hasProperty('target')) &#123; target = project.property('target') &#125;// 配置flutter命令运行参数 ... def addFlutterDeps = &#123; variant -&gt; String flutterBuildMode = buildModeFor(variant.buildType) if (flutterBuildMode == 'debug' &amp;&amp; project.tasks.findByName('$&#123;flutterBuildPrefix&#125;X86Jar')) &#123; Task task = project.tasks.findByName("compile$&#123;variant.name.capitalize()&#125;JavaWithJavac") if (task) &#123; // 如果是debug模式，则找到 javac任务，将上面创建的添加x86架构的任务添加到javac任务上。 task.dependsOn project.flutterBuildX86Jar &#125; task = project.tasks.findByName("compile$&#123;variant.name.capitalize()&#125;Kotlin") if (task) &#123; // 如果找到kotlin任务，对于纯kotlin项目 task.dependsOn project.flutterBuildX86Jar &#125; &#125; // 创建FlutterTask FlutterTask flutterTask = project.tasks.create(name: "$&#123;flutterBuildPrefix&#125;$&#123;variant.name.capitalize()&#125;", type: FlutterTask) &#123; flutterRoot this.flutterRoot flutterExecutable this.flutterExecutable buildMode flutterBuildMode localEngine this.localEngine localEngineSrcPath this.localEngineSrcPath targetPath target verbose verboseValue fileSystemRoots fileSystemRootsValue fileSystemScheme fileSystemSchemeValue trackWidgetCreation trackWidgetCreationValue compilationTraceFilePath compilationTraceFilePathValue buildHotUpdate buildHotUpdateValue buildSharedLibrary buildSharedLibraryValue targetPlatform targetPlatformValue sourceDir project.file(project.flutter.source) intermediateDir project.file("$&#123;project.buildDir&#125;/$&#123;AndroidProject.FD_INTERMEDIATES&#125;/flutter/$&#123;variant.name&#125;") extraFrontEndOptions extraFrontEndOptionsValue extraGenSnapshotOptions extraGenSnapshotOptionsValue &#125; // 创建复制flutter资源任务，在assets中包含了dart资源和dart编译后的代码资源，在该任务中会将这些dart编译后生成的产物都复制到Android apk构建时资源的最终输出目录中去，这样在构建apk时，会将这个dart资源也一起打包进去。 Task copyFlutterAssetsTask = project.tasks.create(name: "copyFlutterAssets$&#123;variant.name.capitalize()&#125;", type: Copy) &#123; // 该任务依赖于 flutterTask，必须要等dart编译完代码后才开始复制 dependsOn flutterTask dependsOn variant.mergeAssets dependsOn "clean$&#123;variant.mergeAssets.name.capitalize()&#125;" into variant.mergeAssets.outputDir with flutterTask.assets // 将flutterTask.getAssets输出的资源复制 &#125; // 在processResouces任务执行前，会执行复制dart资源的任务，然后复制dart资源的任务执行前，会执行flutterTask任务去编译dart代码。 variant.outputs[0].processResources.dependsOn(copyFlutterAssetsTask) &#125; if (project.android.hasProperty("applicationVariants")) &#123; project.android.applicationVariants.all addFlutterDeps &#125; else &#123; project.android.libraryVariants.all addFlutterDeps &#125; &#125; 12345678910111213141516171819CopySpec getAssets() &#123; return project.copySpec &#123; from "$&#123;intermediateDir&#125;" include "flutter_assets/**" // the working dir and its files if (buildMode != 'debug' || compilationTraceFilePath) &#123; if (buildSharedLibrary) &#123; include "app.so" &#125; else &#123; // 如果是非debug模式，会只输入以下四个文件资源 include "vm_snapshot_data" include "vm_snapshot_instr" include "isolate_snapshot_data" include "isolate_snapshot_instr" &#125; &#125; &#125; &#125; 执行flutterTask，编译dart资源 123456789101112131415161718192021222324252627282930void buildBundle() &#123; if (!sourceDir.isDirectory()) &#123; throw new GradleException("Invalid Flutter source directory: $&#123;sourceDir&#125;") &#125; intermediateDir.mkdirs() if (buildMode == "profile" || buildMode == "release") &#123; // 配置release模式下的flutter命令参数 ... &#125; project.exec &#123; // executable 就是flutter命令 executable flutterExecutable.absolutePath // dart代码目录 workingDir sourceDir // 可以配置本地引擎 if (localEngine != null) &#123; args "--local-engine", localEngine args "--local-engine-src-path", localEngineSrcPath &#125; // 配置flutter 命令参数，执行的是flutter build bundle 来编译dart代码。 args "build", "bundle" args "--suppress-analytics" args "--target", targetPath // 配置参数 ... &#125;&#125; 编译dart代码在flutterTask中会执行flutter build bundle 来编译dart代码，具体build bundle是怎样编译的我们放在下一篇中详细讲解。 总之flutterTask通过执行flutter build bundle编译dart，将生成的dart资源放在build目录中。然后copyFlutterAssetsTask执行，将build目录中的dart资源复制到android的build/intermediates中，最后在processResources任务中和Android的assets资源一起被处理。]]></content>
      <categories>
        <category>刀枪棍棒（Android）</category>
        <category>伏魔杖法（Flutter）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[Android-ReactNative渲染流程]]></title>
    <url>%2FAndroid-ReactNative%E6%B8%B2%E6%9F%93%E6%B5%81%E7%A8%8B%2F</url>
    <content type="text"><![CDATA[注册组件在RN项目中必须使用AppRegistry.registerComponent(appkey,component)来注册组件。在上一篇中的runJSBundle里面，加载js文件后，交给JSC运行(evaluateSourceCode函数)，就会开始执行index.js，执行里面的代码，在index.js里面就会调用AppRegistry来注册组件。 12345678910111213141516171819202122232425registerComponent( appKey: string, componentProvider: ComponentProvider, section?: boolean,): string &#123; // 为该appkey准备了一个runnable runnables[appKey] = &#123; componentProvider, run: appParameters =&gt; &#123; // appParameters启动参数，来自原生调用传入的 // 开始渲染 renderApplication( componentProviderInstrumentationHook(componentProvider), appParameters.initialProps, appParameters.rootTag, wrapperComponentProvider &amp;&amp; wrapperComponentProvider(appParameters), appParameters.fabric, ); &#125;, &#125;; if (section) &#123; sections[appKey] = runnables[appKey]; &#125; return appKey;&#125;, 启动组件在上一篇说到，当Context创建完毕后，会启动js组件(defaultJSEntryPoint函数)，通过调用js函数AppRegister.runApplication来启动。 1234567891011121314151617181920runApplication(appKey: string, appParameters: any): void &#123; invariant( runnables[appKey] &amp;&amp; runnables[appKey].run, 'Application ' + appKey + ' has not been registered.\n\n' + "Hint: This error often happens when you're running the packager " + '(local dev server) from a wrong folder. For example you have ' + 'multiple apps and the packager is still running for the app you ' + 'were working on before.\nIf this is the case, simply kill the old ' + 'packager instance (e.g. close the packager terminal window) ' + 'and start the packager in the correct app folder (e.g. cd into app ' + "folder and run 'npm start').\n\n" + 'This error can also happen due to a require() error during ' + 'initialization or failure to call AppRegistry.registerComponent.\n\n', ); // 运行该appkey注册的runnable runnables[appKey].run(appParameters);&#125; 解析组件通过ReactNativeRenderer来解析组件视图，然后会调用组件的render方法获取渲染内容。 1234567891011121314151617181920212223242526function renderApplication&lt;Props: Object&gt;( RootComponent: React.ComponentType&lt;Props&gt;, initialProps: Props, rootTag: any, WrapperComponent?: ?React.ComponentType&lt;*&gt;, fabric?: boolean,) &#123; let renderable = ( &lt;AppContainer rootTag=&#123;rootTag&#125; WrapperComponent=&#123;WrapperComponent&#125;&gt; &lt;RootComponent &#123;...initialProps&#125; rootTag=&#123;rootTag&#125; /&gt; &lt;/AppContainer&gt; ); if ( RootComponent.prototype != null &amp;&amp; RootComponent.prototype.unstable_isAsyncReactComponent === true ) &#123; const AsyncMode = React.unstable_AsyncMode; renderable = &lt;AsyncMode&gt;&#123;renderable&#125;&lt;/AsyncMode&gt;; &#125; if (fabric) &#123; require('ReactFabric').render(renderable, rootTag); &#125; else &#123; // require('ReactNative').render(renderable, rootTag); &#125;&#125; 加载原生组件此处拿DrawerLayoutAndroid组件来举例。 1234567891011121314151617181920 // 在DrawerLayoutAndroid组件的render函数中返回一个AndroidDrawerLayout组件，这是它的真正实现，是一个原生组件。 render: function() &#123;... return ( &lt;AndroidDrawerLayout &#123;...this.props&#125; ref=&#123;RK_DRAWER_REF&#125; drawerWidth=&#123;this.props.drawerWidth&#125; drawerPosition=&#123;this.props.drawerPosition&#125; drawerLockMode=&#123;this.props.drawerLockMode&#125; style=&#123;[styles.base, this.props.style]&#125; onDrawerSlide=&#123;this._onDrawerSlide&#125; onDrawerOpen=&#123;this._onDrawerOpen&#125; onDrawerClose=&#123;this._onDrawerClose&#125; onDrawerStateChanged=&#123;this._onDrawerStateChanged&#125;&gt; &#123;childrenWrapper&#125; &#123;drawerViewWrapper&#125; &lt;/AndroidDrawerLayout&gt; ); &#125;, requireNativeComponent (Js层)在js加载的时候会先注册该组件的属性设置回调，然后在真正创建该组件的时候调用该回调获取组件的属性配置。 12345678910111213141516// 通过requireNativeComponent函数可以加载指定的原生组件const AndroidDrawerLayout = requireNativeComponent('AndroidDrawerLayout');const requireNativeComponent = (uiViewClassName: string): string =&gt; // 注册该组件对应的属性设置回调，在创建该组件时调用 createReactNativeComponentClass(uiViewClassName, () =&gt; &#123; const viewConfig = UIManager[uiViewClassName]; ... return viewConfig; &#125;); // 注册在viewConfigCallbacks中exports.register = function(name: string, callback: ViewConfigGetter): string &#123; viewConfigCallbacks.set(name, callback); return name;&#125;; 创建VIewcreateInstance (JS层)12345678910111213141516171819202122232425262728createInstance: function( type, props, rootContainerInstance, hostContext, internalInstanceHandle) &#123; var tag = allocateTag(), // 获取注册的属性设置回调 viewConfig = ReactNativeViewConfigRegistry.get(type); type = diffProperties( null, emptyObject$1, props, viewConfig.validAttributes ); // 调用native的UIManager模块的createView方法来创建 UIManager.createView( tag, viewConfig.uiViewClassName, rootContainerInstance, type ); rootContainerInstance = new ReactNativeFiberHostComponent(tag, viewConfig); instanceCache[tag] = internalInstanceHandle; instanceProps[tag] = props; return rootContainerInstance;&#125;, 调用Java Module方法 (Java层)调用Java层UIManagerModule的createView方法来创建对应原生视图，具体调用原理可以看上一篇的Android-ReactNative通信流程。 12345678910@ReactMethodpublic void createView(int tag, String className, int rootViewTag, ReadableMap props) &#123; if (DEBUG) &#123; String message = "(UIManager.createView) tag: " + tag + ", class: " + className + ", props: " + props; FLog.d(ReactConstants.TAG, message); PrinterHolder.getPrinter().logMessage(ReactDebugOverlayTags.UI_MANAGER, message); &#125; mUIImplementation.createView(tag, className, rootViewTag, props);&#125; 将创建view的请求添加到队列中 (Java层)UIManagerModule的createView方法最终会走到UIVIewOperationQueue中，在该类中维护了一个操作队列，主线程不停的执行该对象中的操作。由于UIManagerModule.createView是在native消息线程中执行的，所以会将创建View的请求封装成一个操作加入到队列中。 123456789101112131415public void enqueueCreateView( ThemedReactContext themedContext, int viewReactTag, String viewClassName, @Nullable ReactStylesDiffMap initialProps) &#123; synchronized (mNonBatchedOperationsLock) &#123; mNonBatchedOperations.addLast( new CreateViewOperation( themedContext, viewReactTag, viewClassName, initialProps)); &#125;&#125; 操作队列的执行 (Java层)当Activity触发onResume时，UIViewOperationQueue会向ReactChoreographer中添加一个回调。 123456/* package */ void resumeFrameCallback() &#123; mIsDispatchUIFrameCallbackEnqueued = true; // 添加回调 ReactChoreographer.getInstance() .postFrameCallback(ReactChoreographer.CallbackType.DISPATCH_UI, mDispatchUIFrameCallback);&#125; 123456789101112131415161718192021222324252627282930313233343536373839public synchronized void postFrameCallback( CallbackType type, ChoreographerCompat.FrameCallback frameCallback) &#123; // 将添加的回调加入到回调队列的尾部 mCallbackQueues[type.getOrder()].addLast(frameCallback); mTotalCallbacks++; Assertions.assertCondition(mTotalCallbacks &gt; 0); if (!mHasPostedCallback) &#123; // 向内部的Choreographer添加一个回调，实际上就是在主线程的Handler中执行mReactChoreographerDispatcher的runnable mChoreographer.postFrameCallback(mReactChoreographerDispatcher); mHasPostedCallback = true; &#125;&#125;public void postFrameCallback(FrameCallback callbackWrapper) &#123; if (IS_JELLYBEAN_OR_HIGHER) &#123; choreographerPostFrameCallback(callbackWrapper.getFrameCallback()); &#125; else &#123; mHandler.postDelayed(callbackWrapper.getRunnable(), 0); &#125;&#125;// 实际上执行的是doFrame方法，在该方法中会遍历回调队列，执行所有回调的doFrame方法。private class ReactChoreographerDispatcher extends ChoreographerCompat.FrameCallback &#123; @Override public void doFrame(long frameTimeNanos) &#123; synchronized (ReactChoreographer.this) &#123; mHasPostedCallback = false; for (int i = 0; i &lt; mCallbackQueues.length; i++) &#123; int initialLength = mCallbackQueues[i].size(); for (int callback = 0; callback &lt; initialLength; callback++) &#123; mCallbackQueues[i].removeFirst().doFrame(frameTimeNanos); mTotalCallbacks--; &#125; &#125; maybeRemoveFrameCallback(); &#125; &#125;&#125; 也就是说当Activity触发OnResume，UIViewOperationQueue添加一个callback到回调队列中，并向主线程添加一个处理该回调队列的runnable，主线程执行该callback的doFrame方法。在该callback结束的时候，它将自己再次添加到回调队列中，然后回调队列向主线程添加执行runnable，主线程执行改callback。这样将无限的循环下去，直到该callback被移除，比如说当onPause发生时。 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556private class DispatchUIFrameCallback extends GuardedFrameCallback &#123; private static final int FRAME_TIME_MS = 16; private final int mMinTimeLeftInFrameForNonBatchedOperationMs; private DispatchUIFrameCallback( ReactContext reactContext, int minTimeLeftInFrameForNonBatchedOperationMs) &#123; super(reactContext); mMinTimeLeftInFrameForNonBatchedOperationMs = minTimeLeftInFrameForNonBatchedOperationMs; &#125; @Override public void doFrameGuarded(long frameTimeNanos) &#123; try &#123; dispatchPendingNonBatchedOperations(frameTimeNanos); &#125; finally &#123; &#125; // 处理UI操作队列 flushPendingBatches(); // 再次添加自己，无限循环的执行 ReactChoreographer.getInstance().postFrameCallback( ReactChoreographer.CallbackType.DISPATCH_UI, this); &#125; private void dispatchPendingNonBatchedOperations(long frameTimeNanos) &#123; while (true) &#123; // frameTimeNanos是主线程开始处理回调队列的时间，此处计算的目前为止，主线程此次执行该回调队列的时间 long timeLeftInFrame = FRAME_TIME_MS - ((System.nanoTime() - frameTimeNanos) / 1000000); //mMinTimeLeftInFrameForNonBatchedOperationMs默认为8 // 主线程处理一条消息的时间不要超过8秒，如果超过，则停止，等待下次消息再处理。 if (timeLeftInFrame &lt; mMinTimeLeftInFrameForNonBatchedOperationMs) &#123; break; &#125; UIOperation nextOperation; synchronized (mNonBatchedOperationsLock) &#123; // 如果操作队列空了，则退出while if (mNonBatchedOperations.isEmpty()) &#123; break; &#125; // 取出队列对头的操作 nextOperation = mNonBatchedOperations.pollFirst(); &#125; try &#123; long nonBatchedExecutionStartTime = SystemClock.uptimeMillis(); // 执行操作 nextOperation.execute(); mNonBatchedExecutionTotalTime += SystemClock.uptimeMillis() - nonBatchedExecutionStartTime; &#125; catch (Exception e) &#123; mIsInIllegalUIState = true; throw e; &#125; &#125; &#125;&#125; 真正的创建view (Java层)执行CreateViewOperation的execute方法，具体在NativeViewHierarchyManager的createView中。ViewManager是在ReactPackage的createViewManagers中注册的。 12345678910111213141516171819202122public synchronized void createView( ThemedReactContext themedContext, int tag, String className, @Nullable ReactStylesDiffMap initialProps) &#123; UiThreadUtil.assertOnUiThread(); try &#123; // 通过类名找到注册的ViewManager ViewManager viewManager = mViewManagers.get(className); // 通过指定的ViewManager来创建View View view = viewManager.createView(themedContext, mJSResponderHandler); mTagsToViews.put(tag, view); mTagsToViewManagers.put(tag, viewManager); view.setId(tag); if (initialProps != null) &#123; // 更新属性 viewManager.updateProperties(view, initialProps); &#125; &#125; finally &#123; Systrace.endSection(Systrace.TRACE_TAG_REACT_VIEW); &#125;&#125; 添加View到视图finalizeInitialChildren (C++层)在createInstance后，会就调用finalizeInitialChildren方法将该View添加到父布局中。 123456789finalizeInitialChildren: function(parentInstance) &#123; if (0 === parentInstance._children.length) return !1; var nativeTags = parentInstance._children.map(function(child) &#123; return "number" === typeof child ? child : child._nativeTag; &#125;); // 和创建一样，调用的是native方法 UIManager.setChildren(parentInstance._nativeTag, nativeTags); return !1;&#125;, 和上面一样通过Bridge走到c++，然后通过c++走到了java层，最终执行了UIManager.setChildren方法。 123456@ReactMethodpublic void setChildren( int viewTag, ReadableArray childrenTags) &#123; mUIImplementation.setChildren(viewTag, childrenTags);&#125; 在setChildren方法中最终会添加一个ManageChildrenOperation操作到UI操作队列中，这个操作队列和创建View时的操作队列不是同一个。这个操作队列专门处理UI的更新操作。 12345678public void enqueueManageChildren( int reactTag, @Nullable int[] indicesToRemove, @Nullable ViewAtIndex[] viewsToAdd, @Nullable int[] tagsToDelete) &#123; mOperations.add( new ManageChildrenOperation(reactTag, indicesToRemove, viewsToAdd, tagsToDelete));&#125; 这个队列中的操作是在c++调用onBatchComplete时执行，而onBatchComplete是在c++处理完js对native方法调用请求的时候会触发的。 onBatchComplete (Java层)1234567891011121314@Overridepublic void onBatchComplete() &#123; int batchId = mBatchId; mBatchId++; for (UIManagerModuleListener listener : mListeners) &#123; listener.willDispatchViewUpdates(this); &#125; try &#123; // 在该方法中会添加一个runable到DispatchUIRunnable中去，然后在DispatchUIFrameCallback中的flushPendingBatches方法会执行该runnable mUIImplementation.dispatchViewUpdates(batchId); &#125; finally &#123; Systrace.endSection(Systrace.TRACE_TAG_REACT_JAVA_BRIDGE); &#125;&#125; flushPendingBatches (Java层)此时执行的runnable是在onBatchComplete中添加的runnable，在该runnable中会处理UI操作队列中的操作。 12345678910111213141516171819202122232425262728293031323334Runnable runOperations = new Runnable() &#123; @Override public void run() &#123; try &#123; long runStartTime = SystemClock.uptimeMillis(); if (nonBatchedOperations != null) &#123; for (UIOperation op : nonBatchedOperations) &#123; op.execute(); &#125; &#125; if (batchedOperations != null) &#123; for (UIOperation op : batchedOperations) &#123; op.execute(); &#125; &#125; if (mIsProfilingNextBatch &amp;&amp; mProfiledBatchCommitStartTime == 0) &#123; mProfiledBatchCommitStartTime = commitStartTime; mProfiledBatchLayoutTime = layoutTime; mProfiledBatchDispatchViewUpdatesTime = dispatchViewUpdatesTime; mProfiledBatchRunStartTime = runStartTime; &#125; mNativeViewHierarchyManager.clearLayoutAnimation(); if (mViewHierarchyUpdateDebugListener != null) &#123; mViewHierarchyUpdateDebugListener.onViewHierarchyUpdateFinished(); &#125; &#125; catch (Exception e) &#123; mIsInIllegalUIState = true; throw e; &#125; finally &#123; Systrace.endSection(Systrace.TRACE_TAG_REACT_JAVA_BRIDGE); &#125; &#125; &#125;; 添加到父布局 (Java层)然后就会执行上面setChildren中添加的ManageChildrenOperation操作 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859public synchronized void manageChildren( int tag, @Nullable int[] indicesToRemove, @Nullable ViewAtIndex[] viewsToAdd, @Nullable int[] tagsToDelete) &#123; UiThreadUtil.assertOnUiThread(); final ViewGroup viewToManage = (ViewGroup) mTagsToViews.get(tag); final ViewGroupManager viewManager = (ViewGroupManager) resolveViewManager(tag); int lastIndexToRemove = viewManager.getChildCount(viewToManage); if (indicesToRemove != null) &#123; for (int i = indicesToRemove.length - 1; i &gt;= 0; i--) &#123; int indexToRemove = indicesToRemove[i]; View viewToRemove = viewManager.getChildAt(viewToManage, indexToRemove); if (mLayoutAnimationEnabled &amp;&amp; mLayoutAnimator.shouldAnimateLayout(viewToRemove) &amp;&amp; arrayContains(tagsToDelete, viewToRemove.getId())) &#123; // The view will be removed and dropped by the 'delete' layout animation // instead, so do nothing &#125; else &#123; viewManager.removeViewAt(viewToManage, indexToRemove); &#125; lastIndexToRemove = indexToRemove; &#125; &#125; if (viewsToAdd != null) &#123; for (int i = 0; i &lt; viewsToAdd.length; i++) &#123; ViewAtIndex viewAtIndex = viewsToAdd[i]; View viewToAdd = mTagsToViews.get(viewAtIndex.mTag); if (viewToAdd == null) &#123; &#125; // 添加view到父布局中 viewManager.addView(viewToManage, viewToAdd, viewAtIndex.mIndex); &#125; &#125; if (tagsToDelete != null) &#123; for (int i = 0; i &lt; tagsToDelete.length; i++) &#123; int tagToDelete = tagsToDelete[i]; final View viewToDestroy = mTagsToViews.get(tagToDelete); if (viewToDestroy == null) &#123; &#125; if (mLayoutAnimationEnabled &amp;&amp; mLayoutAnimator.shouldAnimateLayout(viewToDestroy)) &#123; mLayoutAnimator.deleteView(viewToDestroy, new LayoutAnimationListener() &#123; @Override public void onAnimationEnd() &#123; viewManager.removeView(viewToManage, viewToDestroy); dropView(viewToDestroy); &#125; &#125;); &#125; else &#123; dropView(viewToDestroy); &#125; &#125; &#125;&#125;]]></content>
      <categories>
        <category>刀枪棍棒（Android）</category>
        <category>五虎断门刀（Android原生）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[Android-ReactNative通信流程]]></title>
    <url>%2FAndroid-ReactNative%E5%90%AF%E5%8A%A8%E6%B5%81%E7%A8%8B%2F</url>
    <content type="text"><![CDATA[ReactAndroidReactActivity一个ReactActivity对应着一个RN组件(通过AppRegistry.registerComponent注册)，而ReactActivity的功能都包装在ReactActivityDelegate中。 ReactActivity实际上类似于一个浏览器窗口，在一个窗口内对一个网页访问，并进行页面的跳转和回退。 ReactRootViewReactRootView是ReactNative组件的根布局，是一个ViewGroup(继承自FrameLayout)，所有的RN视图都会被添加到该布局下，然后该布局会被添加到Activity上，因此每个ReactActivity都有着一个ReactRootView。RN组件内的页面跳转实际上就是在该ReactRootView上重绘。 ReactNativeHost每个RN组件的配置，设置jsbundle文件的文件名和js模块名称以及开发模式等。 getJSMainModuleName : 远程服务获取bundle时，获取的模块名称 getBundleAssetName : 本地加载bundle时，获取asset文件的文件名 getUseDeveloperSupport : 是否开启debug模式 getPackages : 配置js和native通信module，必须包含MainReactPackage ReactInstanceManager : 与js通信的管理类，CatalystInstance是一个异步JSC桥梁，提供了调用js方法的JavaAPI 每个ReactActivity都需要一个Host配置，对应不同的jsbundle配置。在ReactActivity中默认会去获取Application中的ReactNativeHost，默认Application需要继承ReactApplication接口，并实现其getReactNativeHost方法。如果我们不想在Application中实现该接口(必须我们有多个RN组件需要多个ReactNativeHost对象)，就必须重写ReactActivity中getReactNativeHost方法。 123protected ReactNativeHost getReactNativeHost() &#123; return ((ReactApplication) getPlainActivity().getApplication()).getReactNativeHost();&#125; 加载RN组件启动ReactActivity首先启动ReactActivity，创建ReactActivity时会同时创建ReactActivityDelegate，ReactActivity的getMainComponentName方法对应的是js组件的注册名称(js中AppRegistry.registerComponent注册时的appKey)，在ReactActivity的onCreate回调中，开始加载该js组件。 12345678910111213141516171819202122protected void onCreate(Bundle savedInstanceState) &#123; if (mMainComponentName != null) &#123; loadApp(mMainComponentName); &#125;&#125;protected void loadApp(String appKey) &#123; if (mReactRootView != null) &#123; throw new IllegalStateException("Cannot loadApp while app is already running."); &#125; // 创建根布局 mReactRootView = createRootView(); // 启动该js组件，加载jsbundle文件，并通过jsc来构建virtualDom来解析js组件 // 将js控件解析成对应的View，并添加到根布局上，组成视图树。 mReactRootView.startReactApplication( // 创建js通信管理器 getReactNativeHost().getReactInstanceManager(), appKey, getLaunchOptions()); // 将解析完成的布局放到Activity中渲染 getPlainActivity().setContentView(mReactRootView);&#125; 创建ReactRootViewReactRootView继承FrameLayout，是整个RN组件的根布局，解析出来的View都会添加到该布局上。该View主要处理了测量和事件的分发拦截，包括触摸、手势、焦点等。还设置了OnGlobalLayout监听，该方法会在布局重新layout或者系统UI发生改变时触发(具体在ViewRootImpl.performTraversals中)，在该监听中将发生的改变通知给js模块。 12345678910111213@Overridepublic void onGlobalLayout() &#123; if (mReactInstanceManager == null || !mIsAttachedToInstance || mReactInstanceManager.getCurrentReactContext() == null) &#123; return; &#125; // 键盘状态改变 checkForKeyboardEvents(); // 屏幕方向改变 checkForDeviceOrientationChanges(); // 屏幕分辨率改变 checkForDeviceDimensionsChanges();&#125; 创建ReactInstanceManagerReactInstanceManager是一个CatalystInstance实例的管理者，而CatalysInstance是一个异步的JSC桥梁，负责与js进行通信，调用js方法。 1234567891011121314151617181920212223protected ReactInstanceManager createReactInstanceManager() &#123; // 获取ReactNativeHost中的配置来创建 ReactInstanceManagerBuilder builder = ReactInstanceManager.builder() .setApplication(mApplication) .setJSMainModulePath(getJSMainModuleName()) .setUseDeveloperSupport(getUseDeveloperSupport()) .setRedBoxHandler(getRedBoxHandler()) .setJavaScriptExecutorFactory(getJavaScriptExecutorFactory()) .setUIImplementationProvider(getUIImplementationProvider()) .setJSIModulesPackage(getJSIModulePackage()) .setInitialLifecycleState(LifecycleState.BEFORE_CREATE); for (ReactPackage reactPackage : getPackages()) &#123; builder.addPackage(reactPackage); &#125; String jsBundleFile = getJSBundleFile(); if (jsBundleFile != null) &#123; builder.setJSBundleFile(jsBundleFile); &#125; else &#123; builder.setBundleAssetName(Assertions.assertNotNull(getBundleAssetName())); &#125; ReactInstanceManager reactInstanceManager = builder.build(); return reactInstanceManager;&#125; 123456789101112131415161718192021222324252627282930public ReactInstanceManager build() &#123; // 创建ReactInstanceManager return new ReactInstanceManager( mApplication, // 应用Application mCurrentActivity, // 当前ReactActivity mDefaultHardwareBackBtnHandler, // 处理js不处理的硬件回退事件 mJavaScriptExecutorFactory == null // JSC执行器 // 用来创建JSC执行器的工厂 ? new JSCJavaScriptExecutorFactory(appName, deviceName) : mJavaScriptExecutorFactory, (mJSBundleLoader == null &amp;&amp; mJSBundleAssetUrl != null) // JSBundle加载器 ? JSBundleLoader.createAssetLoader( // 创建一个匿名实例，从Assets中加载jsbundle mApplication, mJSBundleAssetUrl, false /*Asynchronous*/) : mJSBundleLoader, mJSMainModulePath, // Host中配置的js模块名称 mPackages, // Host中配置的通信module mUseDeveloperSupport, // Host中配置的debug mBridgeIdleDebugListener, // Bridge空闲状态监听 Assertions.assertNotNull(mInitialLifecycleState, "Initial lifecycle state was not set"), mUIImplementationProvider, // 用来在UIManagerModule中创建UIImplementation mNativeModuleCallExceptionHandler, // JS调用native出异常的处理类 mRedBoxHandler, // RedBox就是JS错误的红色页面，该接口可以拦截和处理其页面上的错误信息。 mLazyNativeModulesEnabled, mLazyViewManagersEnabled, mDevBundleDownloadListener, mMinNumShakes, mMinTimeLeftInFrameForNonBatchedOperationMs, mJSIModulesPackage);&#125; 当使用JSBundleLoader去加载jsbundle文件时，就会调用该匿名实例的loadScript方法从assets中加载。 123456789101112public static JSBundleLoader createAssetLoader( final Context context, final String assetUrl, final boolean loadSynchronously) &#123; return new JSBundleLoader() &#123; @Override public String loadScript(CatalystInstanceImpl instance) &#123; instance.loadScriptFromAssets(context.getAssets(), assetUrl, loadSynchronously); return assetUrl; &#125; &#125;;&#125; 1234567891011121314151617181920212223242526272829303132ReactInstanceManager(...) &#123; // 构造函数 ... synchronized (mPackages) &#123; PrinterHolder.getPrinter() .logMessage(ReactDebugOverlayTags.RN_CORE, "RNCore: Use Split Packages"); mPackages.add( // 添加CoreModulePackage通信模块 new CoreModulesPackage( this, new DefaultHardwareBackBtnHandler() &#123; @Override public void invokeDefaultOnBackPressed() &#123; ReactInstanceManager.this.invokeDefaultOnBackPressed(); &#125; &#125;, uiImplementationProvider, lazyViewManagersEnabled, minTimeLeftInFrameForNonBatchedOperationMs)); if (mUseDeveloperSupport) &#123; mPackages.add(new DebugCorePackage()); &#125; mPackages.addAll(packages); &#125; mJSIModulePackage = jsiModulePackage; // ReactChoreographer是对Choreographer的包装，用来在绘制一帧的时候执行一些自定义的回调 ReactChoreographer.initialize(); if (mUseDeveloperSupport) &#123; mDevSupportManager.startInspector(); &#125; &#125; 启动jsbundle组件在创建完ReactInstanceManager后，会通过它在后台异步创建ReactContext，最终逻辑在ReactInstanceManager.runCreateReactContextOnNewThread中。 12345678910111213141516@ThreadConfined(UI)private void recreateReactContextInBackground( JavaScriptExecutorFactory jsExecutorFactory, JSBundleLoader jsBundleLoader) &#123; final ReactContextInitParams initParams = new ReactContextInitParams( jsExecutorFactory, jsBundleLoader); if (mCreateReactContextThread == null) &#123; // 此时代表ReactContext已经创建过程已经结束或未开始，可以直接重新创建 runCreateReactContextOnNewThread(initParams); &#125; else &#123; // 此时代表正在创建ReactContext，需要等待 mPendingReactContextInitParams = initParams; &#125;&#125; 12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455565758 @ThreadConfined(UI) private void runCreateReactContextOnNewThread(final ReactContextInitParams initParams) &#123; UiThreadUtil.assertOnUiThread(); mCreateReactContextThread = new Thread( new Runnable() &#123; @Override public void run() &#123; ... mHasStartedCreatingInitialContext = true; try &#123; // 提高线程的优先级，避免被UI线程中途抢占 Process.setThreadPriority(Process.THREAD_PRIORITY_DISPLAY); // 创建ReactApplicationContext final ReactApplicationContext reactApplicationContext = createReactContext( // 通过工厂创建JSCScriptExecutor initParams.getJsExecutorFactory().create(), // 此处的Loader就是上面的匿名Loader initParams.getJsBundleLoader()); // 在此之前如果其他地方需要重新创建Context，那么会先将参数放入mPendingReactContextInitParams中，再讲其放到主线程消息队列中稍后再次执行该方法。 // 在此之后如果需要重建，则重新创建一个线程启动就行 mCreateReactContextThread = null; final Runnable maybeRecreateReactContextRunnable = new Runnable() &#123; @Override public void run() &#123; if (mPendingReactContextInitParams != null) &#123; //看见标记还在，重新创建ReactContext runCreateReactContextOnNewThread(mPendingReactContextInitParams); mPendingReactContextInitParams = null; &#125; &#125; &#125;; Runnable setupReactContextRunnable = new Runnable() &#123; @Override public void run() &#123; try &#123; // 启动ReactContext setupReactContext(reactApplicationContext); &#125; catch (Exception e) &#123; mDevSupportManager.handleException(e); &#125; &#125; &#125;;// 在native线程中执行启动ReactContext任务 reactApplicationContext.runOnNativeModulesQueueThread(setupReactContextRunnable); // 在主线程中启动重新创建任务 UiThreadUtil.runOnUiThread(maybeRecreateReactContextRunnable); &#125; catch (Exception e) &#123; mDevSupportManager.handleException(e); &#125; &#125; &#125;); mCreateReactContextThread.start(); &#125; 创建ReactContextReactContext是对ContextWrapper的再次包装，实际上就是一个上下文，而ReactApplicationContext实际上就是ApplicationContext。 12345678910111213141516171819202122232425262728293031323334353637383940414243 private ReactApplicationContext createReactContext( JavaScriptExecutor jsExecutor, JSBundleLoader jsBundleLoader) &#123; final ReactApplicationContext reactContext = new ReactApplicationContext(mApplicationContext); NativeModuleCallExceptionHandler exceptionHandler = mNativeModuleCallExceptionHandler != null ? mNativeModuleCallExceptionHandler : mDevSupportManager; reactContext.setNativeModuleCallExceptionHandler(exceptionHandler);// 解析mPackages中包含的module NativeModuleRegistry nativeModuleRegistry = processPackages(reactContext, mPackages, false);// 创建CatalysInstance的实现类实例 CatalystInstanceImpl.Builder catalystInstanceBuilder = new CatalystInstanceImpl.Builder() // 创建js和native消息执行的线程命名配置 .setReactQueueConfigurationSpec(ReactQueueConfigurationSpec.createDefault()) .setJSExecutor(jsExecutor) .setRegistry(nativeModuleRegistry) // 解析mPackages后得到的已注册的module集合 .setJSBundleLoader(jsBundleLoader) .setNativeModuleCallExceptionHandler(exceptionHandler); final CatalystInstance catalystInstance; try &#123; catalystInstance = catalystInstanceBuilder.build(); &#125; finally &#123; &#125; if (mJSIModulePackage != null) &#123; catalystInstance.addJSIModules(mJSIModulePackage .getJSIModules(reactContext, catalystInstance.getJavaScriptContextHolder())); &#125; if (mBridgeIdleDebugListener != null) &#123; catalystInstance.addBridgeIdleDebugListener(mBridgeIdleDebugListener); &#125; if (Systrace.isTracing(TRACE_TAG_REACT_APPS | TRACE_TAG_REACT_JS_VM_CALLS)) &#123; catalystInstance.setGlobalVariable("__RCTProfileIsProfiling", "true"); &#125; // 加载jsbundle catalystInstance.runJSBundle(); // 将CatalystInstance实例和ReactContext关联 reactContext.initializeWithInstance(catalystInstance); return reactContext; &#125; 创建CatalysInstanceImplCatalysInstanceImpl是与js通信的bridge的真正实现。ReactQueueConfigurationImpl中保存了三个线程，分别是UI线程，native执行的线程，js执行的线程。 1234567891011121314151617181920212223242526272829 private CatalystInstanceImpl( final ReactQueueConfigurationSpec reactQueueConfigurationSpec, final JavaScriptExecutor jsExecutor, final NativeModuleRegistry nativeModuleRegistry, final JSBundleLoader jsBundleLoader, NativeModuleCallExceptionHandler nativeModuleCallExceptionHandler) &#123; mHybridData = initHybrid(); // 调用jni方法创建HybridData对象// 根据上面的js和native消息执行的线程命名配置，创建真正的线程实现MessageQueueThreadImpl，并启动这些线程，每个线程中都有一个looper在轮训消息。在MessageQueueThreadImpl中有一个mHandler，负责向这些线程的looper发送消息。 mReactQueueConfiguration = ReactQueueConfigurationImpl.create( reactQueueConfigurationSpec, new NativeExceptionHandler()); mBridgeIdleListeners = new CopyOnWriteArrayList&lt;&gt;(); mNativeModuleRegistry = nativeModuleRegistry; mJSModuleRegistry = new JavaScriptModuleRegistry(); mJSBundleLoader = jsBundleLoader; mNativeModuleCallExceptionHandler = nativeModuleCallExceptionHandler; mNativeModulesQueueThread = mReactQueueConfiguration.getNativeModulesQueueThread(); mTraceListener = new JSProfilerTraceListener(this);// 调用jni方法 initializeBridge( new BridgeCallback(this), jsExecutor, mReactQueueConfiguration.getJSQueueThread(), mNativeModulesQueueThread, mNativeModuleRegistry.getJavaModules(this), mNativeModuleRegistry.getCxxModules()); mJavaScriptContextHolder = new JavaScriptContextHolder(getJavaScriptContext()); &#125; 在加载CatalystInstanceImpl类时，加载了HybridData类，就会加载libfb.so，然后会执行上面说的动态注册jni方法。12345public class HybridData &#123; static &#123; SoLoader.loadLibrary("fb"); &#125;&#125; initHybrid (C++层)根据注册信息，我们知道会执行CatalysInstanceImpl.cpp中的initHybrid函数。该函数会在c++层创建一个com/facebook/jni/HybridData.java对象，然后将其返回保存在java层的CatalysInstanceImpl对象中。然后还会创建一个Hybrid.h/BaseHybridClass对象，然后将其对象指针设置给刚刚创建的HybridData对象(HybridData.mDestructor.mNativePointer)。 mNativePointer变量只是用来维护当前HybridData实例是否可用的标记(通过HybridData.isValid方法)，当退出等操作时，会执行HybridData.resetNative来清空释放标记。 12345678910111213141516171819202122 # src/main/jni/react/jni/CatalysInstanceImpl.cpp jni::local_ref&lt;CatalystInstanceImpl::jhybriddata&gt; CatalystInstanceImpl::initHybrid( jni::alias_ref&lt;jclass&gt;) &#123; return makeCxxInstance(); &#125; # src/main/jni/first-party/fb/include/fb/fbjni/Hybrid.h static local_ref&lt;detail::HybridData&gt; makeHybridData(std::unique_ptr&lt;T&gt; cxxPart) &#123; // 创建一个com/facebook/jni/HybridData.java对象 auto hybridData = detail::HybridData::create(); setNativePointer(hybridData, std::move(cxxPart)); return hybridData; &#125;# src/main/jni/first-party/fb/include/fb/fbjni/CoreClasses-inl.h static local_ref&lt;JC&gt; newInstance(Args... args) &#123; // 获取com/facebook/jni/HybridData的class引用 static auto cls = JC::javaClassStatic(); static auto constructor = cls-&gt;template getConstructor&lt;typename JC::javaobject(Args...)&gt;(); // 创建该class实例 return cls-&gt;newObject(constructor, args...);&#125; 123456789101112131415161718192021222324252627# src/main/jni/first-party/fb/include/fb/fbjni/Hybrid.h template&lt;typename T&gt;void setNativePointer(T t, std::unique_ptr&lt;detail::BaseHybridClass&gt; new_value) &#123; getHolder(t)-&gt;setNativePointer(std::move(new_value));&#125;template&lt;typename T&gt;local_ref&lt;HybridDestructor&gt; getHolder(T t) &#123; // 获取com/facebook/jni/HybridData.java类中的mDestructor字段的值 static auto holderField = t-&gt;getClass()-&gt;template getField&lt;HybridDestructor::javaobject&gt;("mDestructor"); // 返回的是转换后的HybridDestructor对象 return t-&gt;getFieldValue(holderField);&#125;template &lt;typename T=detail::BaseHybridClass&gt;void setNativePointer(std::unique_ptr&lt;T&gt; new_value) &#123; // 获取com/facebook/jni/HybridData$Destructor类中的mNativePointer字段引用 static auto pointerField = javaClassStatic()-&gt;getField&lt;jlong&gt;("mNativePointer"); // 获取当前对象中该字段的值 auto old_value = std::unique_ptr&lt;T&gt;(reinterpret_cast&lt;T*&gt;(getFieldValue(pointerField))); if (new_value &amp;&amp; old_value) &#123; FBCRASH("Attempt to set C++ native pointer twice"); &#125; // new_value是BaseHybridClass对象实例的指针，将该指针设置到mNativePointer字段中 // 也就是设置到com/facebook/jni/HybridData$Destructor.mNativePointer中 setFieldValue(pointerField, reinterpret_cast&lt;jlong&gt;(new_value.release()));&#125; initializeBridge (C++层)在initializeBridge方法中，主要是创建了NativeToJsBridge对象，并将其实例保存在CatalystInstanceImpl.instance.nativeToJsBridge_中。 1234567891011121314151617181920212223242526272829# src/main/jni/react/jni/CatalysInstanceImpl.cppvoid CatalystInstanceImpl::initializeBridge( jni::alias_ref&lt;ReactCallback::javaobject&gt; callback, // This executor is actually a factory holder. JavaScriptExecutorHolder* jseh, jni::alias_ref&lt;JavaMessageQueueThread::javaobject&gt; jsQueue, jni::alias_ref&lt;JavaMessageQueueThread::javaobject&gt; nativeModulesQueue, jni::alias_ref&lt;jni::JCollection&lt;JavaModuleWrapper::javaobject&gt;::javaobject&gt; javaModules, jni::alias_ref&lt;jni::JCollection&lt;ModuleHolder::javaobject&gt;::javaobject&gt; cxxModules) &#123; // 创建c++层的native消息线程 moduleMessageQueue_ = std::make_shared&lt;JMessageQueueThread&gt;(nativeModulesQueue); // 将modules中的module转换成NativeModule对象，并绑定了module执行的线程也就是上面的native消息线程 // 创建ModuleRegistry对象，内部维护modules列表 moduleRegistry_ = std::make_shared&lt;ModuleRegistry&gt;( buildNativeModuleList( std::weak_ptr&lt;Instance&gt;(instance_), javaModules, cxxModules, moduleMessageQueue_)); // instance 是cxxreact/Instance.h中定义的，在CatalystInstanceImpl创建时创建该对象并赋值给instance。 instance_-&gt;initializeBridge // 创建JInstanceCallback对象，绑定传进来的callback和所执行的线程(native消息线程) folly::make_unique&lt;JInstanceCallback&gt;( callback, moduleMessageQueue_), jseh-&gt;getExecutorFactory(), // 得到JSCExecutorFactory folly::make_unique&lt;JMessageQueueThread&gt;(jsQueue),//创建c++层的js消息线程 moduleRegistry_);&#125; 12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152# cxxreact/Instance.cppvoid Instance::initializeBridge( std::unique_ptr&lt;InstanceCallback&gt; callback, std::shared_ptr&lt;JSExecutorFactory&gt; jsef, std::shared_ptr&lt;MessageQueueThread&gt; jsQueue, std::shared_ptr&lt;ModuleRegistry&gt; moduleRegistry) &#123; callback_ = std::move(callback); moduleRegistry_ = std::move(moduleRegistry); // 在js消息线程中创建NativeToJsBridge对象 jsQueue-&gt;runOnQueueSync([this, &amp;jsef, jsQueue]() mutable &#123; // 在js消息线程中执行下列代码 // 创建NativeToJsBridge对象 nativeToJsBridge_ = folly::make_unique&lt;NativeToJsBridge&gt;( jsef.get(), moduleRegistry_, jsQueue, callback_); std::lock_guard&lt;std::mutex&gt; lock(m_syncMutex); m_syncReady = true; m_syncCV.notify_all(); &#125;); CHECK(nativeToJsBridge_);&#125;# src/main/jni/react/jni/JMessageQueueThread.cppvoid JMessageQueueThread::runOnQueueSync(std::function&lt;void()&gt;&amp;&amp; runnable) &#123; // 获取com/facebook/react/bridge/queue/MessageQueueThread.java对象的class引用，然后拿到其isOnThread方法引用 static auto jIsOnThread = JavaMessageQueueThread::javaClassStatic()-&gt; getMethod&lt;jboolean()&gt;("isOnThread"); if (jIsOnThread(m_jobj)) &#123; wrapRunnable(std::move(runnable))(); &#125; else &#123; std::mutex signalMutex; std::condition_variable signalCv; bool runnableComplete = false; // 如果当前不是在主线程，通过获取方法引用来调用 // com/facebook/react/bridge/queue/MessageQueueThread.java对象的runOnQueue方法 // 该方法会将runnable通过Handler发送到js消息线程的消息队列中 runOnQueue([&amp;] () mutable &#123; std::lock_guard&lt;std::mutex&gt; lock(signalMutex); runnable(); runnableComplete = true; signalCv.notify_one(); &#125;); std::unique_lock&lt;std::mutex&gt; lock(signalMutex); // 当前线程等待js线程执行完该runnable signalCv.wait(lock, [&amp;runnableComplete] &#123; return runnableComplete; &#125;); &#125;&#125; 加载并运行jsbundle通过JSBundleLoader去加载jsbundle.js文件，默认是从assets中加载，可以通过在ReactInstanceManagerBuilder中提前设置自己的JSBundleLoader，实现自己的加载逻辑。 12345@Overridepublic void runJSBundle() &#123; // 此处的Loader是上面的匿名Loader mJSBundleLoader.loadScript(CatalystInstanceImpl.this);&#125; 所以执行的是instance.loadScriptFromAssets(context.getAssets(), assetUrl, loadSynchronously);最终调用的是jni方法jniLoadScriptFromAssets去加载。 jniLoadScriptFromAssets (C++层)所谓unbundle，是是对bundle的优化，当一个bundle非常大时，如果一次性全部加载整个可能非常慢，也占内存，可以通过编译时开启unbundle特性来解决，将bundle分割成多个文件，使用时加载需要的。 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748# src/main/jni/react/jni/CatalysInstanceImpl.cppvoid CatalystInstanceImpl::jniLoadScriptFromAssets( // AssetManager引用 jni::alias_ref&lt;JAssetManager::javaobject&gt; assetManager, // 文件名称 const std::string&amp; assetURL, // 是否同步加载，默认不同步加载 bool loadSynchronously) &#123; const int kAssetsLength = 9; // strlen("assets://"); auto sourceURL = assetURL.substr(kAssetsLength); // 通过AssetManager.open加载文件流 auto manager = extractAssetManager(assetManager); auto script = loadScriptFromAssets(manager, sourceURL); if (JniJSModulesUnbundle::isUnbundle(manager, sourceURL)) &#123; // 加载的是unbundle auto bundle = JniJSModulesUnbundle::fromEntryFile(manager, sourceURL); auto registry = RAMBundleRegistry::singleBundleRegistry(std::move(bundle)); instance_-&gt;loadRAMBundle( std::move(registry), std::move(script), sourceURL, loadSynchronously); return; &#125; else &#123; instance_-&gt;loadScriptFromString(std::move(script), sourceURL, loadSynchronously); &#125;&#125;# cxxreact/NativeToJsBridge.cppvoid NativeToJsBridge::loadApplication( std::unique_ptr&lt;RAMBundleRegistry&gt; bundleRegistry, std::unique_ptr&lt;const JSBigString&gt; startupScript, std::string startupScriptSourceURL) &#123; // 将消息发送到js消息线程中处理 runOnExecutorQueue( [bundleRegistryWrap=folly::makeMoveWrapper(std::move(bundleRegistry)), startupScript=folly::makeMoveWrapper(std::move(startupScript)), startupScriptSourceURL=std::move(startupScriptSourceURL)] (JSExecutor* executor) mutable &#123; auto bundleRegistry = bundleRegistryWrap.move(); if (bundleRegistry) &#123; executor-&gt;setBundleRegistry(std::move(bundleRegistry)); &#125; // 在js消息线程中去解析js文件流 executor-&gt;loadApplicationScript(std::move(*startupScript), std::move(startupScriptSourceURL)); &#125;);&#125; 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354# cxxreact/JSCExecutor.cppvoid JSCExecutor::loadApplicationScript( std::unique_ptr&lt;const JSBigString&gt; script, std::string sourceURL) &#123; SystraceSection s( "JSCExecutor::loadApplicationScript", "sourceURL", sourceURL); std::string scriptName = simpleBasename(sourceURL); ReactMarker::logTaggedMarker( ReactMarker::RUN_JS_BUNDLE_START, scriptName.c_str()); String jsSourceURL(m_context, sourceURL.c_str());// TODO t15069155: reduce the number of overrides here#ifdef WITH_FBJSCEXTENSIONS // WITH_FBJSCEXTENSIONS宏是否被定义，rn的jsc走这 if (auto fileStr = dynamic_cast&lt;const JSBigFileString*&gt;(script.get())) &#123; JSContextLock lock(m_context); JSLoadSourceStatus jsStatus; auto bcSourceCode = JSCreateSourceCodeFromFile( fileStr-&gt;fd(), jsSourceURL, nullptr, &amp;jsStatus); switch (jsStatus) &#123; case JSLoadSourceIsCompiled: // 如果js代码已经编译过 if (!bcSourceCode) &#123; throw std::runtime_error("Unexpected error opening compiled bundle"); &#125; // 运行js代码 evaluateSourceCode(m_context, bcSourceCode, jsSourceURL); // js调用native请求处理 flush(); ReactMarker::logMarker(ReactMarker::CREATE_REACT_CONTEXT_STOP); ReactMarker::logTaggedMarker( ReactMarker::RUN_JS_BUNDLE_STOP, scriptName.c_str()); return; case JSLoadSourceErrorVersionMismatch: throw RecoverableError(explainLoadSourceStatus(jsStatus)); case JSLoadSourceErrorOnRead: case JSLoadSourceIsNotCompiled: // Not bytecode, fall through. break; &#125; &#125;#elif defined(__APPLE__) // IOS使用系统的JSC ...#endif &#123; ...&#125; flush (C++层)flush方法用于处理js中对native模块调用请求，m_flushedQueueJS执行的是MessageQueue.js中的flushedQueue方法，获取_queue数组，该数组中保存了js中对native模块的调用请求。在callNativeModules中会对请求列表一一处理。 123456789101112131415161718# cxxreact/JSCExecutor.cppvoid JSCExecutor::flush() &#123; SystraceSection s("JSCExecutor::flush"); if (m_flushedQueueJS) &#123; callNativeModules(m_flushedQueueJS-&gt;callAsFunction(&#123;&#125;)); return; &#125; auto global = Object::getGlobalObject(m_context); auto batchedBridgeValue = global.getProperty("__fbBatchedBridge"); if (!batchedBridgeValue.isUndefined()) &#123; bindBridge(); // 从js的调用队列中获取调用请求，然后依次调用指定module的方法 callNativeModules(m_flushedQueueJS-&gt;callAsFunction(&#123;&#125;)); &#125; else if (m_delegate) &#123; callNativeModules(Value::makeNull(m_context)); &#125;&#125; 启动ReactContext在native消息线程中执行。 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647 private void setupReactContext(final ReactApplicationContext reactContext) &#123; CatalystInstance catalystInstance = Assertions.assertNotNull(reactContext.getCatalystInstance());// 安装所有的NativeModule,调用NativeModule.initialize catalystInstance.initialize(); mDevSupportManager.onNewReactContextCreated(reactContext); mMemoryPressureRouter.addMemoryPressureListener(catalystInstance); moveReactContextToCurrentLifecycleState(); synchronized (mAttachedRootViews) &#123; for (ReactRootView rootView : mAttachedRootViews) &#123; // 将ReactRootView添加到UIManager中作为RootView,并启动js组件 attachRootViewToInstance(rootView, catalystInstance); &#125; &#125; ReactInstanceEventListener[] listeners = new ReactInstanceEventListener[mReactInstanceEventListeners.size()]; final ReactInstanceEventListener[] finalListeners = mReactInstanceEventListeners.toArray(listeners); UiThreadUtil.runOnUiThread( new Runnable() &#123; @Override public void run() &#123; for (ReactInstanceEventListener listener : finalListeners) &#123; listener.onReactContextInitialized(reactContext); &#125; &#125; &#125;); // 将js线程和native线程的优先级恢复默认 reactContext.runOnJSQueueThread( new Runnable() &#123; @Override public void run() &#123; Process.setThreadPriority(Process.THREAD_PRIORITY_DEFAULT); &#125; &#125;); reactContext.runOnNativeModulesQueueThread( new Runnable() &#123; @Override public void run() &#123; Process.setThreadPriority(Process.THREAD_PRIORITY_DEFAULT); &#125; &#125;); &#125; 启动js组件将RootView添加到UIManager中后，就调用js组件的进入点，启动js组件。具体调用原理可以看上一篇的Android-ReactNative通信流程。 1234567891011121314151617181920212223242526272829303132private void defaultJSEntryPoint() &#123; try &#123; if (mReactInstanceManager == null || !mIsAttachedToInstance) &#123; return; &#125; ReactContext reactContext = mReactInstanceManager.getCurrentReactContext(); if (reactContext == null) &#123; return; &#125; CatalystInstance catalystInstance = reactContext.getCatalystInstance(); WritableNativeMap appParams = new WritableNativeMap(); appParams.putDouble("rootTag", getRootViewTag()); @Nullable Bundle appProperties = getAppProperties(); if (appProperties != null) &#123; appParams.putMap("initialProps", Arguments.fromBundle(appProperties)); &#125; if (getUIManagerType() == FABRIC) &#123; appParams.putBoolean("fabric", true); &#125; mShouldLogContentAppeared = true; String jsAppModuleName = getJSModuleName(); // 获取AppRegistry这个js模块，运行期runApplication方法，实际上运行的是AppRegistry.js的runApplication函数 catalystInstance.getJSModule(AppRegistry.class).runApplication(jsAppModuleName, appParams); &#125; finally &#123; Systrace.endSection(TRACE_TAG_REACT_JAVA_BRIDGE); &#125;&#125; js组件运行 (JS层)12345678910111213141516171819202122runApplication(appKey: string, appParameters: any): void &#123; const msg = 'Running application "' + appKey + '" with appParams: ' + JSON.stringify(appParameters) + '. ' + '__DEV__ === ' + String(__DEV__) + ', development-level warning are ' + (__DEV__ ? 'ON' : 'OFF') + ', performance optimizations are ' + (__DEV__ ? 'OFF' : 'ON'); infoLog(msg); BugReporting.addSource( 'AppRegistry.runApplication' + runCount++, () =&gt; msg, ); ... // 运行 runnables[appKey].run(appParameters);&#125;,]]></content>
      <categories>
        <category>刀枪棍棒（Android）</category>
        <category>五虎断门刀（Android原生）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[Android-ReactNative通信流程]]></title>
    <url>%2FAndroid-ReactNative%E9%80%9A%E4%BF%A1%E6%B5%81%E7%A8%8B%2F</url>
    <content type="text"><![CDATA[JNI注册Android中注册JNI方法有两种方式，一种是静态注册，一种是动态注册。 动态注册因为JNI允许我们提供一个函数映射表(native函数和jni函数对应表)。而在执行System.loadLibrary加载so库时，会执行该so的JNI_OnLoad函数，利用这个时机可以动态注册JIN方法。具体实现的话是通过JNINativeMethod结构保存映射关系，然后通过RegisterNatives函数来将该映射关系注册。 静态注册如果没有在JNI_OnLoad中将JNI方法注册(将方法在进程中的地址增加到ClassObject-&gt;directMethods中)，则在调用的时候解析javah风格的函数(比如Java_com_example_hellojni_HelloJni_stringFromJNI)，进行静态注册。静态注册根据函数名来建立java方法和jni函数的对应关系。静态注册需要根据方法名本地搜索，比较耗时。 RN中的JNI注册JNI_OnLoad12345678910111213141516171819202122232425# src/main/jni/react/jni/OnLoad.cppextern "C" JNIEXPORT jint JNI_OnLoad(JavaVM* vm, void* reserved) &#123; // 调用各个类的注册方法 return initialize(vm, [] &#123; gloginit::initialize(); JSCJavaScriptExecutorHolder::registerNatives(); ProxyJavaScriptExecutorHolder::registerNatives(); CatalystInstanceImpl::registerNatives(); CxxModuleWrapperBase::registerNatives(); CxxModuleWrapper::registerNatives(); JCxxCallbackImpl::registerNatives(); NativeArray::registerNatives(); NativeDeltaClient::registerNatives(); ReadableNativeArray::registerNatives(); WritableNativeArray::registerNatives(); NativeMap::registerNatives(); ReadableNativeMap::registerNatives(); WritableNativeMap::registerNatives(); ReadableNativeMapKeySetIterator::registerNatives(); #ifdef WITH_INSPECTOR JInspector::registerNatives(); #endif &#125;);&#125; CatalystInstanceImpl.registerNatives先拿CatalystInstanceImpl来举例子，先准备好映射数据，然后构建映射表，再通过FindClass获取对应java类的引用，通过这些参数使用RegisterNatives函数来注册。 1234567891011# src/main/jni/react/jni/CatalystInstanceImpl.h class CatalystInstanceImpl : public jni::HybridClass&lt;CatalystInstanceImpl&gt; &#123; // 该类对应的Java类，通过该路径来加载java类的class引用 public: static constexpr auto kJavaDescriptor = "Lcom/facebook/react/bridge/CatalystInstanceImpl;"; static jni::local_ref&lt;jhybriddata&gt; initHybrid(jni::alias_ref&lt;jclass&gt;); ~CatalystInstanceImpl() override; static void registerNatives();&#125; 123456789101112131415161718192021# src/main/jni/react/jni/CatalysInstanceImpl.cpp void CatalystInstanceImpl::registerNatives() &#123; registerHybrid(&#123; // 返回&#123;java方法名，java类命名空间(CatalysInstanceImpl.kJavaDescriptor),JNIMethod指针&#125; makeNativeMethod("initHybrid", CatalystInstanceImpl::initHybrid), makeNativeMethod("initializeBridge", CatalystInstanceImpl::initializeBridge), makeNativeMethod("jniExtendNativeModules", CatalystInstanceImpl::extendNativeModules), makeNativeMethod("jniSetSourceURL", CatalystInstanceImpl::jniSetSourceURL), makeNativeMethod("jniRegisterSegment", CatalystInstanceImpl::jniRegisterSegment), makeNativeMethod("jniLoadScriptFromAssets", CatalystInstanceImpl::jniLoadScriptFromAssets), makeNativeMethod("jniLoadScriptFromFile", CatalystInstanceImpl::jniLoadScriptFromFile), makeNativeMethod("jniLoadScriptFromDeltaBundle", CatalystInstanceImpl::jniLoadScriptFromDeltaBundle), makeNativeMethod("jniCallJSFunction", CatalystInstanceImpl::jniCallJSFunction), makeNativeMethod("jniCallJSCallback", CatalystInstanceImpl::jniCallJSCallback), makeNativeMethod("setGlobalVariable", CatalystInstanceImpl::setGlobalVariable), makeNativeMethod("getJavaScriptContext", CatalystInstanceImpl::getJavaScriptContext), makeNativeMethod("jniHandleMemoryPressure", CatalystInstanceImpl::handleMemoryPressure), &#125;); JNativeRunnable::registerNatives();&#125; registerHybridjavaClassStatic函数是通过FindClass(name)来加载对应java类的class引用(加载的类名就是上面定义的kJavaDescriptor)，得到jclass对象，而JClass是对它的包装。 123static void registerHybrid(std::initializer_list&lt;NativeMethod&gt; methods) &#123; javaClassStatic()-&gt;registerNatives(methods);&#125; JClass.registerNatives将JNI函数映射表与该jclass对象绑定 1234567891011121314151617# src/main/jni/first-party/fb/include/fb/fbjni/CoreClases-inl.hinline void JClass::registerNatives(std::initializer_list&lt;NativeMethod&gt; methods) &#123; const auto env = internal::getEnv(); // 构建native和jni方法的映射关系表 JNINativeMethod jnimethods[methods.size()]; size_t i = 0; for (auto it = methods.begin(); it &lt; methods.end(); ++it, ++i) &#123; // JNINativeMethod结构包含方法名、方法签名、jni函数指针 jnimethods[i].name = it-&gt;name; jnimethods[i].signature = it-&gt;descriptor.c_str(); jnimethods[i].fnPtr = reinterpret_cast&lt;void*&gt;(it-&gt;wrapper); &#125; // 通过RegisterNatives函数来注册，参数为(java class引用，映射表，方法数) // 所以这里的self()就是jclass auto result = env-&gt;RegisterNatives(self(), jnimethods, methods.size()); FACEBOOK_JNI_THROW_EXCEPTION_IF(result != JNI_OK);&#125; Java调用Js 在Java层注册Js层module在Java层提供了一系列的Js层module的接口类，里面定义了可以调用Js层module的方法。在Java层中通过代理创建这些接口的实例，让外层直接调用这些接口的方法。而方法的真正实现都会通过jni转向c++层，而后转向js层。 123456// 这就是AppRegistry.js的接口类，里面定义了Java层可以调用的三个方法public interface AppRegistry extends JavaScriptModule &#123; void runApplication(String appKey, WritableMap appParameters); void unmountApplicationComponentAtRootTag(int rootNodeTag); void startHeadlessTask(int taskId, String taskKey, WritableMap data);&#125; Java层CatalystInstanceImpl.getJSModulegetJSModule方法是通过动态代理生成指定接口实例。 12345678910111213141516171819public synchronized &lt;T extends JavaScriptModule&gt; T getJavaScriptModule( CatalystInstance instance, Class&lt;T&gt; moduleInterface) &#123; // moduleInterface 就是对应JSModule的Java接口 // 取缓存 JavaScriptModule module = mModuleInstances.get(moduleInterface); if (module != null) &#123; return (T) module; &#125; // 通过Proxy创建动态代理对象 JavaScriptModule interfaceProxy = (JavaScriptModule) Proxy.newProxyInstance( moduleInterface.getClassLoader(), new Class[]&#123;moduleInterface&#125;, // 代理对象的具体实现是在InvocationHandler中 new JavaScriptModuleInvocationHandler(instance, moduleInterface)); // 缓存 mModuleInstances.put(moduleInterface, interfaceProxy); return (T) interfaceProxy;&#125; 代理对象的真正实现是在InvocationHandler的invoke方法中。 12345678@Overridepublic @Nullable Object invoke(Object proxy, Method method, @Nullable Object[] args) throws Throwable &#123; NativeArray jsArgs = args != null ? Arguments.fromJavaArgs(args) : new WritableNativeArray(); mCatalystInstance.callFunction(getJSModuleName(), method.getName(), jsArgs); return null;&#125; CatalystInstanceImpl.callFunction将JSModuleName、MethodName、Args这三个参数传给C++层，通过C++层来传递给JS层。 123456789101112public void callFunction(PendingJSCall function) &#123; if (!mAcceptCalls) &#123; synchronized (mJSCallsPendingInitLock) &#123; if (!mAcceptCalls) &#123; mJSCallsPendingInit.add(function); return; &#125; &#125; &#125; // 调用了JNI方法 function.call(this);&#125; CatalystInstanceImpl.jniCallJSFunction1private native void jniCallJSFunction(String module,String method,NativeArray arguments); C++层CatalystInstanceImpl.jniCallJSFunction最终会调用到NativeToJsBridge，它专门负责Native对Js的调用。 12345void CatalystInstanceImpl::jniCallJSFunction(std::string module, std::string method, NativeArray* arguments) &#123; instance_-&gt;callJSFunction(std::move(module), std::move(method), arguments-&gt;consume());&#125; NativeToJsBridge.callFunction所有的Native对Js的调用都由NativeToJsBridge来负责调度，所有Native对Js的调用都会发到JS消息线程中处理。 123456789101112void NativeToJsBridge::callFunction( std::string&amp;&amp; module, std::string&amp;&amp; method, folly::dynamic&amp;&amp; arguments) &#123; ... // 将消息发送到JS消息线程中处理 runOnExecutorQueue([module = std::move(module), method = std::move(method), arguments = std::move(arguments), systraceCookie] (JSExecutor* executor) &#123; ... executor-&gt;callFunction(module, method, arguments); &#125;);&#125; JSCExecutor.callFunctionJSCExecutor负责与Js进行真正的接触，在JSCExecutor中保存了JS运行环境的全局变量MessageQueue对象的引用，以及其相关方法的引用。通过该对象的引用和方法引用，可以调用该对象的方法。这就是c++调用js方法的真相。 而m_callFunctionReturnFlushedQueueJS就是获取的MessageQueue.callFunctionReturenFlushedQueue函数引用。这些对象引用都是在bindBridge中进行的。 在callAsFunction中会通过JSC引擎来调用m_callFunctionReturnFlushedQueueJS这个函数。此时代码就进入了JS层。 12345678910111213141516171819202122232425void JSCExecutor::callFunction( const std::string&amp; moduleId, const std::string&amp; methodId, const folly::dynamic&amp; arguments) &#123; auto result = [&amp;] &#123; JSContextLock lock(m_context); try &#123; if (!m_callFunctionReturnResultAndFlushedQueueJS) &#123; // 绑定JS运行环境中的MessageQueue对象引用 bindBridge(); &#125; // 调用MessageQueue.callFunctionReturenFlushedQueue函数 return m_callFunctionReturnFlushedQueueJS-&gt;callAsFunction( &#123;Value(m_context, String::createExpectingAscii(m_context, moduleId)), Value(m_context, String::createExpectingAscii(m_context, methodId)), Value::fromDynamic(m_context, std::move(arguments))&#125;); &#125; catch (...) &#123; std::throw_with_nested( std::runtime_error("Error calling " + moduleId + "." + methodId)); &#125; &#125;(); // 返回的结果是js调用native方法的请求队列，处理这些请求 callNativeModules(std::move(result));&#125; JSCExecutor.bindBridge获取js环境中的全局属性 MessageQueue，以及MessageQueue中一些函数引用。 123456789101112131415161718192021222324252627282930313233343536void JSCExecutor::bindBridge() throw(JSException) &#123; SystraceSection s("JSCExecutor::bindBridge"); std::call_once(m_bindFlag, [this] &#123; auto global = Object::getGlobalObject(m_context); // 获取js运行环境的全局变量__fbBatchedBridge的引用 // __fbBatchedBridge就是MessageQueue对象实例 auto batchedBridgeValue = global.getProperty("__fbBatchedBridge"); if (batchedBridgeValue.isUndefined()) &#123; auto requireBatchedBridge = global.getProperty("__fbRequireBatchedBridge"); if (!requireBatchedBridge.isUndefined()) &#123; batchedBridgeValue = requireBatchedBridge.asObject().callAsFunction(&#123;&#125;); &#125; if (batchedBridgeValue.isUndefined()) &#123; throw JSException( "Could not get BatchedBridge, make sure your bundle is packaged correctly"); &#125; &#125; auto batchedBridge = batchedBridgeValue.asObject(); // 获取MessageQueue的各个方法引用 // 获取MessageQueue.callFunctionReturnFlushedQueue函数引用 m_callFunctionReturnFlushedQueueJS = batchedBridge.getProperty("callFunctionReturnFlushedQueue").asObject(); // 获取MessageQueue.invokeCallbackAndReturnFlushedQueue函数引用 m_invokeCallbackAndReturnFlushedQueueJS = batchedBridge.getProperty("invokeCallbackAndReturnFlushedQueue") .asObject(); // 获取MessageQueue.flushedQueue函数引用 m_flushedQueueJS = batchedBridge.getProperty("flushedQueue").asObject(); // 获取MessageQueue.callFunctionReturnResultAndFlushedQueue函数引用 m_callFunctionReturnResultAndFlushedQueueJS = batchedBridge.getProperty("callFunctionReturnResultAndFlushedQueue") .asObject(); &#125;);&#125; Js层MessageQueue.callFunctionReturnResultAndFlushedQueue12345678910111213callFunctionReturnResultAndFlushedQueue( module: string, method: string, args: any[],) &#123; let result; this.__guard(() =&gt; &#123; // 执行JS方法 result = this.__callFunction(module, method, args); &#125;); // 返回JS对Native方法调用请求的请求队列 return [result, this.flushedQueue()];&#125; MessageQueue.__callFunctiongetCallableModule会从所有注册的module列表中找到指定name的module。注册是通过MessageQueue.registerCallableModule来注册的。 123456789__callFunction(module: string, method: string, args: any[]): any &#123; ... // 获取到指定Module对象 const moduleMethods = this.getCallableModule(module); // 执行该对象的指定方法，这样就完成了Java层对JS方法的调用 const result = moduleMethods[method].apply(moduleMethods, args); Systrace.endEvent(); return result;&#125; 12// 比如这就是AppRegistry Module的注册BatchedBridge.registerCallableModule('AppRegistry', AppRegistry); MessageQueue全局属性在c++层获取的全局属性__fbBatchedBridge，就是在这里注册的，其值就是MessageQueue对象实例。 123456const BatchedBridge = new MessageQueue();Object.defineProperty(global, '__fbBatchedBridge', &#123; configurable: true, value: BatchedBridge,&#125;); Js调用Java 在Js层注册Java层moduleNativeModules (JS层)global.nativeModuleProxy是在c++层的JSCExecutor创建的时候设置的，值是所有Java层module列表。 12345678910111213141516171819202122232425262728293031// 将genModule设置成全局属性，方便c++层调用该方法创建js层的java module对象。global.__fbGenNativeModule = genModule;let NativeModules: &#123;[moduleName: string]: Object&#125; = &#123;&#125;;if (global.nativeModuleProxy) &#123; // 获取全局变量nativeModuleProxy，将其导出 NativeModules = global.nativeModuleProxy;&#125; else if (!global.nativeExtensions) &#123; // 如果c++层并没有设置该值，则自己手动创建 // __fbBatchedBridgeConfig是在c++层设置的Java层module的配置列表 const bridgeConfig = global.__fbBatchedBridgeConfig; const defineLazyObjectProperty = require('defineLazyObjectProperty'); (bridgeConfig.remoteModuleConfig || []).forEach( (config: ModuleConfig, moduleID: number) =&gt; &#123; // 创建js层java module对象 const info = genModule(config, moduleID); if (!info) &#123; return; &#125; if (info.module) &#123; NativeModules[info.name] = info.module; &#125; else &#123; defineLazyObjectProperty(NativeModules, info.name, &#123; get: () =&gt; loadModule(info.name, moduleID), &#125;); &#125; &#125;, );&#125;module.exports = NativeModules; 设置nativeModuleProxy (C++层)123456789101112131415161718JSCExecutor::JSCExecutor( std::shared_ptr&lt;ExecutorDelegate&gt; delegate, std::shared_ptr&lt;MessageQueueThread&gt; messageQueueThread, const folly::dynamic&amp; jscConfig) throw(JSException) : m_delegate(delegate), m_messageQueueThread(messageQueueThread), m_nativeModules(delegate ? delegate-&gt;getModuleRegistry() : nullptr), m_jscConfig(jscConfig) &#123; initOnJSVMThread(); &#123; // 设置js运行环境全局属性nativeModuleProxy installGlobalProxy( m_context, "nativeModuleProxy", exceptionWrapMethod&lt;&amp;JSCExecutor::getNativeModule&gt;()); &#125;&#125; JSCNativeModules.getModule (C++层)12345678JSValueRef JSCNativeModules::getModule(JSContextRef context, JSStringRef jsName) &#123; ... // 创建module auto module = createModule(moduleName, context); ... auto result = m_objects.emplace(std::move(moduleName), std::move(*module)).first; return static_cast&lt;JSObjectRef&gt;(result-&gt;second);&#125; JSCNativeModules.createModule (C++层)1234567891011121314151617181920212223folly::Optional&lt;Object&gt; JSCNativeModules::createModule(const std::string&amp; name, JSContextRef context) &#123; if (!m_genNativeModuleJS) &#123; // 获取NativeModules.genModule函数 auto global = Object::getGlobalObject(context); m_genNativeModuleJS = global.getProperty("__fbGenNativeModule").asObject(); m_genNativeModuleJS-&gt;makeProtected(); &#125; // 获取该module的配置信息，方法描述签名集合等 auto result = m_moduleRegistry-&gt;getConfig(name); if (!result.hasValue()) &#123; return nullptr; &#125; // 根据module的配置信息，调用genModule函数创建js层的java module对象 Value moduleInfo = m_genNativeModuleJS-&gt;callAsFunction(&#123; Value::fromDynamic(context, result-&gt;config), Value::makeNumber(context, result-&gt;index) &#125;); folly::Optional&lt;Object&gt; module(moduleInfo.asObject().getProperty("module").asObject()); ReactMarker::logTaggedMarker(ReactMarker::NATIVE_MODULE_SETUP_STOP, name.c_str()); return module;&#125; ModuleRegistry.getConfig (C++层)123456789101112131415161718192021222324folly::Optional&lt;ModuleConfig&gt; ModuleRegistry::getConfig(const std::string&amp; name) &#123; auto it = modulesByName_.find(name); size_t index = it-&gt;second; NativeModule *module = modules_[index].get(); folly::dynamic config = folly::dynamic::array(name); &#123; // 获取module中定义的常亮 config.push_back(module-&gt;getConstants()); &#125; &#123; // 获取module中定义的方法 std::vector&lt;MethodDescriptor&gt; methods = module-&gt;getMethods(); folly::dynamic methodNames = folly::dynamic::array; folly::dynamic promiseMethodIds = folly::dynamic::array; folly::dynamic syncMethodIds = folly::dynamic::array; ... if (config.size() == 2 &amp;&amp; config[1].empty()) &#123; return nullptr; &#125; else &#123; return ModuleConfig&#123;index, config&#125;; &#125;&#125; JavaNativeModule.getConstants (C++层)1234567891011folly::dynamic JavaNativeModule::getConstants() &#123; // 调用Java层的JavaModuleWrapper的getConstants方法 static auto constantsMethod = wrapper_-&gt;getClass()-&gt;getMethod&lt;NativeMap::javaobject()&gt;("getConstants"); auto constants = constantsMethod(wrapper_); if (!constants) &#123; return nullptr; &#125; else &#123; return cthis(constants)-&gt;consume(); &#125;&#125; JavaNativeModule.getMethods (C++层)12345678910111213141516std::vector&lt;MethodDescriptor&gt; JavaNativeModule::getMethods() &#123; std::vector&lt;MethodDescriptor&gt; ret; syncMethods_.clear(); // 调用Java层的JavaModuleWrapper获取所有由ReactMethod注解标记的方法 auto descs = wrapper_-&gt;getMethodDescriptors(); for (const auto&amp; desc : *descs) &#123; auto methodName = desc-&gt;getName(); auto methodType = desc-&gt;getType(); ... ret.emplace_back( std::move(methodName), std::move(methodType) ); &#125; return ret;&#125; JavaModuleWrapper.getMethodDescriptors (Java层)获取该module中所有拥有ReactMethod注解标记的方法 123456789101112131415161718192021222324252627282930private void findMethods() &#123; Set&lt;String&gt; methodNames = new HashSet&lt;&gt;(); Class&lt;? extends NativeModule&gt; classForMethods = mModuleClass; Class&lt;? extends NativeModule&gt; superClass = (Class&lt;? extends NativeModule&gt;) mModuleClass.getSuperclass(); if (ReactModuleWithSpec.class.isAssignableFrom(superClass)) &#123; classForMethods = superClass; &#125; Method[] targetMethods = classForMethods.getDeclaredMethods(); for (Method targetMethod : targetMethods) &#123; // 该方法是否拥有ReactMethod注解 ReactMethod annotation = targetMethod.getAnnotation(ReactMethod.class); if (annotation != null) &#123; String methodName = targetMethod.getName(); MethodDescriptor md = new MethodDescriptor(); JavaMethodWrapper method = new JavaMethodWrapper(this, targetMethod, annotation.isBlockingSynchronousMethod()); md.name = methodName; md.type = method.getType(); if (md.type == BaseJavaModule.METHOD_TYPE_SYNC) &#123; md.signature = method.getSignature(); md.method = targetMethod; &#125; // 加入方法列表 mMethods.add(method); mDescs.add(md); &#125; &#125;&#125; NativeModules.genModule (JS层)123456789101112131415161718function genModule( config: ?ModuleConfig, moduleID: number,): ?&#123;name: string, module?: Object&#125; &#123; // 提取出Java module 配置信息中的属性 const [moduleName, constants, methods, promiseMethods, syncMethods] = config; // 创建Moduel对象 const module = &#123;&#125;; methods &amp;&amp; // 为module创建所有的方法 methods.forEach((methodName, methodID) =&gt; &#123; const methodType = isPromise ? 'promise' : isSync ? 'sync' : 'async'; // 创建方法 module[methodName] = genMethod(moduleID, methodID, methodType); &#125;); Object.assign(module, constants); return &#123;name: moduleName, module&#125;;&#125; NativeModules.genMethod (JS层)为所有的Java module对象创建需要的方法，并提供实现(此处类似于Java层创建接口和代理对象一样)。在JS层创建Java module在JS层中虚假对象和方法，便于外部直接调用Java module的方法，该对象的方法实现和代理对象一样，都会传递到C++层中，然后通过C++层传递到Java层。 12345678910111213141516171819202122function genMethod(moduleID: number, methodID: number, type: MethodType) &#123; let fn = null; if (type === 'promise') &#123; ... &#125; else if (type === 'sync') &#123; ... &#125; else &#123; // 所有Java module方法的真实实现 fn = function(...args: Array&lt;any&gt;) &#123; // 通过MessageQueue调用c++层，然后c++层调用Java层对象，执行Java module的方法 BatchedBridge.enqueueNativeCall( moduleID, methodID, args, onFail, onSuccess, ); &#125;; &#125; fn.type = type; return fn;&#125; 至此，所有的Java module都在JS层创建了一个虚假对象，该对象持有module中注册的方法，在JS中可以直接通过普通对象的方式来调用，而module方法的真正实现就是上面的 fn ，会调向c++层。 Js层NativeModules.xxxNativeModule.xxxMethod由于上面讲过，在JS层为每个Java module 都创建了一个虚拟对象和方法实现，所以可以通过普通对象一样调用Java module。而这些许虚拟对象的方法实现都是上面的 fn 函数。所以会调用BatchedBridge.enqueueNativeCall方法。 12345678const NativeModules = require('NativeModules');// 想要调用UIManagerModule.createView方法UIManager.createView( tag, // reactTag viewConfig.uiViewClassName, // viewName rootContainerInstance, // rootTag updatePayload // props); MessageQueue.enqueueNativeCall在MessageQueue中维护了一个队列_queue，这个队列中保存了每次JS层对Java module的调用请求。 12345678910111213141516171819202122232425262728enqueueNativeCall( moduleID: number, methodID: number, params: any[], onFail: ?Function, onSucc: ?Function,) &#123; this._callID++; // 将本次Java module方法调用请求加入队列中 this._queue[MODULE_IDS].push(moduleID); this._queue[METHOD_IDS].push(methodID); this._queue[PARAMS].push(params); const now = new Date().getTime(); if ( global.nativeFlushQueueImmediate &amp;&amp; now - this._lastFlush &gt;= MIN_TIME_BETWEEN_FLUSHES_MS ) &#123; // 如果上一次刷新_queue队列距离现在已经过去5秒，则立刻刷新 const queue = this._queue; this._queue = [[], [], [], this._callID]; this._lastFlush = now; // nativeFlushQueueImmediate是在c++层添加的c++方法引用 global.nativeFlushQueueImmediate(queue); &#125; // 否则，等待c++层主动来获取队列&#125; C++层JSCExecutor.nativeFlushQueueImmediate12345678910JSValueRef JSCExecutor::nativeFlushQueueImmediate( size_t argumentCount, const JSValueRef arguments[]) &#123; if (argumentCount != 1) &#123; throw std::invalid_argument("Got wrong number of args"); &#125; flushQueueImmediate(Value(m_context, arguments[0])); return Value::makeUndefined(m_context);&#125; JsToNativeBridge.callNativeModulesJsToNativeBridge专门处理所有的Js层对Java层的调用。 12345678910111213141516void callNativeModules( JSExecutor&amp; executor, folly::dynamic&amp;&amp; calls, bool isEndOfBatch) override &#123; for (auto&amp; call : parseMethodCalls(std::move(calls))) &#123; // 以此执行每次调用请求 m_registry-&gt;callNativeMethod(call.moduleId, call.methodId, std::move(call.arguments), call.callId); &#125; if (isEndOfBatch) &#123; if (m_batchHadNativeModuleCalls) &#123; // 如果请求队列不为空，则执行完毕后通知Java层，JS层对Java层module调用处理完毕 m_callback-&gt;onBatchComplete(); m_batchHadNativeModuleCalls = false; &#125; m_callback-&gt;decrementPendingJSCalls(); &#125;&#125; ModuleRegistry.callNativeMethod该modules_列表是在CatalystInstanceImpl.initializeBridge时，创建ModuleRegistry对象时赋值的。 1234567void ModuleRegistry::callNativeMethod(unsigned int moduleId, unsigned int methodId, folly::dynamic&amp;&amp; params, int callId) &#123; if (moduleId &gt;= modules_.size()) &#123; throw std::runtime_error( folly::to&lt;std::string&gt;("moduleId ", moduleId, " out of range [0..", modules_.size(), ")")); &#125; modules_[moduleId]-&gt;invoke(methodId, std::move(params), callId);&#125; JavaNativeModule.invoke1234567891011void JavaNativeModule::invoke(unsigned int reactMethodId, folly::dynamic&amp;&amp; params, int callId) &#123; // 在native消息线程中执行 messageQueueThread_-&gt;runOnQueue([this, reactMethodId, params=std::move(params), callId] &#123; static auto invokeMethod = wrapper_-&gt;getClass()-&gt;getMethod&lt;void(jint, ReadableNativeArray::javaobject)&gt;("invoke"); // 执行Java层JavaModuleWrapper.invoke方法 invokeMethod( wrapper_, static_cast&lt;jint&gt;(reactMethodId), ReadableNativeArray::newObjectCxxArgs(std::move(params)).get()); &#125;);&#125; JInstanceCallback.onBatchComplete该ReactCallback对象是在CatalystInstanceImpl.initializeBridge注册的时候创建的一个BridgeCallback对象。 123456789void onBatchComplete() override &#123; // 在native消息线程中 messageQueueThread_-&gt;runOnQueue([this] &#123; // 执行ReactCallback.onBatchComplete方法 static auto method = ReactCallback::javaClassStatic()-&gt;getMethod&lt;void()&gt;("onBatchComplete"); method(jobj_); &#125;);&#125; Java层JavaModuleWrapper.invoke1234567public void invoke(int methodId, ReadableNativeArray parameters) &#123; if (mMethods == null || methodId &gt;= mMethods.size()) &#123; return; &#125; mMethods.get(methodId).invoke(mJSInstance, parameters);&#125; JavaMethodWrapper.invoke12345678910111213141516171819202122232425public void invoke(JSInstance jsInstance, ReadableNativeArray parameters) &#123; String traceName = mModuleWrapper.getName() + "." + mMethod.getName(); try &#123; if (!mArgumentsProcessed) &#123; processArguments(); &#125; int i = 0, jsArgumentsConsumed = 0; try &#123; for (; i &lt; mArgumentExtractors.length; i++) &#123; mArguments[i] = mArgumentExtractors[i].extractArgument( jsInstance, parameters, jsArgumentsConsumed); jsArgumentsConsumed += mArgumentExtractors[i].getJSArgumentsNeeded(); &#125; &#125; catch (UnexpectedNativeTypeException e) &#123; &#125; try &#123; // 反射执行真正的Java module的方法 mMethod.invoke(mModuleWrapper.getModule(), mArguments); &#125; &#125; finally &#123; &#125;&#125;]]></content>
      <categories>
        <category>刀枪棍棒（Android）</category>
        <category>五虎断门刀（Android原生）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[RePlugin之插件Activity启动]]></title>
    <url>%2FRePlugin%E4%B9%8B%E6%8F%92%E4%BB%B6Activity%E5%90%AF%E5%8A%A8%2F</url>
    <content type="text"><![CDATA[Host中启动插件Activity为什么同一配置要多个坑？同一坑多次启动不行？ 启动插件Activity开启一个占坑的插件Activity，需要使用RePlugin.startActivity来启动，为了避免Hook更多的API，所以无法在原生启动过程中(Instrumentation)动态替换，因此在进入原生启动流程之前，先做好准备工作和替换。 框架预处理PluginLibraryInternalProxy.startActivity 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859606162636465666768 public boolean startActivity(Context context, Intent intent, String plugin, String activity, int process, boolean download) &#123; ... // 是否启动下载 // 若插件不可用（不存在或版本不匹配），则直接弹出“下载插件”对话框 // 因为已经打开UpdateActivity，故在这里返回True，告诉外界已经打开，无需处理 if (download) &#123; // 如果在PluginTable.PLUGINS中找不到该插件，则尝试下载 if (PluginTable.getPluginInfo(plugin) == null) &#123; ... // 如果用户在下载即将完成时突然点按“取消”，则有可能出现插件已下载成功，但没有及时加载进来的情况 // 因此我们会判断这种情况，如果是，则重新加载一次即可，反之则提示用户下载 // 原因：“取消”会触发Task.release方法，最终调用mDownloadTask.destroy，导致“下载服务”的Receiver被注销，即使文件下载了也没有回调回来 // NOTE isNeedToDownload方法会调用pluginDownloaded再次尝试加载 // 如果插件文件存在，则不需要再次下载，直接加载。如果文件不存在，则需要下载 if (isNeedToDownload(context, plugin)) &#123; // 通知上层插件不存在 return RePlugin.getConfig().getCallbacks().onPluginNotExistsForActivity(context, plugin, intent, process); &#125; &#125; &#125;... // 如果插件状态出现问题，则每次弹此插件的Activity都应提示无法使用，或提示升级（如有新版） // Added by Jiongxuan Zhang if (PluginStatusController.getStatus(plugin) &lt; PluginStatusController.STATUS_OK) &#123; ... // 通知上层插件不存在 return RePlugin.getConfig().getCallbacks().onPluginNotExistsForActivity(context, plugin, intent, process); &#125; // 若为首次加载插件，且是“大插件”，则应异步加载，同时弹窗提示“加载中” // Added by Jiongxuan Zhang if (!RePlugin.isPluginDexExtracted(plugin)) &#123; PluginDesc pd = PluginDesc.get(plugin); if (pd != null &amp;&amp; pd.isLarge()) &#123; // 加载大插件，通知上层启动等待弹窗 return RePlugin.getConfig().getCallbacks().onLoadLargePluginForActivity(context, plugin, intent, process); &#125; &#125; // WARNING：千万不要修改intent内容，尤其不要修改其ComponentName // 因为一旦分配坑位有误（或压根不是插件Activity），则外界还需要原封不动的startActivity到系统中 // 可防止出现“本来要打开宿主，结果被改成插件”，进而无法打开宿主Activity的问题 // 缓存打开前的Intent对象，里面将包括Action等内容 Intent from = new Intent(intent); // 帮助填写打开前的Intent的ComponentName信息（如有。没有的情况如直接通过Action打开等） if (!TextUtils.isEmpty(plugin) &amp;&amp; !TextUtils.isEmpty(activity)) &#123; from.setComponent(new ComponentName(plugin, activity)); &#125;// 分配坑位 ComponentName cn = mPluginMgr.mLocal.loadPluginActivity(intent, plugin, activity, process); if (cn == null) &#123; // 坑位获取失败 return false; &#125; // 将Intent指向到“坑位”。这样： // from：插件原Intent // to：坑位Intent intent.setComponent(cn);// 启动坑位Activity context.startActivity(intent); // 通知外界，已准备好要打开Activity了 // 其中：from为要打开的插件的Intent，to为坑位Intent RePlugin.getConfig().getEventCallbacks().onPrepareStartPitActivity(context, from, intent); return true; &#125; 分配坑位Activity1234567891011121314151617181920212223242526272829303132333435363738394041public ComponentName loadPluginActivity(Intent intent, String plugin, String activity, int process) &#123; ActivityInfo ai = null; String container = null; PluginBinderInfo info = new PluginBinderInfo(PluginBinderInfo.ACTIVITY_REQUEST); try &#123; // 获取 ActivityInfo(可能是其它插件的 Activity，所以这里使用 pair 将 pluginName 也返回) ai = getActivityInfo(plugin, activity, intent); if (ai == null) &#123; // 从插件的PackageInfo中找不到该插件Activity的ActivityInfo return null; &#125; // 存储此 Activity 在插件 Manifest 中声明主题到 Intent intent.putExtra(INTENT_KEY_THEME_ID, ai.theme); ... // 根据 activity 的 processName，选择进程 ID 标识 if (ai.processName != null) &#123; process = PluginClientHelper.getProcessInt(ai.processName); &#125; // 容器选择（启动目标进程） IPluginClient client = MP.startPluginProcess(plugin, process, info); if (client == null) &#123; return null; &#125; // 插件所在进程远程分配坑位 container = client.allocActivityContainer(plugin, process, ai.name, intent); ... &#125; catch (Throwable e) &#123; ... &#125; // 分配失败 if (TextUtils.isEmpty(container)) &#123; return null; &#125; PmBase.cleanIntentPluginParams(intent); ... // 就坑位Activity信息返回 return new ComponentName(IPC.getPackageName(), container);&#125; 获取插件Activity的ActivityInfo12345678910111213141516171819public ActivityInfo getActivityInfo(String plugin, String activity, Intent intent) &#123; // 获取插件对象 Plugin p = mPluginMgr.loadAppPlugin(plugin); if (p == null) &#123; // 获取插件对象失败 return null; &#125; ActivityInfo ai = null; // activity 不为空时，从插件声明的 Activity 集合中查找 if (!TextUtils.isEmpty(activity)) &#123; // Plugin.mLoader.mComponents中保存了该插件所有的组件列表，该组件信息从插件的PackageInfo中获取 ai = p.mLoader.mComponents.getActivity(activity); &#125; else &#123; // activity 为空时，根据 Intent 匹配 ai = IntentMatcherHelper.getActivityInfo(mContext, plugin, intent); &#125; return ai;&#125; 解压插件在doLoad方法中，会将插件包解压，将dex和so文件提取出来，通过应用的PackageManager加载该插件apk，然后获取该插件的PackageInfo。 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384private boolean loadLocked(int load, boolean useCache) &#123; // 若插件被“禁用”，则即便上次加载过（且进程一直活着），这次也不能再次使用了 // Added by Jiongxuan Zhang int status = PluginStatusController.getStatus(mInfo.getName(), mInfo.getVersion()); if (status &lt; PluginStatusController.STATUS_OK) &#123; // 加载失败 return false; &#125; if (mInitialized) &#123; if (mLoader == null) &#123; return false; &#125; if (load == LOAD_INFO) &#123; boolean rl = mLoader.isPackageInfoLoaded(); return rl; &#125; if (load == LOAD_RESOURCES) &#123; boolean rl = mLoader.isResourcesLoaded(); return rl; &#125; if (load == LOAD_DEX) &#123; boolean rl = mLoader.isDexLoaded(); return rl; &#125; boolean il = mLoader.isAppLoaded(); // 如果已经初始化过该插件，则直接返回是否被加载 return il; &#125; mInitialized = true; ... // 这里先处理一下，如果cache命中，省了后面插件提取（如释放Jar包等）操作 if (useCache) &#123; boolean result = loadByCache(load); // 如果缓存命中，则直接返回 if (result) &#123; return true; &#125; &#125; Context context = mContext; ClassLoader parent = mParent; PluginCommImpl manager = mPluginManager; // 第一次尝试加载 boolean rc = doLoad(logTag, context, parent, manager, load); if (rc) &#123; try &#123; // 第一次加载成功，至此，该插件已开始运行 PluginManagerProxy.addToRunningPluginsNoThrows(mInfo.getName()); &#125; catch (Throwable e) &#123; ... &#125; return true; &#125; ... // 清除第一次加载失败的残留 // 清空数据对象 mLoader = null; // 删除优化dex文件 File odex = mInfo.getDexFile(); if (odex.exists()) &#123; odex.delete(); &#125; if (Build.VERSION.SDK_INT &lt; Build.VERSION_CODES.LOLLIPOP) &#123; // support for multidex below LOLLIPOP:delete Extra odex,if need try &#123; FileUtils.forceDelete(mInfo.getExtraOdexDir()); &#125; catch (IOException e) &#123; e.printStackTrace(); &#125; &#125; // 尝试第二次加载 rc = doLoad(logTag, context, parent, manager, load); if (!rc) &#123; // 第二次失败则直接返回失败 return false; &#125; ... try &#123; // 第二次成功，至此，该插件已开始运行 PluginManagerProxy.addToRunningPluginsNoThrows(mInfo.getName()); &#125; catch (Throwable e) &#123; ... &#125; return true; &#125; 处理 final boolean loadDex(ClassLoader parent, int load) &#123; try &#123; PackageManager pm = mContext.getPackageManager(); mPackageInfo = Plugin.queryCachedPackageInfo(mPath); if (mPackageInfo == null) &#123; // 通过应用的PackageManager加载插件APK，获得插件的PackageInfo // getPackageArchiveInfo可以获取没有安装(没有在PMS中注册的)的应用的包信息 mPackageInfo = pm.getPackageArchiveInfo(mPath, PackageManager.GET_ACTIVITIES | PackageManager.GET_SERVICES | PackageManager.GET_PROVIDERS | PackageManager.GET_RECEIVERS | PackageManager.GET_META_DATA); if (mPackageInfo == null || mPackageInfo.applicationInfo == null) &#123; mPackageInfo = null; return false; &#125; // 将插件包信息中的资源路径(此处修改applicationInfo资源路径后，就可以通过pm.getResourcesForApplication方法获取资源Resources对象)和so路径指向之前解压的路径 mPackageInfo.applicationInfo.sourceDir = mPath; mPackageInfo.applicationInfo.publicSourceDir = mPath; if (TextUtils.isEmpty(mPackageInfo.applicationInfo.processName)) &#123; mPackageInfo.applicationInfo.processName = mPackageInfo.applicationInfo.packageName; &#125; ... mPackageInfo.applicationInfo.nativeLibraryDir = ld.getAbsolutePath(); &#125;... // 创建或获取ComponentList表 // Added by Jiongxuan Zhang mComponents = Plugin.queryCachedComponentList(mPath); if (mComponents == null) &#123; // ComponentList mComponents = new ComponentList(mPackageInfo, mPath, mPluginObj.mInfo); // 动态注册插件中声明的 receiver // 将插件manifest中静态注册的receiver通过常驻进程动态注册，虽然无法实现静态注册，但是由于常驻进程存活时间长，所以即使插件进程退出，也可以接受到通知。 regReceivers(); // 缓存表：ComponentList // 将PackageInfo中的组件都缓存到列表中，方便查询 synchronized (Plugin.FILENAME_2_COMPONENT_LIST) &#123; Plugin.FILENAME_2_COMPONENT_LIST.put(mPath, new WeakReference&lt;&gt;(mComponents)); &#125; /* 只调整一次 */ // 调整插件中组件的进程名称 adjustPluginProcess(mPackageInfo.applicationInfo); // 调整插件中 Activity 的 TaskAffinity adjustPluginTaskAffinity(mPluginName, mPackageInfo.applicationInfo); &#125; if (load == Plugin.LOAD_INFO) &#123; return isPackageInfoLoaded(); &#125; mPkgResources = Plugin.queryCachedResources(mPath); // LOAD_RESOURCES和LOAD_ALL都会获取资源，但LOAD_INFO不可以（只允许获取PackageInfo） if (mPkgResources == null) &#123; // Resources try &#123; if (BuildConfig.DEBUG) &#123; // 如果是Debug模式的话，防止与Instant Run冲突，资源重新New一个 Resources r = pm.getResourcesForApplication(mPackageInfo.applicationInfo); mPkgResources = new Resources(r.getAssets(), r.getDisplayMetrics(), r.getConfiguration()); &#125; else &#123; // 通过上面获得的PackageInfo和对applicationInfo内资源路径的修改，得到一个指向之前解压的资源的Resources对象 mPkgResources = pm.getResourcesForApplication(mPackageInfo.applicationInfo); &#125; &#125; catch (NameNotFoundException e) &#123; return false; &#125; if (mPkgResources == null) &#123; return false; &#125; // 缓存表: Resources synchronized (Plugin.FILENAME_2_RESOURCES) &#123; Plugin.FILENAME_2_RESOURCES.put(mPath, new WeakReference&lt;&gt;(mPkgResources)); &#125; &#125; if (load == Plugin.LOAD_RESOURCES) &#123; return isResourcesLoaded(); &#125; mClassLoader = Plugin.queryCachedClassLoader(mPath); if (mClassLoader == null) &#123; // ClassLoader String out = mPluginObj.mInfo.getDexParentDir().getPath(); //changeDexMode(out); // Log.i("dex", "load " + mPath + " ..."); if (BuildConfig.DEBUG) &#123; // 因为Instant Run会替换parent为IncrementalClassLoader，所以在DEBUG环境里 // 需要替换为BootClassLoader才行 // Added by yangchao-xy &amp; Jiongxuan Zhang parent = ClassLoader.getSystemClassLoader(); &#125; else &#123; // 线上环境保持不变 parent = getClass().getClassLoader().getParent(); // TODO: 这里直接用父类加载器 &#125; String soDir = mPackageInfo.applicationInfo.nativeLibraryDir; long begin = 0; boolean isDexExist = false; // 通过解压的dex路径和so路径，创建一个PluginDexClassLoader，用来加载该插件的class mClassLoader = RePlugin.getConfig().getCallbacks().createPluginClassLoader(mPluginObj.mInfo, mPath, out, soDir, parent); if (mClassLoader == null) &#123; return false; &#125; // 缓存表：ClassLoader synchronized (Plugin.FILENAME_2_DEX) &#123; Plugin.FILENAME_2_DEX.put(mPath, new WeakReference&lt;&gt;(mClassLoader)); &#125; &#125; if (load == Plugin.LOAD_DEX) &#123; return isDexLoaded(); &#125;// 创建插件的上下文对象 mPkgContext = new PluginContext(mContext, android.R.style.Theme, mClassLoader, mPkgResources, mPluginName, this); &#125; catch (Throwable e) &#123; return false; &#125; return true; &#125; 创建插件Application安装完插件后，会检查插件是否需要创建插件application，如果需要，则创建插件的application，并调用其attachBaseContext和onCreate回调，并将上面创建的PluginContext作为mBase。 1234567891011121314151617181920 private void callAppLocked() &#123; // 获取并调用Application的几个核心方法 if (!mDummyPlugin) &#123; // NOTE 不排除A的Application中调到了B，B又调回到A，或在同一插件内的onCreate开启Service/Activity，而内部逻辑又调用fetchContext并再次走到这里 // NOTE 因此需要对mApplicationClient做判断，确保永远只执行一次，无论是否成功 if (mApplicationClient != null) &#123; // 已经初始化过，无需再次处理 return; &#125;// 通过反射创建插件manifest中配置的application mApplicationClient = PluginApplicationClient.getOrCreate( mInfo.getName(), mLoader.mClassLoader, mLoader.mComponents, mLoader.mPluginObj.mInfo);// 将PluginContext作为参数传给application if (mApplicationClient != null) &#123; mApplicationClient.callAttachBaseContext(mLoader.mPkgContext); mApplicationClient.callOnCreate(); &#125; &#125; else &#123; &#125; &#125; 启动目标进程回到第3步，拿到了已经安装好的插件对象，从插件的组件列表(Plugin.mLoader.mComponents)中获得启动的Activity的信息，通过该Activity的process信息和插件信息从常驻进程服务那获得该插件的PluginClient服务对象，通过该对象和插件进程进行通信，如果插件进程没有启动，则通过provider启动该进程。 12345678910111213141516171819202122232425262728293031323334353637383940 final IPluginClient startPluginProcessLocked(String plugin, int process, PluginBinderInfo info) &#123;... // PluginProcessMain.schedulePluginProcessLoop(PluginProcessMain.CHECK_STAGE1_DELAY); // 获取插件进程在常驻进程中注册的服务代理对象 IPluginClient client = PluginProcessMain.probePluginClient(plugin, process, info); if (client != null) &#123; return client; &#125; // 分配，如果插件进程没有启动 int index = IPluginManager.PROCESS_AUTO; try &#123; index = PluginProcessMain.allocProcess(plugin, process); &#125; catch (Throwable e) &#123; &#125; // 分配的坑位不属于UI、和自定义进程，就返回。 if (!(index == IPluginManager.PROCESS_UI || PluginProcessHost.isCustomPluginProcess(index) || PluginManager.isPluginProcess(index))) &#123; return null; &#125; // 启动，通过provider启动插件进程 boolean rc = PluginProviderStub.proxyStartPluginProcess(mContext, index); if (!rc) &#123; return null; &#125; // 再次获取该插件进程注册的服务代理 client = PluginProcessMain.probePluginClient(plugin, process, info); if (client == null) &#123; if (LOGR) &#123; LogRelease.e(PLUGIN_TAG, "spp pc n"); &#125; return null; &#125; return client; &#125; 目标进程分配坑位根据启动Activity的启动模式和样式从注册的坑位中分配一个合适的坑位 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051final String bindActivity(String plugin, int process, String activity, Intent intent) &#123; /* 获取插件对象 */ Plugin p = mPluginMgr.loadAppPlugin(plugin); if (p == null) &#123; return null; &#125; /* 获取 ActivityInfo */ ActivityInfo ai = p.mLoader.mComponents.getActivity(activity); if (ai == null) &#123; return null; &#125; if (ai.processName == null) &#123; ai.processName = ai.applicationInfo.processName; &#125; if (ai.processName == null) &#123; ai.processName = ai.packageName; &#125; /* 获取 Container */ String container; // 自定义进程 if (ai.processName.contains(PluginProcessHost.PROCESS_PLUGIN_SUFFIX2)) &#123; String processTail = PluginProcessHost.processTail(ai.processName); container = mACM.alloc2(ai, plugin, activity, process, intent, processTail); &#125; else &#123; container = mACM.alloc(ai, plugin, activity, process, intent); &#125; if (TextUtils.isEmpty(container)) &#123; return null; &#125; /* 检查 activity 是否存在 */ Class&lt;?&gt; c = null; try &#123; c = p.mLoader.mClassLoader.loadClass(activity); &#125; catch (Throwable e) &#123; if (LOGR) &#123; LogRelease.e(PLUGIN_TAG, e.getMessage(), e); &#125; &#125; if (c == null) &#123; return null; &#125; return container;&#125; 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657private final ActivityState allocLocked(ActivityInfo ai, HashMap&lt;String, ActivityState&gt; map, String plugin, String activity, Intent intent) &#123; // 坑和状态的 map 为空 if (map == null) &#123; return null; &#125; // 首先找上一个活的，或者已经注册的，避免多个坑到同一个activity的映射 for (ActivityState state : map.values()) &#123; if (state.isTarget(plugin, activity)) &#123; return state; &#125; &#125; // 新分配：找空白的，第一个 for (ActivityState state : map.values()) &#123; if (state.state == STATE_NONE) &#123; state.occupy(plugin, activity); return state; &#125; &#125; ActivityState found; // 重用：则找最老的那个 found = null; for (ActivityState state : map.values()) &#123; if (!state.hasRef()) &#123; if (found == null) &#123; found = state; &#125; else if (state.timestamp &lt; found.timestamp) &#123; found = state; &#125; &#125; &#125; if (found != null) &#123; found.occupy(plugin, activity); return found; &#125; // 强挤：最后一招，挤掉：最老的那个 found = null; for (ActivityState state : map.values()) &#123; if (found == null) &#123; found = state; &#125; else if (state.timestamp &lt; found.timestamp) &#123; found = state; &#125; &#125; if (found != null) &#123; found.finishRefs(); found.occupy(plugin, activity); return found; &#125; // never reach here return null;&#125; 启动坑位Activity回到第2步中，在获取到坑位Activity后，直接将其放到Intent中，然后正常启动 123456789101112131415161718192021222324252627282930 ComponentName cn = mPluginMgr.mLocal.loadPluginActivity(intent, plugin, activity, process); if (cn == null) &#123; if (LOG) &#123; LogDebug.d(PLUGIN_TAG, "plugin cn not found: intent=" + intent + " plugin=" + plugin + " activity=" + activity + " process=" + process); &#125; return false; &#125; // 将Intent指向到“坑位”。这样： // from：插件原Intent // to：坑位Intent intent.setComponent(cn); if (LOG) &#123; LogDebug.d(PLUGIN_TAG, "start activity: real intent=" + intent); &#125;// if (RePluginInternal.FOR_DEV) &#123;// try &#123;// String str = cn.getPackageName() + "/" + cn.getClassName();// if (LOG) &#123;// LogDebug.d(PLUGIN_TAG, "str=" + str);// &#125;// new ProcessBuilder().command("am", "start", "-D", "--user", "0", "-n", str).start();// &#125; catch (IOException e) &#123;// e.printStackTrace();// &#125;// &#125; else &#123; context.startActivity(intent); ClassLoader替换坑位坑位Activity被正常启动，经过AMS和当前进程以及目标进程多次来回后，在performLaunchActivity时，使用CLassLoader来加载该Activity类。使用的是appContext.getClassLoader，而ContextImpl中的classLoader是从PackageInfo中获取的，在上一篇中提到，唯一的Hook点就是在启动的时候将PackageInfo.mClassLoader改成RePluginClassLoader对象，所以最终会进入RePluginClassLoader.loadClass方法。在该方法中会使用插件的PluginDexClassLoader来加载坑位对应的Activity，然后返回到performLaunchActivity中继续启动。 12345678910111213141516171819 private Activity performLaunchActivity(ActivityClientRecord r, Intent customIntent) &#123; ContextImpl appContext = createBaseContextForActivity(r); Activity activity = null; try &#123; java.lang.ClassLoader cl = appContext.getClassLoader(); activity = mInstrumentation.newActivity( cl, component.getClassName(), r.intent); StrictMode.incrementExpectedActivityCount(activity.getClass()); r.intent.setExtrasClassLoader(cl); r.intent.prepareToEnterProcess(); if (r.state != null) &#123; r.state.setClassLoader(cl); &#125; &#125; catch (Exception e) &#123; &#125;... return activity; &#125; 加载真正的插件Activity在RepluginClassLoader的loadClass方法中，会先通过PMF.loadClass加载类，如果失败，则由原生的PathDexClassLoader来加载。 12345678910111213141516171819202122232425262728293031323334353637383940final Class&lt;?&gt; loadClass(String className, boolean resolve) &#123; // 加载Service中介坑位 if (className.startsWith(PluginPitService.class.getName())) &#123; return PluginPitService.class; &#125; // 如果Activity坑位列表中包含该类，则尝试将坑位替换成原本的Activity if (mContainerActivities.contains(className)) &#123; Class&lt;?&gt; c = mClient.resolveActivityClass(className); if (c != null) &#123; // 返回插件Activity return c; &#125; return DummyActivity.class; &#125; // 如果Service坑位列表中包含该类，则尝试将坑位替换成原本的Service if (mContainerServices.contains(className)) &#123; Class&lt;?&gt; c = loadServiceClass(className); if (c != null) &#123; return c; &#125; return DummyService.class; &#125; // 如果Provider坑位列表中包含该类，则尝试将坑位替换成原本的Provider if (mContainerProviders.contains(className)) &#123; Class&lt;?&gt; c = loadProviderClass(className); if (c != null) &#123; return c; &#125; // 输出warn日志便于查看 // use DummyProvider orig= if (LOGR) &#123; LogRelease.w(PLUGIN_TAG, "p m hlc u d p o " + className); &#125; return DummyProvider.class; &#125; return loadDefaultClass(className);&#125; 1234567891011121314151617181920212223242526 final Class&lt;?&gt; resolveActivityClass(String container) &#123; String plugin = null; String activity = null; // 先找登记的，如果找不到，则用forward activity PluginContainers.ActivityState state = mACM.lookupByContainer(container); if (state == null) &#123; return ForwardActivity.class; &#125; plugin = state.plugin; activity = state.activity; Plugin p = mPluginMgr.loadAppPlugin(plugin); if (p == null) &#123; return null; &#125;// 获取插件的ClassLoader ClassLoader cl = p.getClassLoader(); Class&lt;?&gt; c = null; try &#123; // 使用插件的PluginDexClassLoader来加载插件Activity c = cl.loadClass(activity); &#125; catch (Throwable e) &#123; &#125; return c; &#125; PluginActivity替换上下文实际上插件的每个Activity在编译的时候都会改成继承RePlugin自己定义的Activity(这些都定义在plugin-lib中)，该Activity会在声明周期触发时，通过反射调用host-lib进行一系列的操作。在attach中会将Activity的上下文改成PluginContext，这样当我们使用context.getResources等方法时，得到的就是插件的resources。 12345678public abstract class PluginActivity extends Activity &#123; @Override protected void attachBaseContext(Context newBase) &#123; // 通过反射创建PluginContext，这样插件Activity的上下文就是PluginContext，其内部的ClassLoader和Resources就是该插件的ClassLoader和Resources。 newBase = RePluginInternal.createActivityContext(this, newBase); super.attachBaseContext(newBase); &#125;&#125; 123final Context createBaseContext(Context newBase) &#123; return new PluginContext(newBase, android.R.style.Theme, mClassLoader, mPkgResources, mPluginName, this);&#125; 流程如下： graph TD A[启动Activity_A] --> B{插件是否下载} B-->|否|C[通知上层插件不存在] B-->|是|D{插件是否安装} D-->|否|E[解压插件,处理dex,获得PackageInfo] D-->|是|F[获取A的ActivityInfo] E-->F F-->G[启动目标进程,并获取目标进程Binder服务代理] G-->H[通知目标进程服务分配坑位,获得坑位Activity] H-->I[启动坑位Activity] I-->J[AMS正常启动坑位Activity] J-->K[目标进程newActivity] K-->L[RePluginClassLoader将坑位替换] L-->M[使用目标插件的PluginDexClassLoader加载真正的Activity_A] M-->N[拿到替换的Activity对象继续和AMS交互] N-->O[attach时替换Activity的上下文]]]></content>
      <categories>
        <category>刀枪棍棒（Android）</category>
        <category>五虎断门刀（Android原生）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[RePlugin之Host进程启动]]></title>
    <url>%2FRePlugin%E4%B9%8BHost%E8%BF%9B%E7%A8%8B%E5%90%AF%E5%8A%A8%2F</url>
    <content type="text"><![CDATA[常驻进程RePlugin默认开启常驻进程，所谓常驻进程就是单独启动一个进程来运行插件服务，可以在gradle中配置关闭常驻进程，那么服务会运行在UI进程。 1234repluginHostConfig &#123; /** 是否使用常驻进程？ */ persistentEnable false&#125; 启用常驻进程下插件服务的启动UI进程启动创建RePluginApplication，在attachBaseContext函数中开始初始化RePlugin框架。 初始化RePlugin通过RePlugin.App.attachBaseContext函数初始化123456789101112131415public static void attachBaseContext(Application app, RePluginConfig config) &#123; ... // 标记当前进程是UI进程还是常驻进程 IPC.init(app); ... // 通过反射获取gradle中repluginHostConfig中的配置信息 HostConfigHelper.init(); ... // 创建PmBase对象，初始化插件管理框架，并且Hook应用的ClassLoader PMF.init(app); // 挂载所有插件 PMF.callAttach(); sAttached = true;&#125; 创建PmBase对象123456789101112131415161718192021222324252627PmBase(Context context) &#123; mContext = context; // 添加客户端进程对应的provider和service的authorities，用来启动对应的组件 // processIndex是子进程个数，不能超过2 if (PluginManager.sPluginProcessIndex == IPluginManager.PROCESS_UI || PluginManager.isPluginProcess()) &#123; String suffix; if (PluginManager.sPluginProcessIndex == IPluginManager.PROCESS_UI) &#123; suffix = "N1"; &#125; else &#123; suffix = "" + PluginManager.sPluginProcessIndex; &#125; // 如果是UI进程则启动ProviderN1和ServiceN1 // 如果是插件进程则启动Provider0、Provider1和Service0、Service1 mContainerProviders.add(IPC.getPackageName() + CONTAINER_PROVIDER_PART + suffix); mContainerServices.add(IPC.getPackageName() + CONTAINER_SERVICE_PART + suffix); &#125; // 4.创建Binder服务PluginClient，每个客户端都拥有一个该服务，常驻进程持有所有客户端进程该服务的代理对象， // 常驻进程通过mClient服务的代理调用客户端的PluginProcessPer服务 mClient = new PluginProcessPer(context, this, PluginManager.sPluginProcessIndex, mContainerActivities); // mLocal = new PluginCommImpl(context, this); // mInternal = new PluginLibraryInternalProxy(this);&#125; 创建PluginClient服务每个客户端都有一个PluginClient服务，该服务包含PluginServiceServer子服务和一个坑管理容器，因此每个客户端进程都有一个自己的坑容器。123456789101112PluginProcessPer(Context context, PmBase pm, int process, HashSet&lt;String&gt; containers) &#123; mContext = context; mPluginMgr = pm; // 在服务中又创建了子服务 PluginServiceServer mServiceMgr = new PluginServiceServer(context); // 创建插件容器管理，坑的分配 mACM = new PluginContainers(); // UI进程和自定义子进程初始化给定启动模式的activity对应manifest中占的坑的名称 // 也就是设置坑位 mACM.init(process, containers);&#125; activity坑 12345678910111213141516171819// 这种是UI线程对应的坑&lt;activity android:theme="@ref/0x01030010" android:name="com.qihoo360.replugin.sample.host.loader.a.ActivityN1STTS0" android:exported="false" android:launchMode="2" android:screenOrientation="1" android:configChanges="0x4b0" /&gt;// 这种事自定义进程对应的坑&lt;activity android:theme="@ref/0x01030006" android:name="com.qihoo360.replugin.sample.host.loader.a.Activity0_singleTask1" android:exported="false" android:process=":loader0" android:taskAffinity=":loader0" android:launchMode="2" android:screenOrientation="1" android:configChanges="0x4b0" /&gt; 初始化PmBase由于开启常驻进程模式，所以UI进程作为客户端，会走initForClient分支12345678910111213141516171819202122232425262728293031323334353637void init() &#123; ... if (HostConfigHelper.PERSISTENT_ENABLE) &#123; // （默认）“常驻进程”作为插件管理进程，则常驻进程作为Server，其余进程作为Client if (IPC.isPersistentProcess()) &#123; // 初始化“Server”所做工作 initForServer(); &#125; else &#123; // 连接到Server initForClient(); &#125; &#125; else &#123; // “UI进程”作为插件管理进程（唯一进程），则UI进程既可以作为Server也可以作为Client if (IPC.isUIProcess()) &#123; // 1. 尝试初始化Server所做工作， initForServer(); // 2. 注册该进程信息到“插件管理进程”中 // 注意：这里无需再做 initForClient，因为不需要再走一次Binder PMF.sPluginMgr.attach(); &#125; else &#123; // 其它进程？直接连接到Server即可 initForClient(); &#125; &#125; // 最新快照 PluginTable.initPlugins(mPlugins); // 输出 if (LOG) &#123; for (Plugin p : mPlugins.values()) &#123; LogDebug.d(PLUGIN_TAG, "plugin: p=" + p.mInfo); &#125; &#125;&#125; initForClient客户端尝试连接服务端,在PluginProcessMain中拥有两个非常重要的变量，一个是sPluginHostLocal，一个是sPluginHostRemote，对于客户端进程来说，使用sPluginHostRemote，该变量是服务在客户端的代理。而对于服务端来说，使用sPluginHostLocal，该变量是真正的服务对象。 常驻进程拥有IPluginHost服务(PmBase.mHostSvc)，客户端通过持有其服务代理(PluginProcessMain.sPluginHostRemote)与服务通信。客户端拥有IPluginClient服务(PmBase.mClient)，常驻进程通过持有期服务代理(ProcessClientRecord.client)与服务通信。1234567private final void initForClient() &#123; // 1. 先尝试连接 PluginProcessMain.connectToHostSvc(); // 2. 然后从常驻进程获取插件列表 refreshPluginsFromHostSvc();&#125; 连接服务端，创建常驻进程AIDL : Stub就是Binder服务对象，在onTransact中接受客户端消息并处理，其自己也可以是客户端通过transact发送消息给自己，所以在同一进程中，客户端和服务端都是同一Binder对象。而Stub.Proxy就是客户端的服务代理类，通过内部的mRemote，也就是BinderProxy对象与Binder服务进行跨进程交互，因为BinderProxy是给客户端用的，所以其只有transact方法用来发消息，而不能接受消息。 客户端进程通过Stub.Proxy.mRemote.transact发送消息到服务端进程的Binder对象的onTransact方法中。 asInterface : 将服务Binder转换，如果是真的Binder对象，其queryLocalInterface返回的是自身，则asInterface返回的还是该对象，但是如果是BinderProxy对象，那么其queryLocalInterface返回的是空，则会对其进行包装再返回，包装成Stub.Proxy对象，该Proxy对象内部mRemote变量就是BinderProxy对象。 12345678910111213141516171819202122232425static final void connectToHostSvc() &#123; Context context = PMF.getApplicationContext(); // 通过provider启动常驻进程并拿到服务Binder IBinder binder = PluginProviderStub.proxyFetchHostBinder(context); ... sPluginHostRemote = IPluginHost.Stub.asInterface(binder); try &#123; PluginManagerProxy.connectToServer(sPluginHostRemote); // 将当前进程的"正在运行"列表和常驻做同步 // TODO 若常驻进程重启，则应在启动时发送广播，各存活着的进程调用该方法来同步 PluginManagerProxy.syncRunningPlugins(); &#125; catch (RemoteException e) &#123; // 获取PluginManagerServer时出现问题，可能常驻进程突然挂掉等，当前进程自杀 if (LOGR) &#123; LogRelease.e(PLUGIN_TAG, "p.p p.h l3a: " + e.getMessage(), e); &#125; System.exit(1); &#125; // 注册该进程信息到“插件管理进程”中 PMF.sPluginMgr.attach();&#125; 由于插件服务并没有注册在ServiceManager中，所以不能通过SM来获取，这里是通过ContentProvider来获取，并且该provider会运行在新的进程中，该进程就作为常驻进程。 此处启动的是ProcessPitProviderPersist，运行的进程名是GuardService。12345&lt;provider android:name="com.qihoo360.replugin.component.process.ProcessPitProviderPersist" android:exported="false" android:process=":GuardService" android:authorities="com.qihoo360.replugin.sample.host.loader.p.main" /&gt; 在query中如果该provider服务不存在，会先创建该provider以及其所在的进程,该进程创建会重新创建application，然后走一遍上面的初始化流程。 query中返回的binder是常驻进程的PmBase.mHostSvc对象，通过Parcelable将Binder对象从常驻进程传递到UI进程，在序列化时通过Parcel.readStrongBinder来创建Binder对象，如果是同一进程创建的就是Binder对象，如果在不同进程，那么创建的就是BinderProxy对象。1234567891011121314private static final IBinder proxyFetchHostBinder(Context context, String selection) &#123; // Cursor cursor = null; try &#123; Uri uri = ProcessPitProviderPersist.URI; cursor = context.getContentResolver().query(uri, PROJECTION_MAIN, selection, null, null); ... // 此处拿到的是BinderProxy对象 IBinder binder = BinderCursor.getBinder(cursor); return binder; &#125; finally &#123; CloseableUtils.closeQuietly(cursor); &#125;&#125; 常驻进程初始化，initForServer每个进程都会创建一个自己的application对象，所以会再次触发初始化流程，常驻进程创建后，会重复走一遍上面的流程，只是在走到第5步时，会执行initServer方法12345678910111213141516171819202122232425private final void initForServer() &#123; ... // 该对象就是服务Binder，返回给UI进程的就是该Binder mHostSvc = new PmHostSvc(mContext, this); // 将服务赋值给mPluginHostLocal，并获取PluginManagerServer PluginProcessMain.installHost(mHostSvc); PluginProcessMain.schedulePluginProcessLoop(PluginProcessMain.CHECK_STAGE1_DELAY); // 搜索所有本地插件，读取assets/plugins-builtin.json文件 mAll = new Builder.PxAll(); Builder.builder(mContext, mAll); // 构建插件对象，保存在PmBase.mPlugins中 refreshPluginMap(mAll.getPlugins()); try &#123; List&lt;PluginInfo&gt; l = PluginManagerProxy.load(); if (l != null) &#123; // 将"纯APK"插件信息并入总的插件信息表中，方便查询 // 这里有可能会覆盖之前在p-n中加入的信息。本来我们就想这么干，以"纯APK"插件为准 refreshPluginMap(l); &#125; &#125; catch (RemoteException e) &#123; ... &#125;&#125; 创建服务时，会创建两个子服务PluginServiceServer和PluginManagerService。123456PmHostSvc(Context context, PmBase packm) &#123; mContext = context; mPluginMgr = packm; mServiceMgr = new PluginServiceServer(context); mManager = new PluginManagerServer(context);&#125; 此时UI进程等待在provider.query方法中等待，直到常驻进程的application初始化完毕，然后进程会创建provider对象，并执行provider.query方法返回数据后才开始执行。 常驻进程-初始化本地插件列表此时常驻进程执行完initServer后，常驻进程会继续第5步执行PluginTable.initPlugins(mPlugins),这个mPlugins就是上面搜索的本地插件对象。会将所有插件都放到PluginTable.PLUGINS这个静态Map中，key为plugin.alias，方便全局获取。到此，PmBase的初始化基本就完了。 常驻进程-Hook应用的ClassLoader回到第2步，在初始化PmBase之后，会Hook应用，将Application.mBase.mPackageInfo.mClassLoader改成RePluginClassLoader对象。并将当前线程的contextClassLoader也改了。此时，PMF初始化完毕，会执行PMF的callAttach方法，该方法中会拿到应用原生的ClassLoader，并将其attach到所有的插件对象上。如果此时是插件子进程，那么就会安装默认的插件。 provider.query常驻进程初始化完毕，执行ProcessPitProviderPersist.query方法，将PmBase.mHostSvc服务对象通过Parcelable返回给UI进程。 UI进程-获取服务端代理回到第7步，此时UI进程通过provider.query去获取服务，由于在不同进程，此时返回的是BinderProxy对象，那么在asInterface中返回的就是IPluginHost.Stub.Proxy对象了，会将该对象赋值给PluginProcessMain.sPluginHostRemote。此时UI进程拿到了常驻进程的服务代理对象。 UI进程-客户端连接服务端在第7步中，先连接服务端,通过PluginManagerProxy.connectToServer,获取服务端中的子服务PluginManagerServer服务(PmHostSvc.mManager.mStub)。保存在PluginManagerProxy.sRemote中。123456789101112131415161718192021222324252627282930313233PluginManagerProxy.connectToServer(sPluginHostRemote);public static void connectToServer(IPluginHost host) throws RemoteException &#123; if (sRemote != null) &#123; if (LogDebug.LOG) &#123; LogDebug.e(TAG, "connectToServer: Already connected! host=" + sRemote); &#125; return; &#125; // 该sRemote对象是IPluginManagerServer.Stub.Proxy对象 sRemote = host.fetchManagerServer();&#125;public IPluginManagerServer fetchManagerServer() throws RemoteException &#123; Parcel _data = Parcel.obtain(); Parcel _reply = Parcel.obtain(); IPluginManagerServer _result; try &#123; _data.writeInterfaceToken("com.qihoo360.loader2.IPluginHost"); this.mRemote.transact(29, _data, _reply, 0); _reply.readException(); // readStrongBinder会将Binder服务序列化成BinderProxy，然后asInterface后， 返回IPluginManagerServer.Stub.Proxy对象。 _result = com.qihoo360.replugin.packages.IPluginManagerServer.Stub.asInterface(_reply.readStrongBinder()); &#125; finally &#123; _reply.recycle(); _data.recycle(); &#125; return _result;&#125; UI进程-同步插件列表然后在第7步中，会同步当前进程加载插件列表到常驻进程服务端12345678910111213141516171819202122public static void syncRunningPlugins() throws RemoteException &#123; if (sRunningSynced) &#123; // 已经同步过，无需再次同步，直到常驻进程挂掉 return; &#125; if (!sRunningList.hasRunning()) &#123; // 没有正在运行的插件，无需同步。若该进程被刚刚加载时会出现此情况 return; &#125; // 不判断sRemote在不在，因为本应该在sRemote获取后就马上调用 sRemote.syncRunningPlugins(sRunningList); sRunningSynced = true;&#125;private void syncRunningPluginsLocked(PluginRunningList list) &#123; // 复制一份List，这样无论是否为跨进程，都不会因客户端对List的修改而产生影响 PluginRunningList newList = new PluginRunningList(list); // 所谓同步，就是将该进程的已加载插件列表维护到一个map中，key为进程名。 // mProcess2PluginsMap中维护了所有进程已加载插件列表。 mProcess2PluginsMap.put(list.mProcessName, newList); ...&#125; 设置UI进程Binder的死亡代理第7步的最后，会将当前进程的信息注册到常驻进程中，并将PluginProcessPer(IPluginClient)服务传到到常驻进程中，在常驻进程中添加对该Binder的死亡代理，当客户端死亡时，通知到常驻进程中进行一系列处理。服务进程也会通过该Binder调用客户进程的服务。12345678910111213141516171819202122232425262728293031323334353637// 客户端发起attach，将当前进程的IPluginClient服务传过去final void attach() &#123; // try &#123; // 此处的getPluginHost拿到的就是从provider处拿到的服务，也就是sPluginHostRemote。 mDefaultPluginName = PluginProcessMain.getPluginHost().attachPluginProcess(IPC.getCurrentProcessName(), PluginManager.sPluginProcessIndex, mClient, mDefaultPluginName); &#125; catch (Throwable e) &#123; if (LOGR) &#123; LogRelease.e(PLUGIN_TAG, "c.n.a: " + e.getMessage(), e); &#125; &#125;&#125;// 服务端接受，创建ProcessClientRecord对象未作客户端Binder的死亡代理,此处binder是BinderProxy对象，client是IPluginClient.Stub.Proxy对象，pms是服务端的PluginManagerService子服务对象。static final String attachProcess(int pid, String process, int index, IBinder binder, IPluginClient client, String def, PluginManagerServer pms) &#123; synchronized (PROCESSES) &#123; // 查询插件进程的默认插件，如果是UI进程，则返回'ui' String plugin = attachProcessLocked(pid, process, index, binder, client, def); // 构建死亡代理对象 ProcessClientRecord pr = new ProcessClientRecord(pms); pr.name = process; pr.plugin = plugin; pr.pid = pid; pr.index = index; pr.binder = binder; pr.client = client; ALL.put(process, pr); try &#123; // 注册死亡代理，当客户端的binder死亡时，会触发该代理的binderDied方法，然后会进行一些数据的销毁。 pr.binder.linkToDeath(pr, 0); &#125; catch (Throwable e) &#123; ... &#125; return plugin; &#125;&#125; UI进程-获取服务端的插件列表第7步连接服务端完毕之后，回到第6步，从服务端获取插件列表123456789101112131415161718192021222324252627282930private void refreshPluginsFromHostSvc() &#123; List&lt;PluginInfo&gt; plugins = null; try &#123; // listPlugins返回的是PluginTable.PLUGINS列表的当前快照(克隆一份新的列表)， 而不是PLUGINS对象。而PLUGINS对象就是在第9步中初始化的插件列表 plugins = PluginProcessMain.getPluginHost().listPlugins(); &#125; catch (Throwable e) &#123; ... &#125; // 判断是否有需要更新的插件 // FIXME 执行此操作前，判断下当前插件的运行进程，具体可以限制仅允许该插件运行在一个进程且为自身进程中 List&lt;PluginInfo&gt; updatedPlugins = null; // 获取插件json信息中是否有upinfo字段 if (isNeedToUpdate(plugins)) &#123; ... try &#123; // 调用PluginManagerService的load方法去更新 updatedPlugins = PluginManagerProxy.updateAllPlugins(); &#125; catch (RemoteException e) &#123; e.printStackTrace(); &#125; &#125; // 构建插件对象，保存在客户端的PmBase.mPlugins中 if (updatedPlugins != null) &#123; refreshPluginMap(updatedPlugins); &#125; else &#123; refreshPluginMap(plugins); &#125;&#125; UI进程-维护本地插件列表此时UI进程执行完initClient方法，会继续第5步执行PluginTable.initPlugins(mPlugins),这个mPlugins就是上面从服务端拿到的插件对象。会将所有插件都放到PluginTable.PLUGINS这个静态Map中，key为plugin.alias，方便全局获取。到此，PmBase的初始化基本就完了。（此处其实就是从服务进程的PluginTable.PLUGINS中拷贝一份数据放到客户进程中的PluginTable.PLUGINS中） UI进程-Hook应用的ClassLoader接下来和常驻进程一样，在初始化PmBase之后，会Hook，将Application.mBase.mPackageInfo.mClassLoader改成RePluginClassLoader对象。并将当前线程的contextClassLoader也改了。此时，PMF初始化完毕，会执行PMF的callAttach方法，该方法中会拿到应用原生的ClassLoader，并将其attach到所有的插件对象上。如果此时是插件子进程，那么就会安装默认的插件。 UI进程和常驻进程都已经初始化完毕。gradle配置1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253class RepluginConfig &#123; /** 自定义进程的数量(除 UI 和 Persistent 进程) */ def countProcess = 3 /** 是否使用常驻进程？ */ def persistentEnable = true /** 常驻进程名称（也就是上面说的 Persistent 进程，开发者可自定义）*/ def persistentName = ':GuardService' /** 背景不透明的坑的数量 */ def countNotTranslucentStandard = 6 def countNotTranslucentSingleTop = 2 def countNotTranslucentSingleTask = 3 def countNotTranslucentSingleInstance = 2 /** 背景透明的坑的数量 */ def countTranslucentStandard = 2 def countTranslucentSingleTop = 2 def countTranslucentSingleTask = 2 def countTranslucentSingleInstance = 3 /** 宿主中声明的 TaskAffinity 的组数 */ def countTask = 2 /** * 是否使用 AppCompat 库 * com.android.support:appcompat-v7:25.2.0 */ def useAppCompat = false /** HOST 向下兼容的插件版本 */ def compatibleVersion = 10 /** HOST 插件版本 */ def currentVersion = 12 /** plugins-builtin.json 文件名自定义,默认是 "plugins-builtin.json" */ def builtInJsonFileName = "plugins-builtin.json" /** 是否自动管理 plugins-builtin.json 文件,默认自动管理 */ def autoManageBuiltInJsonFile = true /** assert目录下放置插件文件的目录自定义,默认是 assert 的 "plugins" */ def pluginDir = "plugins" /** 插件文件的后缀自定义,默认是".jar" 暂时支持 jar 格式*/ def pluginFilePostfix = ".jar" /** 当发现插件目录下面有不合法的插件 jar (有可能是特殊定制 jar)时是否停止构建,默认是 true */ def enablePluginFileIllegalStopBuild = true&#125;]]></content>
      <categories>
        <category>刀枪棍棒（Android）</category>
        <category>五虎断门刀（Android原生）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[Java是解释执行吗？]]></title>
    <url>%2FJava%E6%98%AF%E8%A7%A3%E9%87%8A%E6%89%A7%E8%A1%8C%E5%90%97%2F</url>
    <content type="text"><![CDATA[Java是解释执行吗？ 并不完全是，我们编写的Java代码，首先通过javac编译成字节码，然后在运行时被JVM内嵌的解释器将字节码逐行解释为机器码。但是每次都逐行解释效率非常低，所以JVM提供了JIT编译器，用于将热点代码直接编译成机器码并缓存，后续直接调用而不需要再次编译或解释。那么对于部分热点代码就是编译执行了，剩下的非热点代码依旧还是解释执行。 动态编译–JIT编译器原理 对于Java代码，刚开始都是被编译器编译成字节码文件，然后字节码文件会被交给JVM解释执行，而JIT编译技术会将运行频率很高的字节码直接编译成机器指令以提高性能。 server模式和client模式JIT编译器在运行时有两种模式，server模式相对于client模式来说，启动时速度较慢，但是一旦运行起来，性能会有很大提升。原因是client模式使用的是C1轻量级编译器，而server模式启动的是C2重量级编译器，C2比C1编译的更加彻底，所以启动较慢，但是启动后性能更好。 分层编译由C1编译器和C2编译器相互协作共同来执行编译任务，分层编译策略将会根据编译器执行编译、优化的规模与耗时，划分出不同的编译层次。 第0层: 程序由解释器解释执行 第1层: 程序由C1编译器编译执行，并简单优化 第2层: 程序由C2编译器编译执行，并进行耗时优化 第3层: 程序由C1编译器编译执行，并进行性能优化 第4层: 程序由C2编译器编译执行，并进行完全优化 优化代码缓存大小JIT在编译时，会将编译好的机器码缓存起来，方便下次直接调用，但是JVM提供的缓存是有限的，如果缓存满了，那么整个程序都将只能使用解释执行。 通过修改-XX:ReservedCodeCacheSize来最大化代码缓存大小。 编译线程JIT是方法级的，当一个方法拥有编译资格，它就会排队等待编译，编译是一个异步的过程，所以允许代码在编译时被继续执行，此时执行的依旧是解释执行，当下一次再调用该方法，如果该方法已经编译完成，则直接执行缓存的编译好的机器码。 队列的优先级是根据哪个方法的调用计数器高来分配优先级。 静态编译–AOT]]></content>
      <categories>
        <category>内功心法（CS基础）</category>
        <category>北冥神功（编程语言）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[FastThreadLocal快在哪]]></title>
    <url>%2FFastThreadLocal%E5%BF%AB%E5%9C%A8%E5%93%AA%2F</url>
    <content type="text"><![CDATA[ThreadLocal原理ThreadLocal本质上就是在Thread对象中维护了一个Map对象，Map的key为ThreadLocal自身，value为要存的值。将数据保存在Thread中而不是保存在ThreadLocal中，这一设计使得当线程对象销毁时，在该线程的所有变量都会随之销毁。以ThreadLocal对象为key,使得一个Thread对象可以同时被多个ThreadLocal对象使用，每个ThreadLocal对应一个值，保存在Thread对象的Map中。123456789101112131415161718192021222324252627// 该Map在Thread对象中，由ThreadLocal对象维护ThreadLocal.ThreadLocalMap threadLocals = null;public void set(T value) &#123; Thread t = Thread.currentThread(); // 获取该线程的Map ThreadLocalMap map = getMap(t); // 将当前ThreadLocal和值存入Map if (map != null) map.set(this, value); else createMap(t, value);&#125;public T get() &#123; Thread t = Thread.currentThread(); ThreadLocalMap map = getMap(t); if (map != null) &#123; ThreadLocalMap.Entry e = map.getEntry(this); if (e != null) &#123; @SuppressWarnings("unchecked") T result = (T)e.value; return result; &#125; &#125; return setInitialValue();&#125; FastThreadLocal优化点由于ThreadLocal使用的是hash map数据结构，其查找需要通过hash值计算索引，再通过索引查找值，其速度比数组结构要慢。所以Netty提供了一种数组结构的ThreadLocal，也就是FastThreadLocal。其实现原理是在Thread对象中维护了一个InternalThreadLocalMap对象，该InternalThreadLocalMap对象内部维护了一个数组，数组中保存要存的值，不保存对应的FastThreadLocal对象，而是每个FastThreadLocal自己维护该值在数组中对应的索引。一个FastThreadLocal所保存值对应的索引在其创建的时候已经确定，其所保存的所有Thread都应用该索引位置。123456// 创建FastThreadLocal，确定其所对应的数组索引。// 该值是静态原子类型的，所有ThreadLocal对象共享，所以不会出现索引冲突值被覆盖等问题。static final AtomicInteger nextIndex = new AtomicInteger();public FastThreadLocal() &#123; index = InternalThreadLocalMap.nextVariableIndex();&#125;]]></content>
      <categories>
        <category>内功心法（CS基础）</category>
        <category>神照功（网络协议）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[Netty编解码器]]></title>
    <url>%2FNetty%E7%BC%96%E8%A7%A3%E7%A0%81%E5%99%A8%2F</url>
    <content type="text"><![CDATA[ByteToMessageCodec字节转换成消息对象的编解码器。实现了ChannelInboundHandlerAdapter和ChannelOutboundHandler，说明无论是对外还是对内都会经过该Handler。ByteToMessageCodec内部维护encoder和decoder两个对象，负责编码和解码。对于编解码器只需要处理read和write两种请求，当发生read时会将请求传递给decoder对象去解码，当发生write时会将请求传递给encoder对象编码。123456789@Override public void channelRead(ChannelHandlerContext ctx, Object msg) throws Exception &#123; decoder.channelRead(ctx, msg); &#125; @Override public void write(ChannelHandlerContext ctx, Object msg, ChannelPromise promise) throws Exception &#123; encoder.write(ctx, msg, promise); &#125; 再来看看encoder和decoder这两个对象，encoder是一个MessageToByteEncoder消息对象转换成字节的编码器，decoder是一个ByteToMessageDecoder字节转换成消息对象的解码器。对外的请求会经过Encoder编码器编码成字节数据，而读取远端的数据则会经过Decoder解码成消息对象再传递给上层。所以上层在设置读取消息的Handler时必须设置在编解码器之后。123public abstract class ByteToMessageDecoder extends ChannelInboundHandlerAdapter &#123;&#125;public abstract class MessageToByteEncoder&lt;I&gt; extends ChannelOutboundHandlerAdapter &#123;&#125; Encoder1234567891011121314151617181920212223242526272829303132333435363738394041@Overridepublic void write(ChannelHandlerContext ctx, Object msg, ChannelPromise promise) throws Exception &#123; ByteBuf buf = null; try &#123; // 确认该消息对象的确是当前编码器定义的编码对象，否则传递给其它Handler处理。 if (acceptOutboundMessage(msg)) &#123; @SuppressWarnings("unchecked") I cast = (I) msg; // 创建对应ByteBuf对象 buf = allocateBuffer(ctx, cast, preferDirect); try &#123; // 做具体编码操作 encode(ctx, cast, buf); &#125; finally &#123; // 如果消息对象实现了引用计数接口ReferenceCounted，则手动释放该对象。 ReferenceCountUtil.release(cast); &#125; // 如果buf中有数据，则传递给其它Handler处理 if (buf.isReadable()) &#123; ctx.write(buf, promise); &#125; else &#123; // 如果buf为空，则释放该buf buf.release(); // 传递一个专门的空Buf给其它Handler处理 ctx.write(Unpooled.EMPTY_BUFFER, promise); &#125; buf = null; &#125; else &#123; // 传递给其它Handler处理。 ctx.write(msg, promise); &#125; &#125; catch (EncoderException e) &#123; throw e; &#125; catch (Throwable e) &#123; throw new EncoderException(e); &#125; finally &#123; if (buf != null) &#123; buf.release(); &#125; &#125;&#125; Decoder将ByteBuf中字节数据解码成想要的对象。由于存在粘包等现象，所以解码的结果可能是一个对象列表，而不仅仅只是一个对象。CodecOutputList就是该解码器的结果列表。对于半包和粘包等情况，会将残包的数据保留下来，下次解码时将其和新的数据包合并一起处理。1234567891011121314151617181920212223242526272829303132333435363738394041424344454647@Overridepublic void channelRead(ChannelHandlerContext ctx, Object msg) throws Exception &#123; // 如果消息是ByteBuf则处理，否则传递下去 if (msg instanceof ByteBuf) &#123; // 会从RECYCLER缓存器中获取一个对象列表实例。 CodecOutputList out = CodecOutputList.newInstance(); try &#123; ByteBuf data = (ByteBuf) msg; first = cumulation == null; if (first) &#123; // 如果是第一次则使用当前数据包解码 cumulation = data; &#125; else &#123; // 默认使用内存拷贝，将data数据拷贝到一个新的ByteBuf上，并将其返回 // 如果不是第一次则将当前数据包和上次的缓存数据包结合起来一起解码，用来解决半包，粘包等问题 cumulation = cumulator.cumulate(ctx.alloc(), cumulation, data); &#125; // 解码 callDecode(ctx, cumulation, out); &#125; catch (DecoderException e) &#123; throw e; &#125; catch (Exception e) &#123; throw new DecoderException(e); &#125; finally &#123; // 如果数据已经读完，则释放该ByteBuf if (cumulation != null &amp;&amp; !cumulation.isReadable()) &#123; numReads = 0; cumulation.release(); cumulation = null; &#125; else if (++ numReads &gt;= discardAfterReads) &#123; // numReads表示当前数据没有读完的解码次数，因为每次数据没有读完，则需要将数据存储下来，等待下次解码时一起读取，所以需要设置一定的次数限制，防止OOM。 // discardAfterReads默认为16次。 // 如果超过16次，则重置并清空一部分未读数据。 numReads = 0; discardSomeReadBytes(); &#125; int size = out.size(); decodeWasNull = !out.insertSinceRecycled(); // 将解码结果传递下去 fireChannelRead(ctx, out, size); out.recycle(); &#125; &#125; else &#123; ctx.fireChannelRead(msg); &#125;&#125; 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051protected void callDecode(ChannelHandlerContext ctx, ByteBuf in, List&lt;Object&gt; out) &#123; try &#123; // 循环读取ByteBuf中所有数据 while (in.isReadable()) &#123; int outSize = out.size(); // 如果已经有数据包解码完毕，则将解码完成的对象传递下去，不需要等到所有数据到解码完成才传递。 if (outSize &gt; 0) &#123; fireChannelRead(ctx, out, outSize); out.clear(); // 如果当前解码器已经被移除，则停止解码 if (ctx.isRemoved()) &#123; break; &#125; outSize = 0; &#125; // 未读字节大小 int oldInputLength = in.readableBytes(); // 调用decode方法做具体的解码 decodeRemovalReentryProtection(ctx, in, out); if (ctx.isRemoved()) &#123; break; &#125; // 如果解码结果依旧为0，表明当前数据包不完整，无法解析成一个对象，需要等待后面的数据包。 // 此时decode方法不能读取buffer中一个字节，必须保证数据包完整才能读取。 if (outSize == out.size()) &#123; // 如果未读字节没有变化，则表明没有新数据包进来，则停止解码，等待下次解码。 if (oldInputLength == in.readableBytes()) &#123; break; &#125; else &#123; // 表明有新的数据包加进来，中止本次操作，再次尝试解码。 continue; &#125; &#125; // 如果有解码结果，但是未读字节还是没有变化，说明已经解码的字节还存在ByteBuf中，会出现重复消息的问题，抛出异常。 if (oldInputLength == in.readableBytes()) &#123; throw new DecoderException( StringUtil.simpleClassName(getClass()) + ".decode() did not read anything but decoded a message."); &#125; if (isSingleDecode()) &#123; // 是否只解码一次，对于一次解码操作，只解码一个对象，对于某些协议有用。 break; &#125; &#125; &#125; catch (DecoderException e) &#123; throw e; &#125; catch (Exception cause) &#123; throw new DecoderException(cause); &#125;&#125;]]></content>
      <categories>
        <category>内功心法（CS基础）</category>
        <category>神照功（网络协议）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[Pipeline之Handler链]]></title>
    <url>%2FPipeline%E4%B9%8BHandler%E9%93%BE%2F</url>
    <content type="text"><![CDATA[ChannelPipeline采用责任链模式设计。在责任链模式中，由每个对象对其下个对象的引用而连接起来的一条链，请求在这条链上传递，知道链上某个对象处理该请求并中止传递。发送请求者并不知道链上的哪个对象处理该请求，从而使得系统可以在不影响发送端的情况下动态的重新分配责任。 DefaultChannelPipelineHandler链的默认实现类，维护了链中的头结点和尾结点，DefaultChannelPipeline对象在构建时，就会创建头结点和尾结点对象。之后添加的Handler都会添加到两者中间，addLast会添加到尾结点的前面，addFirst会添加到头结点的后面。在添加时可以给Handler指定名称，如果名称有重复会抛出异常。Handler分为两种，一种是实现ChannelInboundHandler接口的，一种是实现ChannelOutboundHandler接口的。ChannelInboundHandler接口主要处理从外到内的请求，将远端操作通知本地（比如注册，激活，读等），从Head结点开始传递，直到tail结点。而ChannelOutboundHandler接口主要处理从内到外的操作，将本地操作通知远端（比如连接，绑定，写）等，从tail结点开始传递，到Head结点后转由unsafe类进行真正处理。12tail = new TailContext(this); // tail实现了ChannelInboundHandler接口head = new HeadContext(this); // head实现了ChannelOutboundHandler和ChannelInboundHandler接口 上文讲到的ServerBootstrapAcceptor类，就是一个ChannelInboundHandlerAdapter类型的Handler。所以当连接完成对上层发出通知时，通知从Head结点开始传递，到达ServerBootstrapAcceptor中处理并中止传递。 AbstractChannelHandlerContextAbstractChannelHandlerContext是对ChannelHandler的一个包装类，Pipeline中真正的对象实际上是AbstractChannelHandlerContext。在AbstractChannelHandlerContext中维护了next、prev属性，所以这是一个双向链表结构。123volatile AbstractChannelHandlerContext next; // 下个结点volatile AbstractChannelHandlerContext prev; // 上个结点private final ChannelHandler handler; // 真正Handler对象 接下来讲到的编解码器也是根据Pipeline来实现的。]]></content>
      <categories>
        <category>内功心法（CS基础）</category>
        <category>神照功（网络协议）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[ByteBuf]]></title>
    <url>%2FByteBuf%2F</url>
    <content type="text"><![CDATA[ByteBuf为了简化ByteBuffer的操作，Netty自定义了ByteBuf,相对于ByteBuffer的读写索引共用，Netty将其分开，定义了读索引和写索引，避免了每次读写都需要flip方法来转换。并且定义了扩容方法，解决了ByteBuffer容量无法修改的问题。 基本属性 readerIndex : 读索引 writerIndex : 写索引 markReaderIndex : 标记读索引位置 markWriterIndex : 标记写索引位置 readableBytes : 可读字节大小(writerIndex - readerIndex) writableBytes : 可写字节大小(capacity - writerIndex) resetReaderIndex : 还原到标记读索引位置 resetWriterIndex : 还原到标记写索引位置 池化：将用过的对象保存，重复使用。 堆外内存：使用JVM堆栈外的内存，该内存JVM无法干预，JVM的GC只能回收ByteBuf对象本身，而不能回收其指向的堆外内存，所以需要手动释放。 Netty的ByteBuf实现分为池化和非池化，其中又分为堆内存和堆外内存实现。由于堆外内存需要手动释放，所以需要自己维护引用数来标记是否释放。 AbstractReferenceCountedByteBuf该类就是引用计数的实现类，用来管理内存的回收。计数器使用的是AtomicIntegerFieldUpdater而不是AtomicInteger，因为ByteBuf对象会有很多，如果使用AtomicInteger类型，那么每个对象都会有一个该属性，开销大。所以使用volatile修饰的原始int类型，再辅助AtomicIntegerFieldUpdater对象，该类是通过反射拿到类字段对象，无论多少对象，其字段对象都是一份，所以可以修饰成static共享。1234private static final AtomicIntegerFieldUpdater&lt;AbstractReferenceCountedByteBuf&gt; refCntUpdater = AtomicIntegerFieldUpdater.newUpdater(AbstractReferenceCountedByteBuf.class, "refCnt");private volatile int refCnt; 每次调用retain方法就会引用+1，每次调用release方法就会引用-1，如果引用为0则释放内存，具体的释放逻辑由各实现类实现。1234/** * Called once &#123;@link #refCnt()&#125; is equals 0. */protected abstract void deallocate(); 对于非池化的ByteBuf，如果使用的是堆外内存则通过反射调用NIO堆外内存实现类DirectByteBuffer中方法释放内存,最终还是通过unsafe类操作堆外内存。如果使用的是堆内内存则不需要再手动释放，由GC控制释放就行。123456789public void run() &#123; if (address == 0) &#123; // Paranoia return; &#125; unsafe.freeMemory(address); address = 0; Bits.unreserveMemory(size, capacity);&#125; 对于池化的ByteBuf，无论是堆外还是堆内内存释放时都会将该该对象缓存在一个栈里，下次使用直接复用。123456789101112131415@Overridepublic void recycle(Object object) &#123; if (object != value) &#123; throw new IllegalArgumentException("object does not belong to handle"); &#125; // 释放时，将自身入栈 stack.push(this);&#125;static PooledHeapByteBuf newInstance(int maxCapacity) &#123; // 从缓存栈中取，如果栈为空则new一个新的。 PooledHeapByteBuf buf = RECYCLER.get(); buf.reuse(maxCapacity); return buf; &#125; UnpooledHeapByteBuf该类是非池化堆内内存的实现，基于字节数组。 扩容,每次扩容就是创建一个新的大小的数组，将原来部分数据复制过去。1234567891011121314151617181920212223242526272829303132333435363738@Overridepublic ByteBuf capacity(int newCapacity) &#123; checkNewCapacity(newCapacity); int oldCapacity = array.length; byte[] oldArray = array; if (newCapacity &gt; oldCapacity) &#123; // 新的容量大于旧的容量. // 创建新的字节数组,将旧数组中的数据put到新的数组中. byte[] newArray = allocateArray(newCapacity); System.arraycopy(oldArray, 0, newArray, 0, oldArray.length); setArray(newArray); freeArray(oldArray); &#125; else if (newCapacity &lt; oldCapacity) &#123; // 新的容量小于旧的容量 // 创建新的字节数组.将旧数组中的未读数据put到新的数组中. byte[] newArray = allocateArray(newCapacity); int readerIndex = readerIndex(); if (readerIndex &lt; newCapacity) &#123; // 新的容量大于读索引. int writerIndex = writerIndex(); if (writerIndex &gt; newCapacity) &#123; // 新的容量小于写索引.重设写索引为新的容量. writerIndex(writerIndex = newCapacity); &#125; // 将旧数组中的数据写入到新的数组中. System.arraycopy(oldArray, readerIndex, newArray, readerIndex, writerIndex - readerIndex); &#125; else &#123; // 新的容量小于读索引.则表示没有未读数据. // 则直接将读写索引位置为capacity. // 表示当前Buffer已读写满. setIndex(newCapacity, newCapacity); &#125; setArray(newArray); freeArray(oldArray); &#125; return this;&#125; UnpooledDirectByteBuf该类是非池化堆外内存的实现，基于DirectByteBuffer。 扩容,每次都新创建一个DirectByteBuffer，将旧Buffer的部分数据复制到新的上面。12345678910111213141516171819202122232425262728293031323334353637383940414243444546@Overridepublic ByteBuf capacity(int newCapacity) &#123; checkNewCapacity(newCapacity); int readerIndex = readerIndex(); int writerIndex = writerIndex(); int oldCapacity = capacity; if (newCapacity &gt; oldCapacity) &#123; // 新的容量大于旧的容量. // 创建新的Buffer,将旧Buffer中的数据put到新的Buffer中. ByteBuffer oldBuffer = buffer; ByteBuffer newBuffer = allocateDirect(newCapacity); // position = 0 ---&gt; limit = oldCapacity oldBuffer.position(0).limit(oldBuffer.capacity()); newBuffer.position(0).limit(oldBuffer.capacity()); newBuffer.put(oldBuffer); // 从0开始写旧数据(写).写limit - position个. newBuffer.clear(); // 将Buffer状态恢复到初始状态. setByteBuffer(newBuffer); // 重新设置Buffer. &#125; else if (newCapacity &lt; oldCapacity) &#123; // 新的容量小于旧的容量 // 创建新的Buffer.将旧Buffer中的未读数据put到新的Buffer中. ByteBuffer oldBuffer = buffer; ByteBuffer newBuffer = allocateDirect(newCapacity); if (readerIndex &lt; newCapacity) &#123; // 新的容量大于读索引. if (writerIndex &gt; newCapacity) &#123; // 新的容量小于写索引.重设写索引为新的容量. writerIndex(writerIndex = newCapacity); &#125; // 将旧Buffer中的数据写入到新的Buffer中. // position = readerIndex ---&gt; limit = writerIndex oldBuffer.position(readerIndex).limit(writerIndex); newBuffer.position(readerIndex).limit(writerIndex); newBuffer.put(oldBuffer); // 从readerIndex开始写旧数据(只写未读数据).写limit - position个. newBuffer.clear(); // 将Buffer状态恢复到初始状态. &#125; else &#123; // 新的容量小于读索引.则表示没有未读数据. // 则直接将读写索引位置为capacity. // 表示当前Buffer已读写满. setIndex(newCapacity, newCapacity); &#125; setByteBuffer(newBuffer); &#125; return this;&#125; PooledHeapByteBuf，PooledDirectByteBufPooledHeapByteBuf池化堆内内存实现，基于字节数组。PooledDirectByteBuf池化堆外内存实现，基于DirectByteBuffer。拥有一个缓存栈，每次获取对象都从栈中获取。缓存栈是采用ThreadLocal来实现，每个线程都有自己的一个缓存栈。1234567891011121314151617181920212223242526272829// FastThreadLocal自定义的一个ThreadLocal。private final FastThreadLocal&lt;Stack&lt;T&gt;&gt; threadLocal = new FastThreadLocal&lt;Stack&lt;T&gt;&gt;() &#123; @Override protected Stack&lt;T&gt; initialValue() &#123; return new Stack&lt;T&gt;(Recycler.this, Thread.currentThread(), maxCapacityPerThread, maxSharedCapacityFactor,ratioMask, maxDelayedQueuesPerThread); &#125;&#125;;private static final Recycler&lt;PooledDirectByteBuf&gt; RECYCLER = new Recycler&lt;PooledDirectByteBuf&gt;() &#123; @Override protected PooledDirectByteBuf newObject(Handle&lt;PooledDirectByteBuf&gt; handle) &#123; return new PooledDirectByteBuf(handle, 0); &#125;&#125;;// 每次new对象都从栈获取，如果栈为空，则会调用上面的newObject获取对象。static PooledDirectByteBuf newInstance(int maxCapacity) &#123; PooledDirectByteBuf buf = RECYCLER.get(); buf.reuse(maxCapacity); return buf;&#125;// 每次释放都将对象放入缓存栈中。@Overridepublic void recycle(Object object) &#123; if (object != value) &#123; throw new IllegalArgumentException("object does not belong to handle"); &#125; stack.push(this);&#125; PoolChunk PoolArena PoolChunkListAndroid中使用在Android中,只能使用非池化堆内存ByteBuf.123456789101112131415String allocType = SystemPropertyUtil.get( "io.netty.allocator.type", PlatformDependent.isAndroid() ? "unpooled" : "pooled");if (PlatformDependent.hasUnsafe()) &#123; return directBuffer(initialCapacity);&#125;return heapBuffer(initialCapacity);private static boolean hasUnsafe0() &#123; if (isAndroid()) &#123; logger.debug("sun.misc.Unsafe: unavailable (Android)"); return false; &#125;&#125;]]></content>
      <categories>
        <category>内功心法（CS基础）</category>
        <category>神照功（网络协议）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[Netty线程模型]]></title>
    <url>%2FNetty%E7%BA%BF%E7%A8%8B%E6%A8%A1%E5%9E%8B%2F</url>
    <content type="text"><![CDATA[服务端线程模型Netty的服务端线程模型类似与Reactor多线程模型。采用的是Acceptor线程和IO线程分离的模式。将连接请求处理和读写处理分开，避免了因读写阻塞而导致无法处理客户端连接请求，导致客户端连接超时。 有一个专门的线程池（Acceptor）用来处理客户端的连接请求。 有一个专门的线程池（IO）用来处理读写操作。 一个线程处理N条连接，一个连接对应一个线程。 从Acceptor线程池中随机选择一个线程处理客户端连接请求，连接建立后将创建的SocketChannel注册到IO线程池中的某个线程上，由该线程负责SocketChannel的读写和编解码工作。 示例代码1234567891011121314151617181920212223242526272829303132// Acceptor线程池EventLoopGroup bossGroup = new NioEventLoopGroup(1);// IO线程池EventLoopGroup workerGroup = new NioEventLoopGroup();try &#123; ServerBootstrap b = new ServerBootstrap(); b.group(bossGroup, workerGroup) .channel(NioServerSocketChannel.class) .option(ChannelOption.SO_BACKLOG, 1000) .handler(new LoggingHandler(LogLevel.INFO)) .childHandler(new ChannelInitializer&lt;SocketChannel&gt;() &#123; @Override public void initChannel(SocketChannel ch) throws Exception &#123; ChannelPipeline p = ch.pipeline(); if (sslCtx != null) &#123; p.addLast(sslCtx.newHandler(ch.alloc())); &#125; //p.addLast(new LoggingHandler(LogLevel.INFO)); p.addLast(new EchoServerHandler()); &#125; &#125;); // Start the server. ChannelFuture f = b.bind(PORT).sync(); // Wait until the server socket is closed. f.channel().closeFuture().sync();&#125; finally &#123; // Shut down all event loops to terminate all threads. bossGroup.shutdownGracefully(); workerGroup.shutdownGracefully();&#125; ServerBootstrap是服务端连接的引导类，服务端通过它来配置和绑定端口。bind方法中首先创建NioServerSocketChannel对象。NioServerSocketChannel是对NIO的ServerSocketChannel对象的包装，其内部维护了该对象。 12345678910111213141516final ChannelFuture initAndRegister() &#123; Channel channel = null; try &#123; // 创建NioServerSocketChanneld对象 channel = channelFactory.newChannel(); init(channel); // 初始化Handler链，注册ServerBootstrapAcceptor处理器 &#125; catch (Throwable t) &#123; &#125; ChannelFuture regFuture = config().group().register(channel); return regFuture;&#125;// NioServerSocketChannel在创建的时候，会创建一个ServerSocketChannel对象。public NioServerSocketChannel() &#123; this(newSocket(DEFAULT_SELECTOR_PROVIDER));&#125; 再从EventLoopGroup线程池中随机获取一个EventLoop线程对象，将创建的NioServerSocketChannel对象绑定到该线程上。EventLoop线程通过DefaultEventExecutorChooserFactory工厂来获取，该工厂内部维护了一个EventLoop实例集合，该集合是在EventLoopGroup初始化时创建。 1234567891011protected MultithreadEventExecutorGroup(int nThreads, Executor executor,EventExecutorChooserFactory chooserFactory, Object... args) &#123; // 如果在创建EventLoopGroup时不声明，则nThreads默认为0 children = new EventExecutor[nThreads]; for (int i = 0; i &lt; nThreads; i ++) &#123; try &#123; children[i] = newChild(executor, args); success = true; &#125; &#125; chooser = chooserFactory.newChooser(children);&#125; EventLoop是一个单线程执行器，里面维护了一个任务队列。在第一次添加任务到队列中时，会执行初始化任务，在初始化任务中执行当前线程的run方法，启动当前线程。在NioEventLoop的run方法中无限轮询接收处理事件。 12345678910111213141516171819202122232425262728293031323334353637383940@Override protected void run() &#123; for (;;) &#123; try &#123; switch (selectStrategy.calculateStrategy(selectNowSupplier, hasTasks())) &#123; case SelectStrategy.CONTINUE: continue; case SelectStrategy.SELECT: select(wakenUp.getAndSet(false)); if (wakenUp.get()) &#123; selector.wakeup(); &#125; default: &#125; cancelledKeys = 0; needsToSelectAgain = false; final int ioRatio = this.ioRatio; if (ioRatio == 100) &#123; try &#123; // 处理注册的已就绪事件 processSelectedKeys(); &#125; finally &#123; // 每次都会执行一下任务队列。 runAllTasks(); &#125; &#125; else &#123; final long ioStartTime = System.nanoTime(); try &#123; processSelectedKeys(); &#125; finally &#123; final long ioTime = System.nanoTime() - ioStartTime; runAllTasks(ioTime * (100 - ioRatio) / ioRatio); &#125; &#125; &#125; catch (Throwable t) &#123; handleLoopException(t); &#125; &#125;&#125; 将NioServerSocketChannel对象注册到该EventLoop线程上，每个EventLoop对象都维护一个Selector对象，将NioServerSocketChannel内部的ServerSocketChannel注册到该Selector对象上。每个NioServerSocketChannel对象都维护一个pipeline。 12345678910111213AbstractChannel.this.eventLoop = eventLoop;@Override protected void doRegister() throws Exception &#123; boolean selected = false; for (;;) &#123; try &#123; selectionKey = javaChannel().register(eventLoop().unwrappedSelector(), 0, this); return; &#125; catch (CancelledKeyException e) &#123; &#125; &#125;&#125; 注册完成会发送channelRegistered通知，每当Channel注册到EventLoop上都会触发channelRegistered通知，移除会触发channelUnregistered通知。在收到注册通知后会添加接收器ServerBootstrapAcceptor，将其注册到Handler链上，专门将连接完成的客户端连接 注册到IO线程池中处理其读写事件。 1234567891011121314151617181920// 连接通道channelRegistered会触发initChannel回调。p.addLast(new ChannelInitializer&lt;Channel&gt;() &#123; @Override public void initChannel(final Channel ch) throws Exception &#123; final ChannelPipeline pipeline = ch.pipeline(); ChannelHandler handler = config.handler(); if (handler != null) &#123; pipeline.addLast(handler); &#125; ch.eventLoop().execute(new Runnable() &#123; @Override public void run() &#123; // 添加接收器ServerBootstrapAccepto pipeline.addLast(new ServerBootstrapAcceptor( ch, currentChildGroup, currentChildHandler, currentChildOptions, currentChildAttrs)); &#125; &#125;); &#125;&#125;); 绑定端口,创建一个绑定端口的任务，将其加入到绑定的线程的任务队列中去执行。最终调用NioServerSocketChannel中的原生ServerSocketChanneld对象去绑定端口。 1234567891011121314151617181920212223private static void doBind0( final ChannelFuture regFuture, final Channel channel, final SocketAddress localAddress, final ChannelPromise promise) &#123; channel.eventLoop().execute(new Runnable() &#123; @Override public void run() &#123; if (regFuture.isSuccess()) &#123; channel.bind(localAddress, promise).addListener(ChannelFutureListener.CLOSE_ON_FAILURE); &#125; else &#123; promise.setFailure(regFuture.cause()); &#125; &#125; &#125;);&#125;@Overrideprotected void doBind(SocketAddress localAddress) throws Exception &#123; if (PlatformDependent.javaVersion() &gt;= 7) &#123; javaChannel().bind(localAddress, config.getBacklog()); &#125; else &#123; javaChannel().socket().bind(localAddress, config.getBacklog()); &#125;&#125; 绑定完成后，发送channelActive通知，如果当前通道已经处于激活状态，则不会重复发送该通知。如果开启了autoRead机制，则注册ACCEPT事件接收客户端连接请求。 123456789101112131415161718192021private void readIfIsAutoRead() &#123; if (channel.config().isAutoRead()) &#123; channel.read(); &#125;&#125;@Overrideprotected void doBeginRead() throws Exception &#123; // Channel.read() or ChannelHandlerContext.read() was called final SelectionKey selectionKey = this.selectionKey; if (!selectionKey.isValid()) &#123; return; &#125; readPending = true; final int interestOps = selectionKey.interestOps(); if ((interestOps &amp; readInterestOp) == 0) &#123; selectionKey.interestOps(interestOps | readInterestOp); &#125;&#125; 在EventLoop的run方法中接收客户端连接请求的ACCEPT事件。 1234567891011121314151617181920212223private void processSelectedKey(SelectionKey k, AbstractNioChannel ch) &#123; final AbstractNioChannel.NioUnsafe unsafe = ch.unsafe(); try &#123; int readyOps = k.readyOps(); // 处理CONNECT事件 if ((readyOps &amp; SelectionKey.OP_CONNECT) != 0) &#123; int ops = k.interestOps(); ops &amp;= ~SelectionKey.OP_CONNECT; k.interestOps(ops); unsafe.finishConnect(); &#125; // 处理WRITE事件 if ((readyOps &amp; SelectionKey.OP_WRITE) != 0) &#123; ch.unsafe().forceFlush(); &#125; // 处理READ和ACCEPT事件 if ((readyOps &amp; (SelectionKey.OP_READ | SelectionKey.OP_ACCEPT)) != 0 || readyOps == 0) &#123; unsafe.read(); &#125; &#125; catch (CancelledKeyException ignored) &#123; unsafe.close(unsafe.voidPromise()); &#125;&#125; 接收客户端连接请求，调用原生ServerSocketChannel的accept方法接收该连接请求，并将返回的SocketChannel对象包装成NioSocketChannel对象。并将创建的NioSocketChannel对象通过读事件发送到Handler链处理，最终会被第9步中添加的ServerBootstrapAcceptor处理该消息。 123456789101112@Overrideprotected int doReadMessages(List&lt;Object&gt; buf) throws Exception &#123; SocketChannel ch = SocketUtils.accept(javaChannel()); try &#123; if (ch != null) &#123; buf.add(new NioSocketChannel(this, ch)); return 1; &#125; &#125; catch (Throwable t) &#123; &#125; return 0;&#125; ServerBootstrapAcceptor专门用来将客户端连接注册到IO线程池中。从IO线程池中随机取出一个NioEventLoop线程对象，将接受客户端连接产生的SocketChannel对象绑定到该线程对象中。和上面步骤一样，首先从EventLoopGroup(与上面的不是同一个)对应的工厂中获取一个EventLoop实例对象，然后启动该EventLoop线程，在其run方法中自循接收读写事件。将SocketChannel注册到该EventLoop对象中的Selector中。并发送ChannelRegistered通知。 123456789101112131415161718@Override@SuppressWarnings("unchecked")public void channelRead(ChannelHandlerContext ctx, Object msg) &#123; final Channel child = (Channel) msg; try &#123; // childGroup就是IO线程池。child是accpet操作后得到的SocketChannel对象。 childGroup.register(child).addListener(new ChannelFutureListener() &#123; @Override public void operationComplete(ChannelFuture future) throws Exception &#123; if (!future.isSuccess()) &#123; forceClose(child, future.cause()); &#125; &#125; &#125;); &#125; catch (Throwable t) &#123; forceClose(child, t); &#125;&#125; 客户端线程模型客户端只需要一个IO线程池来处理连接就可以，每次连接都从IO线程池中取出一个线程，该线程绑定了SocketChannel对象，负责该对象的连接和读写操作。 示例代码12345678910111213141516171819EventLoopGroup group = new NioEventLoopGroup();try &#123; Bootstrap b = new Bootstrap(); b.group(group) .channel(NioSocketChannel.class) .option(ChannelOption.CONNECT_TIMEOUT_MILLIS, 10000) //10秒超时 .option(ChannelOption.SO_KEEPALIVE, true) .handler(new ChannelInitializer&lt;SocketChannel&gt;() &#123; @Override public void initChannel(SocketChannel ch) throws Exception &#123; ChannelPipeline p = ch.pipeline(); if (sslCtx != null) &#123; p.addLast(sslCtx.newHandler(ch.alloc(), HOST, PORT)); &#125; //p.addLast(new LoggingHandler(LogLevel.INFO)); p.addLast(new EchoClientHandler()); &#125; &#125;); final DefaultPromise f = (DefaultPromise) b.connect(HOST, PORT); Bootstrap是客户端连接的引导类。客户端通过它来配置和连接服务端。在连接时，首先创建了NioSocketChannel对象，该对象是对NIO的SocketChannel对象的包装，其内部维护了该对象。 123456789101112131415final ChannelFuture initAndRegister() &#123; Channel channel = null; try &#123; // 创建NioSocketChanneld对象 channel = channelFactory.newChannel(); init(channel); // 初始化Handler链 &#125; catch (Throwable t) &#123; &#125; ChannelFuture regFuture = config().group().register(channel); return regFuture;&#125;public NioSocketChannel(SelectorProvider provider) &#123; this(newSocket(provider)); &#125; 再从EventLoopGroup线程池中随机获取一个EventLoop线程对象，将创建的NioSocketChannel对象绑定到该线程上。EventLoop线程通过DefaultEventExecutorChooserFactory工厂来获取，该工厂内部维护了一个EventLoop实例集合，该集合是在EventLoopGroup初始化时创建。 EventLoop是一个单线程执行器，里面维护了一个任务队列。在第一次添加任务到队列中时，会执行初始化任务，在初始化任务中执行当前线程的run方法，启动当前线程。在NioEventLoop的run方法中无限轮询接收处理事件。 将NioSocketChannel对象注册到该EventLoop线程上，每个EventLoop对象都维护一个Selector对象，将NioSocketChannel内部的SocketChannel注册到该Selector对象上。每个NioSocketChannel对象都维护一个pipeline。发送注册完成事件，触发ChannelInitializer.initChannel回调。 注册完成后，开始正式连接，调用原生SocketChannel对象的connect对象进行异步联接。该方法会直接返回，如果返回true表明连接成功，否则表明正在连接中。如果连接成功就发送ChannelActive通知。否则就注册CONNECT事件等待服务端连接完成消息，并且根据设置的超时时间来添加定时任务，过了超时时间还没有接收到服务端连接成功的消息，则认为连接超时，取消连接任务。 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859606162636465666768@Overridepublic final void connect(final SocketAddress remoteAddress, final SocketAddress localAddress, final ChannelPromise promise) &#123; try &#123; boolean wasActive = isActive(); // 连接 if (doConnect(remoteAddress, localAddress)) &#123; fulfillConnectPromise(promise, wasActive); &#125; else &#123; connectPromise = promise; requestedRemoteAddress = remoteAddress; // 添加定时任务，处理超时 int connectTimeoutMillis = config().getConnectTimeoutMillis(); if (connectTimeoutMillis &gt; 0) &#123; connectTimeoutFuture = eventLoop().schedule(new Runnable() &#123; @Override public void run() &#123; ChannelPromise connectPromise = AbstractNioChannel.this.connectPromise; ConnectTimeoutException cause = new ConnectTimeoutException("connection timed out: " + remoteAddress); if (connectPromise != null &amp;&amp; connectPromise.tryFailure(cause)) &#123; close(voidPromise()); &#125; &#125; &#125;, connectTimeoutMillis, TimeUnit.MILLISECONDS); &#125; // 如果连接任务被取消，则定时任务也取消 promise.addListener(new ChannelFutureListener() &#123; @Override public void operationComplete(ChannelFuture future) throws Exception &#123; if (future.isCancelled()) &#123; if (connectTimeoutFuture != null) &#123; connectTimeoutFuture.cancel(false); &#125; connectPromise = null; close(voidPromise()); &#125; &#125; &#125;); &#125; &#125; catch (Throwable t) &#123; promise.tryFailure(annotateConnectException(t, remoteAddress)); closeIfClosed(); &#125;&#125;@Overrideprotected boolean doConnect(SocketAddress remoteAddress, SocketAddress localAddress) throws Exception &#123; if (localAddress != null) &#123; doBind0(localAddress); &#125; boolean success = false; try &#123; boolean connected = SocketUtils.connect(javaChannel(), remoteAddress); // 如果连接不成功，则会注册CONNECT事件等待服务端连接返回。 if (!connected) &#123; selectionKey().interestOps(SelectionKey.OP_CONNECT); &#125; success = true; return connected; &#125; finally &#123; if (!success) &#123; doClose(); &#125; &#125;&#125; 如果当时连接没有成功，则等待服务端连接通知，在EventLoop中接收CONNECT消息。调用原生SocketChannel的finishConnect方法完成连接，并且发送ChannelActive通知。 1234567891011121314151617181920212223242526272829303132private void processSelectedKey(SelectionKey k, AbstractNioChannel ch) &#123; final AbstractNioChannel.NioUnsafe unsafe = ch.unsafe(); try &#123; int readyOps = k.readyOps(); // 处理CONNECT事件 if ((readyOps &amp; SelectionKey.OP_CONNECT) != 0) &#123; int ops = k.interestOps(); ops &amp;= ~SelectionKey.OP_CONNECT; k.interestOps(ops); unsafe.finishConnect(); &#125; // 处理WRITE事件 if ((readyOps &amp; SelectionKey.OP_WRITE) != 0) &#123; ch.unsafe().forceFlush(); &#125; // 处理READ和ACCEPT事件 if ((readyOps &amp; (SelectionKey.OP_READ | SelectionKey.OP_ACCEPT)) != 0 || readyOps == 0) &#123; unsafe.read(); &#125; &#125; catch (CancelledKeyException ignored) &#123; unsafe.close(unsafe.voidPromise()); &#125;&#125;@Overridepublic final void finishConnect() &#123; try &#123; boolean wasActive = isActive(); doFinishConnect(); fulfillConnectPromise(connectPromise, wasActive); &#125;&#125; 接收到channelActive通知。如果开启了autoRead机制，则注册REAT事件自动读取服务端消息。 123456789101112131415161718192021private void readIfIsAutoRead() &#123; if (channel.config().isAutoRead()) &#123; channel.read(); &#125;&#125;@Overrideprotected void doBeginRead() throws Exception &#123; // Channel.read() or ChannelHandlerContext.read() was called final SelectionKey selectionKey = this.selectionKey; if (!selectionKey.isValid()) &#123; return; &#125; readPending = true; final int interestOps = selectionKey.interestOps(); if ((interestOps &amp; readInterestOp) == 0) &#123; selectionKey.interestOps(interestOps | readInterestOp); &#125;&#125;]]></content>
      <categories>
        <category>内功心法（CS基础）</category>
        <category>神照功（网络协议）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[NIO深入]]></title>
    <url>%2FNIO%E6%B7%B1%E5%85%A5%2F</url>
    <content type="text"><![CDATA[BufferNIO中的Buffer用于和NIOChannel进行交互，数据从Channel中读到Buffer中，数据从Buffer中写入到Channel中。Buffer本质是是一块可读写的内存区域，这块内存区域被包装成Buffer对象，并提供了一系列方法访问该内存。Buffer不是线程安全的，多线程时需要同步。 基础属性 容量capacity : 缓冲区能够容纳的数据的最大数量，在创建的时候设定，不可再更改。 上界limit : 缓冲区现存数据的计数，用来读写转换记录写的位置。 位置position : 下一个要被读写的元素的索引。每次读写都会更新位置。 标记mark : 备忘位置，用于重置位置。0 &lt;= mark &lt;= position &lt;= limit &lt;= capacity 重要方法 allocate : 创建一个Buffer对象，参数为capacity。 wrap ：使用包装方式创建Buffer对象。 duplicate : 创建一个与原始Buffer相似的Buffer,两者共享数据，但是每个Buffer都有各自的position和limit。 slice : 创建一个切片Buffer,共享数据。 flip ：转换读写状态，每次转换都将position的值赋给limit，将position设为0。从写转读，是从0开始读到limit（也就是已写数据）。从读转写，会将limit的值赋给position，也就是抵消了flip的操作，还原到上上次，从上次写的位置开始写。 remaining ：limit-position，未读数据大小。 clear : 清空Buffer,position设为0，limit设为capacity的值。数据并未清除。可以重新开始写数据。 compact : 将未读的数据拷贝到Buffer头部，将position设为未读数据后面，这样未读数据会保留，重写数据从其后面开始写。 mark : 标记。 reset : 恢复到mark的position。 equals : 两个Buffer相等的条件，1.类型相同，2.Buffer中剩余的数据个数相同，3.Buffer中剩余的数据相同。 实现类型，Buffer的实现类型分为两种。 direct类型：采用直接内存实现，直接内存不受JVM控制，是由系统直接分配的，JVM无法GC回收，必须手动回收该部分内存。 heap类型：采用JVM堆栈实现。 大端、小端每个基本数据类型都是以连续字节序列的形式存储在内存中。如果字节的最高字节在左边，位于低位地址中，则就是大端字节顺序。如果字节的最低字节在左边，位于低位地址中，则就是小端字节顺序。 ChannelChannel可以用来异步的读写数据，但是必须通过Buffer来读写，读取到一个Buffer中，或者从一个Buffer中写入。Channel可以以阻塞和非阻塞模式运行。只有面向流的通道才能使用非阻塞模式，如Socket和Pipe。而FileChannel只能是阻塞模式。SelectableChannel继承该接口的Channel都是可以以非阻塞模式运行的。Channel可以是单向也可以是双向。ReadableByteChannel继承该接口的Channel是可读的，WritableByteChannel继承该接口的Channel是可写的，两个接口都继承的Channel就是双向可读写的。 FileChannel从文件中读取数据，是一个连接到文件的通道。FileChannel没有继承SelectableChannel接口，无法设置成非阻塞模式。FileChannel不能直接创建，必须要通过文件流来获取，比如InputStream、RandomAccessFile等。12RandomAccessFile randomAccessFile = new RandomAccessFile(file, "rw");FileChannel channel = randomAccessFile.getChannel(); 内存映射文件适合用来加载大文件map : 该方法可以在一个打开的文件和一个MappedByteBuffer之间建立一个虚拟内存映射。映射文件的大小如果超过文件的实际大小，那么文件会被增大。 SocketChannel通过TCP读写网络数据。SocketChannel实现了读写接口，负责传输数据。每个SocketChannel都对应一个Socket对象。读写操作都加上了线程锁，是线程安全的，同一时刻只能有一个读操作和写操作。123public static SocketChannel open() throws IOException &#123; return SelectorProvider.provider().openSocketChannel();&#125; 1234567891011private Socket socket;public Socket socket() &#123; Object var1 = this.stateLock; synchronized(this.stateLock) &#123; if(this.socket == null) &#123; this.socket = SocketAdaptor.create(this); &#125; return this.socket; &#125;&#125; connect、write、read： 阻塞模式下，线程会一直阻塞到线程结果返回。 非阻塞模式，异步处理，该方法直接返回，需要通过监测返回值来处理。 ServerSocketChannel监听TCP连接，为每一个连接创建一个对应的SocketChannel，通过创建的SocketChannel读写网络数据。123public static ServerSocketChannel open() throws IOException &#123; return SelectorProvider.provider().openServerSocketChannel();&#125; accept、write、read： 阻塞模式下，线程会一直阻塞到线程结果返回。 非阻塞模式，异步处理，该方法直接返回，需要通过监测返回值来处理。 DataGramChannel通过UDP读写网络数据 SelectorSelector能够监测多个NIO通道，并能检测到通道是否准备好。只有非阻塞模式的通道才可以被注册到Selector中，一个通道可以被注册到多个Selector上。 从而一个线程就可以管理多个Channel,避免了线程切换的开销。123public static Selector open() throws IOException &#123; return SelectorProvider.provider().openSelector();&#125; 为了将Channel和Selector结合使用，需要将Channel注册到Selector上。ops为注册的事件类型，分为connect,accept,read,write四种。Channel触发一种事件代表该事件已经就绪。比如Channel成功连接到对端主机，就会触发connect事件；Channel准备好接收新的连接，就会触发accept事件；Channel上有数据可读，就会触发read事件；Channel上有数据等待写，就会触发write事件。123public final SelectionKey register(Selector sel, int ops) throws ClosedChannelException &#123; return register(sel, ops, null);&#125; 注册完成后，会返回一个SelectionKey对象。该对象包含多个集合，interest集合是已注册事件类型的集合，ready集合是Channel已经准备就绪的事件集合。 一旦向Selector注册了多个Channel，就可以调用Selector.select()方法。该方法返回已注册事件中已经准备就绪的Channel。 select : 阻塞到至少一个Channel在已注册的事件上就绪为止。并返回Channel的个数。 select(long timeout) : 和select一样，但是最多会阻塞timeout毫秒。 selectNow : 非阻塞，直接返回。因此可能返回0。 如果select返回为0，则可以通过selectedKeys()方法得到对应准备就绪的Channel注册时的SelectionKey对象。该方法返回一个Set集合，通过遍历该集合处理所有Channel就绪的事件。由于Selector并不会主动从selectedKeys集合中移除selectionKey对象，所以在处理完后，需要主动从集合中删除该对象。 如果某个线程阻塞在select方法上，想让其立刻返回，可以在其它线程中通过用一个Selector对象调用wakeup()方法直接唤醒阻塞线程。如果有其它线程调用了wakeup()方法，但是当前没有线程阻塞在select()方法上，下个调用select()方法的线程会立刻醒来(与LockSupport的park和unpark相似)。 close()方法会关闭该Selector,注销所有的SelectionKey对象。但是并不会关闭Channel。]]></content>
      <categories>
        <category>内功心法（CS基础）</category>
        <category>神照功（网络协议）</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[TCP讲解]]></title>
    <url>%2FTCP%E8%AE%B2%E8%A7%A3%2F</url>
    <content type="text"><![CDATA[TCP是一个面向连接的、可靠的、流协议。在OSI模型中属于传输层协议。 序列号序列号是按顺序给发送数据的每个字节都标上的编号。接收端查询接收数据TCP首部的序列号和数据的长度，计算出自己下一步应该接收的序列号，将其作为确认应答返回给发送端。 确认应答当发送端的数据到达接收端主机时，接收端主机会返回一个已经收到消息的通知，这个消息叫做确认应答（ACK）。如果在一定时间内没有收到ACK，发送端认为可能丢包，则进行重发。接收端主机如果收到相同数据会放弃接收该数据，不会造成重复接收相同数据的情况。 超时重发TCP每次发包都会计算往返时间及其偏差，并将这个时间作为重发超时的时间，如果超过该时间仍然没有收到接收端的确认应答，则认为发送超时，准备重发。如果重发一定次数仍然失败，则判断网络异常，强制关闭连接。因为有重发机制，所以数据并不是一发送就从缓冲区中删除，而是等到接收端的确认应答返回，确认该数据已经被接收，才会从缓冲区删除。 连接管理三次握手TCP在数据通信之前，通过TCP首部发送一个SYN包作为建立连接的请求并等待确认应答，如果接收端发来确认应答，则认为可以进行数据通信，并向接收端返回确认应答。因此连接的建立需要来回发送三个包才能完成。 客户端发送SYN(向服务端请求建立连接) 服务端发送SYN/ACK（通知客户端服务端已经准备好了） 客户端回应ACK(通知服务端客户端已经准备好了) 三次握手后，双方都知道对方已经准备好了，连接建立。 四次挥手在通信结束后也会进行断开连接的处理，发送FIN包给接收端。由于TCP是全双工模式，可以单端发送数据，所以需要4次挥手。2、3步不能合并，是因为此时服务端可能还有数据没有发送，不能立刻就关闭，所以先发送ACK回应，等数据处理完再发送FIN请求客户端关闭。 客户端发送FIN/ACK（向服务端请求断开连接，标明客户端没有数据需要发送，可以关闭，但是还可以接收服务端数据） 服务端回应ACK(通知客户端可以关闭连接) 服务端发送FIN/ACK（向客户端请求断开连接，标明服务端没有数据需要发送，可以关闭） 客户端回应ACK(通知服务端可以关闭连接，服务端关闭连接，客户端等待一段时间后也关闭连接) 数据分段在建立连接时，会确定发送数据包的单位，也就是最大消息长度MSS。该值由最小的一方为准，避免传输途中二次切片。在发送数据或者重发时，都是以MSS的大小将数据进行分割发送。 滑动窗口机制在发送大数据时，如果每发送一段数据就需要等待一个确认应答，那么效率非常低下。为了解决该问题，提出了滑动窗口机制。窗口实现了一个数据缓冲区，避免了发送一段数据就对应一个确认应答的低效问题，改成每发送窗口大小段数据应对一个确认应答。窗口大小就是无需等待确认应答就可以连续发送数据的最大值，该值由接收端主机控制，接收端主机将自己可以接收的缓冲区大小放入TCP首部字段中通知给发送端。 在窗口控制时，如果确认应答丢失，并不会马上就重发数据，而是可以通过下一个确认应答来进行确认，因为可能数据已经被接收，只是确认应答丢失，所以如果收到了下一个确认应答，则表明无需重发，避免了无效的重复。 如果是某一段数据丢失，接收端会一直发送该段数据的确认应答，如果发送端连续三次收到同一个确认应答，则认为该段数据丢失，重发该段数据。这种重发机制比超时机制更高效。 流控制接收端主机向发送端主机通知自己可以接收数据的大小，发送端会发送不超过该限制的数据。也就是调整窗口的大小。当接收端的缓冲区面临数据溢出时，窗口大小的值会变小通知给发送端，从而控制数据发送量。当接收端主机缓冲区满了时，窗口大小为0，发送端会停止发送，一直等待，过了重发超时的时间还没有收到窗口更新的通知，会主动发送一个窗口探测包给接收端。当接收端主机缓冲区空闲时，则会通知发送端窗口更新继续发送数据。 拥塞控制当网络出现拥塞时，如果突然发送一个较大的数据，可能会导致整个网络瘫痪。为了防止该问题，在通信开始时，通过一个慢启动算法得出的数值，对发送数据大小进行控制。 延迟确认应答捎带应答将TCP的确认应答和回执数据通过一个包发送。]]></content>
      <categories>
        <category>内功心法（CS基础）</category>
        <category>神照功（网络协议）</category>
      </categories>
  </entry>
</search>
